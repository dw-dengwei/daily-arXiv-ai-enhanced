<div id=toc></div>

# Table of Contents

- [cs.IT](#cs.IT) [Total: 6]
- [cs.DB](#cs.DB) [Total: 5]
- [cs.DS](#cs.DS) [Total: 14]
- [cs.GT](#cs.GT) [Total: 7]
- [cs.IR](#cs.IR) [Total: 9]


<div id='cs.IT'></div>

# cs.IT [[Back]](#toc)

### [1] [Direct Factorization of the Karhunen-Loève Transform of AR(1) Sources](https://arxiv.org/abs/2608.06522)
*Yuriy A. Reznik*

Main category: cs.IT

TL;DR: 本文发现AR(1)平稳源的Karhunen-Loève变换(KLT)并非传统认为的稠密无结构矩阵，而是可以通过递归因子分解来表示。在任意阶数N≥2和相关系数ρ∈(0,1)下，精确AR(1) KLT都能分解为蝶形级、半阶副本以及由秩一边界扰动产生的正交校正。所有校正级具有Cauchy结构，通过快速多极子方法可实现O(N log N)的精确KLT算法，与FFT和快速正弦变换同复杂度。


<details>
  <summary>Details</summary>
Motivation: 传统的AR(1) KLT被认为是一个无结构的稠密矩阵，其频率是Ray和Driver超越方程的根，难以直接计算。因此研究领域转向了快速近似方法（最著名的是DCT）。本文旨在证明这种退却是过早的——精确KLT实际上具有可递归分解的结构。

Method: 利用Sherman-Morrison恒等式和经典的特征值更新方法（secular eigenvalue updates），从协方差矩阵推导出递归因子分解。偶数阶时，变换分解为蝶形级、两个半阶KLT副本和由秩一边界扰动生成的正交校正；奇数阶时，分解为两个单侧预测残差的KLT副本和一个吸收中心样本的arrowhead级。残差KLT本身也以半阶递归。所有校正级具有Cauchy结构，可通过快速多极子方法(FMM)实现。

Result: 得到的精确KLT算法复杂度为O(N log N)，与FFT和快速正弦变换同阶。当ρ→1时，因子分解退化为已知的DCT-II奇偶分裂。分解常数被证明是ρ的代数函数，对N≤8可用根式表示。给出了N=2到8的完整精确KLT模块。

Conclusion: 精确AR(1) KLT并非无结构矩阵，而是具有优雅的递归因子分解结构。这推翻了长期以来的假设，表明无需依赖近似变换（如DCT），精确KLT可以以与快速变换相同的复杂度高效计算。

Abstract: The Karhunen--Loève transform (KLT) of the stationary AR(1) source is the classical optimality benchmark of transform coding. Its frequencies are roots of the transcendental equations of Ray and Driver, and the transform has therefore long been treated as an unstructured dense matrix. Rather than compute the exact KLT, the field turned to fast approximations of it, most famously the discrete cosine transform (DCT). This paper shows that the retreat was premature. The exact AR(1) KLT admits direct recursive factorizations, at every order $N\ge2$ and every correlation coefficient $ρ\in(0,1)$: at even orders, the transform reduces to a butterfly stage, two half-order copies of itself, and orthogonal corrections generated by rank-one boundary perturbations of the covariance; at odd orders, to two copies of the KLT of the one-sided prediction residual and an arrowhead stage absorbing the center sample. The residual KLT recurses through half order as well. All correction stages are Cauchy-structured, and applying them by the fast multipole method yields exact-KLT algorithms of $O(N\log N)$ complexity---the same order as the FFT and the fast sinusoidal transforms. The factorizations follow from the covariance matrix by the Sherman--Morrison identity and the classical secular eigenvalue updates, and degenerate, as $ρ\to1$, to the known parity splittings of the DCT-II. As secondary results, the factorization constants are shown to be algebraic functions of $ρ$---in radicals for all $N\le8$---and complete exact KLT modules for $N=2,\dots,8$ are given.

</details>


### [2] [Graph Invariants under Lexicographic Products: Shannon Capacity and Related Parameters](https://arxiv.org/abs/2608.06573)
*Igal Sason*

Main category: cs.IT

TL;DR: 本文研究了有限图的字典序积下的Lovász theta函数、分数Haemers数和Shannon容量。作者给出了三个已知结果的初等自包含证明，证明了Shannon容量在字典序积下（两种顺序）具有超可乘性，并与强积进行比较。通过结合这些比较，得到了确定字典序积容量的界限和充分条件。文中还确定了涉及Kneser图、其补图及其q-模拟的字典序积的Shannon容量，证明了完全外因子保持Shannon容量，并确定了迭代字典序幂的容量。


<details>
  <summary>Details</summary>
Motivation: 字典序积图建模了零误差通信中的分层混淆结构（一层描述类间混淆，另一层描述类内混淆）。理解Lovász theta函数、分数Haemers数和Shannon容量在这些积下的行为对于零误差信道的容量计算至关重要，但此前相关结果缺乏统一的初等证明，且Shannon容量在字典序积下的性质尚不明确。

Method: 本文采用初等且自包含的方法给出Lovász theta函数和分数Haemers数在字典序积下可乘性的新证明，以及分数与普通Lovász theta函数相等性的证明。通过将字典序积与强积进行比较，建立Shannon容量的超可乘性，推导出确定字典序积容量的界限和充分条件，并应用于Kneser图及其相关图族。

Result: 证明了Shannon容量在字典序积下（两种顺序）的超可乘性；表明Lovász上界与分数Haemers上界不可比较；确定了涉及Kneser图、其补图及q-模拟的字典序积的Shannon容量；证明了完全外因子保持Shannon容量；确定了迭代字典序幂（包括自互补、顶点传递或强正则图的幂）的容量。

Conclusion: 本文系统性地刻画了字典序积下各容量参数的行为，给出了统一的初等证明框架，并为多个图族的字典序积容量提供了精确值。最后提出了关于字典序积与强积关系的开放问题，为该领域后续研究指明了方向。

Abstract: We study the Lovász theta function, the fractional Haemers number, and the Shannon capacity under lexicographic products of finite graphs. This product models zero-error communication in which one graph describes confusability between classes and the other within each class. We give alternative, elementary, and self-contained proofs of three known results: the multiplicativity of the Lovász theta function and the fractional Haemers number under lexicographic products, and the equality of the fractional and ordinary Lovász theta functions. We prove that the Shannon capacity is supermultiplicative under lexicographic products in both orders and compare the ordered products with the strong product. Combined with the multiplicativity of the former parameters under strong products, these comparisons yield bounds and sufficient conditions for determining lexicographic-product capacities exactly. We show that the resulting Lovász and fractional Haemers upper bounds are incomparable and determine the Shannon capacities of products involving Kneser graphs, their complements, and $q$-analogues of Kneser graphs. We further prove that a complete outer factor preserves the Shannon capacity under lexicographic products. Finally, we determine the capacities of iterated lexicographic powers, including powers of self-complementary graphs that are vertex-transitive or strongly regular, and pose an open problem concerning lexicographic and strong products.

</details>


### [3] [A Pairwise-Error-Probability Framework for One-Shot Information Theory](https://arxiv.org/abs/2608.06577)
*Nir Elkayam,Meir Feder*

Main category: cs.IT

TL;DR: 本文提出了一种基于成对错误概率（PEP）和随机平局打破机制的单发（有限块长）信道编码框架，通过概率积分变换恒等式统一了随机编码可达性和固定码反证，并推导了两种变分恒等式，将匹配最大似然解码下的谱表示为联合凸问题，进而给出先验优化的极小极大元反证的线性规划，以及随机编码界的凹规划求解方法，恢复了多个经典单发界。


<details>
  <summary>Details</summary>
Motivation: 传统有限块长信道编码理论中，可达性（随机编码下界）与反证（固定码上界）往往需要不同的技术路径和工具，缺乏统一的框架。本文旨在通过PEP和随机平局打破的统一视角，建立一套能够同时处理可达性与反证问题，并在任意解码度量下有效的通用框架，同时将先验优化问题转化为可高效求解的凸/凹规划。

Method: 1) 基于带随机平局的解码器的PEP建立概率积分变换恒等式，统一刻画随机编码可达性与固定码反证；2) 通过Neyman-Pearson β泛函和反向信道两种途径推导度量加权尾部泛函的变分恒等式；3) 在匹配最大似然解码下，将错误谱表示为在测试水平和输入先验上联合凸的函数，结合反向信道表示给出有限维线性规划形式的先验优化极小极大元反证；4) 对无记忆信道进行类型缩减后，线性规划的规模在块长上为多项式大小；5) 将随机编码界的先验优化建模为具有显式梯度的凹规划，用一阶方法直接求解。

Result: 该框架恢复了多个经典单发界，包括Polyanskiy-Poor-Verdu的随机编码联合界和极小极大元反证、Han-Verdu的信息谱界以及Matthews的线性规划反证。在AWGN和二元Z信道上的数值实验展示了可达性-反证对比以及先验优化的效果。

Conclusion: 本文建立的统一PEP框架为有限块长信道编码提供了既能推导可达性又能给出精确反证的统一数学工具，并通过先验优化将相关界转化为可计算的高效凸优化问题，为后续理论和算法研究奠定了坚实基础。

Abstract: We develop a one-shot (finite-blocklength) channel-coding framework based on the pairwise error probability (PEP) of a decoder with randomized tie-breaking. The tie-breaking rule yields a probability-integral-transform identity: the induced error spectrum describes both random-coding achievability and exact fixed-code converse statements, for an arbitrary decoding metric. We derive two variational identities for metric-weighted tail functionals of the PEP, one through the Neyman-Pearson $β$-functional and one through a reverse channel, valid for an arbitrary metric. Under matched maximum-likelihood decoding they specialize to representations of the spectrum itself, which is then jointly convex in the testing level and the input prior; combined with the reverse-channel representation, this gives a linear program for the prior-optimized minimax meta-converse -- finite-dimensional in general and, for memoryless channels with fixed alphabets, of size polynomial in the blocklength after a type reduction. Prior optimization of the random-coding bound is formulated as a concave program over input distributions with an explicit gradient, solved by a direct first-order method. The framework recovers several classical one-shot bounds, including the random-coding union bound and minimax meta-converse of Polyanskiy-Poor-Verdu, the information-spectrum bounds of Han-Verdu, and the linear-programming converse of Matthews. Numerical examples on the AWGN and binary Z-channels illustrate the achievability-converse comparison and the effect of prior optimization.

</details>


### [4] [Entanglement-Assisted Quantum Locally Recoverable Codes: Bounds, Optimal Constructions, and Achievability](https://arxiv.org/abs/2608.06854)
*Vijay Kumar,Ramakrishna Bandi*

Main category: cs.IT

TL;DR: 本文研究了基于CSS类稳定子构造的纠缠辅助量子局部可恢复码（EA-qLRCs），由一对经典局部可恢复码（cLRCs）构建，无需对偶包含条件。作者给出了局部恢复信道定义、稳定子充分条件、多种类Singleton、Griesmer、Plotkin和球堆积型逆界，以及Cadambe-Mazumdar类界；还给出了达到Singleton界的充要条件（单码情形简化为对偶包维数条件），并从Tamo-Barg码和循环码构建了最优/次优实例，最后通过两个Gilbert-Varshamov类可达性界统一了所有界。


<details>
  <summary>Details</summary>
Motivation: 量子局部可恢复码（qLRCs）在量子纠错和分布式量子存储中具有重要应用，但已有构造通常需要对偶包含条件，限制了码参数的选择。本文旨在通过纠缠辅助机制放宽这一限制，从经典LRC对构建更灵活的量子LRC，并建立完整的理论边界框架。

Method: 采用CSS类稳定子构造，从一对经典LRC（cLRCs）构建EA-qLRCs，无需对偶包含条件。定义了局部恢复信道，给出了稳定子判据；推导了多种逆界（Singleton、Griesmer、Plotkin、球堆积类及Cadambe-Mazumdar类）；给出了达到Singleton界的充要条件（单码情形简化为hull维数条件s=dim(C∩C⊥)，纠缠数c=n-k-s）；从Tamo-Barg码和循环码构建具体码族；提出两个Gilbert-Varshamov类可达性界（经典奇偶校验增广和级联码构造），通过单项式等价论证证明对q>3无条件成立。

Result: 循环码族构造达到Singleton类界，产生最优EA-qLRCs；Tamo-Barg构造仅在k≤r退化区达到界（此时局部性无意义）；两个GV类可达性界在q>3时无条件成立；所有逆界和可达性界在最大纠缠体制下可统一比较，刻画了可达与禁区的速率-距离-局部性区域。

Conclusion: 本文建立了纠缠辅助量子局部可恢复码的完整理论框架，包括构造方法、多种参数界、最优性条件和具体码族，为设计高效量子局部可恢复码提供了系统性指导。关键发现是：经典最优LRC构造（如Tamo-Barg）并非直接适用于量子情形，而循环码族能产生真正最优的EA-qLRCs。

Abstract: This paper studies entanglement-assisted quantum locally recoverable codes (EA-qLRCs) built via a CSS-like stabilizer construction from pairs of classical locally recoverable codes (cLRCs), without requiring dual-containment. We define such codes through local recovery channels, give a sufficient stabilizer criterion for the construction, and derive Singleton-, Griesmer-, Plotkin-, and sphere-packing-like converse bounds on the parameters of the resulting pure CSS-like EA-qLRCs, along with a Cadambe--Mazumdar-like bound that, as in the classical case, lacks a closed form, plus a comparison of their relative tightness across finite-length and asymptotic regimes. We give necessary and sufficient conditions for a pure CSS-like EA-qLRC to attain the Singleton-like bound with equality; for the single-code case $\mathcal{C}_1=\mathcal{C}_2=\mathcal{C}$, this reduces to a simple condition on the hull dimension $s=\dim(\mathcal{C}\cap\mathcal{C}^\perp)$, which also fixes the entanglement count via $c=n-k-s$. We present CSS-like EA-qLRC constructions from classical LRC families---Tamo--Barg and cyclic codes---and characterize when these attain the Singleton-like bound, showing the cyclic families yield optimal codes while the Tamo--Barg construction, though valid, attains the bound only in the degenerate regime $k \le r$, where locality is vacuous. We complement these constructions with two Gilbert--Varshamov-like achievability bounds, via a classical parity-check augmentation and a sharper concatenated-code construction, and show both hold unconditionally for field size $q>3$ via a monomial-equivalence argument. Finally, we unify all bounds---converse and achievability alike---under a common maximally entangled regime, giving a single comparison of the achievable and forbidden rate--distance--locality region for CSS-like EA-qLRCs.

</details>


### [5] [The Exact Second Generalized Covering Radius of Binary Primitive Triple-Error-Correcting BCH Codes](https://arxiv.org/abs/2608.07215)
*Isaac Barouch Essayag,Aryeh Lev Zabokritskiy*

Main category: cs.IT

TL;DR: 本文精确确定了二进制本原三纠错BCH码（C_m = BCH(3,m)，长度为2^m-1）的第二广义覆盖半径：对所有m≥5，R_2(C_m)=8。这等价于每个二维症候子空间至多包含在8个校验列的二进制张成空间中，且最坏情况下8列是必要的。


<details>
  <summary>Details</summary>
Motivation: 广义覆盖半径是编码理论中衡量码字覆盖能力的重要参数，此前该BCH码族的第二广义覆盖半径仅知道下界，尚未精确确定。精确确定这一参数对整个BCH码族的理论研究和实际应用都有重要意义。

Method: 通过对二进制本原三纠错BCH码的校验矩阵（parity-check columns）结构进行分析，证明任意二维症候子空间可以被不超过8个校验列的二进制张成空间覆盖，同时通过构造反例说明8列在最坏情况下是必要的，从而匹配已知下界并完成精确确定。

Result: 对所有m≥5，精确得到第二广义覆盖半径R_2(C_m)=8。该结果与已知下界匹配，完整解决了整个二进制本原三纠错BCH码族这一参数问题。

Conclusion: 本文完全确定了二进制本原三纠错BCH码族的第二广义覆盖半径，为整个参数族给出了精确的闭合答案，填补了该领域的空白。

Abstract: Let $C_m=\operatorname{BCH}(3,m)$ be the binary primitive triple-error-correcting BCH code of length $2^m-1$. We determine its second generalized covering radius exactly: $R_2(C_m)=8$ for every $m\geq 5$. Equivalently, every two-dimensional syndrome subspace is contained in the binary span of at most eight parity-check columns, and eight columns are necessary in the worst case. This matches the known lower bound and settles the parameter for the entire binary primitive triple-error-correcting BCH family.

</details>


### [6] [Efficient Discrete Position Design for Movable Antenna Systems: Low Complexity and Robustness](https://arxiv.org/abs/2608.07413)
*Haonan Wang,Xianghao Yu,Rui Wang,Ang Li,Ying-Jun Angela Zhang*

Main category: cs.IT

TL;DR: 本文研究了离散可移动天线（MA）辅助的MU-MIMO上行通信系统中的互信息最大化问题。作者将该问题证明为单调子模最大化问题（受2系统约束），并提出了一种低复杂度的距离约束子模位置搜索算法，理论保证达到最优解的1/3。该算法在完美和非完美CSI下均能实现至少90%的最优MI增益，且计算复杂度比分支定界方法降低34.4倍。


<details>
  <summary>Details</summary>
Motivation: 可移动天线（MA）通过动态重塑天线阵列引入额外空间自由度以提升通信性能，但现有MA设计算法因离散定位选择导致极高的计算复杂度，阻碍了实际应用。因此需要寻找高效的解决方案来降低复杂度，同时保持优异的通信性能。

Method: 1) 在完美CSI假设下，将离散MA定位问题形式化为数学优化问题；2) 证明该设计问题属于"单调子模最大化受2系统约束"问题类别；3) 提出低复杂度的距离约束子模位置搜索算法，理论上证明至少能达到最优解的1/3；4) 将该方法扩展到非完美CSI场景，验证其对信道估计误差的鲁棒性。

Result: 数值结果表明：在完美和非完美CSI假设下，所提方案均能实现至少90%的最优解MI增益；算法实现数量级的复杂度降低，比分支定界方法快34.4倍，同时保持显著的MI增益。

Conclusion: 所提出的基于子模优化的MA设计算法在保证接近最优性能的同时，大幅降低了计算复杂度，为可移动天线技术的实际部署提供了可行的解决方案，且对信道估计误差具有鲁棒性。

Abstract: Building on advances in reconfigurable antenna techniques, movable antennas (MAs) can dynamically reshape antenna arrays and introduce additional spatial degrees of freedom (DoFs), thereby further improving communication performance. Despite these benefits, existing MA design algorithms often entail prohibitively high computational complexity from discrete positioning selection, which prevents practical implementations of MAs. In this paper, we investigate efficient solutions for the mutual information (MI) maximization problem of a multi-user multiple-input multiple-output (MU-MIMO) uplink communication system aided by discrete MAs. To this end, we first formulate the discrete MA positioning problem with the assumption of perfect channel state information (CSI). Then, we prove that the design problem falls into the category of monotone submodular maximization subject to a 2-system constraint. Accordingly, we propose a low-complexity distance-constrained submodular position search algorithm, which is theoretically shown to achieve at least 1/3 of the optimum. Furthermore, we extend our approach to scenarios with imperfect CSI, and show that the proposed submodular optimization-based design remains robust against channel estimation errors. Numerical results demonstrate that the proposed scheme can achieve at least 90% of the optimal solution's MI gain under both perfect and imperfect CSI assumptions. Remarkably, the algorithm achieves orders-of-magnitude complexity reduction (e.g., 34.4x faster than the branch-and-bound approach) while maintaining significant MI gains.

</details>


<div id='cs.DB'></div>

# cs.DB [[Back]](#toc)

### [7] [From Interpretation to Compilation: A Compilation-Based Execution Engine for Semantic Operator Systems](https://arxiv.org/abs/2608.06677)
*Wenkai Dong,Yifan Wang*

Main category: cs.DB

TL;DR: SemBaker是一个面向语义算子系统的编译式执行引擎，通过将LLM调用从数据循环中移出，仅需一次LLM调用生成确定性Python函数并在本地执行，配合成本优化器选择原生或编译执行路径，在保持处理质量的同时实现4.8-6.3倍加速和5.4-10.7倍成本降低。


<details>
  <summary>Details</summary>
Motivation: 现有语义算子系统采用基于解释的执行方式，即对每个数据项都调用LLM来解释算子谓词并产生结果。虽然表达力强，但这种设计将昂贵的模型调用置于数据处理循环内，导致延迟和成本随输入基数线性增长，成为系统扩展性的瓶颈。

Method: SemBaker作为外部插件而非替换后端原生执行，采用编译式执行：对选定的语义过滤、映射和连接算子，仅调用一次LLM生成确定性的Python函数，然后在本地执行该函数而无需逐项调用LLM。系统包含基于成本的优化器，为每个算子路由到原生或编译执行路径，同时编译过程与流水线执行重叠进行。通过轻量适配器支持Palimpzest、LOTUS、Nirvana和DocETL等系统。

Result: 在三个包含200个查询的QA工作负载上，SemBaker实现了平均4.8至6.3倍的加速，以及平均5.4至10.7倍的成本降低，同时处理质量与原有系统相当。

Conclusion: 编译式执行是提升语义算子系统性能的有效范式，通过将昂贵的LLM调用从数据处理热路径中移除，能够在不牺牲处理质量的前提下显著改善延迟和成本，且作为插件形式具有跨系统的良好兼容性。

Abstract: Semantic operators extend data processing with natural-language predicates. Existing semantic operator systems commonly execute these operators through interpretation-based execution: for every data item, an LLM interprets the operator predicate and directly produces the corresponding result. Although expressive, this design places expensive model invocations inside the data-processing loop, causing latency and monetary cost to scale with input cardinality.
  We present SemBaker, a compilation-based execution engine for semantic operator systems. SemBaker acts as an external plugin rather than replacing a backend's native execution. For selected semantic filters, maps, and joins, it invokes an LLM once to generate a deterministic Python function and executes that function locally without per-item LLM calls. A cost-based optimizer routes each operator to native or compiled execution, while compilation overlaps pipeline execution. SemBaker supports Palimpzest, LOTUS, Nirvana, and DocETL through thin adapters. Across three 200-query QA workloads, SemBaker achieves average speedups of 4.8 to 6.3 times and average cost reductions of 5.4 to 10.7 times, with competitive processing quality.

</details>


### [8] [Duration-constrained Interval Joins](https://arxiv.org/abs/2608.06856)
*Naoya Ehara,Daichi Amagata*

Main category: cs.DB

TL;DR: 本文提出了一种支持重叠时长约束的区间连接算法，解决了传统区间连接仅关注重叠存在而忽略重叠时长的问题，通过剪枝策略和优化技术显著提升了效率。


<details>
  <summary>Details</summary>
Motivation: 传统区间连接算法返回所有存在重叠的区间对，即使重叠非常微小，这些区间对可能是噪声或对后续分析无意义，同时增加了连接时间和计算开销。

Method: 提出了一种高效的时长约束区间连接算法，直接集成长度约束到连接过程中以避免生成不必要的区间对，并设计了两种优化技术进一步提升算法效率。

Result: 在三个真实世界区间数据集上进行了大量实验，结果表明该算法优于所有适用于该问题的现有技术。

Conclusion: 通过将重叠时长约束融入区间连接过程而非事后过滤，能够有效避免不必要的区间对生成和时长计算，从而大幅提升效率。

Abstract: Many databases, including temporal, uncertain, spatial, and trajectory databases, use interval data, and interval joins are among the most frequently used operators. Many studies proposed efficient interval join algorithms, but they do not consider the overlap duration. They return any pairs of intervals, even if they overlap very slightly, e.g., with no essential correlation or relationship. Subsequent applications may suffer from such interval pairs, as they may be noise or unnecessary for the analysis. Furthermore, outputting such pairs also increases join time. To address the above issues, this paper addresses the problem of duration-constrained interval join. Given two interval collections $R$ and $S$ and an overlap duration constraint $ε$, this problem returns all interval pairs $(r,s)$ such that $r \in R$, $s \in S$, and the overlap duration between $r$ and $s$ is at least $ε$. A straightforward approach for this problem is to run a state-of-the-art interval join algorithm and then filter qualified interval pairs. However, this is inefficient, as it generates unnecessary interval pairs and incurs duration computations, which cannot overcome the above efficiency concern. We propose an efficient algorithm for this problem that removes the above drawback. Furthermore, we propose two optimization techniques to improve the efficiency of our algorithm. We conduct extensive experiments on three real-world interval datasets, and the results demonstrate that our algorithm outperforms existing techniques applicable to our problem.

</details>


### [9] [MIRA: Evidence-Verified Repair Memory for Text-to-SQL Correction](https://arxiv.org/abs/2608.06950)
*Yining Liu,Chenyu Yang,Boyan Li,Rui Mao,Yuyu Luo*

Main category: cs.DB

TL;DR: MIRA是一个可插拔的SQL修正器，通过将历史修正转化为独立可复用的记忆条目，并利用数据库证据指导和适配记忆重用来修复Text-to-SQL生成的语义错误SQL，在BIRD和ScienceBenchmark上分别提升执行准确率16.53%和8.78%。


<details>
  <summary>Details</summary>
Motivation: Text-to-SQL代理生成的SQL虽然在语法上可执行，但语义上常常不正确。现有的SQL修正方法将多个错误及其修复打包成粗粒度的经验整体复用，导致引入无关编辑，甚至将原本正确的查询变错。因此需要细粒度的记忆重用来可靠地修复SQL。

Method: MIRA将历史修正转化为独立可复用的修复记忆条目；对每个当前查询，基于问题和SQL检索相关记忆条目；然后利用数据库证据验证每个记忆条目的相关性，最后将受支持的记忆条目适配到当前SQL上。MIRA是可插拔的修正器，不依赖参数更新。

Result: 在BIRD和ScienceBenchmark两个基准上，使用三个Text-to-SQL代理生成的1,785个测试查询（覆盖14个数据库）进行评估，MIRA分别提升了16.53%和8.78%的执行准确率。

Conclusion: MIRA证明了细粒度的记忆条目复用加上数据库证据适配，可以显著提升Text-to-SQL修正的可靠性，避免粗粒度经验复用带来的副作用，为SQL自动修正提供了有效且可插拔的解决方案。

Abstract: Text-to-SQL agents still produce executable yet semantically incorrect SQL. A reliable SQL corrector must repair incorrect queries without corrupting correct ones. Confirmed corrections from the same database can be reused without parameter updates. Existing methods, however, often bundle multiple errors and their repairs into a single coarse-grained experience. Applying the entire experience can introduce irrelevant edits and turn an initially correct query into an incorrect one. Reliable reuse therefore depends on three decisions: what to retain from a historical correction, when to activate the resulting memory, and how to adapt it to the current SQL. We propose MIRA (Memory-Item Reuse and Adaptation), a pluggable SQL corrector that uses database evidence to guide memory reuse. MIRA converts historical corrections into independently reusable repair memory items. For each current query, it retrieves memory items using the question and SQL. It then checks each item against database evidence and adapts the supported items to the current SQL. We evaluate 1,785 test queries generated by three Text-to-SQL agents across 14 databases from BIRD and ScienceBenchmark. MIRA improves execution accuracy by 16.53% and 8.78% on BIRD and ScienceBenchmark, respectively.

</details>


### [10] [RECAST: A Region-Scoped Adaptive Index for Exact Similarity Search](https://arxiv.org/abs/2608.06962)
*Yining Liu,Rui Mao*

Main category: cs.DB

TL;DR: RECAST是一个面向精确相似性搜索的区域感知自适应索引，通过按查询区域累积已计算距离并利用其剪枝效果变化来检测查询偏移，在五个真实数据集和四种工作负载模式下相比自适应基线AV-tree减少高达64%的距离计算和46%的查询时间。


<details>
  <summary>Details</summary>
Motivation: 现有精确相似性搜索索引（如预构建索引）无法适应查询流在特定区域的集中分布，而自适应索引（如AV-tree）虽然能利用查询过程中计算的距离来构建索引，但丢弃了许多已计算距离且未能有效组织这些距离以供复用，导致在查询集中区域表现不佳。

Method: RECAST维护查询区域，在每个区域内累积先前查询中已计算的距离（paid distances）用于精确剪枝，并通过监控这些距离剪枝效果的变化来推断查询是否仍集中在当前区域。当查询发生偏移时，RECAST递归地将查询工作分派给子区域，确保已计算的距离仅在其仍然有效的区域被累积和复用。

Result: 在五个真实数据集和四种工作负载模式下，RECAST的累计成本一致低于自适应基线和大多数预构建基线，相比最先进的自适应基线AV-tree，每查询距离计算减少高达64%，查询时间减少高达46%。

Conclusion: RECAST通过区域作用域的自适应索引设计，有效应对了真实查询流的局部集中特性，证明了按区域精细化管理已计算距离的复用策略能够显著提升精确相似性搜索的效率。

Abstract: Similarity search in metric spaces is widely used in bioinformatics, data mining, and recommender systems. Exact similarity search is dominated by distance computations, while real query streams often concentrate in specific regions rather than spreading uniformly across the space. Pre-built indexes are constructed before the query stream and cannot adapt when queries concentrate in poorly served regions. Adaptive indexes such as AV-tree build the index from distances computed while answering queries, but discard many of those distances and do not organize the retained distances effectively for reuse. We propose RECAST, a region-scoped adaptive index for exact similarity search. RECAST maintains query regions, accumulates distances already computed while answering earlier queries (paid distances) within each region for exact pruning, and uses changes in their pruning effect to infer whether incoming queries remain concentrated in the current region. When queries shift, RECAST recursively dispatches query work to child regions, so paid distances are accumulated and reused only where they remain effective. On five real-world datasets under four workload patterns, RECAST achieves consistently lower cumulative cost than the adaptive baseline and most pre-built baselines, reducing per-query distance computations by up to 64% and query time by up to 46% compared with the state-of-the-art adaptive baseline AV-tree.

</details>


### [11] [Toward a Causal Data Management Ecosystem for Decision Making and Agentic AI](https://arxiv.org/abs/2608.07214)
*Dazhuo Qiu,Yingli Zhou,Amedeo Pachera,Angela Bonifati,Andrea Mauri*

Main category: cs.DB

TL;DR: 现代AI已从单一模型演变为由经典ML预测器、深度多模态模型、大语言模型和智能体组成的生态系统，其运作本质上是数据集成问题。然而仅靠集成不足以支撑可信决策，因为预测常受混杂因素干扰。论文提出需要显式因果层，并主张构建一个共享、持久、可查询的"因果世界系统"（CWS）来弥合关联与因果之间的差距，支持智能体的规范性和反事实分析。


<details>
  <summary>Details</summary>
Motivation: AI生态系统中的知识分散在数十个异构、独立治理的数据源中，需要协调和持续维护，这是数据集成问题；但集成本身不足以支持可信行动——预测结果受多个相互作用因素影响，事件、决策和变量与仅仅伴随的因素纠缠在一起，基于关联信号行动会导致混杂决策。尤其当智能体自主行动时，必须预期行动的后果而不能仅从历史共现中外推。

Method: 论文提出构建因果世界系统（Causal World System, CWS）——一个共享、持久、可查询的显式因果层，叠加在AI生态系统之上，用于区分结果的驱动因素与相关因素，从而实现规范性分析和反事实分析。

Result: 论文主要论证了因果推理是弥合关联与因果差距的关键，并提出了CWS作为实现该目标的架构构想（属于立场/愿景论文，无具体实验结果）。

Conclusion: 集成AI生态系统需要显式的因果层作为基础设施，CWS是实现可信、可靠自主智能体的必要架构方向。

Abstract: Modern AI is no longer a single model but an ecosystem: classical ML predictors, deep and multimodal models, large language models, and agents, each trained and tuned over different data sources and each producing outputs at scale that become inputs to the others. Operating such an ecosystem is fundamentally a data integration problem - the knowledge it depends on is fragmented across dozens of heterogeneous, independently governed sources that must be reconciled and continually maintained. Yet integration alone is not enough. The predictions these systems make are shaped by many interacting factors, and the events, decisions, and variables that drive an outcome are routinely entangled with the ones that merely accompany it; treated as a basis for action, such correlational signals invite confounded decisions. This becomes acute once agents act autonomously: to be trustworthy and reliable, an agent must anticipate the consequences of its actions, not merely extrapolate from what has co-occurred before. Causal reasoning is what closes this gap, distinguishing the drivers of an outcome from its correlates, and enabling prescriptive and counterfactual analysis over the ecosystem's data. We therefore argue that the integrated ecosystem needs an explicit causal layer, and we propose to build it as a shared, persistent, queryable Causal World System (CWS).

</details>


<div id='cs.DS'></div>

# cs.DS [[Back]](#toc)

### [12] [A Divide-and-Conquer Engine for Lexicographical Permutations: Accelerating State Evolution via Hybrid Software-Hardware CPU Instructions](https://arxiv.org/abs/2608.06384)
*Yusheng Hu*

Main category: cs.DS

TL;DR: 本文提出LexCHA，一种硬件-软件协同设计的架构，通过原生SIMD指令加速字典序排列生成。利用排列的"分形同构"特性，将全局状态演化解耦为宏软件unranking阶段和微硬件块构建阶段，并用预计算的确定性掩码流替代条件分支，实现无分支执行流，显著超越std::next_permutation等标量实现。


<details>
  <summary>Details</summary>
Motivation: 传统字典序排列算法（如std::next_permutation）受限于密集的控制逻辑和频繁的分支预测失败，成为性能瓶颈。作者希望打破这一局限，通过硬件-软件协同设计来突破标量实现的吞吐率上限。

Method: LexCHA利用排列的分形同构性质，将问题解耦为两个阶段：宏观的软件unranking阶段（管理全局状态演化）和微观的硬件块构建阶段（利用SIMD指令进行流式向量混排）。通过预计算的确定性掩码流，将传统条件分支替换为向量shuffle操作，实现无分支执行。

Result: 评估显示，LexCHA在现代处理器的标量实现基础上实现了显著更高的吞吐率，大幅超越现有最先进的std::next_permutation实现。

Conclusion: LexCHA证明了通过硬件-软件协同设计，结合排列的数学结构与SIMD硬件能力，可以根本性地消除排列算法中的分支瓶颈，为深度学习范式下的排列生成提供了新的高性能路径。

Abstract: Traditional lexicographical permutation algorithms, epitomized by \texttt{std::next\_permutation}, are fundamentally bottlenecked by dense control logic and frequent branch mispredictions. This paper introduces LexCHA(Lexicographical Co-designed Hardware Acceleration), a novel hardware-software co-designed architecture that accelerates permutation via native SIMD instructions. Exploiting the inherent fractal isomorphism of permutations, LexCHA decouples global state evolution into a macro software unranking phase and a micro hardware block-construction phase. By utilizing pre-computed deterministic mask streams, LexCHA replaces traditional conditional branching with streaming vector shuffles, achieving a branch-free execution flow. Evaluations demonstrate that LexCHA demonstrates significantly higher throughput than scalar implementations of modern processors, outperforming existing state-of-the-art \texttt{std::next\_permutation} implementations significantly.

</details>


### [13] [Improved Algorithms for Learning Fourier-sparse Signals](https://arxiv.org/abs/2608.06385)
*Dongrun Cai,Xue Chen,Xiaowei Shao,Yile Wang*

Main category: cs.DS

TL;DR: 本文研究经典稀疏傅里叶变换问题：在固定时间窗口[-T,T]内，面对有界ℓ2范数对抗性噪声，学习具有任意（离网）频率的k-傅里叶稀疏信号x(t)。主要贡献包括：(1) 将插值的样本复杂度从之前的k⁴·(log FT)^O(1)大幅改进为k²·O(log(kFT/ε))²，虽运行时间为(kFT/ε)^O(k)；(2) 提出高效算法，第一种需m=k^{3.75}·(log FT)^O(1)个样本和m^{ω+o(1)}时间；在信号增长不超过切比雪夫多项式的假设下，第二种算法进一步将样本复杂度降至k³·(log FT)^O(1)。


<details>
  <summary>Details</summary>
Motivation: 稀疏傅里叶变换是一个可追溯到1795年Prony工作的经典问题。实际应用中，频率往往是"离网"的（任意位于带限[-F,F]内），且信号会受到对抗性噪声干扰。之前算法的样本复杂度上界为k⁴·(log FT)^O(1)，与理论下界Ω(k log FT)之间存在较大差距，因此需要更优的插值算法来缩小这一差距。

Method: 论文提出两类结果：(1) 第一个结果通过改进的稀疏插值技术，将样本复杂度从k⁴降至k²·O(log(kFT/ε))²，与下界Ω(k log FT)的差距约为k倍；(2) 第二个结果给出高效算法：第一种方法采用m=k^{3.75}·(log FT)^O(1)个样本，通过矩阵乘法加速（指数ω）实现m^{ω+o(1)}时间；第二种方法在假设k-傅里叶稀疏信号的任何增长不超过(k-1)次切比雪夫多项式增长的条件下，将样本复杂度进一步降至k³并保持类似的时间复杂度。

Result: (1) 样本复杂度上界从k⁴·(log FT)^O(1)改进为k²·O(log(kFT/ε))²，与下界Ω(k log FT)的差距缩小到约k倍；(2) 提供两种高效插值算法，分别达到k^{3.75}和k³的样本复杂度，时间均为m^{ω+o(1)}（ω为矩阵乘法指数）。

Conclusion: 本文显著改进了离网稀疏傅里叶信号在噪声下插值的样本复杂度，将上界从k⁴降至k²，并提供了接近最优的高效算法。虽然第一个结果在运行时间上仍为指数级，但第二个结果证明了在合理假设下可以实现多项式时间的近最优插值，缩小了样本复杂度上下界之间的差距。

Abstract: A classical problem in sparse Fourier transforms, which dates back to the work by Prony in 1795 at least, is to learn a $k$-Fourier-sparse signal $x(t):=\sum_{j=1}^k α_j e^{2 π\mathbf{i} f_j t}$ with arbitrary frequencies $f_1,\ldots,f_k$. We study this problem of learning $x(t)$ in a fixed time window $[-T,T]$ under adversarial noise with bounded $\ell_2$ norm, where the frequencies $f_1,\ldots,f_k$ may be "off-grid" -- arbitrarily located in a given bandlimit $[-F,F]$. In particular, our goal is to output a sparse interpolation $\tilde{x}$ such that $\tilde{x}(t) \approx x(t)$ in the time window $[-T,T]$.
  1. Our first result shows that the sample complexity of interpolation is $k^2 \cdot O(\log \frac{k FT}ε)^2$. While its running time is $(\frac{k FT}ε)^{O(k)}$, this improves the previous upper bound $k^{4} \cdot (\log FT)^{O(1)}$ on the sample complexity substantially and leaves a gap of about $k$ to the lower bound $Ω(k \log FT)$.
  2. Our second result provides efficient algorithms to interpolate $x(t)$. The first algorithm takes $m=k^{3.75} \cdot (\log FT)^{O(1)}$ samples and $m^{ω+o(1)}$ time ($ω$ is the matrix multiplication exponent). Assuming that the growth of any $k$-Fourier-sparse signal cannot be significantly larger than the growth of the degree-$(k-1)$ Chebyshev polynomial -- specifically, $x(t) \le e^{k \cdot O\big( \sqrt{\frac{|t|}{T}-1} \big)} \cdot \underset{s \in [-1,1]}{\max} |x(s)|$ for any $t \notin [-T,T]$, the second algorithm further improves the sample complexity to $m'=k^{3} \cdot (\log FT)^{O(1)}$ and the time complexity to $(m')^{ω+o(1)}$.

</details>


### [14] [The Price of Order in the Logarithmic Method](https://arxiv.org/abs/2608.06388)
*Sichen Wang,Zhipeng Lu,Jingbang Chen*

Main category: cs.DS

TL;DR: 本文研究了插入仅限（insertion-only）场景下的对数方法（logarithmic method）静态到动态转换，在强物化合并栈模型中分析了写-读乘积（write-read product）的界限。结果表明：成员查询和本地证书的最优写读乘积为 Θ(n log² n)，有序/范围查询为 Θ(n log³ n)，而选择（select）查询为 Θ(n²)。这揭示了查询在扫描前揭示信息的多少决定了动态开销的本质。


<details>
  <summary>Details</summary>
Motivation: 对数方法是一种经典的静态到动态转换技术，其组件与合并（component-and-merge）的机制也是写优化有序索引的基础。理解在该框架下插入与查询之间的根本权衡（以写读乘积度量）对设计高效动态有序数据结构至关重要，特别是要弄清精确有序查询和选择查询为何需要额外的对数甚至二次方的开销。

Method: 作者在强物化合并栈模型（strongly materialized merge-stack model）中分析插入n个元素后的行为，该模型假设组件不可变、按顺序合并、查询时对活动组件进行一次正向扫描。通过推导不同查询类型（成员查询、局部证书、有序查询、范围查询、选择查询）下的写读乘积下界与上界，获得精确的 Θ 界限。

Result: 给出了各查询类型的最优写读乘积：成员查询和本地证书为 Θ(n log² n)；具有命名键或端点的有序和范围查询为 Θ(n log³ n)；选择查询为 Θ(n²)。这些界限表明对数方法并非对所有查询都施加统一的动态开销。

Conclusion: 在对数方法的物化单向访问模型下，最优开销取决于查询在扫描开始前揭示的信息量。这精确定位了访问模型障碍：额外的对数来自于精确有序/范围查询，而选择查询则面临二次方屏障。该发现为理解动态有序数据结构的性能极限提供了理论基础。

Abstract: The logarithmic method is a classical static-to-dynamic transformation: it stores one dynamic ordered set as several immutable static components and rebuilds them by merges. The same component-and-merge discipline underlies write-optimized ordered indexes, where cheap insertions must be reconciled with exact ordered queries. In this paper, we study the insertion-only version after $n$ insertions, over abstract keys, in a strongly materialized merge-stack model with sequential component merges and one forward scan of the live components per query. We bound the product between the total amount of data written during the $n$ insertions and the worst-case amount of data read by a single query, known as the write-read product. The optimal bounds are as follows:
  - Membership and local certificates: $Θ(n\log^2 n)$.
  - Order and range queries with named keys or endpoints: $Θ(n\log^3 n)$.
  - Select: $Θ(n^2)$.
  Thus, the logarithmic method does not impose a universal dynamic overhead: under materialized one-way access, the optimum depends on what information the query reveals before the scan starts. This pinpoints the access-model obstruction behind the extra logarithm for exact order and range queries, and the quadratic barrier for select.

</details>


### [15] [Online Correlation Clustering with Metric Weights](https://arxiv.org/abs/2608.06566)
*Sami Davies,Benjamin Moseley,Heather Newman*

Main category: cs.DS

TL;DR: 本文研究了在概率约束和三角不等式约束下的加权相关聚类（correlation clustering）的在线版本。作者给出了第一个针对该自然最小化变体的确定性在线算法，其竞争比为O(1)，突破了标准在线相关聚类中Ω(n)的下界。


<details>
  <summary>Details</summary>
Motivation: 标准在线相关聚类问题过于困难，即使随机化算法也无法获得优于Ω(n)的竞争比。先前工作通过松弛在线模型（如允许反悔、随机到达顺序或用离线样本初始化）来规避该下界。本文的核心动机是探索输入本身的结构是否足以克服这个下界，而非修改在线模型。

Method: 研究在概率约束（w^+_uv + w^-_uv = 1）和三角不等式约束（负权重w^-满足三角不等式）下的加权相关聚类在线版本。作者设计了一个确定性在线算法，该算法在对抗性到达顺序下维持聚类，并保证总加权不一致成本在离线最优的O(1)因子范围内。

Result: 给出了第一个在完全在线模型下针对自然最小化相关聚类变体的常数竞争比在线算法，竞争比为O(1)（与离线最优相比），且能对抗对抗性到达顺序。

Conclusion: 结果表明，边权上的度量一致性（metric consistency）属性能够在在线相关聚类中区分可处理与不可处理的实例，揭示了输入结构在克服在线算法下界中的关键作用。

Abstract: The standard online version of correlation clustering is prohibitively hard, as even randomized algorithms cannot achieve competitive ratio better than $Ω(n)$. Prior works bypass this lower bound by relaxing the online model through recourse, random arrival order, or seeding the algorithm with an offline sample of the underlying input. We instead ask whether additional structure in the input itself can overcome this lower bound. We study weighted correlation clustering under probability constraints, where $w^+_{uv}+w^-_{uv}=1$ for every $uv$ edge, and triangle inequality constraints, where the negative weights $w^-$ satisfy triangle inequality. While this version of correlation clustering is well-studied in the offline setting, we initiate its online study and give a deterministic online algorithm that maintains a clustering whose total weighted disagreement cost is within an $O(1)$ factor of the offline optimum, against adversarial arrival order. This is the first constant-competitive online algorithm for a natural minimization variant of correlation clustering in the fully online model, and shows that metric consistency on the edge weights separates tractable from intractable online instances.

</details>


### [16] [Online Multi-Level Aggregation with Per-Batch Maximum Delay](https://arxiv.org/abs/2608.06796)
*Tianhang Lu,Runtian Ren,Shengcai Liu*

Main category: cs.DS

TL;DR: 本文研究有限有根树上的在线多级聚合问题，目标是最小化每批服务的最大延迟。论文证明离线最优解具有连续的到达块规范化形式，可通过多项式时间动态规划计算。基于此动态规划设计了DP-Envelope在线算法族：确定性版本达到2-竞争比，随机化版本（采样全局参数）达到e/(e-1)-竞争比，两者均在非退化有根树上最优。


<details>
  <summary>Details</summary>
Motivation: 解决在线多级聚合问题（multi-level aggregation）中每批最大延迟目标下的最优竞争比问题，填补确定性下界（固定节点）与随机化下界匹配的空白，并将结果推广到更一般的静态服务系统。

Method: 1) 证明离线最优解存在"连续到达块"（consecutive-arrival-block）规范化形式，据此构建多项式时间动态规划。2) 用该动态规划定义截止时间，构建DP-Envelope在线算法族。3) 确定性版本采用端点参数实现2-竞争比；随机化版本通过对全局参数采样（密度e^θ/(e-1)）实现e/(e-1)-竞争比。4) 先在直线度量上建立直觉，再推广到一般树及更广泛的次模服务成本系统。

Result: 确定性算法达到2-竞争比（匹配已知固定节点下界），随机化算法达到e/(e-1)-竞争比（证明匹配的随机化下界），两项保证在任意非退化有根树上均为最优。结果进一步扩展到任意可实现的静态服务系统，只要联合服务成本是归一化、非递减、次模的。

Conclusion: 本文在在线多级聚合问题上同时给出了确定性和随机化最优竞争比，且证明方法具有一般性，可推广到更广泛的带次模服务成本的服务系统，为该类问题的算法设计提供了统一的动态规划框架。

Abstract: We study online multi-level aggregation on finite rooted trees with a per-batch maximum-delay objective. A service pays for a rooted subtree and for the maximum waiting time among the requests cleared by that service. We show that the offline optimum admits a consecutive-arrival-block normal form and can be computed by a polynomial-time dynamic program. The same dynamic program defines the deadlines of a family of online algorithms, which we call DP-Envelope. Its deterministic endpoint is $2$-competitive. Sampling one global parameter with density $e^θ/(e-1)$ leads to an $e/(e-1)$-competitive randomized algorithm against an oblivious adversary. The deterministic guarantee matches the known fixed-node lower bound, and we prove a matching randomized lower bound. Thus, both guarantees are optimal on every nondegenerate rooted tree. We first develop the line metric as a warm-up, where the algorithm and its nested block partitions have a direct geometric interpretation. Finally, we show that the upper bounds extend to every realizable static service system with a normalized, nondecreasing, submodular joint service cost.

</details>


### [17] [Stochastic Gradient Meets Randomized Rounding: New Algorithms for Node-Weighted Steiner Problems](https://arxiv.org/abs/2608.06807)
*Joseph Koutsoutis,Jesse Lerner,Roie Levin,Jiawei Yu*

Main category: cs.DS

TL;DR: 本文提出了一种新的O(log n)近似算法，同时解决节点加权斯坦纳树和节点加权斯坦纳森林问题。该算法匹配Klein & Ravi的最优界（除非P=NP不可能更好），并能在终端对随机顺序到达的在线场景中工作。方法结合了LearnOrCover框架和Berman & Coulston的Augmented Greedy算法，离线运行时简化为一种随机舍入方案。


<details>
  <summary>Details</summary>
Motivation: 节点加权斯坦纳树/森林问题是最优近似度已知的经典问题（O(log n)），但现有算法仅适用于离线场景。作者希望实现相同近似比的同时，能够应对终端对随机顺序到达的在线设置，这是一个此前未解决的挑战。

Method: 作者将两种互补的算法工具相结合：(1) Gupta等人提出的LearnOrCover框架（FOCS '21），(2) Berman & Coulston针对边加权在线斯坦纳森林的Augmented Greedy算法。单独使用任何一种都不够，但两者的分析可以相互配合（dovetail）以证明O(log n)的保证。此外，离线运行时该算法退化为简单的随机舍入方案，从概念上将节点加权斯坦纳森林归约到边加权斯坦纳森林。

Result: 实现了O(log n)近似比的在线算法，适用于节点加权斯坦纳树和节点加权斯坦纳森林问题，终端对按随机顺序到达。该界与Klein & Ravi的离线最优界匹配，且没有P=NP假设下的下界矛盾。

Conclusion: 通过巧妙地结合LearnOrCover框架与Augmented Greedy算法，作者成功地将最优近似比扩展到在线随机顺序设置。离线情况下的随机舍入视角为节点加权到边加权问题的归约提供了新的思路，可能具有更广泛的应用前景。

Abstract: We give a new $O(\log n)$ approximation algorithm for Node Weighted Steiner Tree and Node Weighted Steiner Forest. Our algorithm matches the bounds of Klein & Ravi [J. Algorithms '95] which are best possible unless P = NP, but have the advantage that they work in the online setting when the terminal pairs are revealed in random order.
  To obtain our results, we combine the LearnOrCover framework due to Gupta, Kehne, Levin [FOCS '21] with the Augmented Greedy algorithm of Berman & Coulston [STOC '97] for online edge-weighted Steiner Forest. Neither algorithm suffices on its own, but the analyses dovetail to imply our guarantee. Run offline, the algorithm reduces to a very simple randomized rounding scheme that (in spirit) reduces Node Weighted Steiner Forest to Edge Weighted Steiner Forest, and we hope this idea finds further applications.

</details>


### [18] [From Enumeration to Covering: Near-Optimal Densest P-Partite Subgraph Search over Large Heterogeneous Information Networks](https://arxiv.org/abs/2608.06906)
*Lu Chen,Chengfei Liu,Rui Zhou,Jiajie Xu,Jianxin Li*

Main category: cs.DS

TL;DR: 本文研究异构信息网络（HIN）中的最稠密P部子图问题，提出了首个超越二部图情况的近最优密度近似算法。通过用多对数级别的代表权重集覆盖所有可行权重集（而非穷举），并将每个固定权重子问题转化为加权超模稠密子图实例进行近最优求解，实现了(1-δ)/(1+η)的近似保证，同时为每个固定i提供PTAS。算法采用自适应剥离方案，无需物化超大规模元路径实例，并通过 incumbent 驱动归约进一步加速。


<details>
  <summary>Details</summary>
Motivation: 现有最先进方法对每个可行权重集（数量达O((n/i)^i)）逐一求解，且每项仅能达到1/i近似保证。作者质疑这种穷举枚举和松散逼近是否必要，旨在突破二部图（i=2）之外没有近最优密度近似的瓶颈。

Method: (1) 用覆盖替代枚举：构建多对数级别且有数据依赖界限制的代表权重集，覆盖所有可行权重集，仅损失可调因子1+η；(2) 将每个固定权重子问题建模为加权超模稠密子图实例并近最优求解，整体近似保证提升至(1-δ)/(1+η)；(3) 自适应剥离求解器不物化元路径实例（其数量可超图规模数个数量级）；(4) incumbent 驱动归约在求解前丢弃无效代表权重集。

Result: 在五个真实HIN上的实验表明，所提算法相比基于枚举的基线实现了显著加速，并能验证返回子图的近最优性。这是首个二部图（i=2）之外情形的近最优密度近似，为每个固定i提供了PTAS。

Conclusion: 作者证明穷举枚举和松散逼近均可避免，实现了对任意固定i情形的近最优近似（PTAS），在理论保证和实际效率两方面都显著优于现有方法。

Abstract: Given a heterogeneous information network (HIN) and a query meta-path P of length i, the densest P-partite subgraph problem finds the subgraph, spanning the i typed layers of P, that maximizes a parameter-free density: the number of meta-path instances over the geometric mean of the layer sizes. It has applications across bibliographic, e-commerce, and biomedical networks. The state-of-the-art approximation linearizes the geometric-mean objective by fixing per-layer weights, but solves one subproblem for every feasible weight set, of which there are $O((n/i)^i)$, and on each achieves only a $1/i$ approximation. We show that neither the exhaustive enumeration nor the loose guarantee is necessary. First, we replace enumeration by covering: polylogarithmically many representative weight sets, localized further by a data-dependent bound, cover all feasible ones while losing only a tunable factor $1+η$ in density. Second, we cast each fixed-weight subproblem as a weighted supermodular densest-subgraph instance and solve it near-optimally, lifting the overall guarantee to $(1-δ)/(1+η)$. To our knowledge, this is the first near-optimal density approximation beyond the bipartite ($i=2$) case, and it yields a PTAS for every fixed i. Algorithmically, our solver is an adaptive peeling scheme that never materializes the meta-path instances, whose number can exceed the graph size by orders of magnitude. An incumbent-driven reduction further discards representative weight sets before their subproblems are solved. Experiments on five real HINs show that our algorithms achieve substantial speedups over enumeration-based baselines and can further certify the near-optimality of the returned subgraph.

</details>


### [19] [Streaming Algorithms for Monotonicity Testing](https://arxiv.org/abs/2608.07073)
*Amir Azarmehr,Soheil Behnezhad,Lily Chung,Alma Ghafari,Jane Lange,Ronitt Rubinfeld*

Main category: cs.DS

TL;DR: 本文首次在流式环境下研究单调性测试问题的空间复杂度。给定一个n顶点的DAG（边以任意顺序到达）和布尔函数f，目标是使用Õ(n)空间逼近f到单调性的距离。主要结果是提出了一个(2+ε)逼近算法，需要√n^{1+o(1)}次pass，并证明这是任何O(1)逼近算法的最优pass复杂度（除非改进st-可达性的流式算法）。


<details>
  <summary>Details</summary>
Motivation: 单调性测试的查询复杂度已被广泛研究，但空间复杂度（尤其是流式设置下的空间复杂度）此前未被探索。本文填补了这一空白，首次研究在流式环境中（边按任意顺序到达，空间远小于图规模）估计到单调性距离的问题。

Method: 核心方法是将问题规约为在G的传递闭包（子图）中估计最大匹配的大小。作者首次将次线性时间算法与流式设置联系起来。虽然现成的次线性算法只能得到n√n^{1+o(1)} pass的算法，但作者通过允许更强类型的查询（如顶点查询和子集查询），且这些查询可以像标准邻接矩阵和列表查询一样高效实现，从而显著改进了pass复杂度。

Result: 提出了(2+ε)逼近单调性距离的算法，运行√n^{1+o(1)}次pass。同时证明对于任何O(1)逼近，这个pass复杂度是最优的（除非在st-可达性流式算法上取得突破）。

Conclusion: 本文确立了流式单调性测试的pass-空间权衡，并将次线性时间算法与流式算法建立联系。下界表明该问题与st-可达性这一经典流式难题紧密相关，为进一步研究提供了方向。

Abstract: Consider a poset - or equivalently an $n$-vertex DAG $G=(V, E)$ - and a boolean function $f: V \rightarrow \{0, 1\}$ on its vertex set. We say $f$ is monotone if $f(u) \leq f(v)$ for all $(u, v) \in E$. While there is extensive literature on the query complexity of testing monotonicity, we focus instead on the space complexity and initiate the study of this problem in the streaming setting. Namely, the edges of $G$ arrive in an arbitrary order, and the goal is to estimate distance to monotonicity of a given function $f$ using $\widetilde{O}(n)$ space. Note that while this space allows receiving and storing $f$, it is much smaller than the input graph $G$ which could have up to $Ω(n^2)$ edges.
  Our main result is an algorithm that $(2+ε)$-approximates distance to monotonicity in $\sqrt{n}^{1+o(1)}$ passes. We also prove that this is the best pass-complexity one can hope for, for any $O(1)$-approximation, short of improving the state-of-the-art streaming algorithm for $st$-reachability, which is a very well-studied problem.
  On the technical side, our algorithm approximates the size of maximum matching in (a subgraph of) the transitive closure of $G$. While the maximum matching problem has received significant attention in the streaming setting, the fact that we are computing it in the transitive closure requires very different ideas. In fact, a main contribution of our work is to connect sublinear time algorithms for estimating the maximum matching size to the streaming setting for the first time. While existing off-the-shelf sublinear time algorithms only result in an $n\sqrt{n}^{1+o(1)}$ pass algorithm in our setting, we show how to significantly improve upon them by allowing stronger queries (such as vertex and subset queries) that can be implemented just as efficiently as more standard adjacency matrix and list queries for our problem.

</details>


### [20] [Near-Optimal Replacement Path Coverings](https://arxiv.org/abs/2608.07124)
*Davide Bilò,Keerti Choudhary,Sarel Cohen,Martin Schirneck*

Main category: cs.DS

TL;DR: 本文研究图论中(L,f)-替换路径覆盖（RPC）问题的覆盖值上下界，给出两个简洁的构造，将覆盖值精确至近紧的~Θ((L+f)^{L+f}/(L^L f^f))·poly(f)，适用于f=O(L)的广泛范围。


<details>
  <summary>Details</summary>
Motivation: RPC是容错数据结构设计中的重要工具，此前的上下界之间存在差距：Weimann和Yuster给出上界~O(fL^f)，Karthik和Parter给出下界Ω((L/f)^f)，Bilò等人仅在小敏感度f=o(log L)或互补范围给出了改进结果，但真实覆盖值仍是一个开放问题。

Method: 作者提出了两个令人惊讶的简洁构造：一个改进上界，一个改进下界，从而在f=O(L)的广泛范围内将RPC覆盖值确定为近紧的~Θ((L+f)^{L+f}/(L^L f^f))·poly(f)。

Result: 获得了RPC覆盖值的近紧界~Θ((L+f)^{L+f}/(L^L f^f))·poly(f)，将有效范围从之前的f=o(log L)（上界）和f=Ω(log L)（下界）扩展到f=O(L)。

Conclusion: 通过两个简洁的构造，作者解决了RPC真实覆盖值的开放问题，在f=O(L)的范围内给出了近紧的覆盖值，显著统一并改进了此前的工作。

Abstract: Let $L$ and $f$ be positive integers. An $(L,f)$-replacement path covering (RPC) for a graph $G$ is a family $\mathcal{G}$ of subgraphs such that, for every set $F$ of at most $f$ edges, there is a subfamily $\mathcal{G}_F \subseteq \mathcal{G}$ with the following properties. (1) No subgraph in $\mathcal{G}_F$ contains an edge of $F$. (2) For each pair of vertices $s,t$ that have a shortest path in $G{-}F$ with at most $L$ edges, one such path also exists in some subgraph in $\mathcal{G}_F$. The total number $|\mathcal{G}|$ of subgraphs is called the covering value.
  RPCs are an important tools in the design of fault-tolerant data structures. Weimann and Yuster [TALG 2013] presented an RPC with covering value $\widetilde{O}(f L^f)$. Karthik and Parter [TALG 2024] showed that $Ω( (L/f)^f )$ subgraphs are necessary. Recently, Bilò, Chechik, Choudhary, Cohen, and Schirneck [ICALP 2026] devised a new approach for very small sensitivities $f = o(\log L)$ with covering value $\widetilde{O}(f e^f (L/f)^{f+o(1)})$. They also showed that any RPC in the complementary range $f = Ω(\log L)$ must contain $Ω( (\sqrt{f e^f}/L) \cdot (L/f)^f)$ subgraphs. This left open the question of what is the true covering value.
  We give two surprisingly simple constructions that improve both the upper and lower bound. This results in a near-tight covering value of $\widetildeΘ(\frac{(L+f)^{L+f}}{L^L f^f}) \cdot \mathsf{poly}(f)$ for the much wider range of $f = O(L)$.

</details>


### [21] [The Sync Heap: Delete First, Ask Questions Later](https://arxiv.org/abs/2608.07134)
*Benjamin Aram Berendsohn,Egor Gorbachev,László Kozma*

Main category: cs.DS

TL;DR: 本文通过将"删除最小元素"与"向用户揭示被删除元素身份"两个操作解耦，设计了一种名为sync heap的新型数据结构，打破了比较模型下堆操作中插入和删除最小元素至少其一必须对数时间的经典假设，实现了插入和删除的常数摊还时间支持。


<details>
  <summary>Details</summary>
Motivation: 堆（优先队列）是计算机科学中最基本的数据结构之一，教科书长期假设在比较模型中插入和删除最小元素至少有一个操作必须花费对数时间。作者质疑这一假设，认为对数瓶颈并非删除操作本身的固有代价，而是"立即学习被删除元素身份"的信息成本。

Method: 作者设计了sync heap数据结构，其核心思想是延迟操作的执行和整理，直到查询操作强制它"同步"并揭示当前状态。该结构使用Chazelle在最小生成树算法中引入的soft heap作为关键组件，通过解耦删除本身与揭示删除元素身份的过程来绕过排序障碍。

Result: 当用户仅常数次检查堆状态时，插入和删除操作都可以在常数摊还时间内完成。作为应用，该结果将教科书中单位时间调度问题从O(n log n)改进到最优的O(n)。该数据结构简单、基于比较且确定性的，结果达到渐近最优。

Conclusion: 该工作表明对数屏障并非删除最小值的固有成本，而是"立即获知被删除元素"的信息代价。通过解耦这两个概念并引入sync heap，作者实现了堆操作复杂性的新权衡，并证明在特定场景下插入和删除都可以达到常数摊还复杂度。

Abstract: Heaps (priority queues) are among the best-studied data structures in computer science. In this paper, we critically revisit the textbook assumption that in the comparison model at least one of the two standard heap operations of inserting an element and deleting the minimum must take logarithmic time.
  By decoupling the deletion itself from the act of revealing the identity of the deleted element to the user, we avoid the sorting barrier and obtain a novel trade-off between the complexities of heap operations. This shows that the logarithmic barrier is not inherently the cost of deleting the minimum but rather the information cost of immediately learning which element was deleted. In the special case when the user inspects the heap state only constantly many times, we show that both insertions and deletions can be supported in constant amortized time. As an application, this yields a runtime improvement from $\mathcal{O}(n \log n)$ to the optimal $\mathcal{O}(n)$ for a textbook unit-time scheduling problem.
  We obtain our results by designing a new data structure, the sync heap, which gains speed by rearranging and compacting its operations until queries force it to synchronize and reveal its state. As a key component, we use the soft heap introduced by Chazelle as part of his minimum spanning tree algorithm. Our data structure is simple, comparison-based, and deterministic, and our results are asymptotically optimal.

</details>


### [22] [A Tight Bound for Facial Distance Patterns in Planar Graphs](https://arxiv.org/abs/2608.07187)
*Viktor Fredslund-Hansen,Shay Mozes,Oren Weimann*

Main category: cs.DS

TL;DR: 本文研究平面图中基于面顶点距离模式的数量上界问题。先前Li和Parter证明O(k³)的上界，本文将其改进为O(k²)，匹配已知下界并解决了ISAAC'22中的猜想。该改进带来三个直接推论（Okamura-Seymour度量压缩、常数时间精确距离或acles的空间改进、分布式直径计算加速），并产生一个新推论：集中式Õ(n^{8/5})时间的直径计算算法，优于SODA'18的Õ(n^{5/3})算法。


<details>
  <summary>Details</summary>
Motivation: 平面图中从任意顶点到指定面（含k个顶点）的距离模式数量上界是影响多个图算法（如度量压缩、距离或acles、直径计算）空间与时间复杂度的关键参数。从O(k³)改进到O(k²)不仅匹配已知下界、解决开放猜想，还能直接优化多个下游算法的性能。

Method: 本文采用了一个"简单的证明"（由OpenAI的GPT 5.6-Sol模型发现），将距离模式数量的上界从O(k³)降至O(k²)。具体证明细节未在摘要中展开，但该证明被描述为简单，并成功匹配了已知下界。随后将该新界代入三个已知结果框架中，并推导出新的直径计算算法。

Result: (1) 距离模式上界从O(k³)改进为O(k²)，匹配下界；(2) 改进Okamura-Seymour度量的压缩；(3) 减少常数时间精确距离或acles所需空间；(4) 加速分布式直径计算算法；(5) 新发现集中式Õ(n^{8/5})时间直径算法，优于之前的Õ(n^{5/3})。

Conclusion: 通过将距离模式数量上界紧化为O(k²)，本文不仅解决了开放猜想，还产生了多个重要推论，尤其是在集中式直径计算方面实现了显著加速，揭示了无权与有权平面图在直径计算时间上的新差距。

Abstract: Let $G$ be an undirected unweighted planar graph and let $S=(s_0,\dots,s_{k-1})$ be the vertices of a designated face, listed in cyclic order. Consider a vector that stores the distances from an arbitrary vertex $v$ to all vertices of $S$. The pattern of $v$ is obtained by taking the difference between every pair of consecutive values in this vector. Li and Parter [STOC'19] proved an upper bound of $O(k^3)$ on the number of unique patterns over all vertices of $G$. We improve this to $O(k^2)$, matching a known lower bound and settling a conjecture in [ISAAC'22]. The simple proof was found by OpenAI's GPT 5.6-Sol model.
  Plugging this new bound into known results has the following three immediate implications for undirected unweighted planar graphs: (1) it gives an improved compression of the Okamura-Seymour metric (2) it improves the space required by constant-time exact distance oracles, and (3) it improves the fastest distributed algorithm for computing the diameter. We further present a previously unknown and nontrivial implication: a (centralized) $\tilde{O}(n^{8/5})$-time algorithm for computing the diameter, improving over the $\tilde{O}(n^{5/3})$ algorithm of [SODA'18] which works for weighted directed planar graphs. Thus, there is currently a gap between the time for computing the diameter between weighted and unweighted planar graphs.

</details>


### [23] [Linear-Time Verification of Rings and Fields](https://arxiv.org/abs/2608.07272)
*Youlong Ding*

Main category: cs.DS

TL;DR: 本文给出确定性 O(n²) 时间算法来验证环结构，解决了该问题的确定性复杂度。作为推论，也得到确定性 O(n²) 时间的域验证算法。算法是初等的，完全避免使用有限单群分类定理（CFSG）。


<details>
  <summary>Details</summary>
Motivation: 此前关于环验证的问题已有随机 O(n²log(1/δ)) 算法和域的确定性 O(n²) 算法，但这些结果依赖于 CFSG 这一深层数学工具。本文的目标是去除对 CFSG 的依赖，并以初等方法获得环验证的确定性最优算法。

Method: 作者提出了初等的确定性算法来验证给定两个 n×n 表是否定义环结构。该算法避免了此前工作中依赖 CFSG 的机制（Evra, Gadot, Klein, Komargodski 在 FOCS 2024 的成果），通过更直接的方法在 O(n²) 时间内完成验证。

Result: 获得了确定性 O(n²) 时间复杂度的环验证算法，解决了该问题的确定性复杂度；由此也直接得到确定性 O(n²) 时间的域验证算法。

Conclusion: 本文同时解决了环验证的确定性时间复杂度问题，并表明这些问题可以在不依赖 CFSG 的前提下以初等方式在最优确定性时间内解决。

Abstract: We consider the following problems: Given two $n \times n$ tables defining binary operations $+$ and $\cdot$ on a set $S$ of $n$ elements, decide whether $(S,+,\cdot)$ forms a ring or, respectively, a field. Recently, Dudek, Fischer, Gokaj, Jin, Künnemann, Mao, and Redzic (STOC 2026) obtained the following two (near-)optimal results:
  (1) A randomized $O(n^2\log(1/δ))$-time algorithm for verifying rings.
  (2) A deterministic $O(n^2)$-time algorithm for verifying fields.
  Their algorithms build on machinery of Evra, Gadot, Klein, and Komargodski (FOCS 2024), which relies on Classification of Finite Simple Groups (CFSG).
  In this work, we give a deterministic $O(n^2)$-time algorithm for ring verification, resolving the deterministic complexity of this problem. As a corollary, we also obtain a deterministic $O(n^2)$-time algorithm for field verification. Our algorithms are elementary and avoid CFSG machinery entirely.

</details>


### [24] [Learning Nearest-Neighbor Maps from Adaptive Queries](https://arxiv.org/abs/2608.07352)
*Hadley Black,Geelon So*

Main category: cs.DS

TL;DR: 本文研究通过自适应最近邻查询来重构隐藏点集H的问题，证明了在一般范数空间中紧致的查询复杂度下界Θ(nκ)，其中κ是底层范数的"接触数"(kissing number)。在欧几里得范数下，即使在球域中n=2也需要exp(Ω(d))次查询；而在欧几里得球面上，通过维度缩减预处理可将查询复杂度从O(nd)改进到O(min(n,d))，揭示了球面与球域之间的鲜明对比。


<details>
  <summary>Details</summary>
Motivation: 先前的工作仅在特定域（布尔超立方体和ℓ2单位球面）上研究了通过最近邻查询重构隐藏集合的问题。作者希望在更一般的范数空间中对查询复杂度进行统一的刻画，并探究欧几里得空间中维度对查询复杂度的影响，特别是球面与球域之间的本质差异。

Method: 1) 从一般范数空间角度出发，将查询复杂度与底层范数的接触数κ联系起来，证明紧致界Θ(nκ)。2) 在欧几里得域的球和锥中，利用几何结构构造信息论下界证明指数维度依赖。3) 在欧几里得球面上，提出一个随机化的维度缩减预处理步骤，改进Prabhu-Woodruff(ICML 2024)的方法，将查询复杂度从O(nd)降至O(min(n,d))。

Result: 1) 在一般范数空间中，紧致的查询复杂度界为Θ(nκ)。2) 在欧几里得球域中，即使n=2也需要exp(Ω(d))次查询；在锥域中需要n·exp(Ω(d))次。3) 在欧几里得球面上，通过随机化维度缩减，查询复杂度改进为O(min(n,d))，当n=O(1)时可达到O(1)查询。

Conclusion: 本文统一了先前特定域的结果，并将查询复杂度与接触数这一几何量建立联系。工作揭示了欧几里得空间中球面与球域在查询复杂度上的本质区别：球面对小n具有常数查询算法，而球域即使n=2也需要指数级查询。这也指出欧几里得范数下接触数紧致界限的开放问题直接影响到此类查询问题的复杂度刻画。

Abstract: We study the problem of learning nearest-neighbor maps from adaptive queries, which is equivalent to the following problem of reconstructing a hidden set $H$ via a nearest-neighbor query oracle. Let $K \subset \mathbb{R}^d$ be a compact domain in a normed space $(\mathbb{R}^d,\| \cdot\|)$ and let $H \subset K$ be a hidden set of $n$ points. Upon querying $q \in K$, the oracle returns some $h \in H$ with minimum distance from $q$. How many queries are required to exactly recover $H$? Previous work has studied this question in specific domains, namely the Boolean hypercube and the $\ell_2$-unit sphere. We generalize previous work and prove the tight worst-case query complexity bound of $Θ(nκ)$, where $κ$ is the kissing number of the underlying norm.
  In the Euclidean norm, obtaining tight asymptotic bounds on $κ$ is a significant open question, although it is known that $κ= \exp(Θ(d))$. Our second set of results shows that an exponential dependence on $d$ is required even in natural Euclidean domains: $\exp(Ω(d))$ queries are needed in the ball, even when $n=2$, and $n\exp(Ω(d))$ queries are needed in the cone.
  Lastly, we prove a sharper upper bound in the Euclidean sphere. Here, $d$ can be replaced by $\min(n,d)$ via a dimension reduction preprocessing step. This is a randomized version of a procedure due to Prabhu-Woodruff (ICML 2024) where we improve the query complexity from $O(nd)$ to $O(\min(n,d))$. This reveals a striking contrast between the sphere and the ball: when $n = O(1)$, the sphere admits an $O(1)$ query algorithm, whereas the ball requires $\exp(Ω(d))$.

</details>


### [25] [Online Metric TSP: Beyond the $\sqrt{n}$ Barrier](https://arxiv.org/abs/2608.07369)
*Yossi Azar,Debmalya Panigrahi,Or Vardi*

Main category: cs.DS

TL;DR: 本文研究在线度量旅行商问题（TSP）中空间使用与竞争比之间的权衡。核心结果是：使用 m=(1+ε)n 空间的确定性算法可实现 O(log³n/ε) 的竞争比，即从 n 扩展到 2n 空间可将竞争比从 Θ(√n) 提升到 O(log³n)。同时证明了下界：即使 m=n^{1+ε}，任何确定性算法的竞争比至少为 Ω(1/ε)，这意味着即使使用 O(n·polylog(n)) 空间也无法达到常数竞争比。


<details>
  <summary>Details</summary>
Motivation: 在线TSP问题中，经典的任意插入模型已知存在 O(log n) 竞争比的算法。但最近引入的在线度量TSP模型（每个到达点必须分配到数组中不同的单元格）在不同空间限制下表现差异巨大：当 m=n 时竞争比为 Θ(√n)，当 m=2^n 时恢复为 O(log n)。作者希望探索这两个极端之间的空间-竞争比权衡曲线。

Method: 作者提出了一个确定性在线度量TSP算法，使用 m=(1+ε)n 的空间，达到 O(log³n/ε) 的竞争比。该算法通过在在线排序问题（一维特例）先前工作的基础上进行推广。同时，作者构造了一个下界论证，证明当 m=n^{1+ε} 时，任何确定性算法的竞争比至少为 Ω(1/ε)。

Result: 主要结果：使用 2n 空间即可将竞争比从 Θ(√n) 改善到 O(log³n)，这是从指数级到对数级的显著改进。下界结果表明，对于 m=n^{1+ε}，竞争比下界为 Ω(1/ε)，因此即使空间达到 O(n·polylog(n))，确定性算法也无法达到常数竞争比。

Conclusion: 空间-竞争比的权衡中，少量额外空间（从 n 到 2n）就能带来指数级的竞争比提升，但若要实现常数竞争比，需要远超多项式级别的空间。研究为在线度量TSP的空间复杂度与竞争比之间的关系提供了近乎完整的刻画。

Abstract: We study an online variant of the Traveling Salesperson Problem (TSP) in which $n$ points arrive sequentially and must be inserted into an evolving tour. In the classical setting where arbitrary insertions are allowed, an $O(\log n)$-competitive algorithm has been known since the 1970s (Rosenkrantz, Stearns and Lewis 1977, Imase and Waxman 1991). Recently, Abrahamsen, Bercea, Beretta, Klausen, and Kozma [ESA 2024] introduced online metric TSP, a stricter model in which each arriving point must be assigned to a distinct cell of an array of size $m \ge n$, with the final tour order induced by the non-empty cells; the parameter $m$ captures the space usage of the algorithm.
  When $m = 2^{n}$, this model recovers arbitrary insertions and therefore admits an $O(\log n)$-competitive algorithm. In contrast, when $m = n$, i.e., when each point's position is fixed on arrival, Bertram [ESA 2025] recently showed that the competitive ratio is $Θ(\sqrt{n})$. We investigate the tradeoff between space usage and competitiveness between these extremes. We note that this tradeoff was previously explored by the authors [SODA 2026] for the online sorting problem, which is the special case of online metric TSP on a line metric.
  Our main result is a deterministic online metric TSP algorithm using $m = (1+ε) n$ space that achieves a competitive ratio of $O(\log^{3} n / ε)$, for any $ε\le 1$. In particular, increasing the space from $n$ to $2n$ improves the competitive ratio from $Θ(\sqrt{n})$ to $O(\log^{3} n)$. We complement this with a lower bound showing that for $m = n^{1+ε}$, any deterministic algorithm has a competitive ratio $Ω(1/ε)$, for all $ε\ge Ω(\log \log n / \log n)$. Consequently, even with $m = O(n \cdot \mathrm{polylog}(n))$, deterministic algorithms cannot achieve a constant competitive ratio.

</details>


<div id='cs.GT'></div>

# cs.GT [[Back]](#toc)

### [26] [Resource-Aware Intrusion Detection in Infrastructure Networks: A Game-Theoretic Approach](https://arxiv.org/abs/2608.06655)
*Xuanli Lin,Zhaofeng Zhang,Zunzheng Zhang,Kevin S. Chan,Guoliang Xue*

Main category: cs.GT

TL;DR: 本文研究集成感知与通信（ISAC）背景下，基础设施网络中的感知与处理资源分配问题。将防御者与攻击者的交互建模为图安全博弈，分析同时博弈及攻击者观察防御配置（纯策略或混合策略）等情形下的纳什均衡与Stackelberg均衡的存在性、结构和计算复杂度，并开发了构建有效纯配置和求解混合均衡的算法。


<details>
  <summary>Details</summary>
Motivation: 基础设施网络依赖分布式感知来在攻击者到达有价值资产前检测入侵，但感知设备、通信资源和边缘服务器容量均有限，且智能攻击者会适应部署的防御而调整路线。这一资源受限且存在策略交互的场景亟需系统性的博弈论分析来指导资源分配。

Method: 将防御者与攻击者的互动建模为图安全博弈：防御者在一系列感知动作中部署资源（受资源和误报约束），攻击者选择通往有价值目标的路线。分析同时博弈（纳什均衡）和攻击者观察防御（纯策略或混合策略）的Stackelberg均衡，刻画其存在性、结构与计算复杂度；开发构建有效纯配置的算法以及求解混合纳什和混合Stackelberg均衡的精化受限博弈算法。

Result: 算法在可枚举实例上与完全枚举参考解的均值归一化差异很小；方法在穷举策略枚举不可行时仍适用。同时识别了纳什与混合Stackelberg收益存在有序关系或重合的条件。

Conclusion: 攻击者对防御的观察方式（纯配置或混合策略）显著影响均衡行为，某些情况下最优策略计算困难。所提算法在可枚举场景中接近最优，并适用于不可穷举的实际场景，为ISAC驱动的安全资源分配提供了理论保障与实用工具。

Abstract: Infrastructure networks increasingly rely on distributed sensing to detect intrusions before attackers reach valuable assets. Yet sensing devices, communication resources, and edge server capacity are limited, while intelligent attackers can adapt their routes to the deployed defense. Motivated by integrated sensing and communication (ISAC), we study how sensing and processing resources should be allocated under strategic interaction between a defender and an attacker. We formulate their interaction as a graph security game in which the defender deploys sensing actions under resource and false alarm constraints, while the attacker selects routes to valuable targets. We consider simultaneous play and settings in which the attacker observes either a pure defender configuration or a mixed defender strategy. Our analysis characterizes the existence, structure, and computational complexity of the Nash and Stackelberg equilibria, showing how the attacker's observation of the defense affects equilibrium behavior and when optimal strategies become difficult to compute. We develop algorithms that construct effective pure configurations and refine restricted games for mixed Nash and mixed Stackelberg play. On enumerable instances, their solutions have small mean normalized differences from fully enumerated references; the methods also apply when exhaustive strategy enumeration is impractical. We also identify conditions under which Nash and mixed Stackelberg payoffs are ordered or coincide.

</details>


### [27] [Auctioning Attention on Social Networks](https://arxiv.org/abs/2608.06665)
*Andy Lee,Hari Sundaram*

Main category: cs.GT

TL;DR: 本文提出了一种基于拍卖机制的社交媒体信息流构建方法，通过用户竞价来争夺其他用户的注意力，系统性地平衡内容生产者、消费者、平台运营商和社会福利之间的冲突。


<details>
  <summary>Details</summary>
Motivation: 传统社交媒体推荐系统面临多方利益冲突：生产者被迫优化算法、消费者易接触负面外部性内容（如极化、错误信息）、平台追求用户参与度导致过度消费，同时需应对政策制定者和社会压力。现有方法仅关注推荐系统，缺乏系统性考虑各方利益的机制。

Method: 提出一种新颖的基于拍卖的信息流构建机制，用户通过竞价争夺其他用户的注意力。该机制在预算约束下弱激励相容，并引入税收政策来增加具有负面外部性内容的成本，以平衡生产者与消费者福利。通过在常见社交网络拓扑和实证观测网络上进行模拟评估。

Result: 在实证观测网络上，所提拍卖机制的生产者福利比对比算法平均高36.3%，在合成网络上高31.4%。该方法在所有评估的网络类型中都比基线方法产生更公平的注意力分配。

Conclusion: 所提出的拍卖机制在系统层面解决了注意力分配问题，平衡了不同利益相关者的需求，为社交媒体信息流构建提供了一种不同于传统推荐系统的新范式。

Abstract: Social media recommendation systems create conflict: content producers, content consumers, platform operators, and social pressures struggle to direct the allocation of attention in their favor while also facing competing societal pressures. Producers may feel pressure to optimize for recommendation algorithms and consumers may be exposed to content with negative externalities such as polarization and misinformation. Platforms aim to maximize user engagement, oftentimes resulting in over consumption from consumers. There is mounting pressure from policymakers and broader society to address these issues. Prior methods for constructing social media feeds have focused on recommendation systems. Instead, we propose a, to our knowledge, novel auction based method for feed construction where users bid for the attention of other users. Our mechanism systematically considers producers, consumers, platform operators, and social welfare. We show that our auction is weakly incentive compatible under budget constraints. To balance between producer and consumer welfare, we introduce a tax policy to the auction to increase the cost of content with negative externalities. Simulations over common social network topologies and an empirically observed network show how different feed algorithms prioritize different stakeholders. Our proposed auction based mechanisms produce on average 36.3% higher producer welfare than comparison algorithms on the empirically observed network and 31.4% higher producer welfare than comparison algorithms on the synthetic networks. Our methods also produce more equitable distributions of attention than baseline methods across all evaluated network types. Our proposed mechanism addresses attention allocation at a systematic level, balancing between the needs of different stakeholders.

</details>


### [28] [Pessimal Elections for Approximately Dominating Sets](https://arxiv.org/abs/2608.06872)
*Moses Charikar,Prasanna Ramakrishnan,Kangning Wang*

Main category: cs.GT

TL;DR: 本文研究了孔多塞悖论在委员会选举中的推广，证明了一个关于放宽多数阈值来规避悖论的已知结果（任何选举可选O(1/ε²)个获胜者，使得没有失败者在1/2+ε比例的选民中被偏好）在常数因子内是紧的。作者提出了一个由GPT-5.6 Sol Ultra发现的简单构造来证明紧性。


<details>
  <summary>Details</summary>
Motivation: 孔多塞悖论表明在任何选举中，即使获胜者也可能被多数选民偏好于某个失败者。扩展到委员会选举时，即使选择k个获胜者，仍可能存在被所有获胜者多数击败的失败者。最近的研究通过放宽多数阈值（从1/2放宽到1/2+ε）来规避这一阻碍，但该结果的紧性（即下界）尚未被证明。本文的目标是证明O(1/ε²)这一上界在常数因子内是最优的。

Method: 作者提出了一个简单构造（由GPT-5.6 Sol Ultra发现），构造一个选举实例，使得任何选择少于Ω(1/ε²)个获胜者的委员会中，都会存在一个失败者被至少1/2+ε比例的选民偏好于每个获胜者。该构造证明了已知上界O(1/ε²)的紧性（下界为Ω(1/ε²)）。

Result: 证明了放宽多数阈值后委员会选举结果的最优性：任何选举可以选择O(1/ε²)个获胜者以规避悖论的结果是紧的，即下界为Ω(1/ε²)，与上界匹配到常数因子。

Conclusion: 该结果完整刻画了通过放宽多数阈值来规避孔多塞悖论所需的委员会规模，证明此前的上界结果在常数因子内是最优的，从而闭合了这一研究问题的界限。

Abstract: Condorcet's paradox is a foundational result in social choice theory, showing that no matter which candidate wins an election, a majority of voters may prefer some losing candidate. Worse still, even if the election can choose a committee of $k$ winners, some loser may beat every winner in a majority vote.
  Recent work showed that this obstruction can be sidestepped by relaxing the majority threshold. For all $\varepsilon > 0$, any election can select a committee of $O(1/\varepsilon^2)$ winners such that no loser is preferred to every winner by $\frac12 + \varepsilon$ fraction of voters.
  We present a simple construction, found by GPT-5.6 Sol Ultra, which proves that this result is tight up to a constant factor.

</details>


### [29] [Ex-Post Equilibria: Structure and Computation](https://arxiv.org/abs/2608.07025)
*Francesco Giordano,Julien Grand-Clément,Christian Kroer*

Main category: cs.GT

TL;DR: 本文研究同时行动博弈中参数不确定性下的事后均衡（EPE），将其与现有稳健均衡概念进行比较，建立了EPE的博弈论基础（由单调性和集合一致性两个性质完全刻画），并引入最优近似事后均衡概念；针对零和博弈与凹势博弈两类重要博弈，给出了计算EPE及最优近似EPE的复杂度结果和基于辅助极小极大公式的计算方法。


<details>
  <summary>Details</summary>
Motivation: 在现实博弈中，参数不确定性普遍存在，但现有的稳健均衡概念在刻画这种不确定性下的均衡行为时存在不足。同时，事后均衡（EPE）作为一种天然适用于参数不确定性环境的解概念，其理论基础和计算性质尚未得到系统研究，且EPE可能不存在，需要引入近似松弛。

Method: （1）将EPE与现有稳健均衡概念进行比较，并通过博弈论性质（单调性和集合一致性）给出EPE的完全刻画基础；（2）引入最优近似事后均衡的定义，放松精确最优性条件并最小化次优程度；（3）针对零和博弈和凹势博弈两类博弈，分析计算复杂性（给出若干困难性结果）；（4）提出基于辅助极小极大（minimax）公式的一般计算方法。

Result: 建立了EPE作为参数不确定博弈解概念的完整理论框架，证明了其可由单调性和集合一致性两个性质完全刻画；给出了EPE及最优近似EPE的计算复杂性结果（若干困难性结论）；提出了适用于零和博弈和凹势博弈的通用计算方法。

Conclusion: 事后均衡为参数不确定性的博弈提供了理论基础且可由清晰的博弈论性质刻画，而最优近似事后均衡作为精确EPE不存在的自然推广，其计算问题在两类重要博弈中既有困难性结果也有可行的计算方法，为实际应用提供了理论和算法支撑。

Abstract: This paper studies ex-post equilibria (EPEs) in simultaneous-move games with parameter uncertainty. We first compare EPEs with existing notions of robust equilibrium and provide a general foundation for EPEs as a solution concept for games with parameter uncertainty, and we show that EPEs are fully characterized by two game-theoretic properties (monotonicity and set-consistency). Since ex-post equilibria may fail to exist, we introduce the notion of an optimal approximate ex-post equilibrium, in which players adopt approximate best responses while minimizing the degree of suboptimality. We study the problem of computing EPEs and optimal approximate EPEs, focusing on two important classes of games: zero-sum games and concave potential games. We provide several hardness results, as well as a general class of computational approaches based on auxiliary minimax formulations.

</details>


### [30] [Algorithmic Threshold Optimization: Quantitative Modeling of Multiplier Distributions in Crash Games](https://arxiv.org/abs/2608.07103)
*Sourish Sarkar*

Main category: cs.GT

TL;DR: 本文提出一种针对Casino游戏Crash的优化算法，旨在最小化赌场保证性收益的同时最大化获胜玩家人数，通过确定最优停止倍数来平衡赌场与玩家的利益。


<details>
  <summary>Details</summary>
Motivation: Crash是一款多人参与的赌场游戏，玩家在不同时间点随机投入金额，导致单个玩家难以在回合开始前判断自己的投资是否偏高或偏低，从而难以评估实际风险。需要一个优化算法来平衡赌场与玩家双方的利益。

Method: 提出一个确定最优停止倍数的优化算法，该算法通过平衡赌场和玩家双方利益来确定停止倍数。算法不受玩家人数变化或投资金额变化的影响，并进一步分析了算法的计算复杂度和停止倍数统计特性。

Result: 算法在最小化赌场保证性收益的同时能最大化获胜玩家数量，且对玩家人数和投资金额变化具有鲁棒性。此外，算法还能帮助专业玩家做出更明智的投资和风险决策。

Conclusion: 该优化算法为Crash游戏提供了一个兼顾赌场与玩家利益的停止倍数确定方案，具有计算复杂度的可分析性，并对玩家投资决策具有实际指导价值。

Abstract: Crash is a widely played casino game that blends strategic decision-making, probability, and computational analysis, particularly from the perspective of the house. This paper presents an optimization algorithm aimed at minimizing the casino's guaranteed positive earnings while simultaneously maximizing the number of players who win some amount during a given round. Since Crash is inherently a multiplayer game, players place random investment amounts at different points in time, which makes it difficult for any individual player to judge, before the round even begins, whether their intended investment is comparatively high or low, and consequently, how much risk they are actually taking on. The core objective of the proposed algorithm is to determine an optimal stopping multiplier that balances both the casino's and the players' interests. Notably, the algorithm remains unaffected by changes in the number of players or by variation in investment amounts, although in practice casinos typically impose a fixed range on permissible investments. Beyond this, the algorithm can help professional players make more informed decisions about how much to invest and how much risk they are comfortable accepting. We further analyze the algorithm's computational complexity and its stopping-multiplier statistics to give a fuller picture of how it behaves in practice.

</details>


### [31] [Analyzing the Interaction of Optimal Strategies in Mean-Payoff Bidding Games](https://arxiv.org/abs/2608.07383)
*Shaull Almagor,Guy Avni,Julian Ewaied*

Main category: cs.GT

TL;DR: 本文分析了多智能体系统中，当所有智能体都采用对抗性假设（针对最坏对手优化）策略时，系统实际产生的行为。聚焦于竞标博弈（bidding games），在均值收益目标下，分析两个已知最优策略显式构造所生成的实际博弈过程，证明在限制条件下该过程是最终周期的，并开发算法计算玩家效用。


<details>
  <summary>Details</summary>
Motivation: 多智能体系统设计中常假设其他智能体是敌对的，这样可以获得最强保证。但当所有智能体都采用这种对抗性策略时，实际交互并非真正对抗（如所有人防守则无人攻击）。然而分析这种实际交互行为在数学和算法上都极具挑战性，本文试图填补这一空白。

Method: 本文聚焦于两个智能体在图上的竞标博弈：每回合通过拍卖决定谁移动令牌，生成无限路径决定效用。考虑均值收益目标，分析当每个智能体都遵循"对抗性优化"（即针对最坏情况对手优化）策略时产生的实际博弈。考虑两种已知的最优策略显式构造，面临的技术挑战来自竞标博弈的无限多种配置及其复杂动态。作者在限制条件下证明生成的博弈过程是最终周期的（ultimately periodic），并开发算法计算其中的玩家效用。

Result: 在一定的限制条件下，证明了当两个智能体都采用对抗性最优策略时，竞标博弈中生成的实际博弈过程是最终周期的，并开发了相应算法来计算玩家在该过程中的效用。

Conclusion: 该研究首次对"所有智能体都按对抗性假设设计"时系统实际涌现的行为进行了数学分析，表明即使在复杂动态的竞标博弈中，这种实际交互行为也具有可计算的结构（最终周期性），为理解和预测多智能体系统的实际行为提供了新的理论工具。

Abstract: A common assumption when designing an agent in a multi-agent system is that the other agents behave adversarially. This allows a designer to obtain the strongest guarantees when they have no control over nor knowledge about the other agents' behavior. However, when all agents are designed under this adversarial assumption, their actual interaction is not adversarial (e.g., when all players play defensively, no player actually attacks). In such settings, we would like to know what behavior arises in the multi-agent system. However, analyzing the interaction among agents is notoriously challenging, both mathematically and algorithmically. In this paper, we provide such an analysis, focusing on bidding games, played by two agents on a graph as follows. A token is placed on a vertex, and in each turn an auction (bidding) determines which agent moves the token, thus generating an infinite path that determines the agents' utilities. We consider mean-payoff objectives; each vertex is associated with a reward for each player, and the utility in an infinite play is the limit average of the rewards. We analyze the play that is generated when each agent follows a strategy that optimizes against an adversary, and consider the two known explicit constructions of optimal strategies. The technical challenge stems from the infinitely-many configurations of a bidding game and their complicated dynamics. We show that, under some restrictions, the generated play is ultimately periodic, and develop algorithms to compute the players' utilities in it.

</details>


### [32] [Bayesian Fair Division: Truthfulness in Picking Sequence with Correlated Valuations](https://arxiv.org/abs/2608.07414)
*Xiaolin Bu,Biaoshuai Tao*

Main category: cs.GT

TL;DR: 本文研究贝叶斯公平分配模型下顺序分配机制的诚实性问题。作者证明在两代理且估值正相关时，诚实策略构成贝叶斯纳什均衡；但该结论无法推广到两个以上代理的情形，并揭示了与以往不同的新型操纵方式。


<details>
  <summary>Details</summary>
Motivation: 经典顺序分配机制（如轮转机制）已知不诚实：当代理人的首选物品不被他人看重时，代理人可能延迟选择该物品以争夺其他物品。然而，这种操纵依赖于代理人对他人估值的完全信息和物品价值的巨大差异。当代理人只有部分信息且估值大致一致时，机制是否更诚实？这自然引出了贝叶斯公平分配模型的研究。

Method: 在贝叶斯模型下，作者对两代理人情形精确刻画了促使诚实策略成为激励相容的"估值大致一致"程度——即当估值正相关时，诚实策略构成贝叶斯纳什均衡；并进一步考察两个以上代理人的情形，分析诚实性是否成立。

Result: 对于两代理情形，当估值正相关时，诚实策略构成贝叶斯纳什均衡。然而，诚实性在多于两个代理人的情形下不成立，且作者发现了一种不同于以往"延迟高价值但竞争力低的物品"的新型操纵方式。

Conclusion: 该结果揭示了顺序分配机制在诚实性方面的根本局限：即使在部分信息和估值大致一致（正相关）的条件下，诚实性也无法超越两代理人的情形推广至多代理人场景。

Abstract: Sequential allocation mechanisms contain a class of widely studied mechanisms (e.g., round-robin) in the fair division of indivisible goods, where agents take turns picking items in a predefined picking order. It is known that the sequential allocation mechanisms are not truthful: when an agent's most preferred item is not valued by others, the agent may manipulate the mechanism by choosing to defer picking that item and instead competing for another slightly less preferred item that is valued by others. Two underlying reasons are that each agent has perfect knowledge of the others' valuations, and each item's value to each agent can differ significantly. Will the mechanism be more truthful when each agent only has partial information about the others' valuations, which are known to be roughly consistent? This naturally motivates the study of the Bayesian fair division model.
  In this paper, we answer this question affirmatively for two agents. Under the Bayesian model, we precisely characterize the extent of this ``rough consistency'' that incentivizes agents' truth-telling. In particular, we show that for the case of two agents, when the valuations are positively correlated, truth-telling forms a Bayesian Nash equilibrium under the sequential allocation mechanisms. However, we show that truthfulness fails to extend to the setting with more than two agents. For more than two agents, we reveal a new type of manipulation that is different from the above-mentioned manipulation that defers a highly valued but less competitive item. Our result reveals a fundamental limitation on the truthfulness of sequential mechanisms.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [33] [Progressive Alignment of Recommender Foundation Model through Multi-Phase Post-Training](https://arxiv.org/abs/2608.06792)
*Oseong Choi,Hoeinn Kim,Jihoon Lee,Byungsoo Kang,Taeyeong Jang*

Main category: cs.IR

TL;DR: 本文提出一个三阶段渐进式后训练框架(LP-FFT-RFT)，将基础模型在推荐系统中的下游适配与业务指标对齐明确分离，通过线性探测、全量微调和强化微调三个阶段的渐进式训练，显著提升了生产环境中的推荐质量。


<details>
  <summary>Details</summary>
Motivation: 基础模型在推荐中擅长建模长时序用户行为，但通过SFT适配到不同下游场景时，直接优化点击或点赞等任务特定目标，往往与真正决定推荐质量的业务指标不对齐。需要一个框架将下游适配和业务指标对齐显式分离。

Method: 提出三阶段渐进式后训练框架：(1) LP阶段在冻结的预训练表示空间内稳定随机初始化的下游头部；(2) FFT阶段联合微调整个模型以专门化目标任务；(3) RFT阶段使用学习到的奖励模型，通过强化学习将模型与业务目标对齐。关键创新是策略在密集隐式反馈上训练，而业务指标监督仅用于奖励建模。

Result: 离线实验表明LP-FFT-RFT渐进式框架优于单阶段替代方案，基于奖励的对齐比直接用奖励模型排序产生更强的服务策略。大规模在线A/B测试证明该框架相比传统非基础模型基线显著提升生产推荐质量。

Conclusion: 渐进式后训练范式能有效弥合基础模型推荐与真实业务指标之间的差距，通过将策略优化与奖励建模解耦，既避免了稀疏业务目标的直接优化难度，又实现了业务指标的有效对齐，为推荐基础模型的生产部署提供了可行路径。

Abstract: Foundation model(FM) for recommendation has shown strong ability to model long-horizon sequential user behavior. In practice, a single pretrained foundation model is often adapted to diverse downstream serving surfaces through Supervised Fine-Tuning(SFT). However, optimizing task-specific objectives such as clicks or likes does not necessarily align the serving policy with the business metrics that determine recommendation quality.
  We propose a three-phase progressive post-training framework that explicitly separates downstream adaptation from business-metric alignment. The adaptation stage is decomposed into Linear Probing(LP) and Full Fine-Tuning(FFT): LP first stabilizes randomly initialized downstream heads within a frozen pretrained representation space, and FFT then jointly specializes the full model for the target task. On top of this stabilized policy, Reinforcement Fine-Tuning(RFT) aligns the model with practical business objectives using a learned reward model. Rather than directly optimizing the serving policy on sparse business targets, we train the policy on dense implicit feedback and use business-metric supervision only for reward modeling.
  Offline experiments show that the progressive LP-FFT-RFT framework outperforms single-phase alternatives, and that reward-based alignment yields a stronger serving policy than directly using the reward model itself for ranking. Large-scale online A/B tests further show that the proposed framework improves production recommendation quality over a conventional non-foundation baseline. A reference implementation is available at https://github.com/webtoon/rec-fm-progressive-alignment

</details>


### [34] [Rhetorical-Role-Aware Retrieval-Augmented Generation for Legal Question Answering over Indian Supreme Court Judgments](https://arxiv.org/abs/2608.06828)
*Sayed Ayaan Ahmed Sha,Sangeetha Sivanesan,Anand Kumar Madasamy,Navya Binu*

Main category: cs.IR

TL;DR: 本文提出一个面向印度最高法院判决书的法律领域检索增强生成（RAG）框架，通过修辞学分块、融合检索、交叉编码器重排序以及查询分类和改写等技术，提升法律问答的检索相关性和对话能力，并在DeepEval框架下取得了优异的上下文召回率和答案相关性表现。


<details>
  <summary>Details</summary>
Motivation: 法律领域的问答任务需要处理大量上下文信息且要求高可靠性，通用RAG框架在法律场景下检索相关性不足，无法有效应对法律文档的结构特征（如法官姓名、判例结构）以及多轮对话中的用户意图理解问题。

Method: 1) 基于修辞学的文本分块策略；2) 融合检索方法；3) 交叉编码器重排序；4) 利用聊天历史进行查询分类与改写以理解用户意图；5) 特殊处理法律文档结构特征（如隔离法官姓名）以提升检索质量。

Result: 使用DeepEval框架进行评估，在上下文召回率和答案相关性等指标上表现优异，证明该框架在处理需要大量上下文的法律问答任务时非常有效。

Conclusion: 研究结果强调了领域特定增强对于开发可靠且可解释的法律AI系统的重要性，该框架在法律问答场景中具有较强的实用价值。

Abstract: This research paper proposes a Retrieval Augmented Generation (RAG) framework that is specific to the legal field in order to assist interactive retrieval and reason about judgments from the Supreme Court of India. The solution uses an enhanced version of RAG framework which consists of rhetorically based chunking, fusion-based retrieval, and cross encoder reranking methods to increase the relevancy of the information retrieved. In order to improve conversations, the proposed framework uses chat history along with query classification and rewriting in order to understand user intention from successive queries. Additionally, there are features that take into account structural aspects of legal documents, such as isolated names of judges that could have an impact on retrieval quality. The evaluation was done using the DeepEval framework and demonstrated strong performance on metrics including contextual recall and answer relevancy, which proves that the framework is very effective in dealing with legal question-answering tasks that require a lot of context. The results emphasize the importance of domain specific enhancements in developing legal AI systems that are both reliable and explainable.

</details>


### [35] [BZKO: An Ontology for the Card Index of German Post-War Compensation Records](https://arxiv.org/abs/2608.06918)
*Dilek Yargan,Jörg Waitelonis,Mahsa Vafaie,Harald Sack*

Main category: cs.IR

TL;DR: 本文介绍BZK本体（BZKO），一个用于德国联邦中央卡片索引档案资料语义化表示与集成的两层本体，结合BFO现实主义与RiC-O、PROV-O、PiCo等档案标准，实现保留溯源信息的语义集成，为数字人文基础设施提供可复用的语义基础。


<details>
  <summary>Details</summary>
Motivation: 德国联邦中央卡片索引是记录纳粹迫害受害者赔偿申请的重要历史档案，但缺乏语义丰富的表示、集成与重用方式，需要一种兼顾领域语义严谨性与互操作性的本体方案来支撑其在数字人文基础设施中的整合。

Method: 提出两层本体架构：第一层基于BFO建立本体论域领域语义，第二层面向互操作性扩展，整合RiC-O、PROV-O、PiCo等档案与溯源标准，以保持逻辑严谨性、模块化和可重用性。

Result: 建立了可复用的Wiedergutmachung档案材料语义集成基础，为未来知识图谱生成、本体验证以及纳入历史实体和不确定时空信息奠定基础，本体已在GitHub公开。

Conclusion: 该两层本体方法为历史档案数据的语义集成提供了兼顾领域深度与互操作性的可行路径，可推广至数字人文基础设施中的其他历史档案资源。

Abstract: The Central Federal Card Index (Bundeszentralkartei) of Germany is a key archival resource documenting compensation claims submitted by victims of National Socialist persecution and their relatives, within the German Wiedergutmachung process. To enable semantically enriched representation, integration, and reuse of this historically significant collection, we present the BZK Ontology (BZKO). We propose a two-layer ontology for historical archival data that separates ontologically grounded domain semantics from interoperability-oriented extension constructs. The approach combines BFO-based realism with archival standards (RiC-O, PROV-O, PiCo), enabling provenance-preserving semantic integration, while maintaining logical rigor, modularity, and reuse across digital humanities infrastructures. The proposed approach establishes a reusable semantic foundation for the integration of Wiedergutmachung archival materials into digital humanities infrastructures and lays the groundwork for future knowledge graph generation, ontology validation, and the incorporation of additional historical entities and uncertain temporal and spatial information. The ontology is available on https://github.com/ISE-FIZKarlsruhe/bzko.

</details>


### [36] [From Classification to Recommendation: Empirical Analysis of Audio Embedding Models Application for Content-Based Music Recommendation](https://arxiv.org/abs/2608.06928)
*Qingrui Li,Haowei Lou,Chengkai Huang,Quan Z. Sheng,Lina Yao*

Main category: cs.IR

TL;DR: 本文系统评估了六种预训练音频编码器在三种音乐推荐系统（基于内容、序列和基于Semantic-ID的生成式推荐系统）中的表现，并研究了残差量化设计（码本宽度、量化深度、保留的Semantic-ID前缀）对推荐相关信息保留的影响。实验发现音频-文本对齐和音乐领域表示在直接使用预训练嵌入几何时更有效，而基于交互的序列训练会缩小编码器间的性能差异；此外，增加Semantic-ID容量不一定能提升生成式推荐系统性能，且可能引入不稳定性。


<details>
  <summary>Details</summary>
Motivation: 现有预训练音频表示模型主要针对掩码预测、对比学习或音频文本对齐等目标优化，这些目标产生的表示空间未必适合推荐系统。音乐推荐需要捕捉由主观和行为依赖性听者偏好塑造的项目关系。尽管预训练音频嵌入已被用于传统推荐系统，但在生成式推荐系统中的有效性尚未被充分探索。

Method: 在三个不同类型的数据集（内容、序列、Semantic-ID生成式推荐系统）上系统评估六种代表性音频编码器，并进一步调查残差量化设计（包括码本宽度、量化深度和保留的Semantic-ID前缀）如何影响推荐相关信息的保留。

Result: 在两个音乐推荐数据集上的实验表明：音频-文本对齐和音乐领域表示在直接使用预训练嵌入几何时通常更有效；基于交互的序列训练显著缩小了编码器之间的性能差异；增加Semantic-ID容量并不总能改善生成式推荐系统，并可能带来明显的不稳定性。

Conclusion: 这些发现为现代音乐推荐系统在选择音频编码器和设计基于音频的Semantic-ID提供了实用指导。

Abstract: Pretrained audio representation models learned from large-scale corpora have achieved strong performance in audio classification and understanding. However, most existing models are optimized for objectives such as masked prediction, contrastive learning, or audio-text alignment, which do not necessarily produce representation spaces well-suited to recommender systems. Unlike classification, music recommender systems must capture item relationships shaped by subjective and behavior-dependent listener preferences. Although pretrained audio embeddings have been explored in conventional recommender systems, their effectiveness in the rapidly emerging paradigm of generative recommender systems remains underexplored. To address this gap, we systematically evaluate six representative audio encoders across three types of music recommender systems: content-based, sequential, and Semantic-ID-based generative recommender systems. We further investigate how residual-quantization design, including codebook width, quantization depth, and retained Semantic-ID prefixes, affects the preservation of recommendation-relevant information. Experiments on two music recommendation datasets show that audio-text-aligned and music-domain representations are generally more effective when pretrained embedding geometry is used directly, whereas interaction-based sequential training substantially reduces performance differences among encoders. We also find that increasing Semantic-ID capacity does not consistently improve generative recommender systems and may introduce substantial instability. These findings provide practical guidance for selecting audio encoders and designing audio-derived Semantic IDs for modern music recommender systems.

</details>


### [37] [Hierarchical Quantization with Domain-Adaptive Sparse Routing for Generative Cross-Domain Recommendation](https://arxiv.org/abs/2608.06997)
*Haiying He,Xiaopeng Li,Yuchen Gu,Kuo Cai,Bo Chen,Jingtong Gao,Yejing Wang,Derong Xu,Ruiming Tang,Guorui Zhou,Han Li,Xiangyu Zhao*

Main category: cs.IR

TL;DR: 本文提出HD-Rec，一个统一的跨域生成式推荐框架，通过层级域感知量化器（共享粗粒度码本+自适应路由细粒度码本）、域自适应稀疏混合专家模块和跨粒度路由一致性目标，有效建模跨域异构语义和行为模式，在三个公开跨域推荐基准上超越现有方法。


<details>
  <summary>Details</summary>
Motivation: 生成式推荐虽在单域表现优异，但扩展到跨域推荐面临挑战：跨域场景中不同域具有异构的物品语义和行为模式，现有方法依赖全局共享表示或轻量级域适配，在建模不同语义粒度上的异构模式时能力不足。

Method: HD-Rec包含三个核心组件：1）层级域感知量化器，使用全局共享的粗粒度码本和自适应路由的细粒度码本构建语义标识；2）域自适应稀疏混合专家模块，结合持续激活的共享专家与动态选择的具体领域专家；3）跨粒度路由一致性目标，将token级别的路由决策向item级别的一致共识进行正则化，提升多token物品表征的连贯性。

Result: 在三个公开跨域推荐基准数据集上，HD-Rec一致性地超越了竞争性的序列推荐、生成式推荐和跨域推荐基线方法。

Conclusion: HD-Rec通过分层共享与域特定机制的结合，有效解决了跨域推荐中异构语义和行为模式的建模难题，为生成式推荐在跨域场景下的应用提供了有效方案。

Abstract: Generative Recommendation (GenRec) represents a promising paradigm that achieves remarkable empirical success by encoding items as compact Semantic IDs (SIDs) and modeling user behavior via next-token prediction across diverse recommendation scenarios. Extending this paradigm to cross-domain recommendation is challenging because a unified model must accommodate heterogeneous item semantics and behavioral patterns across domains. Existing methods commonly rely on globally shared representations or lightweight domain adaptation, which may provide insufficient capacity for modeling heterogeneous patterns at different semantic granularities. To address these challenges, we propose HD-Rec, a unified generative framework for cross-domain recommendation. HD-Rec employs a hierarchical domain-aware quantizer that constructs semantic identifiers using globally shared coarse-level codebooks and adaptively routed fine-level codebooks. It further introduces a domain-adaptive sparse mixture-of-experts module that combines a continuously activated shared expert with a dynamically selected specialized expert. To improve the coherence of multi-token item representations, we develop a cross-granularity routing consistency objective that regularizes token-level routing decisions toward their item-level consensus. Experiments on three public cross-domain recommendation benchmarks show that HD-Rec consistently improves over competitive sequential, generative, and cross-domain recommendation baselines.

</details>


### [38] [MISO: Model-Internal-State-Guided Optimization for Ranking Models](https://arxiv.org/abs/2608.07035)
*Yongzhe Zhang,Xiaoyu Deng,Yifan He,Mengying Sun,Sheng Luo,Yijia Liu,Hao Yan,Zhuo Li,Yi Meng,Huiping Yao,Swathi Hrishikesh,Jing Chen,Dennis Choi,Steven Liu,Lexi Luo,Keyi Chen,Anish Khazane,Marcio Porto,Xiaoya Wang,Emmy Wang,Kangfu Zheng,Xingyuan Wang,Bilal Fadlallah,Gursharan Singh,Prabhakar Goyal,Alireza Vahdatpour,Santanu Kolay*

Main category: cs.IR

TL;DR: 本文提出MISO（模型内部状态优化）工作流，利用模型内部状态（参数、激活、梯度、归一化统计等）来指导排序模型的局部优化决策，如缩放、替换或淘汰特定组件，在广告排序案例中显著提升了归一化熵，同时大幅减少了验证运行次数。


<details>
  <summary>Details</summary>
Motivation: 排序模型在既有模型家族内反复精修，但选择缩放、替换或淘汰哪个组件往往依赖昂贵且费时的试错法，缺乏系统化的决策指导机制。

Method: MISO从训练好的排序模型中提取模型内部状态（MIS），将其聚合成排序、对齐和比较信号，再将信号转换成少量可解释的候选编辑操作。由于每次重新训练后都会重新提取MIS，MISO支持自适应优化工作流，能跟踪模型行为的演化。

Result: 在广告排序案例研究中，MISO在提升归一化熵的同时，所需的验证运行次数远少于专家驱动和黑盒缩放工作流。

Conclusion: MISO提供了一种介于手动调优与不透明自动搜索之间的实用中间方案，能够以系统化、可解释的方式指导排序模型的局部优化。

Abstract: Ranking models are repeatedly refined within established model families, yet the choice of which component to scale, replace, or retire is often guided by expensive trial-and-error. We present Model Internal State Optimization (MISO), a systems workflow that uses model internal states (MIS), including parameters, activations, gradients, and normalization statistics, to prioritize such local optimization decisions. MISO extracts MIS from a trained ranking model, aggregates them into ranking, alignment, and comparison signals, and converts those signals into a small set of interpretable candidate edits. Because MIS are re-extracted after each retraining cycle, MISO naturally supports an adaptive optimization workflow that tracks evolving model behavior as data distributions and system requirements shift over time. In an ads ranking case study, MISO improves normalized entropy while requiring substantially fewer validation runs than expert-driven and black-box scaling workflows, offering a practical middle ground between manual tuning and opaque automated search.

</details>


### [39] [Teacher Retains Full Tokens, Student Merges Efficiently: TM20K for E-Commerce Sequence Modeling in Ad Recommendation](https://arxiv.org/abs/2608.07055)
*Xinchun Li,Duoru Zheng,Wenlin Zhao,Ziyi Zhou,Jingxuan Tan,Huizhi Yang,Linlan Chen,Dongjian Wang,Dongyue Wang,Xiaosong Li,Hongyue Mao,Yaocheng Tan*

Main category: cs.IR

TL;DR: TM20K是一种面向超长行为序列建模的推荐系统范式，通过全Transformer建模配合两阶段知识蒸馏框架，在保持训练和推理成本几乎不变的情况下，将电子商务推荐序列长度扩展到20K，并在字节跳动的广告推荐系统中成功部署，关键业务指标（如ADSS+1.036%）获得显著提升。


<details>
  <summary>Details</summary>
Motivation: 现有推荐系统通过超长行为序列建模同时考虑用户长期和短期兴趣，但延长序列长度会带来训练效率和推理吞吐量的巨大负担。先前的压缩方法（基于搜索或聚类）牺牲了细粒度信息，而轻量级目标注意力结构又无法充分提取序列特征，需要在效果与效率之间取得平衡。

Method: 提出两阶段知识蒸馏框架：(1) 教师和学生模型均采用全注意力机制而非纯目标序列注意力，以支持有效的序列扩展；(2) 为学生模型提出多种简单且动机明确的token合并方法，显著压缩序列长度同时保持可接受的性能；(3) 教师模型使用完整序列token进行重训练，通过知识蒸馏进一步提升学生模型性能。

Result: TM20K已在字节跳动电商广告推荐系统中成功部署，将电子商务序列长度扩展到20K。相比在线最先进的模型，在关键业务指标上（ADSS +1.036%）获得实质性提升，同时训练和部署成本几乎持平（如服务延迟仅增加5.6%）。

Conclusion: 该研究证明通过全Transformer建模与知识蒸馏框架相结合，可以在超长序列建模中同时实现高效性和有效性，为工业级推荐系统处理超长用户行为序列提供了可行的解决方案。

Abstract: Benefiting from ultra-long behavior sequence modeling, existing recommender systems bring users a better experience via simultaneously considering their long-term and short-term interests. Nevertheless, extended sequence lengths introduce substantial burdens on training efficiency and serving throughput. Prior approaches typically utilize search-based or cluster-based compression on ultra-long sequences at the cost of fine-grained information, or rely on various lightweight target attention structures incapable of sufficient sequential feature extraction. In this paper, we balance the effectiveness and efficiency for ultra-long sequence modeling via full transformer modeling accompanied with a two-stage knowledge distillation framework. First, both teacher and student models take the full attention mechanism rather than pure target-sequence attention for effective sequence scaling. For student models, we propose several simple yet well-motivated token merge approaches, significantly compressing the sequence length while maintaining an acceptable performance. Then, a one-time teacher is heavily trained with full sequence tokens, further boosting the performance of student models via knowledge distillation. The proposed paradigm named TM20K has been successfully deployed in ByteDance's e-commerce advertising recommender system that extends the e-commerce sequence length to 20K, delivering substantial improvements in key business metrics (e.g., ADSS +1.036\%) while keeping the training and serving cost nearly the same as the online state-of-the-art model (e.g., serving latency only +5.6\%).

</details>


### [40] [Invisible to the Machine: Auditing AI Restaurant, Cafe, and Bar Recommendation Against a Complete Market Census](https://arxiv.org/abs/2608.07069)
*Vladimir Pitenin*

Main category: cs.IR

TL;DR: 本文首次对AI助手的地点推荐进行了基于完整市场普查的审计。在巴厘岛两个有限市场（Canggu和Ubud）的4,776个咖啡馆、餐厅和酒吧中，针对四个主流AI系统（ChatGPT、Claude、Gemini、Perplexity）的2,208次搜索响应进行评估。研究发现85.6%的地点从未被任何系统推荐，地点进入推荐列表与文档化程度相关（评分排序无关），而推荐内的排名与评分显著相关。捏造罕见（0.08%），但推荐永久关闭地点达93次（过期数据是主要失败模式），系统间一致性低，两次周期间的差异主要源于随机抽样而非时间漂移。


<details>
  <summary>Details</summary>
Motivation: AI助手正成为本地发现的主要界面，但在食品和饮料领域，AI推荐哪些地点几乎无人知晓，而这类推荐直接涉及收入影响。以往基于抽样（sampled）的审计无法测量完整市场的可见性分布，因此本研究采用完整的普查式（census-denominated）审计方法。

Method: 对巴厘岛两个有界市场（Canggu和Ubud）的全部4,776个咖啡馆、餐厅和酒吧进行完整枚举。对四个生产级AI系统（ChatGPT、Claude、Gemini、Perplexity）施加96个角色条件化查询，收集2,208个搜索接地响应，历时7天，遵循预注册协议。释放协议、注册表构建方法和派生数据。

Result: 85.6%的地点从未被任何系统推荐（即使在有50+评分的成熟地点中也占72.6%）。可见性呈双重边际结构：进入答案与文档化相关（评论量OR 1.64、自有网站OR 1.92、价格信息OR 1.54、第三方网页提及OR 1.44），评分在此边际无效（OR 0.89）；但推荐内的排名与评分显著相关（OR 1.17）。Foursquare存在性无正面效果。捏造罕见（0.08%），但推荐永久关闭地点93次。系统间top-20 Jaccard相似度仅0.33-0.54，两周重测显示变动源于随机抽样而非时间漂移。

Conclusion: AI地点推荐的可见性由"文档化-评分"双重机制支配：能否进入推荐由文档化程度决定，而在推荐内的排名由评分决定。实际失败模式是数据过期（staleness）而非幻觉。系统的推荐变动主要来自抽样随机性而非时间变化。研究提供了首个完整的、可复现的AI地点推荐普查审计。

Abstract: AI assistants are becoming a primary interface for local discovery, yet almost nothing is known about which venues they surface -- especially in food and drink, where recommendations carry direct revenue consequences. We present the first census-denominated audit of AI venue recommendation: a complete enumeration of 4,776 cafes, restaurants, and bars across two bounded markets (Canggu and Ubud, Bali), against which we evaluate 2,208 search-grounded responses from four production AI systems (ChatGPT, Claude, Gemini, Perplexity) to 96 persona-conditioned queries, collected over seven days under a pre-registered protocol. Because we observe the full market, we can measure what sampled audits cannot: 85.6% of venues were never recommended by any system -- 72.6% even among established venues with fifty or more ratings. Visibility follows a two-margin structure. Entry into answers is associated with documentation: review volume (OR 1.64), an own website (OR 1.92), listed price information (OR 1.54), and third-party web mentions (OR 1.44) -- while star rating is null at this margin (OR 0.89). Rank within answers reverses the pattern: among recommended venues, rating significantly predicts first position (OR 1.17). Presence in an open POI dataset (Foursquare), a folk-theorized visibility factor, shows no positive effect at either margin. Outright fabrication is rare (0.08% of mentions), but systems recommended permanently closed venues 93 times -- staleness, not hallucination, is the practical failure mode. Cross-system agreement is low (top-20 Jaccard 0.33-0.54). A two-week test-retest shows cross-period answer similarity comparable to same-day rerun similarity: the churn is sampling stochasticity, not temporal drift. We release our protocol, registry construction method, and derived data.

</details>


### [41] [Exact Adaptive Hybrid Retrieval Without Fixed Top-L Cutoffs](https://arxiv.org/abs/2608.07152)
*Chunran Zhang*

Main category: cs.IR

TL;DR: EAHR是一种精确自适应混合检索方法，通过可恢复的精确稠密/稀疏排名实现完整列表加权RRF融合，无需预设Top-L截断深度。它在所有150个查询-快照组合中复现了完整列表有序Top-20，相比批处理执行实现23-30倍的几何平均延迟加速，但不保证每个请求都加速。


<details>
  <summary>Details</summary>
Motivation: 传统RAG系统固定融合Top-L结果，将未读部分视为零，但截断融合与完整列表融合不等价，且从历史查询选择的深度因查询和语料库更新而不可靠地迁移。

Method: 提出EAHR框架：1) 以完整列表加权RRF的有序Top-K为检索目标，将通道深度视为请求特定的执行状态；2) 使用Per-Vector Scalar Quantization（PVS）和Posting Block-Max（PBM）生成可恢复的精确稠密和稀疏排名；3) 融合边界约束未读贡献，仅在未读内容可能改变Top-K时才请求更多排名。

Result: 在5个测试集和5个时间快照上，完整列表加权RRF保持竞争力，历史固定深度不可靠迁移。EAHR在所有150个查询-快照组合中复现完整列表有序Top-20，批处理与EAHR的几何平均延迟比为23.35（TREC-DL 2019）和30.28（TREC-DL 2020）。反相关排名会耗尽两个列表，部分困难查询更慢。

Conclusion: EAHR不保证每个请求都加速，但它修复了精确结果的同时自适应调整执行深度以适应当前排名，在准确性和效率之间取得了平衡。

Abstract: Modern retrieval-augmented generation (RAG) systems often fuse fixed Top-$L$ results from dense and sparse retrievers, treating later contributions as zero. The cutoff therefore determines both the ranking and its execution cost. Yet truncated fusion is not generally equivalent to complete-list fusion: unread cross-list ranks can change Top-$K$ membership or order even when the observed candidates contain every item in the complete-list Top-$K$. Because channel rankings vary across queries and corpus updates, a depth selected from historical queries may not transfer reliably. We propose Exact Adaptive Hybrid Retrieval (EAHR), which fixes the ordered Top-$K$ defined by complete-list weighted RRF as the retrieval target and treats channel depth as request-specific execution state. Per-Vector Scalar Quantization (PVS) and Posting Block-Max (PBM) produce resumable exact dense and sparse rankings. Fusion bounds unread contributions and requests further ranks only while they can change the Top-$K$. Every successful request therefore matches complete-list fusion without a preset Top-$L$; otherwise, execution continues safely to list exhaustion. Across five test collections and five temporal corpus snapshots, complete-list weighted RRF remained competitive, whereas fixed depths selected from historical queries did not transfer reliably. EAHR reproduced the complete-list ordered Top-20 in all 150 query-snapshot combinations. Under a warm-cache, interleaved, order-balanced protocol, the paired geometric-mean latency ratios of exhaustive batch execution to EAHR were 23.35 on TREC-DL 2019 and 30.28 on TREC-DL 2020. Anti-correlated rankings exhausted both lists, and some difficult queries were slower with EAHR. EAHR does not guarantee a speedup for every request; it fixes the exact result while adapting execution depth to the current rankings.

</details>
