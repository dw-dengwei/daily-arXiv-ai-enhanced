<div id=toc></div>

# Table of Contents

- [cs.IT](#cs.IT) [Total: 10]
- [cs.IR](#cs.IR) [Total: 10]
- [cs.GT](#cs.GT) [Total: 2]
- [cs.DS](#cs.DS) [Total: 7]
- [cs.DB](#cs.DB) [Total: 3]
- [cs.MM](#cs.MM) [Total: 1]


<div id='cs.IT'></div>

# cs.IT [[Back]](#toc)

### [1] [Assembly Theory and the Smallest Grammar Problem](https://arxiv.org/abs/2608.19228)
*Wawrzyniec Bieniawski*

Main category: cs.IT

TL;DR: 本文对八种压缩算法（基于语法和字典的方案）在408条字符串上作为组装指数（ASI）近似器的性能进行了实证评估，并提出了Re-Pair T-NDR——一种分支定界平局消解变体，可在所有研究的算法中提供更紧的ASI上界。


<details>
  <summary>Details</summary>
Motivation: 组装理论通过组装指数（ASI）量化复杂性，该指标已被证明等价于最小直线程序（SLP）的大小。尽管这一等价性将AT与数据压缩领域联系起来，但具体算法作为ASI近似器的实际效果尚未被充分探索。

Method: 对八种压缩算法（涵盖语法型和字典型方案）进行实证评估，数据集包含408条字符串（368条合成字符串和40条天然生物序列）。引入了Re-Pair T-NDR，一种采用分支定界平局消解策略的Re-Pair变体。

Result: Re-Pair T-NDR比其他研究中的压缩算法提供更紧的ASI上界。增加字母表大小可延长压缩算法紧密跟踪ASI的范围，延迟"发散点"的出现。

Conclusion: 压缩算法可作为ASI估计的实用工具，为组装理论中的复杂度量化提供可行的计算近似手段。

Abstract: Assembly theory (AT) quantifies complexity through the assembly index (ASI) -- a metric proven equivalent to the size of the smallest straight-line program (SLP). While this equivalence links AT to data compression -- a field shaped by decades of research -- the practical efficacy of specific algorithms as ASI approximators remains largely unexplored. This study provides an empirical evaluation of eight compression algorithms, spanning grammar-based and dictionary schemes (CAs) across a dataset of 408 strings -- comprising 368 synthetic strings (including max-complexity strings and strings with varying levels of symbol distribution balance, quantified by Shannon entropy) and 40 natural biological sequences (genomic and proteomic). We introduce Re-Pair T-NDR, a branch-and-bound tie-resolving variant of Re-Pair, providing a tighter upper bound on the ASI than the other CAs researched in this study. Increasing alphabet size extends the regime in which CAs closely track the ASI, delaying a ``divergence point'', where a CA deviates from the optimal assembly path. The results establish compression algorithms as practical tools for ASI estimation.

</details>


### [2] [Tri-Hybrid Beamforming for T-RIS-Enabled Base Station](https://arxiv.org/abs/2608.19736)
*Hongtao Zhang,Chenlong Ding*

Main category: cs.IT

TL;DR: 本文提出了一种面向发射端原生透射式可重构智能表面（T-RIS）三混合多用户MIMO下行链路的统一建模与预编码框架，涵盖从连续场描述到级联基带输入输出模型，并在菲涅尔、夫琅禾费及混合场域中统一处理近场聚焦与远场波束赋形，同时提出基于WMMSE的两级求解器以联合优化数字预编码器、模拟网络与T-RIS系数。


<details>
  <summary>Details</summary>
Motivation: 与常规RIS辅助链路不同，发射端原生T-RIS直接参与辐射形成，导致其前端建模复杂，且在硬件约束下加权和速率（WSR）导向的联合预编码设计具有挑战性，亟需统一的建模与优化框架。

Method: 从连续场描述出发，刻画馈电阵列、可编程T-RIS孔径与用户端传播的相互作用，构建级联基带输入输出模型；将同一表示实例化于菲涅尔、夫琅禾费和混合场域；在功率与量化约束下建立WSR最大化问题，并设计两级求解器，结合外层WMMSE更新与WMMSE诱导的孔径场整形及硬件投影。

Result: 仿真验证了建模准确性与收敛性，表明所提出的完整三混合设计相较基线显著提升WSR，同时抑制混合场域间的跨区域泄漏。

Conclusion: 发射端原生T-RIS三混合MIMO可在统一框架下实现数字、模拟射频与电磁域的协同空间处理，相较传统方案在WSR性能与场域干扰抑制方面更具优势。

Abstract: Transmissive reconfigurable intelligent surfaces (T-RISs) integrated into the transmitter provide a viable realization of tri-hybrid multiple-input multiple-output (MIMO), where spatial processing is distributed across the digital, analog radio-frequency (RF), and electromagnetic (EM) domains. However, unlike conventional RIS-assisted links, a transmitter-native T-RIS directly participates in radiation formation, making T-RIS front-end modeling and weighted sum-rate (WSR)-oriented joint precoding challenging under practical hardware constraints. This paper develops a unified modeling and precoding framework for transmitter-native T-RIS tri-hybrid multi-user (MU) downlink transmission. Specifically, starting from a continuous-field description, the received field is characterized by the interaction among the feed array, the programmable T-RIS aperture, and the user-side propagation, leading to a cascaded baseband input-output model for MU precoding. Furthermore, the same representation is instantiated in the Fresnel, Fraunhofer, and mixed-field regimes, so that near-field focusing and far-field angular steering can be handled within one front-end model. Additionally, a WSR maximization problem is formulated over the digital precoder, analog network, and T-RIS coefficients under power and quantization constraints. A two-level solver is then developed by coupling outer weighted minimum mean-square error (WMMSE) updates with WMMSE-induced aperture-field shaping and hardware projection. Simulations validate the modeling accuracy and convergence, and show that the proposed full tri-hybrid design improves WSR over baselines while suppressing mixed-field cross-regime leakage.

</details>


### [3] [Resilience in Trustworthy Wireless Systems](https://arxiv.org/abs/2608.19850)
*Shixiong Wang,Yumeng Zhang,Hongyu Li*

Main category: cs.IT

TL;DR: 本文从可信性（trustworthiness）视角系统性地构建了无线系统韧性的框架，明确了韧性概念、与相关术语的区分、分层实现框架、典型场景应用及工程权衡。


<details>
  <summary>Details</summary>
Motivation: 韧性已成为未来无线系统在动态不确定环境中运行的基本能力，但学术界、工业界和标准化领域对韧性的概念范围、使能机制和实现技术仍然碎片化，缺乏统一系统的框架。

Method: 1) 形式化定义韧性概念，并与可靠性、鲁棒性、适应性、生存性和可恢复性等相关不确定性术语进行区分；2) 建立分层框架，将韧性组织为能力维度和使能机制，并通过不同技术方面量化韧性；3) 以物理链路和无人机网络为代表场景，展示通过架构、操作和算法的联合设计系统化实现韧性。

Result: 提出了系统化的无线系统韧性框架，并通过两个代表性无线场景（物理链路和无人机网络）演示了韧性的系统性实现，同时识别了韧性工程中的基本权衡。

Conclusion: 提升韧性通常需要在资源效率、标称性能、信息获取、实现复杂度和时延等方面付出代价，这些权衡是韧性工程中需要审慎考量的核心问题。

Abstract: Resilience has emerged as a fundamental capability for future wireless systems operating in dynamic and uncertain environments. Although resilience has attracted growing attention across academia, industry, and standardization, its conceptual scope, enabling mechanisms, and realization techniques remain fragmented. This paper presents a systematic framework of resilience in wireless systems from a trustworthiness perspective. We first formalize the concept of resilience and distinguish it from related uncertainty-aware terminologies, including reliability, robustness, adaptability, survivability, and recoverability. We then establish a hierarchical framework that organizes resilience into capability dimensions and enabling mechanisms, and that quantifies resilience through different technical aspects. We further use physical links and unmanned aerial vehicle networks as representative wireless scenarios to demonstrate how resilience can be systematically realized through the joint design of architectures, operations, and algorithms. Finally, we examine the fundamental trade-offs in resilience engineering: Improving resilience generally incurs costs in resource efficiency, nominal performance, information acquisition, implementation complexity, and latency.

</details>


### [4] [Sub-optimality of Marton's Inner Bound for the Two-Receiver Broadcast Channel](https://arxiv.org/abs/2608.19869)
*Mian Huang,Yanxiao Liu,Yi Liu*

Main category: cs.IT

TL;DR: 本文证明了Marton内界（1979年提出的通用离散无记忆广播信道最佳可达区域）并非总是严格最优的，展示了在某些离散无记忆广播信道中，容量区域可以严格大于Marton内界，从而解决了这一长期悬而未决的问题。


<details>
  <summary>Details</summary>
Motivation: Marton内界自1979年提出以来，一直是通用离散无记忆广播信道的最佳已知可达区域，但其是否总是能达到容量区域的问题悬而未决长达数十年，本文旨在解决这一开放问题。

Method: 通过构造特定的离散无记忆广播信道实例，证明其容量区域严格大于Marton内界，从而建立Marton内界的严格次优性。

Result: 成功证明了Marton内界是严格次优的，即存在某些离散无记忆广播信道，其容量区域严格大于Marton内界所给出的区域。

Conclusion: Marton内界并非总是可达容量区域，这结束了一个持续数十年的开放问题，表明需要寻找更紧的广播信道容量刻画方法。

Abstract: Marton's inner bound, the best-known achievable region for a general discrete memoryless broadcast channel, was proposed by Katalin Marton in 1979, and whether it always achieves the capacity region has remained open since then. In this paper, we establish its strict sub-optimality: we show that the capacity region of some discrete memoryless broadcast channels can be strictly larger than Marton's inner bound.

</details>


### [5] [A Layered Simplex Architecture for Large Alphabets](https://arxiv.org/abs/2608.19908)
*Meir Feder,Yaniv Fogel,Ruediger Urbanke*

Main category: cs.IT

TL;DR: 本文提出了一种新的贝叶斯估计器用于大字母表下的概率估计（log loss），其构造极其简单：将独立均匀分布的概率单纯形样本逐坐标相乘并归一化。该估计器具有四个特性：构造简单（深度是唯一结构参数，可通过平均深度消除调参）、遗憾值有显式且高效可计算的表达式、无需调参但性能可与Good-Turing等专门方法竞争、遗憾值的可计算性揭示了数据、字母表大小和深度之间的标度律。


<details>
  <summary>Details</summary>
Motivation: 大字母表下的概率估计（如Good-Turing估计器）是经典问题，但现有方法通常需要调参或缺乏理论可解析性。作者希望设计一种既简单又具有理论可分析性的贝叶斯估计器，同时保持与实际专门方法相当的竞争力。

Method: 提出一种贝叶斯估计器：将概率单纯形上的独立均匀分布样本逐坐标相乘后再归一化，深度是唯一的结构参数，对不同深度取平均以消除调参需求。推导出该混合估计器的遗憾值（相对于已知真实源的码长超量）的显式且高效可计算的表达式，并通过该表达式分析其标度律。

Result: 该方法在合成数据和真实文本基准上与包括Good-Turing在内的专门方法具有竞争力。针对Zipf分布（指数大于1）的目标，遗憾值有简单解读：只要样本仅揭示字母表的一小部分，遗憾值就接近于已发现符号集合的描述长度（每比特描述对应一比特码长），再加上每个符号的额外成本。数据指数即新符号被发现的速率。

Conclusion: 该估计器在简单性、理论可解析性和实际性能之间取得了良好平衡。其遗憾值表达式允许识别数据量、字母表大小和深度之间的标度律，为理解大字母表概率估计的固有代价提供了新的理论视角。

Abstract: Probability estimation over large alphabets under log loss is a well-studied problem, with celebrated methods such as the Good-Turing estimator. We introduce and study a new Bayesian estimator with four notable properties. First, its construction is exceptionally simple: multiply independent uniform draws from the probability simplex coordinate-wise and renormalize. Depth is the only structural parameter, and averaging over depths eliminates the need to tune it. Second, the regret of the resulting mixture, the excess code length it pays relative to a code that knows the source, admits an explicit and efficiently computable expression. Third, despite its simplicity and lack of tuned constants, the estimator is competitive across a diverse set of synthetic and real-text benchmarks with substantially more specialized methods, including Good-Turing. Fourth, the tractability of its regret allows us to identify scaling laws in data, alphabet size, and depth. For Zipf targets with exponent above one, the regret has a simple reading as long as the sample reveals only a small fraction of the alphabet. It closely matches the description length of the set of discovered symbols, at one bit of code per bit of description, plus a further cost per symbol. The data exponent is therefore the rate at which new symbols are discovered.

</details>


### [6] [Real Interference Alignment for Active IRS-Aided Systems: A Rate-Profile Learning-Based Approach](https://arxiv.org/abs/2608.20007)
*Junda Liao,Quanzhong Li,Qi Zhang*

Main category: cs.IT

TL;DR: 本文提出了一种用于主动智能反射面（IRS）辅助系统的实干扰对齐（IA）方案，并设计了基于速率轮廓学习的算法，以在满足最小速率要求和功率约束下最大化可达和速率。


<details>
  <summary>Details</summary>
Motivation: 主动IRS提供了额外的空间自由度，可以低成本实现干扰对齐（IA），但现有方案在IRA辅助系统下的干扰对齐和速率优化问题尚未得到充分研究。

Method: 提出实干扰对齐方案，仅需IRS知道瞬时信道系数（假设直连链路被阻断）；提出基于速率轮廓学习的算法，利用离线训练的可达速率轮廓将原始问题分解为多个可行性子问题，并通过广义特征值分解求解。

Result: 仿真结果表明，所提出的算法在性能上优于传统加权最小均方误差（WMMSE）算法，同时程序执行时间显著减少。

Conclusion: 基于速率轮廓学习的算法能够在主动IRS辅助系统中高效实现实干扰对齐，在提高可达和速率的同时大幅降低计算复杂度。

Abstract: With additional spatial degrees of freedom provided by the active intelligent reflecting surface (IRS), interference alignment (IA) can be achieved at low cost. In this letter, we propose a real IA scheme for an active IRS-aided system. The proposed scheme only requires the IRS to know the instantaneous channel coefficients under the assumption of blocked direct links. To maximize the achievable sum rate subject to individual minimum rate requirements and transmission power constraints, we propose a rate-profile learning-based algorithm. The algorithm uses offline-trained achievable rate profiles to decouple the original problem into multiple feasibility subproblems, which are then solved by generalized eigenvalue decomposition. Simulation results demonstrate that our proposed algorithm outperforms the conventional weighted minimum mean square error algorithm, while requiring significantly less program execution time.

</details>


### [7] [RMWorld: Task-Aware Radio World Models with Value-of-Information Guided Multi-Trial Learning for Multi-UAV Communication Control](https://arxiv.org/abs/2608.20126)
*Xiucheng Wang,Nan Cheng,Junxi Huan*

Main category: cs.IT

TL;DR: 本文提出RMWorld，一个任务感知的无线世界模型框架，通过结合值信息信道校准与可信度多样性多试验选择，解决多无人机通信中无线链路预测误差非均匀导致的控制决策问题。


<details>
  <summary>Details</summary>
Motivation: 多无人机通信控制依赖在测量前预测空中链路流量，但无线电世界模型（radio WM）的误差是非均匀的：全局精确的模型可能在高需求走廊或关联边界处失败，导致速率误差反转控制决策。现有方法分别处理采集和基于模型的控制预算，关注不确定性、覆盖率或乐观回报而非风险降低，缺乏任务感知的链路查询与反事实试验选择机制。

Method: RMWorld框架包含两个核心组件：(1) 值信息信道校准：用贝叶斯残差校正有偏传播公式，通过任务集成后验速率方差的一标签精确减少量来评估每条链路的价值； (2) 可信度多样性多试验选择：通过任务门控log-determinant目标选择反事实分支，再进行冲突投影和固定批次验证。论文还推导了方差缩减恒等式，证明了后验任务风险等价性和子模贪心保证，并建立了范围化的一阶非干涉结果。

Result: 在100个配对的3GPP试验中，RMWorld达到0.949 bit/s/Hz的任务加权RMSE；在30个严重负载的DeepMIMO试验中，相较于Ensemble UCB，中位数积压减少0.967，同时离线滚动成本增加37.5%。

Conclusion: RMWorld通过将任务感知的值信息采集与可信度多样性试验选择相结合，有效解决了无线世界模型非均匀误差下的控制决策问题，显著优于现有基线方法。理论分析（方差缩减恒等式、后验任务风险等价性、子模贪心保证、非干涉结果）为方法的可靠性和可扩展性提供了严谨保障。

Abstract: Reliable multi-UAV communication control depends on predicting which aerial links will serve traffic before measurements are available. Radio world models (radio WMs) make such planning tractable, but their errors are nonuniform: a globally accurate model may still fail along high-demand corridors or association boundaries where rate errors reverse control decisions. This mismatch creates a learning challenge. Link queries must reduce decision-relevant channel uncertainty, while counterfactual trials must be filtered so that biased rollouts do not corrupt the policy. Existing acquisition and model-based control treat these budgets separately, valuing uncertainty, coverage, or optimistic return rather than risk reduction. We present RMWorld, a task-aware radio-WM framework that couples value-of-information channel calibration with credibility-diversity multi-trial selection. A biased propagation formula is corrected by a Bayesian residual, and each link is valued by its exact one-label reduction in locally linearized task-integrated posterior rate variance. Counterfactual branches are selected by a task-gated log-determinant objective, followed by conflict projection and fixed-batch validation. We derive the variance-reduction identity, prove posterior task-risk equivalence and the submodular greedy guarantee, and establish a scoped first-order non-interference result. Across 100 paired 3GPP trials RMWorld reaches 0.949~bit/s/Hz task-weighted RMSE, and across 30 severe-load DeepMIMO trials it reduces median backlog by 0.967 versus Ensemble UCB at 37.5\% more offline rollouts.

</details>


### [8] [The Generalized Random Access Problem for Linear Codes](https://arxiv.org/abs/2608.20152)
*Anina Gruica,Antonio Petrillo,Ferdinando Zullo*

Main category: cs.IT

TL;DR: 本文研究DNA存储系统中多符号同时恢复的随机访问问题。在组合模型中，定义τ_I(G)为恢复信息向量集合I中所有向量所需采样的列数，提出了均匀最坏情况和平均参数，建立了上下界（下界通过单符号恢复变量的顺序统计量表达），并对系统MDS编码器、单纯形编码器及三维平衡拟弧给出了具体结果。


<details>
  <summary>Details</summary>
Motivation: DNA存储系统中随机访问是核心需求，希望在无需测序整个编码对象的前提下恢复选定的信息符号。现有模型只研究单符号恢复，而实际应用中往往需要同时恢复多个符号，因此需要扩展理论框架来研究多符号同时恢复的复杂度特征。

Method: 基于已知的E[τ_I(G)]子集计数公式，引入均匀最坏情况和平均参数；通过顺序统计量建立下界；对系统MDS编码器推导等价期望公式及单调性和渐近性质；对单纯形编码器用高斯二项式系数给出闭式公式；在三维空间中研究平衡拟弧并与MDS基准比较。

Result: 建立了多符号恢复参数的普适上下界，其中下界可退化为m=1时的已知单符号界；系统MDS编码器获得期望公式的等价形式及单调性/渐近结果；单纯形编码器在所有维度下获得闭式公式，全恢复端点与已知覆盖深度公式一致；三维平衡拟弧的值与单纯形和MDS基准进行了比较。

Conclusion: 该研究将DNA存储中的随机访问问题从单符号扩展到多符号同时恢复，在基数极值理论和有限几何两个层面建立了完整的理论框架，为DNA存储系统设计提供了多符号恢复场景下的理论指导。

Abstract: Random access is a central requirement in DNA-based storage systems: one would like to recover selected information symbols without sequencing the whole encoded object. A recent combinatorial model associates to a generator matrix $G\in F_q^{k\times n}$ the random variable $τ_i(G)$, measuring the number of sampled columns needed to recover the information vector $e_i$. We study the cardinality-based extremal and finite-geometric aspects of simultaneous multi-symbol recovery. For a nonempty set $I\subseteq[k]$, let $τ_I(G)$ denote the number of random column samples needed until all vectors $e_i$, $i\in I$, lie in the span of the observed columns. This variable interpolates between the singleton random access problem and the full-recovery problem underlying coverage depth. For each $m$, we introduce uniform worst-case and average parameters over all requested sets $I$ with $|I|=m$. Using the known subset-counting formula for $E[τ_I(G)]$, we establish general upper and lower bounds for these parameters. In particular, the lower bounds are expressed through order statistics of the singleton recovery variables and specialize to the known singleton bounds when $m=1$. For systematic MDS encoders, we record an equivalent form of the known multi-symbol expectation formula and derive monotonicity and asymptotic consequences. For simplex encoders in arbitrary dimension, we obtain closed formulae in terms of Gaussian binomial coefficients; the full-recovery endpoint agrees with the known coverage-depth formula for simplex codes. Finally, in dimension three we study balanced quasi-arcs and compare their values with the simplex and MDS benchmarks.

</details>


### [9] [Algorithms, Complexity, and Entropy of the Bernard-Letac Fair-Sampling Construction](https://arxiv.org/abs/2608.20234)
*Claude Gravel*

Main category: cs.IT

TL;DR: 本文扩展了Bernard和Letac（1971）提出的从未知偏倚独立同分布符号源中均匀随机采样m个结果的方法，提出了五个具有形式化正确性保证和全面复杂度分析的算法。对于素数次p=m的情况进行了更详细的分析，给出了期望抽取次数的精确乘积公式，研究发现一阶近似总是高估实际值且熵下界从未达到。此外，设计了一个七状态自动机来计算二元游走的模2首达核，将公平分配成本从二次降至近线性。


<details>
  <summary>Details</summary>
Motivation: Bernard和Letac在1971年提出的均匀随机采样方法虽然优雅，但其计算和信息论特性尚未被充分理解。作者旨在更深入地分析这一框架，特别是素数情形下的期望成本特性，并寻求更高效的计算实现，以弥补现有理论分析与计算复杂性分析的不足。

Method: 作者提出了五个具有形式化正确性保证的算法，并对Bernard-Letac框架进行了计算和信息论分析。对于素数m=p的情况，利用源的Rényi熵推导出期望抽取次数的精确乘积公式。还设计了一个七状态自动机来计算二元游走的模2首达核，通过算法优化将成本从O(n²)降至近线性。

Result: 研究发现一阶近似总是高估期望抽取次数，且由Rényi熵得出的下界从未被达到。当p趋近1时，期望成本收敛到一个大于1的常数，该常数由整个源分布决定。七状态自动机成功将公平分配成本从二次复杂度降至近线性复杂度。

Conclusion: 该研究深化了对Bernard-Letac采样方法的理论理解，揭示了其熵论特性（包括近似的高估性和下界的不可达性），并通过七状态自动机实现了显著的复杂度改进，为偏倚源的均匀随机采样提供了理论与实践的双重贡献。

Abstract: Bernard and Letac (1971) introduced a method for uniform random sampling among m outcomes from an unknown biased source of independent and identically distributed symbols. The process terminates when the multinomial coefficient of the cumulative symbol counts equals zero modulo m. This study extends the computational and information-theoretic analysis of their construction by presenting five algorithms with formal correctness guarantees and comprehensive complexity analyses. For prime m = p, the Bernard-Letac framework is analyzed in greater detail. The Rényi entropies of the source yield an exact product formula for the expected number of draws. A first-order approximation consistently overestimates this value, and the entropy lower bound is never attained. As p approaches 1, the expected cost converges to a constant greater than 1, determined by the entire source distribution. Furthermore, a seven-state automaton computes the mod-2 first-passage kernel of the binary walk, reducing the fair assignment cost from quadratic to nearly linear.

</details>


### [10] [The Honeycomb Framework for Code Bounds](https://arxiv.org/abs/2608.20287)
*William Gay,Fernando Granha Jeronimo,Lenny Liu*

Main category: cs.IT

TL;DR: 本文提出了一个表示论框架——"蜂巢层级"(honeycomb hierarchy)，用于给出二进制编码率 R₂(δ) 的新的渐近上界。其第一层是 hyperoctahedral 表示图，通过保留所有两行不可约表示和坐标盒传递通道，结合移动投影定理，得到一个显式的四参数指数 κ_HC。该界在所有 0<δ<1/2 范围内优于先前的组合界限，且层级可向两个方向扩展（表示深度和锚定深度），最终可恢复 A₂(n,d)。


<details>
  <summary>Details</summary>
Motivation: 先前最优的二进制编码率上界曲线是 κ_bin = min{κ_CW, κ_H}，其中 κ_H（整立方体指数）和 κ_CW（常权重分支）分别来自不同的方法。作者希望构建一个更统一的表示论框架，以同时改进这两个分支，获得比 MRRW 界限更强的界。

Method: 引入蜂巢层级框架：(1) 第一层使用两行 hyperoctahedral 表示图，保留所有两行不可约表示和坐标盒传递通道，配合移动投影定理，得到四参数指数 κ_HC；(2) 通过增加表示深度，将标量传递推广为蜂巢上的矩阵值传递；(3) 通过增加锚定深度，在稳定集层级中局部化；(4) 提出互补的 Horn-通道层级，其 2×2 层即 κ_HC，3×3 层给出更强的界。

Result: 证明了对 0<δ<1/2，R₂(δ) ≤ κ_best(δ) ≤ κ_bin(δ) ≤ R_2MQC(δ) < M₂(δ)，其中 κ_best = min{κ_CW, κ_HC} 严格优于先前的 κ_bin。还证明了 κ_best ≤ min{κ_CW, κ_bal} < R_2MQC，且 κ_H = R_MQC。层级在两个方向上的扩展都是单调的，最终可恢复 A₂(n,d)。

Conclusion: 蜂巢层级提供了统一的表示论框架，不仅改进了所有中间范围内的二进制编码率上界，还提供了一个通往更紧界限的系统性路径。与 Horn-通道层级的结合意味着在低层次就能改进此前的通用界，为未来突破 MRRW 界限提供了方向。

Abstract: We introduce the honeycomb hierarchy, a representation-theoretic framework that gives new asymptotic upper bounds on $R_2(δ)$. Its first level is the two-row hyperoctahedral representation graph associated with type $S^{(n-k,k)}$. Retaining every two-row irreducible and every coordinate box-transfer channel, together with a moving-projection theorem, yields an explicit four-parameter exponent $κ_{\mathrm{HC}}$. The earlier whole-cube exponent $κ_H$ is a boundary restriction of this optimization, whereas the fully optimized second MRRW exponent $M_2$ is an exact symmetric slice.
  The prior best curve is the combined $κ_{\mathrm{bin}}=\min\{κ_{\mathrm{CW}},κ_H\}$, which uses a constant-weight branch $κ_{\mathrm{CW}}$. Replacing only the whole-cube branch by the honeycomb bound gives $κ_{\mathrm{best}}=\min\{κ_{\mathrm{CW}}, κ_{\mathrm{HC}}\}$. We prove, on $0<δ<1/2$, \[
  R_2(δ)\le κ_{\mathrm{best}}(δ)
  \le κ_{\mathrm{bin}}(δ)
  \le R_{\mathrm{2MQC}}(δ)<M_2(δ),\\[-1mm]
  κ_{\mathrm{best}}(δ)
  \le \min\{κ_{\mathrm{CW}}(δ),
  κ_{\mathrm{bal}}(δ)\}
  <R_{\mathrm{2MQC}}(δ),
  \qquad
  κ_H(δ)=R_{\mathrm{MQC}}(δ). \]
  The hierarchy has two further directions. Increasing the representation depth replaces scalar by matrix-valued transfers on the hive. Increasing the anchor depth localizes it in a stable-set hierarchy. The resulting bounds are monotone in both directions and eventually recover $A_2(n,d)$. A complementary Horn--channel hierarchy gives matrix optimizations whose $2\times2$ level is $κ_{\mathrm{HC}}$ and whose $3\times3$ level is a stronger bound. Already at low levels, they can be used to improve the strongest previous general bounds, while the honeycomb framework provides a route towards tighter bounds.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [11] [CrossQ: Task-Aligned Cross-Token Conditional Quantization for Late Interaction Retrieval](https://arxiv.org/abs/2608.19204)
*Rohit Kumar Salla,Manoj Saravanan,Ramya Manasa Amancherla*

Main category: cs.IR

TL;DR: 本文提出CrossQ，一种针对晚期交互检索模型（如ColBERT）的索引压缩方法。该方法通过在索引时利用轻量文档上下文条件化token编码，并在训练中采用排序对齐目标，在极低存储（2-8 B/token）下显著提升检索质量，最高实现64倍存储压缩比，同时保留约98%的全精度检索性能。


<details>
  <summary>Details</summary>
Motivation: 晚期交互检索器（如ColBERT）虽然检索质量高，但依赖大规模多向量索引，存储开销巨大。现有标准压缩方法仅最小化token重构误差，却忽略了检索排名实际上高度依赖于少量稀疏"胜出"token的分数保真度，导致压缩后排名质量受损。

Method: CrossQ的核心思想是在索引时利用轻量文档上下文条件化token编码（该上下文不存储），从而自适应提升文档内有效token的保真度。训练时采用与排名对齐的目标函数，既保持候选分数分布，又保护难负样本的边际。该方法支持在2、4、8 B/token等不同压缩级别下运行，并可通过轻量微调进一步提高质量。

Result: 在2 B/token下，CrossQ在MRR@10上比最强严格足迹匹配的量化基线提升+0.010，比最强候选匹配系统参考提升+0.012。在9个数据集的BEIR子集上，4 B/token时平均nDCG@10比最强候选匹配系统参考提升+0.009。4 B/token时实现64倍原始token存储缩减（含元数据约61倍，保守对齐核算约58倍）。8 B/token配合轻量微调保留约98%全精度ColBERT的MRR@10。

Conclusion: CrossQ通过在索引时条件化token编码配合排序对齐训练目标，有效改善了晚期交互检索在极低存储预算下的足迹-质量权衡，为内存受限场景下的late-interaction检索提供了更优的压缩方案。

Abstract: Late-interaction retrievers like ColBERT achieve high quality but suffer from large multi-vector indices. Standard compression minimizes token reconstruction error, while ranking depends critically on preserving scores of sparse "winner" tokens. We introduce CrossQ, which adaptively improves effective token fidelity within documents by conditioning token codes on lightweight document context computed at indexing time (but not stored). CrossQ is trained with ranking-aligned objectives that preserve candidate score distributions and protect hard-negative margins. At 2 B/token, CrossQ improves MRR@10 by +0.010 over the strongest strictly footprint-matched quantization baseline and by +0.012 over the strongest candidate-matched system reference. On a nine-dataset BEIR subset, CrossQ improves average nDCG@10 by +0.009 at 4 B/token over the strongest candidate-matched system reference. At 4 B/token, CrossQ achieves 64x raw token-storage reduction, approximately 61x including metadata and approximately 58x under conservative padding/alignment accounting. At 8 B/token, CrossQ with light fine-tuning retains approximately 98% of full-precision ColBERT MRR@10, improving the footprint-quality tradeoff for memory-constrained late-interaction retrieval.

</details>


### [12] [Quantization Beyond Uniform Bit Allocation](https://arxiv.org/abs/2608.19388)
*K. S. Sreeramji,Sabyasachi Basu,Ravishankar Krishnaswamy,Kirankumar Shiragur,Yujia Wang*

Main category: cs.IR

TL;DR: 本文提出了一种可变比特分配（variable bit allocation）框架，针对具有Matryoshka结构（MRL）的嵌入向量，在固定内存预算下将嵌入划分为连续桶并非均匀分配存储位，相比均匀分配基线在PQ和SQ上分别提升最多8%和18%的召回率。


<details>
  <summary>Details</summary>
Motivation: 现有的量化方案大多与嵌入结构无关，对所有维度均匀分配比特。然而现代模型生成的嵌入具有显著的几何结构（如Matryoshka性质），均匀分配在这些结构化的嵌入上效率低下，因此需要探索可变比特分配来提升量化质量。

Method: 提出一个简单的可变比特分配框架：将嵌入划分为连续桶，并在固定内存预算下非均匀分配存储比特。采用贪婪分配策略（greedy allocation），将该框架实例化到乘积量化（PQ）和标量量化（SQ）两种方案中。

Result: 在具有MRL性质的嵌入上，非均匀分配在相同存储预算下始终优于均匀基线。在相同压缩率下，可变分配对PQ提升最多8%召回率，对SQ提升最多18%召回率。改进最大出现在低比特率区域，此时均匀分配对MRL嵌入尤为低效。

Conclusion: 实验结果揭示了结构感知的压缩与索引技术是大型检索系统的一个有前景的新方向，可变比特分配能够有效利用嵌入的内在结构来提升量化性能。

Abstract: Quantization is a fundamental technique to handle the growing sizes of embeddings generated by modern models. Existing quantization schemes are largely embedding agnostic and allocate bits uniformly across dimensions. However, recent models produce embeddings with significant geometric structure. In this work, we investigate whether a variable bit allocation scheme can improve quantization quality under a fixed memory budget. We propose a simple variable bit allocation framework that partitions an embedding into contiguous buckets and allocates storage non-uniformly across them. Using a greedy allocation strategy, we instantiate this framework for both Product Quantization (PQ) and Scalar Quantization (SQ). We perform a series of experiments on embeddings known to have the Matryoshka property (MRL), and consistently observe that non-uniform allocations outperform uniform baselines at identical storage budgets. The largest improvements occur in the low-bit regime, where uniform allocation is particularly inefficient for MRL embeddings. At the same compression rates, variable allocation improves recall by up to 8\% for PQ and up to 18\% for SQ. Our results suggest a new direction for structure-aware compression and indexing techniques for large-scale retrieval systems.

</details>


### [13] [HARP: Hierarchical Adaptive Ranking with Preference-Adaptive Fusion for Query-Based CVE Prioritization](https://arxiv.org/abs/2608.19430)
*Haochen Liu,Zhengzhang Chen,Haoyu Wang,Yanchi Liu,Jundong Li,Haifeng Chen*

Main category: cs.IR

TL;DR: 本文提出HARP框架，用于查询驱动的CVE优先级排序。在该场景下，组织的修复偏好通常是隐式的，难以用文字明确表达，但历史标注的triage案例更容易获得。HARP利用漏洞知识图谱检索证据，通过策略条件化的全局、企业和用户三个视图对候选CVE进行评分，并从支持样本中拟合视图融合权重，无需显式偏好文本描述即可实现有效的优先级排序。


<details>
  <summary>Details</summary>
Motivation: 漏洞优先级排序本质上依赖于运营偏好场景，同一CVE在不同偏好下可能获得不同的修复优先级。现有评分系统和排序方法通常假设固定标准，而实际组织中偏好往往是隐式的，难以用提示词表达，但历史triage案例容易获取。因此需要一种不依赖显式偏好描述、仅依靠历史标注样本进行CVE优先级排序的方法。

Method: HARP是一个基于知识图谱的多视图框架：(1)从漏洞知识图谱检索证据；(2)使用策略条件化的全局视图、企业视图和用户视图分别对候选CVE进行评分；(3)从采样的支持样本中拟合视图融合权重，实现无需显式偏好描述的自适应排序。

Result: 在三个偏好场景和多个骨干大语言模型上的实验表明，HARP在多个基线上表现更优，验证了方法的有效性。

Conclusion: HARP通过利用历史标注样本和知识图谱，有效解决了隐式偏好场景下的CVE优先级排序问题，为实际运营中的漏洞管理提供了实用的解决方案。

Abstract: Vulnerability prioritization is inherently preference dependent, since the same CVE can receive different remediation priority under different operational preference scenarios. Existing scoring systems and ranking methods typically assume a fixed criterion. In practice, organizations already operate under a preference scenario, but this preference is often implicit and difficult to express as a written prompt instruction, while triage queries usually do not encode it. Past validated triage cases under the current scenario are more readily available. We study query-based CVE prioritization in this setting and propose HARP, a graph-grounded multi-view framework that ranks candidates from a natural-language query together with a support bank of historical labeled examples from the current preference scenario, without requiring an explicit textual summary of that scenario. HARP retrieves evidence from a vulnerability knowledge graph, scores candidates with policy-conditioned global, enterprise, and user views, and fits view-fusion weights from sampled supports. Experiments across three preference scenarios and multiple backbone LLMs show that HARP outperforms multiple baselines, expressing our method's effectiveness.

</details>


### [14] [SSR-GRPO: Integrating Supervision and Semantic IDs into Reinforcement Learning for Dense Retrieval in E-commerce](https://arxiv.org/abs/2608.19595)
*Guangxin Song,Xing Fang,Mingmin Jin,Jing Wang,Bokang Wang,Zhentao Song,Junjie Bai,Jianbo Zhu*

Main category: cs.IR

TL;DR: 本文提出SSR-GRPO方法，通过语义标识符（SIDs）和双视角相关性评估、难负样本挖掘等策略，改进R-GRPO在稠密检索中候选样本噪声大和奖励模型偏差的问题，并在大规模电商平台成功部署。


<details>
  <summary>Details</summary>
Motivation: 电商搜索中的嵌入检索（EBR）常面临复杂语义挑战。现有方法（如R-GRPO）虽引入强化学习到稠密检索，但存在两个核心问题：1）由于批量采样有限导致top-K候选样本噪声大；2）使用相似训练的LLM作为奖励模型导致相关性评估存在偏差。

Method: 1）提出双视角相关性评估框架，结合量化学习产生的语义标识符（SIDs）和稠密表示向量生成更无偏的相关性分数；2）利用SIDs的层次相似关系挖掘难负样本，用于设计掩码函数集成到R-GRPO中过滤组内噪声样本；3）构建Retrieval-DPO任务（正负样本对），从成对视角捕获细粒度语义差异。

Result: 大量离线和在线实验验证了SSR-GRPO的有效性，并已在大型电商平台成功部署上线。

Conclusion: SSR-GRPO通过引入语义标识符和双视角评估、难负样本挖掘等机制，有效解决了R-GRPO在稠密检索中的噪声和偏差问题，展示了其在工业级电商搜索场景中的实用价值。

Abstract: Embedding-based retrieval (EBR) is pivotal in e-commerce search but often struggles with complex semantics. While recent methods often fine-tune large language models (LLMs) for representation learning, they typically lack robust mechanisms for handling complex and implicit semantics. While Retrieval-GRPO (R-GRPO) recently introduced reinforcement learning to dense retrieval, it suffers from noisy top-K candidates due to limited batch sampling and biased relevance assessments caused by using similarly trained LLMs as reward models. To tackle these issues, we propose Supervised Retrieval-GRPO with Semantic Identifiers (SSR-GRPO). Specifically, our method first proposes a dual-perspective framework for relevance assessment. It leverages both Semantic Identifiers (SIDs) produced by quantization learning and dense representation vectors to generate more unbiased relevance scores. Furthermore, leveraging the hierarchical similarity relationships of the generated SIDs, we mine a set of hard negative samples that serve two purposes: (1) to design a masking function integrated into R-GRPO, effectively filtering intra-group noisy samples; and (2) to construct a Retrieval-DPO task composed of positive and negative sample pairs, enabling the model to capture fine-grained semantic distinctions from a pair-wise perspective. By integrating these optimization strategies, we propose SSR-GRPO. Extensive offline and online experiments validate SSR-GRPO's effectiveness, and it has been deployed on a large-scale e-commerce platform.

</details>


### [15] [Training-Free LLM-Based Recommendation with Post-LLM Item Refinement Using Collaborative Signals](https://arxiv.org/abs/2608.19665)
*Kyungho Kim,Sunwoo Kim,Geon Lee,Shinhwan Kang,Sojeong Kim,Liam Collins,Bhuvesh Kumar,Donald Loveland,Kijung Shin*

Main category: cs.IR

TL;DR: CoRRe是一种无需训练（training-free）的推荐框架，采用"后LLM"（post-LLM）范式，将协同过滤信号注入到LLM生成的物品表示中，再与LLM生成的用户兴趣进行匹配排序。它利用物品共购图优化物品嵌入的方向，利用物品流行度调整其幅度，在真实数据集上优于现有无训练方法，并与基于训练的方法性能相当或更优。


<details>
  <summary>Details</summary>
Motivation: LLM在无训练推荐中表现出潜力，但生成的用户兴趣往往过于宽泛，难以实现细粒度的物品检索。现有方法采用"前LLM"（pre-LLM）方式融合协同过滤信号（如候选重排或提示增强），收益有限，亟需更有效的方法来注入CF信号。

Method: CoRRe采用后LLM范式：(1) 使用LLM生成物品嵌入和用户兴趣表示；(2) 通过物品-物品共购图优化物品嵌入的方向（direction）；(3) 利用物品流行度调整物品嵌入的幅度（magnitude）；(4) 将精炼后的物品表示与LLM生成的用户兴趣匹配进行排序推荐。

Result: 在真实数据集上的实验表明，CoRRe持续优于现有的无训练方法，并且与基于训练的方法相比达到竞争性或更优的性能，且无需任何模型训练或任务特定的微调。

Conclusion: CoRRe证明了在后LLM阶段注入协同过滤信号是一种比前LLM方式更有效的范式，能够在不训练、不微调的情况下显著提升LLM推荐的细粒度性能，为无训练推荐提供了新的思路。

Abstract: Large language models (LLMs) have shown promise for training-free recommendation, but LLM-generated user interests are often too broad for fine-grained item retrieval. Existing methods incorporate collaborative filtering (CF) signals in a pre-LLM manner through candidate reranking or prompt augmentation, yielding limited gains. We propose CoRRe, a training-free recommendation framework with a post-LLM paradigm that injects CF signals into LLM-generated item representations, which are later matched with LLM-generated user interests for ranking. Specifically, CoRRe refines the directions of item embeddings using an item-item co-purchase graph and their magnitudes using item popularity. Experiments on real-world datasets show that CoRRe consistently outperforms existing training-free methods and achieves competitive or superior performance compared with training-based methods, without requiring any model training or task-specific fine-tuning.

</details>


### [16] [Do Sequential Recommendation Benchmarks Really Require Higher-Order Sequence Modelling?](https://arxiv.org/abs/2608.19833)
*Aleksandr V. Petrov,Praveen Chandar,Paul N. Bennett,Hugues Bouchard,Mounia Lalmas*

Main category: cs.IR

TL;DR: 本文通过两个简单的基于近因加权的成对探针模型（SeqRules和PCTM）评估了序列推荐基准测试的有效性，发现这些广泛使用的基准数据集中大部分并不需要高阶序列建模能力，Transformer类模型在这些基准上的优势可能被高估。


<details>
  <summary>Details</summary>
Motivation: 序列推荐系统越来越多地采用语言模型架构来捕捉复杂、上下文依赖的交互，但现有基准测试是否真正需要这种高阶建模能力尚不清楚。作者希望验证广泛使用的基准数据集能否有效衡量高阶序列建模带来的增益。

Method: 作者设计了两种简单的不学习高阶序列表示的近因加权成对探针模型：Sequential Rules (SeqRules) 和概率协作转移模型 (PCTM)。在eSASRec和采样softmax SASRec的评估协议下，将这两种探针模型与Transformer基线在多个数据集上进行对比。

Result: 在eSASRec协议下，至少一个探针在三个Amazon数据集上超过eSASRec复现结果15-38%，在MovieLens-1M上超过4.4%，但在MovieLens-20M上落后27.3%。在其余四个数据集上，至少一个探针也超过采样softmax SASRec复现结果9-28%。

Conclusion: 大多数广泛使用的基准数据集不能有效衡量高阶序列建模带来的增益。将Transformer模型与强近因加权成对探针进行比较，可以作为检验基准是否有意义地衡量高阶序列建模增益的具体测试。

Abstract: Sequential recommenders increasingly use language-model architectures designed to capture complex, context-dependent interactions. Yet it remains unclear whether widely used benchmarks actually require this modelling capacity. We investigate this question using two simple, recency-weighted pairwise probes that do not learn higher-order sequence representations: Sequential Rules (SeqRules) and our Probabilistic Collaborative Transition Model (PCTM). Using the evaluation protocol of eSASRec, at least one probe exceeds our eSASRec reproduction by 15-38% on three Amazon datasets and by 4.4% on MovieLens-1M, but trails it by 27.3% on MovieLens-20M. On the four remaining datasets, at least one probe also outperforms our sampled-softmax SASRec reproduction by 9-28%, suggesting that these widely used benchmarks are poorly suited to measuring gains from higher-order sequence modelling. More broadly, comparing Transformer-based models against strong recency-weighted pairwise probes provides a concrete test of whether a benchmark can meaningfully measure gains from higher-order sequence modelling.

</details>


### [17] [SCoRD: Semantic-Assisted Continual Retriever-Reranker Distillation for LLM-Based Recommendation](https://arxiv.org/abs/2608.19998)
*Seunghyun Baek,Gyuseok Lee,Seunghan Lee,Wonbin Kweon,Dong Wang,SeongKu Kang*

Main category: cs.IR

TL;DR: SCoRD是一个面向LLM重排序流水线的持续知识蒸馏框架，通过语义推理助手蒸馏用户意图知识，在非平稳数据流下实现检索器-重排序器的高效协同适应。


<details>
  <summary>Details</summary>
Motivation: 现有的两阶段推荐流水线（ID检索器+LLM重排序器）在实际部署中需要持续适应新数据，但反复更新LLM重排序器代价高昂，而仅更新检索器又因容量有限难以从稀疏数据中学习。

Method: SCoRD引入语义推理助手，将LLM推断用户潜在意图的能力蒸馏为可复用的意图级指导；在低置信度序列上选择性蒸馏重排序器知识到检索器；无需重复LLM推理即可指导检索器更新；并将检索器表示和意图漂移信号反馈给重排序器。

Result: 在真实世界数据集上的实验表明，SCoRD能够实现有效且高效的检索器-重排序器协同适应。

Conclusion: SCoRD通过选择性蒸馏、意图级知识复用和双向信号反馈，解决了LLM重排序流水线在持续学习场景下的效率与效果平衡问题。

Abstract: Recommendation systems increasingly adopt a two-stage pipeline, where an ID-based retriever retrieves candidates and an LLM-based reranker refines their rankings. To improve retrieval quality, reranker-to-retriever distillation is commonly used to transfer the reranker's knowledge to the retriever. For practical deployment, however, this pipeline must continually adapt to evolving interests and incoming interactions. A naive solution is to repeatedly update the LLM reranker and distill its latest knowledge, but this incurs prohibitive costs. Updating the retriever alone is cheaper, but its limited capacity makes adaptation from sparse data difficult. We propose SCoRD, a continual knowledge distillation framework for LLM-based reranking pipelines under a non-stationary data stream. SCoRD introduces a semantic reasoning assistant that distills the LLM's ability to infer underlying user intents into reusable intent-level guidance. It selectively distills reranker knowledge to the retriever on low-confidence sequences, guides retriever-only updates without repeated LLM inference, and feeds retriever-derived representations and intent-drift signals back to the reranker. Experiments on real-world datasets show that SCoRD enables effective and efficient retriever-reranker co-adaptation.

</details>


### [18] [Daedalus-150M: A Convolution-Attention Hybrid Designed for CPU Inference](https://arxiv.org/abs/2608.20210)
*Christos Koutsiaris*

Main category: cs.IR

TL;DR: 本文提出了一种"CPU优先"设计的小语言模型：固定目标为单用户、单token、4-bit权重、普通CPU，由此选择架构——18个block中仅6个用全注意力，其余12个用短卷积（内存仅两个时间步宽，不随对话增长重读缓存）。从零训练于59.9B tokens，在五任务基准上得分47.31（预设基线42.20），超越多个同尺寸模型且优于MobileLLM-125M。混合架构相比同规模全注意力模型在质量上有微小优势、4-bit文件小6.3%、解码速度快1.76x，且速度优势随上下文变长而增大。


<details>
  <summary>Details</summary>
Motivation: 现有小语言模型通常沿用大模型的架构设计，再压缩部署到CPU上，这种"先建后压"的思路效率低下。作者反其道而行之，先固定部署目标（CPU、4-bit、单用户），再据此选择架构，目的是在严格受限的硬件条件下最大化推理效率和模型质量。

Method: 采用混合架构：18个block中6个保持全注意力，12个使用短卷积层（内存宽度只有两个时间步，不随上下文增长），从而避免三分之二的网络反复读取越来越大的缓存。模型从零训练于59.9B tokens，并使用4-bit权重量化。实验设置了预设的基准线和先验的获胜条件以公平评估架构（而非训练策略）的作用。

Result: 模型在五任务基准上得47.31分（预设基线42.20），超越GPT-2 124M、Pythia-160M、OPT-125M、GPT-neo-125M（这些模型训练数据量为3-6倍），也超过MobileLLM-125M（训练了1万亿tokens）。验证bits-per-byte为0.8685。相比同规模全注意力模型（相同数据训练），混合架构在质量指标上领先0.81%，下游任务持平，4-bit文件小6.3%，解码快1.76x（2048 tokens上下文），2.08x（对比外部同规模模型）。速度优势在空上下文时≈0，随长度增长——符合机制预测而非单纯更精简模型所致。带宽计算仅预测1.17x，说明单纯内存量无法解释全部差距。还报告了失败经验：未缓解的4-bit质量损失、约一半卷积通道惰性且无法移除、vocabulary过大不值得。

Conclusion: 该工作证明了"以部署目标为先导"的架构设计范式在小语言模型上的有效性：通过限制键架构选择（部分层用短卷积替代注意力）可以在不牺牲质量的情况下显著提升长上下文推理速度。架构选择而非训练策略是性能优势的关键。同时坦诚报告了未成功的探索方向，为后续研究提供了有价值的负面结果。

Abstract: Small language models are usually built like large ones and then squeezed onto a CPU afterwards. We did the opposite: we fixed the target first, one user, one token at a time, 4-bit weights, ordinary CPU, and chose the architecture to suit it. The result keeps full attention in only 6 of its 18 blocks. The other 12 use short convolutions whose memory is two timesteps wide no matter how long the conversation gets, so two thirds of the network never re-reads a growing cache.
  Trained from scratch on 59.9B tokens, the model scores 47.31 on a five-task benchmark against a bar of 42.20 that was fixed before training began. It beats GPT-2 124M, Pythia-160M, OPT-125M and GPT-neo-125M, all trained on three to six times more data, and exceeds MobileLLM-125M's published score despite that model seeing a trillion tokens. Validation bits-per-byte is 0.8685.
  To check the architecture rather than the training recipe, we trained a conventional all-attention model of the same size on the same data, and wrote down the winning condition before scoring either. The hybrid won the chosen quality metric by 0.81%, matched it on downstream tasks, produced a 6.3% smaller 4-bit file, and decoded 1.76x faster at 2048 tokens of context, 2.08x against an external model of similar size. In every measurement the speed advantage is near zero at an empty context and grows with length, which is what the mechanism predicts and what a merely leaner model would not show. A simple bandwidth calculation predicts only 1.17x, so memory volume alone does not explain the gap.
  We also report what did not work: an unmitigated 4-bit quality cost, roughly half the convolution channels ending up inert and impossible to remove, and a vocabulary larger than this model size warrants.

</details>


### [19] [What Makes a Good Fiqh Retriever? Answer Retrieval for Arabic Islamic Jurisprudence](https://arxiv.org/abs/2608.20246)
*Somaya Eltanbouly,Heba Sbahi,Samer Rashwani,Abdessalam Bouchekif,Mutaz al-Khatib,Shahd Gaben,Mohammed Ghaly*

Main category: cs.IR

TL;DR: 本文针对阿拉伯语伊斯兰法学（fiqh）问答任务，构建了"答案承载型"检索测试集，系统评估了稠密、词法、混合、微调及教法学派感知等多种检索策略。最优检索器MRR@5达0.524，微调后提升至0.553，而教法学派感知过滤在学派特定问题上使MRR@5提升超过一倍。错误分析表明主要挑战在于区分包含所需裁决的答案承载段落与主题相似但不含所需裁决的段落。


<details>
  <summary>Details</summary>
Motivation: 现有检索增强生成系统用于伊斯兰问答时通常进行端到端评估，无法将检索失败与生成失败隔离。作者希望单独研究检索环节，特别是阿拉伯语fiqh场景中"答案承载型"检索（段落必须包含问题所要求的裁决才算相关）这一独特挑战。

Method: 构建了阿拉伯语fiqh检索测试集，评估五种检索策略：稠密检索、词法检索、混合检索、微调模型以及教法学派感知（madhhab-aware）过滤。通过MRR@5等指标比较各策略表现，并进行错误分析以理解检索失败的原因。

Result: 最佳检索器MRR@5为0.524；微调提升至0.553；混合检索对强模型增益有限；教法学派感知过滤在学派特定问题上使MRR@5提升超过一倍。错误分析揭示核心难点是区分答案承载段落与主题相似但不含所需裁决的干扰段落。

Conclusion: 独立评估检索环节是发现和解决RAG系统瓶颈的关键。教法学派信息是提升检索效果的强有力信号，而问题与答案承载段落之间的语义匹配（尤其是裁决层面的匹配）仍是当前检索模型面临的核心挑战。

Abstract: Retrieval-Augmented Generation is used for Islamic question answering, but most systems are evaluated end-to-end, making retrieval failures difficult to isolate from generation failures. We study answer-bearing retrieval for Arabic fiqh, where a passage is relevant only if it states the ruling required by the question. We build a retrieval test collection for Arabic fiqh and use it to evaluate dense, lexical, hybrid, fine-tuned, and madhhab-aware retrieval strategies. The best retriever achieves 0.524 MRR@5, while fine-tuning improves performance to 0.553. Hybrid retrieval provides limited gains for strong models, whereas madhhab-aware filtering more than doubles MRR@5 on school-specific questions. We further present an error analysis showing that the main challenge is distinguishing answer-bearing passages from topically similar passages that do not contain the requested ruling.

</details>


### [20] [Projecting BrowseComp-Plus onto ClimbMix: Toward More Realistic Corpora for Agentic Search](https://arxiv.org/abs/2608.20317)
*Sahel Sharifymoghaddam,Lingwei Gu,Yijun Ge,Jimmy Lin*

Main category: cs.IR

TL;DR: 本文提出了BrowseComp-Plus_CM基准，通过投影管道将BrowseComp-Plus的问题证据迁移到NVIDIA的ClimbMix大规模语料库（400B token、553M文档）中，解决了原基准语料规模小且证据与干扰项按查询选择性组装的问题。作者建立了一个将问题分解为原子推理跳跃并在新语料中逐一验证的投影管道，最终从830个测试问题中筛选出57个完全有据可依的问题。实验发现投影将挑战转移到检索环节——最强智能体答案准确性下降5个点，证据召回率从84.3%骤降至21.4%，同时检索调用增加了63%。


<details>
  <summary>Details</summary>
Motivation: 现有的BrowseComp-Plus虽然通过固定语料分离了智能体与检索器的角色，但其约10万篇文档的语料规模过小，且证据和干扰项都是针对基准自身查询专门挑选的，难以真实反映开放网络环境中的智能体搜索能力。需要一个更大规模、与基准无关的语料库来更真实地评估智能体的检索与推理能力。

Method: 提出投影管道：将每个查询问题分解为原子推理步，在ClimbMix（400B token、553M文档的NVIDIA预训练混合语料）中为每一步查找证据支撑；只有通过自动验证、独立智能体和人工评审三重确认的所有步骤都被支持的查询才被保留；该管道与数据集无关，适用于任何可分解为可验证事实的基准问题。

Result: 从830个BrowseComp-Plus测试问题中投影出57个完全有据可依的问题；投影后最强智能体的答案准确性下降5个百分点，证据召回率从84.3%降至21.4%，搜索调用次数增加63%，表明检索难度显著提升。

Conclusion: 投影管道有效将基准评估从"evidence-adjacent"设置转向更大规模、更自然的检索密集型设置，揭示了智能体方法在更大语料下的检索短板。这是系列投影工作的第一步，作者已发布管道、基准和分析代码。

Abstract: The BrowseComp-Plus benchmark disentangled the evaluation of agentic search by replacing opaque web search with a fixed corpus, so that an agent's role can be separated from the retriever's. That corpus, however, holds only about 100K documents and was assembled from the supporting documents of the benchmark's own queries plus mined hard negatives, so the evidence and the distractors were both selected per query. We introduce $\text{BrowseComp-Plus}_{\text{CM}}$, which keeps the BrowseComp-Plus questions but relocates their evidence to ClimbMix, a 400B-token, 553M-document mixture of web text released by NVIDIA for pre-training language models and built without reference to any benchmark. Our main contribution is the projection pipeline that makes this possible: it decomposes each question into atomic reasoning hops and grounds every hop in the new corpus, retaining a question only when automatic verification, an independent agent, and human review all confirm that every hop is supported. The pipeline is dataset-agnostic and applies to any benchmark whose questions decompose into verifiable facts. Applied to the 830 BrowseComp-Plus test questions, our pipeline yields 57 fully grounded questions with question-level relevance judgments. Projection shifts the difficulty onto retrieval, as the strongest agent we evaluate loses five points of answer accuracy but sees its evidence recall fall from 84.3% to 21.4% while issuing 63% more search calls. As the first of a series of projections, we release the pipeline, the benchmark, and our analyses at https://github.com/castorini/cmass.

</details>


<div id='cs.GT'></div>

# cs.GT [[Back]](#toc)

### [21] [Product Gap Mechanisms for Multi-Facility Location](https://arxiv.org/abs/2608.19633)
*Jianhao Jia*

Main category: cs.GT

TL;DR: 本文研究实数轴上多个设施选址的随机策略证明机制，提出了Product-Gap机制，证明其对k个设施可实现2k的紧致近似比（社会成本），并证明k=2和k=3时满足期望策略证明性但k≥4时不再满足。对于两个设施，通过将Product-Gap与Proportional机制混合，得到近似比约3.519的策略证明机制，优于此前4的因子。


<details>
  <summary>Details</summary>
Motivation: 多设施选址是经典的社会选择与算法博弈论问题。虽然已有随机策略证明机制（如Proportional机制）的研究，但在一维实数轴上的多设施场景中，找到同时具备策略证明性和良好近似比的机制仍具挑战性。此前对两个设施的最好结果近似比为4，作者希望设计更优的机制。

Method: 作者提出Product-Gap机制：以正比于所选k个报告位置之间连续间隙乘积的概率选择k个位置并开设设施。作者分析其社会成本近似比，研究其策略证明性（k=2,3时成立，k≥4不成立）。此外，对两个设施情况，将Product-Gap与Lu等人的Proportional机制进行报告无关的混合，并通过优化混合权重获得改进的近似比。

Result: Product-Gap机制对任意k≥2实现2k的紧致近似比；k=2和k=3时是期望策略证明的，分别给出4-和6-近似的策略证明机制；k≥4时非策略证明。对两设施的优化混合机制是策略证明的，紧致近似比为(74+4√3)/23≈3.519，改进了此前4的上界。

Conclusion: 文章系统分析了基于间隙乘积的概率选址机制，为多设施随机策略证明机制提供了新的构造思路。两设施混合机制的近似比改进是理论上的突破，同时揭示了Product-Gap机制在k≥4时的策略证明性失效，为该问题的进一步研究留下开放空间。

Abstract: We study randomized strategyproof mechanisms for locating multiple facilities on the real line. We introduce the \emph{Product-Gap mechanism}, which selects $k$ reported locations with probability proportional to the product of the consecutive gaps between them and opens facilities at the selected locations. We prove that, for every $k\geq 2$, the mechanism achieves a tight approximation ratio of $2k$ for social cost. We then study its incentive properties and show that it is strategyproof in expectation for $k=2$ and $k=3$, but is not strategyproof for $k\geq 4$. In particular, the mechanism gives strategyproof $4$- and $6$-approximations for two and three facilities, respectively. Finally, for two facilities, we combine Product-Gap with the Proportional mechanism of Lu et al. We show that an optimized report-independent mixture is strategyproof and has a tight approximation ratio of $(74+4\sqrt{3})/23\approx 3.519$ on the line, improving upon the previous factor of $4$.

</details>


### [22] [Temporal Fair Division of Indivisible Mixed Manna: Tractable Settings](https://arxiv.org/abs/2608.20033)
*Kui-Wang Choi,Minming Li,Nicholas Teh*

Main category: cs.GT

TL;DR: 本文研究不可分割混合曼纳（mixed manna）的时间公平分配问题。物品随时间到达并需不可撤销地分配，每件物品对某些智能体是好的、对某些是苦差、对其余是中性的。要求每轮之后的累积分配满足"至多一项嫉妒"（EF1）。尽管即使只有物品（goods）时判定TEF1分配是否存在也是NP难的，但本文识别了多个可处理的情形，并给出了相应的在线规则和算法。


<details>
  <summary>Details</summary>
Motivation: 在线公平分配是一个重要且具有挑战性的问题。当物品随时间到达且必须立即分配时，保持公平性（如EF1）非常困难。已有的工作大多只考虑纯goods或纯chores，而现实场景中同一物品对不同智能体可能具有不同性质（好/苦差/中性），即混合manna。此外，在时间维度上维持累积分配的公平性（TEF1）此前很少被系统研究。

Method: (1) 针对至多k种物品类型，提出在线循环规则，保证每轮后达到EF⌈k/2⌉；(2) 针对智能体特定的比例缩放一致情形，设计在线规则保证每轮后EF1且帕累托最优；(3) 针对两段到达序列且共同排序的情形，给出每轮后EF1的规则；(4) 当智能体数量固定且价值为有界整数时，给出精确伪多项式算法判定TEF1存在性；(5) 对goods情形，证明每个TEF1分配每轮给每个智能体至少1/n的最大最小份额，并分析精确时间最大最小份额分配的NP难性。

Result: (1) 至多两种物品类型时，每实例都存在在线TEF1分配；当智能体和类型数量固定时，TEF1存在性可在多项式时间内判定。(2) 在比例缩放已知的情况下，在线规则可同时保证EF1和帕累托最优。(3) 对两段公共排序到达序列，存在每轮EF1的规则。(4) 对有界整数价值情形，给出精确伪多项式算法。(5) goods情形下TEF1保证至少1/n的最大最小份额且下界紧；精确时间最大最小份额分配判定问题对goods和chores均为NP难。

Conclusion: 本文系统性地研究了时间公平分配混合manna问题，在总体NP难的前提下，通过多种结构性假设（有限的物品类型、智能体特定缩放达成一致、公共排序的两段到达序列、有界值）识别出若干可处理的情形，并给出相应的在线规则或算法。此外，建立了TEF1分配与最大最小份额之间的紧界关系，并刻画了精确时间最大最小份额分配的复杂性。

Abstract: We study temporal fair division of indivisible mixed manna. Items arrive over time and must be allocated irrevocably; an item may be a good for some agents, a chore for others, and neutral for the rest. We require the cumulative allocation after every round to be envy-free up to one item (TEF1). Although deciding whether a TEF1 allocation exists is NP-hard even for goods, we identify several tractable settings. First, with at most $k$ item types, an online cyclic rule guarantees EF$\lceil k/2\rceil$ after every item arrival. Thus, every instance with at most two types admits an online TEF1 allocation; moreover, when the numbers of agents and types are fixed, TEF1 existence can be decided in polynomial time. Second, under agreement after agent-specific scaling, provided that the scaling factors are known before arrivals begin, an online rule produces an allocation that is EF1 and Pareto optimal after every item arrival. Third, for a two-part arrival sequence with common rankings, we give a rule that is EF1 after every item arrival. Fourth, when the number of agents is fixed and values are bounded integers, we give an exact pseudo-polynomial algorithm for deciding TEF1 existence. Finally, for goods, every TEF1 allocation gives each agent at least $1/n$ of her maximin share after every round; this factor is tight even for identical valuations and two rounds. Deciding whether an exact temporal maximin-share allocation exists is NP-hard for both goods and chores, even with identical valuations, two agents, and two rounds.

</details>


<div id='cs.DS'></div>

# cs.DS [[Back]](#toc)

### [23] [Breaking the $2^n$ Barrier for Counting Linear Extensions with a Short Elementary Algorithm](https://arxiv.org/abs/2608.19505)
*Keigo Oka*

Main category: cs.DS

TL;DR: 本文给出了一个确定性精确算法，用于在O*(1.89^n)时间内计算任意n元偏序集(Poset)的线性扩展（线性扩展即保持偏序关系的全序）数量。该算法突破了通用的2^n下界，解决了Koivisto在2013年Dagstuhl会议上明确提出的开放问题。


<details>
  <summary>Details</summary>
Motivation: 计算偏序集的线性扩展数量是组合计数中的一个基本问题，其重要性在于它在排序、统计推断（如部分排序数据的排名）等多个领域有广泛应用。此前一般性问题的最佳算法是O*(2^n)，研究者们关注是否存在突破2^n下界的算法。Koivisto在2013年Dagstuhl会议上明确提出了这一问题，本文旨在解决这一开放问题。

Method: 本文算法综合利用了多种技术：(1) 链划分技巧处理远离反链（antichain）的偏序集情形；(2) 对于接近反链的情形，固定一个最大反链，记录其元素中具有反链外可比元素者首次出现的顺序（编码引理从多重度枚举模式）；(3) 模式固定后，每个反链元素具有释放条件和至多一个截止期限，动态规划仅存储每个截止期限类中已释放元素的数量；(4) 用星条（stars-and-bars）计数方法界定总状态数。整个证明精炼了Kozma关于二维偏序集的方法。

Result: 本文在O*(1.89^n)时间内计算任意n元偏序集的线性扩展数量，严格优于此前O*(2^n)的最优结果，打破了2^n的障碍。值得注意的是，1.89^n指的是常数小于2的指数复杂度。

Conclusion: 本文成功突破了计算线性扩展数量的2^n算法下界，给出了O*(1.89^n)的确定性精确算法，解决了Koivisto提出的开放问题。该工作不仅在理论复杂度上取得了突破，也为组合计数问题的算法设计提供了新的技术思路。

Abstract: A linear extension of a finite partially ordered set is a total ordering that respects the partial order. We give a deterministic exact algorithm that counts the linear extensions of an arbitrary $n$-element poset in time $O^*(1.89^n)$, where $O^*(\cdot)$ suppresses polynomial factors. This breaks the $2^n$ barrier for the general problem and resolves a question explicitly posed by Koivisto at Dagstuhl 2013. The proof refines an argument of Kozma for two-dimensional posets. A chain partition handles the case in which the poset is sufficiently far from an antichain. Otherwise, fix a maximum antichain (a largest set of pairwise incomparable elements). For each of its elements that has a comparable element above it outside the antichain, we record only which such element appears first. A decoding lemma enumerates the resulting patterns from their multiplicities. Once a pattern is fixed, each antichain element has a release condition and at most one deadline, so the dynamic program stores only the number of released elements in each deadline class. A stars-and-bars count bounds the total number of states.

</details>


### [24] [A simple and practical $o(\sqrt{n})$-time algorithm for shortest paths in power law graphs](https://arxiv.org/abs/2608.19538)
*Jiaqi Mao*

Main category: cs.DS

TL;DR: 本文提出并分析了Pruned Bidirectional Search (PBS)，一种用于幂律图（参数β∈[2,3)）的简单亚线性近似最短路径算法。PBS无需预处理即可与轻量级索引算法的性能相当：运行时间为O(n^((1-1/log log n)/2))，并以高概率返回长度不超过最短路径41/32倍的结果。若允许n^Θ(2-1/log log n)时间的预处理，查询时间可提升至n^Θ(1/log log n)。实验表明PBS比现有方法快1.84-7.76倍，近似比不超过1.05。


<details>
  <summary>Details</summary>
Motivation: 大图中计算最短路径是基础且具有实际意义的问题。现有算法存在两难：索引类方法需要大量预处理，而遍历类方法时间复杂度较高。作者希望设计一种无需预处理、同时性能可与索引方法媲美的算法，以在幂律图中高效求解最短路径。

Method: 提出的PBS算法是一种剪枝双向搜索方法，利用幂律图的度分布特性进行剪枝。算法不依赖预处理，在双向搜索过程中通过剪枝策略减少搜索空间，实现亚线性运行时间；同时给出了理论分析保证近似比，并扩展了带预处理版本以进一步提升查询效率。

Result: PBS在无预处理情况下运行时间为O(n^((1-1/log log n)/2))，近似比为41/32（高概率）；允许预处理后查询时间可降至n^Θ(1/log log n)。在真实与合成幂律图上的实验显示，PBS比现有方法快1.84-7.76倍，近似比至多1.05。

Conclusion: PBS证明了在幂律图上可以无需索引预处理就能获得近似最短路径的优秀性能，在理论与实践上均优于现有方法，为大规模图上的路径查询提供了新的高效方案。

Abstract: Computing shortest paths in large graphs is, and remains, a fundamental and practically motivated problem. While many algorithms were proposed to calculate shortest path between pairs of vertices efficiently, many of them (index-based methods) require substantial preprocessing, while others (traversal-based methods) have higher time complexity. In this paper, we propose and analyze Pruned Bidirectional Search (PBS), a simple sublinear approximation algorithm for power-law graphs with parameter $β\in[2,3)$: our algorithm does not require any preprocessing, yet exhibits performance comparable to light index-based algorithms (of linear or sublinear index size): that is, PBS runs in time $O(n^{(1-1/\log\log n)/2})$ and, with high probability, returns a path with length within $\frac{41}{32}$ of the shortest path. Moreover, if one does allow a $n^{Θ(2-1/\log\log n)}$-time preprocessing step, its query time improves to $n^{Θ(1/\log\log n})$. We complement our theoretical results by experiments on both real-world and synthetic power-law graphs, which show that PBS is typically $1.84\times$-$7.76\times$ times faster than existing alternatives, while achieving an approximation ratio at most 1.05.

</details>


### [25] [New Complexity Results for Fair Repetitive Scheduling](https://arxiv.org/abs/2608.19952)
*Moran Koren,Michael L. Pinedo,Dvir Shabtay*

Main category: cs.DS

TL;DR: 本文重新审视单机重复调度问题中的公平性求解，给定n个客户端和q个周期（天数），每天每个客户端提交一个作业，目标是构建q个调度方案，使每个客户端获得的服务质量（QoS）达到预定义阈值。作者解决了文献中遗留的三个开放问题，并指出了若干未来研究方向。


<details>
  <summary>Details</summary>
Motivation: 以往研究已提供各种QoS度量下的复杂度分类和近似算法，但仍有几个重要问题悬而未决，需要进一步解决这些开放问题。

Method: 论文通过理论分析方式，对重复调度问题的三个开放问题进行了复杂度判定和算法设计，并识别了新的研究方向。

Result: 解决了先前文献中遗留的三个开放问题，同时提出了若干新的未来研究方向。

Conclusion: 该工作填补了重复公平调度问题中的部分理论空白，为进一步研究指明了方向。

Abstract: We revisit the problem of finding fair solutions to repetitive scheduling problems with a single machine. In this problem, we are given a set of $n$ clients and a planning horizon consisting of $q$ periods (days). Each day, every client submits a single job that must be processed by the machine. The objective is to construct a set of $q$ schedules, one for each day, such that the quality of service (QoS) received by each client meets a predefined threshold. The QoS measure may be any standard scheduling criterion, such as the total waiting time or total completion time of a client's jobs over the entire planning horizon. This problem has been studied in the literature, with previous works providing complexity classifications and approximation algorithms for various QoS measures. Nevertheless, several important questions remain open. In this paper, we resolve three of these questions and identify several additional directions for future research.

</details>


### [26] [The Greedy Superstring Algorithm Achieves Ratio 2 for Strings of Length 6 Already](https://arxiv.org/abs/2608.20018)
*Nikolai Chukhin,Alexander S. Kulikov,Ivan Mihajlin,Alexander Smal*

Main category: cs.DS

TL;DR: 本文研究最短公共超串（SCS）问题中贪心算法的近似比。贪心超串猜想称贪心算法的近似比为2，该猜想已开放40年。作者证明对于长度至少为6的字符串实例，贪心算法的近似比可以达到2；同时完全刻画了长度为3的字符串实例，证明其近似比恰好为9/5。


<details>
  <summary>Details</summary>
Motivation: 贪心超串猜想是SCS领域悬而未决40年的核心问题。该猜想若成立，贪心算法将超越目前已知最优算法的近似保证，同时它也是线性时间的最简单近似算法。然而即使是固定字符串长度k的实例，其近似比ρ_k也尚未完全确定，目前只知道2-1/k ≤ ρ_k ≤ min{(k+1)/2, 3.396}的界限。

Method: 作者通过构造性方法证明：(1) 构造了长度为6的字符串实例，使得贪心算法的近似比恰好达到2，从而证明ρ_k ≥ 2对于所有k ≥ 6成立；(2) 对长度为3的实例进行完全分析，精确确定ρ_3 = 9/5，即给出匹配的上界和下界证明。

Result: 证明了对于所有k ≥ 6，ρ_k ≥ 2，即长度6的字符串已足以使贪心算法达到近似比2；同时完全刻画了ρ_3 = 9/5，彻底解决了长度为3字符串实例的贪心算法最坏情况行为分析。

Conclusion: 该工作部分解决了贪心超串猜想，证实了长度至少为6的字符串可以实现近似比2的下界。此外，对k=3情况的完全刻画填补了该维度的空白。然而，对于k=4和k=5的近似比以及该猜想是否对所有k成立，仍有待进一步研究。

Abstract: In the Shortest Common Superstring (SCS) problem, one is given a set of strings and asked to find a shortest string containing every input string as a substring. The greedy superstring conjecture states that the natural greedy algorithm, which repeatedly merges a pair of strings with maximum overlap, has approximation ratio $2$. The greedy algorithm runs in linear time and is arguably the simplest approximation algorithm for SCS. If the conjecture holds, it would also surpass the approximation guarantees of the best known algorithms. The conjecture has remained open for 40 years. Even the approximation ratio $ρ_k$ for instances whose strings all have length $k$ is unknown; for every $k \ge 3$, we have $2 - 1/k \le ρ_k \le \min\{(k+1)/2, 3.396\}$.
  We prove that strings of length 6 already suffice to achieve approximation ratio $2$: $ρ_k \ge 2$ for every $k \ge 6$. We also prove that $ρ_3 = 9/5$, completely characterizing the worst-case behavior of the greedy algorithm for strings of length 3.

</details>


### [27] [Parameterized Complexity of Temporal Agony](https://arxiv.org/abs/2608.20077)
*Tom-Lukas Breitkopf,Vincent Froese,Anton Herrmann,Pascal Kunz*

Main category: cs.DS

TL;DR: 本文研究了时间网络中层次发现问题的参数化复杂性，即Seg-Agony问题（时间层级痛苦问题）。作者通过系统分析，精确定位了该问题在参数k（可能的排名数量）下的复杂度边界：k=2时可在多项式时间内求解，k=3且顶点最多改变一次排名时为NP难但α固定时可多项式求解，k=4且改变一次时即使α=0也是NP难的。此外，作者还提出了针对常数顶点数的多项式算法，以及对组合参数(n+ℓ)的固定参数可跟踪算法。


<details>
  <summary>Details</summary>
Motivation: 现实世界中的网络通常以多层次结构组织，揭示时间网络中的层次结构是一个重要问题。Tatti在2018年提出了Seg-Agony问题来解决这一挑战，但该问题的计算复杂度尚未被充分理解。作者希望通过参数化复杂度分析，精确定位该问题的计算复杂性边界，为实际应用中的算法选择提供理论指导。

Method: 作者对Seg-Agony问题进行了系统的参数化复杂度分析，主要关注参数k（可能的排名数量）。研究采用了多重技术手段：对k=2的情况设计了多项式时间算法；通过归约证明k=3和k=4时的NP难性；针对常数α提出了多项式算法；对于n和ℓ组合参数，设计了固定参数可跟踪算法。

Result: 研究结果为：(1) k=2时问题可在多项式时间内求解；(2) k=3且ℓ=1时问题是NP难的，但当α为常数时可多项式求解；(3) k=4且ℓ=1时即使α=0问题也是NP难的；(4) 对于常数顶点数n，存在多项式时间算法；(5) 对于组合参数n+ℓ，问题具有固定参数可跟踪性。

Conclusion: 本研究精确刻画了Seg-Agony问题的计算复杂度边界，揭示了该问题在参数k下的复杂性转折点：从k=2的多项式可解跨越到k=3的NP难，并从k=3（α固定时可解）进一步恶化到k=4（即使α=0也NP难）。这些结果为该问题的算法设计提供了清晰的理论指引。

Abstract: Real-world networks are often organized in several layers forming a hierarchy which determines the interaction between the individual components. In order to discover such hierarchies in temporal networks, Tatti [ECML PKDD 2018] introduced the temporal agony problem Seg-Agony. Here, the goal is to assign each vertex a certain rank (from 1 to $k$) such that arcs only point from lower ranks to higher ranks. Backward arcs are penalized depending on the difference between the corresponding ranks. Since arcs may change over time, each vertex is allowed to change its rank $\ell\ge 1$ times in order to minimize the overall penalty $α$ (called temporal agony).
  We study the parameterized complexity of Seg-Agony with a special focus on the number $k$ of possible ranks for which we identify the precise complexity border. We show that the problem is polynomial-time solvable for $k=2$, NP-hard for $k=3$ and $\ell=1$ but polynomial-time solvable for constant $α$, and NP-hard for $k=4$ and $\ell=1$ even for $α=0$. We further show a polynomial-time algorithm for a constant number $n$ of vertices and fixed-parameter tractability for the combined parameter $n+\ell$.

</details>


### [28] [Quantifying over Optimal MSO-Definable Sets on Graphs of Bounded Clique-Width](https://arxiv.org/abs/2608.20175)
*Tatsuya Gima*

Main category: cs.DS

TL;DR: 本文提出了AmCMSO，一种扩展的计数一元/二元二阶逻辑，其中包含引用最小值和最大值满足赋值的谓词。作者在有限团宽图上为AmCMSO₁证明了固定参数可处理的模型检验元定理，在有限树宽图上为AmCMSO₂证明了类似结果。这些元定理产生了双层级图优化问题的固定参数可处理算法，包括拦截问题和预分配问题（用于解的唯一化），以及无需以最优值为参数的最大化最优解多样性算法。相反，允许最优性谓词依赖外部集合变量会使模型检验在多项式层次的每一层都变得困难，即使在固定深度的树上也是如此。


<details>
  <summary>Details</summary>
Motivation: 双层级图优化问题（如拦截问题、预分配问题）在理论上是重要的优化问题，但一直缺乏统一的算法框架。现有逻辑方法（如CMSO）不足以表达涉及最优解性质的优化问题。作者希望通过扩展CMSO逻辑，使其能够直接表达"最优解"相关的性质，从而为这类问题提供统一的固定参数可处理算法。

Method: 作者在CMSO逻辑中引入新的谓词，这些谓词可以引用最小值和最大值满足赋值。具体分为两个变体：AmCMSO₁适用于图类上的一元逻辑（与团宽相关），AmCMSO₂适用于二元逻辑（与树宽相关）。通过建立这些扩展逻辑的模型检验元定理，将双层级优化问题转化为逻辑可表达的问题。<br>同时，作者证明了一个下界结果：如果允许最优性谓词依赖外部集合变量（即离开AmCMSO的限制范围），则即使在固定深度的树上，问题也会变得在多项式层次每一层都困难。

Result: 1) AmCMSO₁在有限团宽图上有FPT模型检验算法。2) AmCMSO₂在有限树宽图上有FPT模型检验算法。3) 这些元定理直接导出双层级优化问题的FPT算法，包括解唯一化相关的拦截和预分配问题，以及最大化最优解多样性的算法（无需以最优值为参数）。4) 下界结果：引入外部集合变量依赖的最优性谓词会使问题在多项式层次所有层级上变得困难，即使在固定深度树上。

Conclusion: 本文通过扩展CMSO逻辑，成功地为双层级图优化问题提供了一套统一的固定参数可处理算法框架，同时精确刻画了这种可处理性的极限——外部集合变量的引入会导致难度急剧上升。这一工作为双层级优化问题的算法研究提供了坚实的理论基础。

Abstract: We introduce $\mathsf{AmCMSO}$, an extension of counting monadic second-order logic ($\mathsf{CMSO}$) with predicates that refer to minimum- and maximum-value satisfying assignments. We establish fixed-parameter tractable model-checking meta-theorems for $\mathsf{AmCMSO}_1$ on graphs of bounded clique-width and for $\mathsf{AmCMSO}_2$ on graphs of bounded treewidth. These meta-theorems yield fixed-parameter tractable algorithms for several bilevel graph optimization problems, including interdiction and preassignment problems for solution uniquification, as well as algorithms for maximizing the diversity of optimal solutions without parameterizing by the optimum value. In contrast, allowing an optimality predicate to depend on an external set variable makes model checking hard for every level of the polynomial hierarchy, even on trees of fixed depth.

</details>


### [29] [Differentially Private Continual Release with Relative Error](https://arxiv.org/abs/2608.20230)
*Bo Li,Wei Wang,Peng Ye*

Main category: cs.DS

TL;DR: 本文研究了差分隐私下持续发布模型中的MaxSum、MinSum、MaxSelect和MinSelect等基本任务。研究发现，若允许相对误差项且输入流是非适应性生成的，则误差可大幅降低；但在适应性数据流下，选择小累积和属性的任务不可避免地需要大误差，而其他任务仍可实现小误差界，揭示了非适应性与适应性流之间的显著分离。


<details>
  <summary>Details</summary>
Motivation: 先前研究表明这些任务在持续发布模型下必须承受较大的纯加性误差，本文旨在探索引入相对误差项后是否能显著降低误差，并考察输入流生成方式（自适应与非自适应）对误差界的影响，以填补该领域理论分析的空白。

Method: 本文通过理论分析分别处理非自适应和自适应两种输入流设定。对于非自适应流，设计算法在允许相对误差项的条件下大幅缩减误差；对于自适应流，通过下界证明证明MinSelect任务的大误差不可避免，同时为其他任务给出小的误差界算法，并配以近乎匹配的上下界结果。

Result: 在非自适应流下，允许相对误差可以显著减小误差；在自适应流下，MinSelect任务需要大误差而下界几乎匹配，同时其他任务仍可保持小误差界，展示了非自适应与自适应流之间的显著分离。

Conclusion: 本文揭示了差分隐私持续发布模型中误差大小与输入流适应性之间的根本关系，确认了相对误差项的价值以及自适应流对某些任务造成的本质限制，为后续在该方向的设计提供了理论指导。

Abstract: This work investigates several fundamental tasks, including $\mathsf{MaxSum}$, $\mathsf{MinSum}$, $\mathsf{MaxSelect}$, and $\mathsf{MinSelect}$, in the continual release model under differential privacy. Previous research has demonstrated that any algorithm for these tasks must admit a large purely additive error. We show that the error can be substantially reduced if a relative error term is allowed, provided that the input stream is generated non-adaptively. However, when input data records can be selected adaptively, we prove that a large error is inevitable for the task of selecting an attribute with a small cumulative sum, whereas small error bounds remain achievable for other tasks. This reveals a significant separation between non-adaptive and adaptive streams. We also complement our algorithms with nearly matching lower bounds.

</details>


<div id='cs.DB'></div>

# cs.DB [[Back]](#toc)

### [30] [Rewriting Ontology-Mediated Property Graph Queries into GQL](https://arxiv.org/abs/2608.20092)
*Bianca Löhnert,Nikolaus Augsten,Cem Okulmus,Magdalena Ortiz*

Main category: cs.DB

TL;DR: 本文提出了首个面向本体介导的导航图查询的实际查询重写技术，利用GQL和SQL/PGQ标准化成果，将本体（DL-Lite变体）和含嵌套双向正则路径查询的GQL片段相结合，并通过初步实验验证了其可行性。


<details>
  <summary>Details</summary>
Motivation: 本体数据访问虽然面向图数据，但实际支持仅限于类SQL语言，缺乏图查询所需的导航和路径匹配功能。理论算法早已存在但从未被实现，主要原因是实际图查询语言缺乏支持常用本体语言重写所需的表达能力。

Method: 利用GQL/SQL/PGQ标准化的最新成果，提出一种实用的查询重写技术。本体采用针对属性图的DL-Lite变体，查询采用包含嵌套双向正则路径查询的GQL片段，可在Cypher中求值，并实现了概念验证原型。

Result: 概念验证原型的初步实验表明，利用本体知识进行图数据查询在实践上已具备可行性，填补了理论算法与实际实现之间的空白。

Conclusion: 通过GQL标准化，本体介导的导航图查询终于可以落地实现，使结合本体知识的图数据查询有望在实际系统中得到应用。

Abstract: Ontology-based data access is intended for graph data, but practical support remains limited to SQL-like languages lacking the navigational and path-matching features fundamental to graph querying. Theoretical algorithms for navigational queries have long been available. Still, they have never been implemented, largely because practical graph query languages fell short of their theoretical counterparts and lacked the expressive power needed to support rewriting common ontology languages.
  The recent standardisation efforts around GQL and SQL/PGQ finally allow us to overcome this barrier and present a practical query rewriting technique for ontology-mediated navigational graph queries. Ontologies are written in a DL-Lite variant tuned to property graphs, and the queries in a GQL fragment with nested two-way regular path queries and which can be evaluated in Cypher. Preliminary experiments with our proof-of-concept prototype suggest that querying graph data with ontological knowledge may finally be within reach.

</details>


### [31] [Inductive Process Discovery from Partially Ordered Event Data](https://arxiv.org/abs/2608.20211)
*Humam Kourani,Tom Breuer,Gyunam Park,Wil M. P. van der Aalst*

Main category: cs.DB

TL;DR: 本文提出将归纳挖掘器（Inductive Miner, IM）从全序迹（totally ordered traces）提升到偏序迹（partially ordered traces）的方法。通过重新定义迹抽象层和递归投影，使得IM能够直接在偏序上操作，避免了对偏序进行线性化所导致的组合爆炸，同时保留IM的递归结构和合理性保证。实验表明该方法避免了线性化的计算开销、降低了对时间戳任意排序的敏感性，并能从更少的观测中学习过程行为。


<details>
  <summary>Details</summary>
Motivation: IM技术族是重要的过程发现方法，但它假设迹（traces）是完全有序的活动序列。然而现实中活动可能有持续时间、事件可能共享粗糙的时间戳、数据可能只约束部分事件对。强制将这些执行转换为任意序列会隐藏固有的并发性，并引入从未被观测为因果约束的序列依赖。偏序能更忠实地表示这些行为，但将偏序集成到IM中发现具有挑战性，因为标准抽象是基于序列的，直接复用需要对每个偏序线性化，在高并发下代价极其昂贵。

Method: 作者没有重新设计挖掘器和切分（cut）检测逻辑，而是将迹抽象层和递归投影重新定义为直接在偏序上操作。核心思想包括：(1) 将IM的迹抽象层提升到偏序迹；(2) 将IM的递归投影扩展到偏序操作；(3) 该方法对全序迹是保守的（conservative），即在全序情况下退化为标准IM；(4) 避免线性化爆炸，同时保持IM的递归结构和良好性质。

Result: 实验结果表明：(1) 所提出的提升方法避免了线性化的组合开销；(2) 降低了对时间戳事件数据中任意打破平局（tie-breaking）的敏感性；(3) 通过在迹级别保留并发性，允许从更少的观测中学习过程行为。

Conclusion: 将IM从全序提升到偏序是可行且有效的——既能保留IM的递归分解和合理性保证，又能更忠实地建模现实中存在并发和时序不确定性的过程，同时避免线性化的计算开销。

Abstract: The Inductive Miner (IM) family is a prominent class of process discovery techniques, combining efficient recursive decomposition with soundness-by-construction guarantees. However, IM techniques usually assume traces to be totally ordered sequences of activity occurrences. This assumption is convenient, but can introduce systematic bias: activities may have durations, events may share coarse timestamps, or the data may constrain only some event pairs. Forcing such executions into arbitrary sequences hides inherent concurrency and may introduce sequential dependencies that were never observed as causal constraints. Partial orders provide a more faithful representation, but integrating them into IM discovery is challenging because standard abstractions are sequence-based; directly reusing them would require linearizing each partial order, which becomes prohibitively expensive under high concurrency. We introduce a lifting of IM discovery from total orders to partially ordered traces. Instead of redesigning the miner and its cut detection logic, we redefine the trace abstraction layer and the recursive projections to operate directly on partial orders. The approach is conservative over totally ordered traces, avoids linearization explosion, and preserves the recursive structure and guarantees that make IM attractive. Experimental results show that the proposed lifting avoids the combinatorial overhead of linearization, reduces sensitivity to arbitrary tie-breaking in timestamped event data, and allows process behavior to be learned from fewer observations by preserving concurrency at the trace level.

</details>


### [32] [Which Eviction Policy Should an LLM Cache Use? A Systematic Study Across Workloads, Capacities, and Encoders](https://arxiv.org/abs/2608.20280)
*Yash Kulkarni,Shubham Harkare,Arvind Suresh Yogesh Babu*

Main category: cs.DB

TL;DR: 本文使用CLEVER协议系统评估了多种语义缓存替换策略（FIFO、LRU、LFU、ARC、GDSF、流式SISO和语义冗余策略），发现没有策略比LFU提升超过0.041个百分点。通过条件打包分析解释了改进空间有限的原因，并审计发现当前评估操作点存在严重问题：在高命中率下只有极少数命中被认为是答案可替换的，质量调整后命中率从51-60%骤降至1.1-2.2%。


<details>
  <summary>Details</summary>
Motivation: 语义缓存在LLM推理中能复用相近查询的响应，但已有的各种缓存淘汰策略从未在统一协议下进行公平比较，导致无法确定哪种策略在实践中表现最好。

Method: 使用CLEVER评估框架，在三个有序去重查询语料库、三种缓存容量和两种编码器下比较七种淘汰策略。通过条件打包理论分析解释策略间差异有限的原因，并通过交叉编码器研究和答案可替换性审计检验操作点的有效性。

Result: 没有任何评估策略在18个设置中比LFU提升超过0.041个百分点；FIFO和流式SISO在紧容量下分别落后LFU达8.67和8.55个百分点。在MiniLM中位最近邻阈值下，仅2.1-3.9%的命中被判定为答案可替换，质量调整后命中率仅为1.1-2.2%。

Conclusion: LFU是当前协议下最强且简单的默认策略。部署时应先建立答案有效性，再通过精确搜索测试微小的策略差异；此外，嵌入模型的阈值不可跨模型迁移，需要更严格的操作点设计。

Abstract: Semantic caches reuse an LLM response when the incoming query embedding lies near a cached query, but proposed eviction policies have rarely been compared under one protocol. Using CLEVER, we evaluate FIFO, LRU, LFU, ARC, GDSF, a single-pass streaming adaptation of SISO, and a semantic-redundancy policy across three ordered, deduplicated query corpora, three cache capacities, and two encoders. No evaluated policy improves on LFU by more than 0.041 percentage points in any of the eighteen settings. Replacement is not irrelevant: FIFO and streaming SISO trail LFU by as much as 8.67 and 8.55 points, respectively, at tight capacity.
  We explain the missing upside with a conditional packing result. Under exact lookup and insert-on-miss, a newly inserted entry cannot have a resident neighbor within the hit radius, so a geometry-aware eviction rule receives little new redundancy signal. A separate audit exposes a larger problem with the evaluated operating point. At MiniLM's median nearest-neighbor threshold, only 2.1-3.9% of sampled LMSYS and QQP hits are judged answer-substitutable, reducing raw hit rates of 51-60% to quality-adjusted rates of 1.1-2.2%. The cross-encoder study further shows that thresholds do not transfer between embedding models. LFU is the strongest simple default in this protocol; deployment decisions should first establish answer validity and then test sub-point policy differences with exact search.

</details>


<div id='cs.MM'></div>

# cs.MM [[Back]](#toc)

### [33] [GRACE: Grounded Reasoning via Adapter Composition and Evidence-Aware Calibration for Educational Visual Question Answering](https://arxiv.org/abs/2608.19355)
*Xinjin Li,Yudi Xia,Xi Zhao,Yiliu Xu,Yining Liu,Cheng Lu,Yujian Long,Yu Ma,Jinghan Cao,Liang Fan,Yeyun Xu*

Main category: cs.MM

TL;DR: 本文提出GRACE框架，利用教育评估中的教学状态信息（学科、技能、年级、视觉上下文、问题意图和选项结构），通过参数高效的适配器组合与证据感知校准，在冻结的多模态大语言模型上解决教育视觉问答问题。在ScienceQA上，GRACE将共享适配器基线的准确率从90.5%提升至93.1%，并消融实验验证了各组件的贡献。


<details>
  <summary>Details</summary>
Motivation: 教育VQA与常规开放式VQA不同，其问题伴随结构化的评估元数据、图表和语义相近的干扰选项，容易产生题目-选项捷径（question-option shortcuts）。现有方法未能充分利用这些教学状态信息作为路由信号来指导模型适配，因此需要一种能利用教育领域特有结构化信息的参数高效适应方法。

Method: GRACE框架包含三个核心组件：(1) 基于教育状态（学科、技能、年级、视觉上下文、问题意图、选项结构）的分因子提示和轻量视觉适配器，对冻结的MLLM进行专门的参数高效适配；(2) 证据感知的选项校准模块，在共享多模态上下文下对所有候选选项进行评分；(3) 通过适配器组合实现教学状态的细粒度路由。

Result: 在ScienceQA数据集上，GRACE将共享适配器基线的整体准确率从90.5%提升到93.1%，图像上下文问题的准确率从88.7%提升到91.2%。消融实验表明，去除教学组成、选项校准和视觉适配器分别导致整体准确率下降1.4、1.0和1.5个百分点，验证了各组件的有效性。

Conclusion: 结构化教育状态信息是参数高效多模态适应的有效路由信号。通过将教学元数据融入适配器设计与校准过程，可以显著提升教育VQA性能，同时保持参数效率。

Abstract: Educational visual question answering, or VQA, requires models to solve curriculum-oriented multiple-choice questions using both language and visual evidence. Compared with conventional open-ended VQA, educational examples often include structured assessment metadata, diagrams or image contexts, and semantically close answer options, creating strong opportunities for question-option shortcuts. We develop and evaluate a parameter-efficient adaptation framework for a frozen multimodal large language model in this setting. We introduce GRACE, Grounded Reasoning via Adapter Composition and Evidence-Aware Calibration, a framework that uses the pedagogical state of each question to specialize lightweight language and vision adaptation. The state combines inference-visible subject, grouped skill, grade, visual-context, question-intent, and option-structure cues. GRACE uses factor-specific prompts and lightweight visual adapters, then applies evidence-aware option calibration to score all candidates under a shared multimodal context. On ScienceQA, GRACE improves a shared-adapter baseline from 90.5 percent to 93.1 percent overall accuracy and from 88.7 percent to 91.2 percent on image-context questions. Removing pedagogical composition, option calibration, or the visual adapter reduces overall accuracy by 1.4, 1.0, and 1.5 points, respectively. These controlled results show that structured educational state is an effective routing signal for parameter-efficient multimodal adaptation.

</details>
