<div id=toc></div>

# Table of Contents

- [cs.GT](#cs.GT) [Total: 1]
- [cs.IT](#cs.IT) [Total: 5]
- [cs.IR](#cs.IR) [Total: 16]
- [cs.DS](#cs.DS) [Total: 10]
- [cs.DB](#cs.DB) [Total: 5]


<div id='cs.GT'></div>

# cs.GT [[Back]](#toc)

### [1] [Sample Complexity of the Second-Best Bilateral Trade](https://arxiv.org/abs/2608.25303)
*Qiaoyun Shi,Shengxin Liu,Zongqi Wan*

Main category: cs.GT

TL;DR: 本文研究了学习近最优双边贸易机制的样本复杂度问题。与以往研究简单或固定价格机制不同，本文聚焦于满足贝叶斯激励相容（BIC）、临时个人理性（IIR）和事前弱预算平衡（WBB）的机制，目标是设计基于样本的机制以达到次优贸易收益（second-best gains-from-trade）基准。作者在三个场景中给出了匹配或近似匹配的上下界。


<details>
  <summary>Details</summary>
Motivation: 以往关于双边贸易机制的学习工作主要局限于简单机制或固定价格机制，而本文希望学习更一般化的、满足BIC/IIR/WBB约束的最优（次优）机制，这在理论上更具有挑战性和普遍性，也能更接近机制设计理论中的理想目标。

Method: 作者在三个不同场景下分析样本复杂度：(1) 规则乘积分布下加性ε-逼近；(2) 相同假设下的乘性(1-α)-逼近；(3) 单调风险率（MHR）假设下的无界分布。每种情况都推导了样本复杂度的上下界。

Result: (1) 规则乘积分布在[0,h]²上，加性ε-逼近的样本复杂度为Θ̃(h²/ε²)；(2) 乘性(1-α)-逼近的样本复杂度为Θ̃(h/(SB(D)α²))，依赖于次优贸易收益SB(D)基准；(3) 无界分布下样本复杂度取决于比值χ_μ(D)=μ(D)/SB(D)。三者均给出匹配或近似匹配的上下界。

Conclusion: 本文首次系统研究了满足BIC/IIR/WBB约束的双边贸易机制的样本复杂度，揭示了不同逼近标准和分布假设下样本复杂度的本质差异，为实际机制设计提供了理论指导。

Abstract: We study the sample complexity of learning near-optimal bilateral trade mechanisms. Unlike previous work on learning simple or fixed-price bilateral-trade mechanisms, we focus on mechanisms satisfying Bayesian incentive compatibility (BIC), interim individual rationality (IIR), and ex-ante weak budget balance (WBB). In other words, our target is to design a sample-based mechanism that achieves the second-best gains-from-trade benchmark.
  We give matching or nearly matching upper and lower bounds in three regimes. For regular product distributions on $[0,h]^2$, additive $\varepsilon$-approximation has sample complexity $\widetildeΘ(h^2/\varepsilon^2)$. For multiplicative $(1-α)$-approximation under the same assumptions, we find that the sample complexity is $\widetildeΘ(h/(\mathrm{SB}(D)α^2))$, which is benchmark-sensitive with unavoidable dependence on the second-best gains from trade $\mathrm{SB}(D)$. We also investigate unbounded distributions under a monotone hazard rate (MHR) assumption. The sample complexity depends on the ratio $χ_μ(D)=μ(D)/\mathrm{SB}(D)$, where $μ(D)$ is the sum of the buyer's expected value and the seller's expected cost.

</details>


<div id='cs.IT'></div>

# cs.IT [[Back]](#toc)

### [2] [Towards Faithful and Efficient Semantic Communication: An Ontological Approach](https://arxiv.org/abs/2608.25422)
*Yixiao Feng,Yueting Wang,Yining Wang,Han Han,Bo Zhang*

Main category: cs.IT

TL;DR: 本文提出一种基于本体驱动的语义通信（ODSC）框架，用于多视角视觉问答（VQA）任务。多个发射器通过视觉语言模型（VLMs）提取场景图的语义信息并传输给接收器。针对VLM提取的场景图存在的冗余、歧义和不一致问题，收发双方共享基于本体的知识库（预定义同义词、推理规则和一致性约束），发射端移除可推理的部分场景图，接收端基于同义词对齐语义信息并检测不一致性。实验表明，与传输完整场景图相比，该框架将语义信息数据量减少高达87.1%，同时回答准确率提升4.5%，MVA指标相比语义过滤方法提升16.0%。


<details>
  <summary>Details</summary>
Motivation: 多视角VQA任务中，多个发射器分别提取并传输场景图语义信息，但VLM提取的场景图存在冗余、歧义和不一致问题，导致通信开销大且接收端答案准确性受影响，亟需一种能够压缩语义信息并提升回答质量的通信框架。

Method: 提出ODSC框架：1）收发双方共享基于本体的知识库，预定义同义词、推理规则和一致性约束；2）发射端根据推理规则移除可推导出的部分场景图以压缩数据；3）接收端基于同义词对齐不同视角的语义信息，并利用一致性约束检测视角间的不一致；4）定义多视角VQA准确率（MVA）指标评估性能。

Result: 与传输完整场景图相比，ODSC框架将语义信息数据量减少高达87.1%，同时回答准确率提升4.5%；与语义信息过滤方法相比，MVA指标提升16.0%。

Conclusion: 基于本体驱动的语义通信框架能有效解决多视角VQA中VLM提取场景图的冗余、歧义和不一致问题，在显著压缩通信数据量的同时提升VQA回答准确率，为语义通信在视觉任务中的应用提供了新思路。

Abstract: In this paper, an ontology-driven semantic communication (ODSC) framework is proposed for multi-view visual question answering (VQA) tasks. In the considered framework, multiple transmitters observe a scene, extract the semantic information (SI) with vision-language models (VLMs), and transmit the scene graphs to a receiver. Due to the completeness, heterogeneity, and uninterpretability of the VLMs, the extracted scene graphs are redundant, ambiguous, and inconsistent. To solve these problems, the transmitters and the receiver share an ontology-based knowledge base that predefines synonyms, inference rules, and consistency constraints. For each transmitter, the proposed ODSC framework removes the partial scene graph that can be inferred based on the inference rules. For the receiver, the proposed framework aligns the SI of different views based on the synonyms and detects the inconsistency among the views based on the constraints. A metric of multi-view VQA accuracy (MVA) is defined to evaluate the proposed framework. Simulation results show that, compared with transmitting the complete scene graphs, the proposed framework reduces the data size of the SI by up to 87.1% while improving the answering accuracy by 4.5%. Moreover, the proposed framework yields up to a 16.0% improvement in terms of the MVA compared with the SI filtering approaches.

</details>


### [3] [Conditional Total Correlation and the Serial Depth of Adaptive Parallel Sampling](https://arxiv.org/abs/2608.25505)
*Chuling Wen,Weijie Liang,Jian Lu*

Main category: cs.IT

TL;DR: 本文研究离散向量的自适应并行采样问题，推导出策略的KL散度与其条件总相关（conditional total correlation）之间的精确恒等式，并据此分析了多种分布（马尔可夫链、伯努利行走、均匀随机排列、平衡二进制串等）的串行深度复杂性，证明条件依赖结构是决定可并行性的根本因素。


<details>
  <summary>Details</summary>
Motivation: 掩码扩散模型中的并行解码需要以回合制方式采样离散向量，但目前缺乏对"回合间并行性"信息代价的精确理论刻画。理解不同分布和采样策略的并行深度（串行轮数）的本质界限，对于设计高效解码算法至关重要。

Method: 提出自适应并行采样框架：每轮由确定性策略基于已观测值选择未揭示坐标，所选坐标按其精确条件边缘分布独立采样。通过前向KL散度度量近似误差，并推导出核心恒等式：任意策略的散度等于其各揭示轮内条件总相关的期望累积，从而将条件总相关确立为回合内并行性的精确信息代价。基于此恒等式，对多种分布进行深度分析，并设计pseudo-cost启发式度量用于实际掩码扩散模型。

Result: 1) 有限阶马尔可夫链可实现零误差调度，轮数复杂度与马尔可夫阶数成正比、与序列长度成对数关系；2) 伯努利行走在任意固定误差预算下具有匹配的对数复杂度下界；3) 从左到右与层次化揭示顺序之间存在线性对数的复杂度分离；4) 均匀随机排列需要线性期望轮数；5) 均匀平衡二进制串深度为平方对数阶，二进制one-hot块为平方根阶；6) 实验表明pseudo-cost能区分不同解码规则，其策略排序与自采样输出质量高度一致。

Conclusion: 条件总相关是回合内并行性的精确信息代价，条件依赖结构而非熵或负对数似然是决定离散向量采样可并行性的根本因素。该理论框架为掩码扩散模型中的并行解码策略设计提供了理论指导和新颖的实用度量工具。

Abstract: Motivated by parallel decoding in masked diffusion models, we study adaptive parallel sampling of discrete vectors: in each round, a deterministic policy selects unrevealed coordinates on the basis of the values observed so far, and the selected coordinates are sampled independently from their exact conditional marginals. Approximation error is measured by forward Kullback-Leibler divergence, and serial depth is the minimum target-averaged number of rounds meeting a prescribed error budget. Our central result is an exact identity: the divergence of every policy equals the expected conditional total correlation accumulated over its reveal rounds, so conditional total correlation is the exact information cost of within-round parallelism. The identity yields zero-error schedules for finite-order Markov chains with round complexity proportional to the Markov order and logarithmic in sequence length, a matching logarithmic characterization of the Bernoulli walk at every fixed error budget, and a linear-versus-logarithmic separation between left-to-right and hierarchical reveal orders. Uniform random permutations require linearly many expected rounds at every fixed budget; their hard-cap round-error tradeoff is an exact integer-composition problem whose fixed-round asymptotics and joint-scaling frontier we determine. Uniform balanced binary strings have depth of order squared logarithm, and binary one-hot blocks have square-root depth, with rectangular versions realizing every polynomial exponent up to one half. These results separate serial depth from entropy and negative log-likelihood, and establish conditional-dependence structure as a fundamental determinant of parallelizability. Experiments with a masked diffusion language model show that the pseudo-cost distinguishes deployed decoding rules and that its policy rankings agree closely with the quality of self-sampled outputs.

</details>


### [4] [Joint Beamforming Design and Port Selection in Fluid Antenna-Assisted Multi-Cell Networks: A Personalized Federated Learning Approach](https://arxiv.org/abs/2608.25514)
*Liwen Gao,Li Zheng,Xing Hao,Ziru Chen,Lin X. Cai*

Main category: cs.IT

TL;DR: 本文研究了多小区流体天线辅助（FAS）网络中的联合波束成形与端口选择问题。提出了一种联邦表示学习（FedRep）框架，采用位置感知双分支深度神经网络（PA-DNN），以信道状态信息和端口位置编码为输入，通过两个任务特定分支联合输出波束成形向量和端口选择。FedRep框架在基站间共享全局波束成形相关参数，同时保留端口选择参数进行本地自适应，支持异构小区间的分布式训练。仿真结果表明，该方法比传统联邦学习和端口选择基准方案获得更高的加权和速率。


<details>
  <summary>Details</summary>
Motivation: 在多小区流体天线辅助网络中，主动波束成形和离散FA端口选择通过小区内和小区间干扰相互耦合，需要联合优化以最大化加权和速率（WSR）。然而，跨异构小区的分布式训练具有挑战性，需要设计有效的联合优化框架。

Method: 提出FedRep框架与位置感知双分支深度神经网络（PA-DNN）。PA-DNN以CSI和端口位置编码为输入，通过两个任务特定分支分别输出波束成形向量和端口选择。FedRep在基站间共享全局波束成形参数，同时保持端口选择参数本地化以适应不同小区特性，实现去中心化训练。

Result: 仿真结果表明，所提出的方案在加权和速率上优于传统联邦学习（FL）方案和端口选择基准方案。

Conclusion: FedRep框架配合PA-DNN能够有效解决多小区FAS网络中波束成形与端口选择的联合优化问题，通过共享-局部的参数分工策略兼顾全局一致性与小区特异性，实现了更优的性能。

Abstract: This paper investigates joint beamforming and port selection in multi-cell fluid antenna-assisted (FAS) networks. In such networks, active beamforming and discrete FA port selection are coupled through intra-cell and inter-cell interference and are jointly optimized to maximize the weighted sum-rate (WSR). We develop a federated representation learning (FedRep) framework with a position-aware dual-branch deep neural network (PA-DNN). The PA-DNN uses channel state information and port positional encoding as inputs, and jointly outputs beamforming vectors and port selections through two task-specific branches. To support decentralized training across heterogeneous cells, the FedRep framework shares global beamforming-related parameters among base stations while keeping port-selection parameters local for cell-specific adaptation. Simulation results show that the proposed scheme achieves a higher weighted sum-rate than conventional FL and port-selection benchmark schemes.

</details>


### [5] [Constrained Maximum Entropy Contiguous Aggregations](https://arxiv.org/abs/2608.25533)
*Roberto Bruno,Ugo Vaccaro*

Main category: cs.IT

TL;DR: 本文研究在给定概率分布p和熵约束R下，计算最大熵的连续聚合分布q的问题，提出了精确的动态规划算法和两个接近最优的贪心算法。


<details>
  <summary>Details</summary>
Motivation: 连续聚合在许多应用场景中自然出现（如将细粒度概率合并为粗粒度类别），而如何在保持聚合后熵不超过给定上限R的前提下最大化熵，是一个既有理论意义又有实际价值的问题，但此前缺乏系统的算法研究。

Method: 提出了三种算法：(1) 精确求解的动态规划算法；(2) 两个时间高效的贪心算法，能以接近最优的方式求解问题。

Result: 动态规划算法能精确求解该问题，而两个贪心算法在时间复杂度上更优，且能产生接近最优的解。

Conclusion: 该问题存在精确解和近似解的多层次算法方案，适用于多种实际应用场景。

Abstract: Given a probability distribution $p = (p_1, \dots, p_n)$ and an integer $1\leq m \leq n$, a contiguous aggregation of $p$ is a probability distribution $q = (q_1, \dots, q_m)$ such that each $q_i$ is a sum of consecutive elements of $p$. Given $p$ and a positive number $R$, we consider the problem of computing a maximum entropy contiguous aggregation $q$ of $p$, under the constraint that its Shannon entropy $H(q)$ is at most $R$. We devise a dynamic programming algorithm that solves the problem exactly, and two time-efficient greedy algorithms that provide close-to-optimal solutions. We discuss a few scenarios where our problem arises.

</details>


### [6] [Quantitative tiling stability from quadratic discrepancy in Hamming spaces](https://arxiv.org/abs/2608.25716)
*Valery,Grishin,Aryeh Lev Zabokritskiy*

Main category: cs.IT

TL;DR: 本文研究了有限Hamming空间中二次球偏差（quadratic ball discrepancy）作为码能量的极值问题。在完美码参数下，精确最小化器即为完美码。作者证明了平铺缺陷稳定性（tiling-defect stability）：超过完美码基准的过量偏差控制球覆盖重数偏离1的平方偏差。对于一错误参数，下界系数为κ_{n,q}/q²≥1，均匀下界1是最优的；对于n≥5的二错误参数，在球填充可除性和两个不同整数Lloyd根条件下获得显式正系数。该缺陷还等价于均匀球噪声下的归一化卡方平滑误差，从而控制空洞、重叠、缺陷环境点、全变差以及Rényi散度。


<details>
  <summary>Details</summary>
Motivation: 理解码在有限Hamming空间中的几何结构，特别是完美码作为能量最小化器的稳定性。当码参数接近完美码参数但不存在完美码时，所有码与完美码的偏离程度如何量化，以及这种偏离如何反映在球覆盖、噪声平滑等性质上。

Method: 利用二次球偏差作为码上的能量函数，在固定字母表大小、长度和码基数下比较所有码。通过平铺缺陷稳定性定理，将过量偏差与控制球覆盖重数偏差联系起来。对一错误参数使用Lloyd积分性和球填充条件推导下界系数；对二错误参数利用Lloyd多项式根和球填充可除性获得显式正系数。对重复码和Golay族直接构造证书，结合完美码分类获得所有非平凡完美码的稳定性。

Result: 获得了平铺缺陷稳定性定理：对于完美码参数，过量二次球偏差平方控制球覆盖重数与1的偏差。一错误参数下系数κ_{n,q}/q²≥1，均匀下界1是尖锐的，而参数依赖的认证系数可更大，且两者可相差很大。二错误参数(n≥5)在球填充可除性和两个不同整数Lloyd根条件下获得显式正系数；字母表大小≥4时系数有闭式表达和固定字母表渐进行为。对所有非平凡完美码（通过重复码、Golay族及完美码分类）均建立了稳定性。

Conclusion: 本文建立了完美码参数框架下二次球偏差的稳定性理论，证明过量偏差控制球覆盖、空洞、重叠、全变差和Rényi散度等多种几何与概率性质。该稳定性不要求码为线性码，也不要求满足距离或纠错约束，具有广泛的适用性。缺陷的卡方平滑误差解释进一步将码论与信息论中的噪声平滑问题联系起来。

Abstract: Quadratic ball discrepancy defines an energy on codes in finite Hamming spaces. At perfect-code parameters, its exact minimizers are the perfect codes. We fix the alphabet size, length, and code cardinality and compare all codes with these parameters. We prove tiling-defect stability: excess discrepancy above the perfect-code benchmark controls the squared deviation of the distinguished ball-covering multiplicity from one. For one-error parameters satisfying sphere-packing and Lloyd integrality, the lower coefficient is $κ_{n,q}/q^2\geq1$. The uniform floor one is sharp, while the certified parameter-dependent coefficient can be much larger. An explicit parameter-dependent upper estimate is also available, and the two certified coefficients can be far apart. For any two-error parameter pair with $n\geq5$ satisfying sphere-packing divisibility and having two distinct integral Lloyd roots in the Hamming weight range, we obtain an explicit positive coefficient without assuming that a perfect code exists. For alphabets of size at least four, this conditional coefficient has a closed form and fixed-alphabet asymptotics. Direct certificates for the repetition and Golay families, combined with perfect-code classification, give tiling-defect stability for every nontrivial perfect code. Here stability concerns the ball-covering multiplicity profile, not symmetric-difference proximity to a particular perfect code. The defect is also a normalized chi-square smoothing error under uniform ball noise, so excess discrepancy controls holes, overlaps, defective ambient points, total variation, and Rényi divergence from uniformity of the ball-noise output. Competing codes need not be linear or satisfy a distance or error-correction constraint.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [7] [SWIM: Step-Wise Integrated Measure for Session-supervised List Evaluation in Generative Re-ranking](https://arxiv.org/abs/2608.25104)
*Yuanhao Pu,Chenghao Zhang,Chao Feng,Xunyong Yang,Xiang Li,Yongqi Liu,Defu Lian,Kaiqiao Zhan,Kun Gai*

Main category: cs.IR

TL;DR: SWIM是一个面向顺序推荐平台的列表级评估器（Evaluator），将用户行为建模为有限时域前缀会话级生存过程，通过递归生存分布与位置条件奖励分解会话目标，并利用因果掩码Transformer并行估计，满足工业延迟约束。


<details>
  <summary>Details</summary>
Motivation: 传统生成-评估（G-E）框架中的评估器通过聚合点式打分来评分列表，隐含地假设曝光项相互独立，这忽略了顺序平台（如短视频应用）中用户连续消费内容时的会话级动态，包括上下文依赖、用户持续观看行为以及重复内容的边际效用递减。

Method: SWIM将用户行为建模为有限时域前缀会话级生存过程，将当前列表对会话目标的贡献分解为递归生存分布（continuation probabilities）与到达位置的条件奖励（reached-position conditional rewards）两部分，并采用因果掩码Transformer并行高效地进行估计。

Result: 大量实验表明，SWIM在列表级重排序任务中显著优于基线方法，在整体推荐参与度（engagement）上取得实质性提升。

Conclusion: SWIM证明了在G-E框架下将会话级动态纳入评估器设计的有效性，为顺序消费平台的重排序阶段提供了符合工业延迟约束的列表级评估方案。

Abstract: Modern industrial recommender systems have increasingly adopted the Generator-Evaluator (G-E) framework for the re-ranking stage. Within this paradigm, the generator produces candidate item lists from a pool filtered by upstream retrieval and ranking modules, while the evaluator scores these lists and selects the highest-scoring one for final exposure per request. However, on sequential platforms (e.g., short-video apps), users consume items continuously, ignoring artificial list boundaries. Conventional evaluators score lists by aggregating point-wise values, implicitly assuming exposure independence. This fails to capture critical session-level dynamics, such as contextual dependencies, user continuation, and diminishing marginal utility from repetitive content.
  To bridge this gap, we propose SWIM (Step-Wise Integrated Measure), a list-level evaluator that models user behaviors as a finite-horizon prefix session-level survival process. SWIM estimates the prefix-conditioned contribution of the current list to the session-level objective by factorizing it into a recursive survival distribution and reached-position conditional rewards. Leveraging a causally-masked Transformer, SWIM efficiently estimates continuation probabilities and utilities in parallel, satisfying strict industrial latency constraints. Extensive experiments demonstrate that SWIM significantly outperforms baselines in listwise reranking tasks, yielding substantial improvements in overall recommendation engagement.

</details>


### [8] [D3ER: Supporting Multi-Modal Recommendation via Disentangle and Distillation-based Dynamic Ensemble](https://arxiv.org/abs/2608.25737)
*Bingnan Wang,Yi Li,Xiongxin Tang,Fanjiang Xu,Jiangmeng Li*

Main category: cs.IR

TL;DR: 本文提出D3ER方法，首次将梯度提升引入多模态推荐，通过交替学习模态同质判别信息（HOI）和模态异质判别信息（HEI），并结合知识蒸馏和全局校正正则化来缓解梯度提升的高存储成本和局部最优问题，在真实数据集上验证了优越性。


<details>
  <summary>Details</summary>
Motivation: 现有多模态推荐方法在联合学习HOI和HEI时，容易削弱两者各自的有效性，缺乏让模型专注各自擅长样本的机制，导致推荐性能受限。

Method: 提出D3ER框架：(1)首次将梯度提升引入多模态推荐，形式化交替学习HOI和HEI的优化目标，使每种信息对应的模型专注各自擅长的样本；(2)引入知识蒸馏降低梯度提升固有的高存储成本；(3)设计全局校正正则化缓解局部最优风险。

Result: 在多个主流真实数据集上的实验表明，D3ER相比现有方法在多模态推荐任务上取得了显著优越的性能。

Conclusion: 通过解耦+蒸馏+动态集成的设计，D3ER有效解决了HOI和HEI联合学习时的相互削弱问题，为多模态推荐提供了一种新的梯度提升范式。

Abstract: Incorporating items' information shared among multiple modalities into a fused representation, multi-modal recommendation (MR) has demonstrated documented success than canonical unimodal recommendation. Although several attempts have been made to extract the discriminative information unique in each modality, existing methods suffer from a core limitation: the joint learning of modal-homogeneity discriminative information (HOI) and modal-heterogeneity discriminative information (HEI) tends to weaken their individual effectiveness. To remedy this deficiency, we propose a novel method, dubbed Disentangle and Distillation-based Dynamic Ensemble for multi-modal Recommendation (D3ER). We introduce gradient boosting into MR for the first time to formalize the optimization objective for alternately learning HOI and HEI. This design enables models dedicated to each type of information to focus on their proficient samples, thereby promoting specialized optimization. Furthermore, to mitigate the inherent high storage cost and risk of local optima in gradient boosting, we enhance our framework with knowledge distillation and a global correction regularization. Experiments on prevalent real-world datasets confirm the superiority of our proposed method on MR.

</details>


### [9] [The "Curse of Knowledge" in LLM Query Simulation: Concept Provenance for Tracing Answer-Side Intrusion](https://arxiv.org/abs/2608.25245)
*Chenglong Ma,Xinye Wanyan,Danula Hettiachchi,Ziqi Xu,Jeffrey Chan*

Main category: cs.IR

TL;DR: 本文提出概念溯源（concept provenance）框架，将LLM生成搜索查询中的概念分配到不同的信息访问边界区域，用于检测候选答案侧的知识入侵问题。实验表明，答案侧概念占非通用概念的7.40%，出现在97/100个主题中，人类验证精度为68.2%，删除这些概念对局部检索有显著影响（d=-0.47），但对聚合评估方差影响不足2%。该框架作为边界合规诊断工具而非评估转变预测器，后生成选择可实现99%的消除效果。


<details>
  <summary>Details</summary>
Motivation: LLM生成的搜索查询广泛用于增强IR评估，但其中可能包含预设答案侧文档知识的概念，违反搜索前用户的信息访问边界。现有验证指标（重叠率、多样性、有效性）无法区分罕见的人类变体与候选答案侧入侵。需要一个能够操作化信息访问边界的框架来检测这些问题。

Method: 提出概念溯源框架，将查询概念分配到四个区域：backstory支持型、人类中心型、人类变体型和候选答案侧。在100个UQV100主题、8个LLM、5种提示条件下对77,004个查询进行分析，使用两个提取管线，结合人类验证和诊断性探测实验。

Result: 概念溯源跨管线token-HCIR Spearman rho为1.0；候选答案侧概念占7.40%的非通用概念，出现于97/100主题中，主题解释约67%方差；人类验证精度68.2%，知识入侵占45.5%，部署入侵占45.0%；删除效应d=-0.47 vs 随机删除d=-0.34，但聚合评估方差贡献不足2%。

Conclusion: 概念溯源是有效的边界合规诊断工具，而非评估转变预测器。在测试条件下，没有任何提示条件能消除入侵，但后生成的概念溯源选择可以实现99%的消除效果。这为LLM增强的IR评估提供了边界合规的质量保障机制。

Abstract: LLM-generated search queries are widely used to augment IR evaluation, yet they may contain concepts that presuppose answer-side document knowledge, violating the information-access boundary of pre-search users. Existing validation metrics, including overlap, diversity, and effectiveness, cannot distinguish rare human-tail variation from candidate answer-side intrusion. We introduce concept provenance, a framework that assigns query concepts to backstory-supported, human-central, human-tail, and candidate answer-side zones, operationalizing a boundary that retrieval metrics alone cannot detect. Applying concept provenance to 77,004 queries across 100 UQV100 topics, 8 LLMs, and 5 prompt conditions with two extraction pipelines, we obtain a cross-pipeline token-HCIR Spearman rho of 1.0 over five condition means. Candidate answer-side concepts constitute 7.40 percent of non-generic concepts and appear in 97 of 100 topics, with topic explaining approximately 67 percent of variance. Human validation yields 68.2 percent relaxed precision, revealing two mechanisms: knowledge intrusion at 45.5 percent and deployment intrusion at 45.0 percent. Diagnostic probes show disproportionate localized retrieval effects, with deletion effect size d = -0.47 compared with d = -0.34 for random deletion, but these concepts explain less than 2 percent of aggregate evaluation variance. Concept provenance therefore serves as a boundary-compliance diagnostic rather than an evaluation-shift predictor. Under the tested conditions, no prompt condition eliminates intrusion; post-generation concept-provenance selection achieves 99 percent elimination.

</details>


### [10] [Rank-Deviation Quality: A Distance-Aware Metric for Multi-Answer Retrieval and Ranking Evaluation](https://arxiv.org/abs/2608.25318)
*Xiaokun Zhou,Alessandro Moschitti,Danielle Class*

Main category: cs.IR

TL;DR: 本文提出Rank-Deviation Quality (RDQ)，一种适用于不同参考项数量（从单个正确答案到多个有效结果）的检索与排序系统评估指标。RDQ根据有序参考列表（ORL）对候选排序打分，每个检索到的参考项贡献其输出位置权重乘以排名偏差惩罚，参考列表外的项得零分。不同于需要绝对相关性等级的指标，RDQ基于序数排名；不同于Kendall tau等秩相关度量，RDQ同时考虑返回了哪些项及其排序方式。


<details>
  <summary>Details</summary>
Motivation: 现有评估指标存在局限性：一些指标需要绝对相关性等级（标注成本高），而秩相关度量（如Kendall's tau）不考虑项的选取与排序如何权衡。此外，实际应用中查询的参考项数量差异很大（从单一正确答案到多个有效结果），需要一个能适应这种变化的统一指标。

Method: RDQ通过有序参考列表(ORL)评估候选排序。每个检索到的参考项贡献输出位置权重（可反映应用界面可见性，如垂直列表或旋转木马）乘以排名偏差惩罚。应用特定参数控制对排序错误的容忍度：较大的参数值强调检索有效参考项，较小的值更重视参考顺序匹配。标注者可通过成对或列表式判断产生序数排名。

Result: 在包含12个系统的5000条查询POI数据集上，RDQ在13个评估指标配置中具有最高的中位数经验功效@100。RDQ在200个查询时达到与其全查询排序的平均tau≥0.8一致性；最强的非RDQ配置RBP(0.9)在250个查询时才达到该阈值。在TREC深度学习基准上，RDQ与NDCG在n=25时中位数功效相当，而NDCG在n=100时更高。

Conclusion: RDQ是一种灵活的评估指标，能够在标注成本（仅需序数判断）和评估质量（同时考虑召回项和排序）之间取得良好平衡，在样本较少时具有竞争优势，适用于实际检索评估场景。

Abstract: We introduce Rank-Deviation Quality (RDQ), an evaluation metric for retrieval and ranking systems that adapts to queries with varying numbers of reference items, from a single correct answer to many valid results. RDQ scores a candidate ranking against an ordered reference list (ORL): each retrieved reference item contributes its output-position weight multiplied by a rank-deviation penalty, and items outside the ORL receive zero credit. Application-specific parameters control tolerance to misordering. Larger values emphasize retrieving valid reference items, whereas smaller values place more weight on matching their reference order. The output-position weights can reflect visibility in the application's interface, such as a vertical list or a carousel. Unlike metrics that require absolute relevance grades, RDQ operates on ordinal rankings, which annotators can produce through pairwise or listwise judgments. Unlike rank-correlation measures such as Kendall's tau, RDQ accounts for both which items are returned and how they are ordered. On a 5,000-query point-of-interest (POI) dataset with 12 systems, RDQ has the highest median empirical power@100 among the 13 evaluated metric configurations. It reaches mean tau >= 0.8 agreement with its own full-query ordering at 200 queries; RBP(0.9), the strongest tested non-RDQ configuration, reaches the same threshold at 250. On TREC Deep Learning benchmarks, where NDCG uses native graded labels and RDQ uses ordinal tiers derived from them, RDQ reaches comparable median power at n=25, while NDCG is higher at n=100.

</details>


### [11] [CRAMER: Control via Request-Aware Masking for Editing Recommenders](https://arxiv.org/abs/2608.25370)
*Zhiyuan Julian Su,Naihe Feng,Zhen Luther Qin,Ga Wu*

Main category: cs.IR

TL;DR: CRAMER是一种通过请求感知掩码技术来控制序列推荐模型的框架，将用户自然语言请求作为控制信号来调节冻结的骨干网络参数，实现即时适应不同请求而无需昂贵的重训练。


<details>
  <summary>Details</summary>
Motivation: 现有的序列推荐模型虽然强大，但难以快速响应用户的即时请求。已有的请求适应方法要么需要重训练整个骨干网络，要么依赖大语言模型的推理能力（提示工程），计算开销大，难以适用于大规模推荐服务。

Method: 受模型控制理论启发，CRAMER将用户请求视为控制信号，通过掩码（masking）方式调节冻结的骨干网络参数，在避免昂贵重训练的同时实现对不同请求的即时适应。

Result: 在多个大规模基准数据集上，CRAMER在多个推荐指标上超越四种最先进的请求感知基线，同时保持了极低的计算开销。此外，该框架展现出更强的可控性和跨领域适应性。

Conclusion: CRAMER为请求感知的序列推荐建立了一种新范式，通过掩码机制实现高效、可控且具有跨领域适应能力的即时推荐控制。

Abstract: Sequential recommendation models, while powerful, have limited flexibility in responding to immediate user requests, making it difficult to adapt their recommendations to the user's timely interests. Unfortunately, existing user request adaptation methods often incur high computational overhead due to either 1) retraining the entire backbone network or 2) leveraging the inference ability of large language models (a.k.a. prompt engineering), limiting their applicability in large-scale recommendation services. This paper presents Control via Request-Aware Masking for Editing Recommenders (CRAMER), a framework that takes users' natural-language requests to immediately change sequential recommendation models' behavior. Specifically, inspired by the model control theory, CRAMER treats user requests as control signals to modulate frozen backbone parameters through masking, achieving instant adaptation to diverse requests while avoiding costly retraining. Experiments on multiple large-scale benchmark datasets show that CRAMER outperforms four state-of-the-art request-aware baselines across multiple recommendation metrics while achieving minimal overhead. Moreover, the proposed framework exhibits enhanced controllability and cross-domain adaptability, establishing a new paradigm for request-aware sequential recommendation.

</details>


### [12] [MOTIF: Motivation-guided Topology Inference for Cold-start Multimodal Recommendation](https://arxiv.org/abs/2608.25381)
*Yurui Shi,Yuchen Miao,Ximing Hu,Zijun Wang,Chang Han*

Main category: cs.IR

TL;DR: MOTIF是一个基于动机引导的拓扑推断框架，专门用于冷启动多模态推荐。它通过离线LLM推理推断动机语义、重建可迁移的商品-商品拓扑结构，并结合加权图对比学习和语义-结构对齐来学习鲁棒的图嵌入，在三个多模态基准上相较最强基线取得最高6.07%的相对提升。


<details>
  <summary>Details</summary>
Motivation: 冷启动多模态推荐面临三个相互耦合的挑战：(i)稀疏交互掩盖了用户意图；(ii)冷商品在拓扑上处于隔离状态；(iii)基于相似性的商品图可能导致语义漂移。现有方法难以同时解决这些问题。

Method: MOTIF包含四个核心组件：语义动机推理（利用离线LLM推理推断动机语义）、知识增强的图重建（重建可迁移的商品-商品拓扑）、加权图对比学习（学习鲁棒的图嵌入）、以及语义-结构对齐。特别地，该方法不将生成的文本注入预测过程，避免了语义漂移问题。

Result: 在三个多模态基准数据集上，MOTIF相较于基于图的方法、多模态方法、冷启动方法和LLM增强的基线方法均展现出一致的性能提升，相对最强近期基线达到最高6.07%的提升。

Conclusion: MOTIF通过动机引导的拓扑推断有效解决了冷启动多模态推荐中的三个耦合挑战，验证了离线LLM推理与图重构相结合在冷启动场景下的有效性。

Abstract: Cold-start multimodal recommendation faces three coupled challenges: (i) sparse interactions obscure user intent, (ii) cold items remain topologically isolated, and (iii) similarity-based item graphs may cause semantic drift. To address these issues, we propose MOTIF, a Motivation-guided Topology Inference framework for cold-start multimodal recommendation. MOTIF integrates Semantic Motivation Reasoning, Knowledge-enhanced Graph Reconstruction, Weighted Graph Contrastive Learning, and Semantic-Structural Alignment. It uses offline LLM reasoning to infer motivation semantics, reconstructs transferable item-item topology, and learns robust graph embeddings without injecting generated text into prediction. Experiments on three multimodal benchmarks show consistent gains over graph-based, multimodal, cold-start, and LLM-enhanced baselines, with up to 6.07% relative improvement over the strongest recent baseline.

</details>


### [13] [DocPC: Document-Level Visual Retrieval via Representative Page Composition](https://arxiv.org/abs/2608.25434)
*Chengsong You,Junwei Zhou,Nan Du*

Main category: cs.IR

TL;DR: 本文提出DocPC，一种基于代表性页面组合的文档级视觉检索框架：通过选取代表性页面并组合成单个网格图像进行文档级索引，将索引图像、向量和存储减少10.1倍，端到端索引时间约减少7.7倍。同时提出多正例对比学习与稀疏调度列表优化结合的训练方法，并引入DocViRe基准数据集。DocPC-ColQwen在DocViRe上NDCG@5达44.09，优于最强页面级基线38.91，同时存储减少10.1倍。


<details>
  <summary>Details</summary>
Motivation: 现有视觉文档检索方法通过视觉语言模型编码页面截图绕过OCR，但仍是页面中心化的（page-centric），与真实场景中需要完整文档检索的需求不匹配。简单的"页面再文档"聚合方式存在线性索引成本高、跨页相关时检索质量下降的问题。

Method: 1) 代表性页面组合（Representative Page Composition）：选取文档中代表性页面，组合成单一网格图像进行文档级索引；2) 多正例对比学习（multi-positive contrastive learning）结合稀疏调度的列表优化（sparsely scheduled listwise optimization），以处理文档级多正例监督问题。

Result: 索引图像、向量和存储减少10.1倍，端到端索引时间约减少7.7倍。DocPC-ColQwen在DocViRe基准上NDCG@5为44.09，显著优于最强页面级基线38.91。

Conclusion: DocPC证明了文档级视觉检索可以通过代表性页面组合在显著降低存储和索引开销的同时提升检索性能，为视觉文档检索从页面级向文档级范式转变提供了有效方案。

Abstract: Visual document retrieval has advanced by encoding page screenshots with vision-language models, bypassing OCR pipelines. However, existing methods remain page-centric, misaligned with real-world scenarios requiring complete document retrieval. A naive page-then-document aggregation suffers from linear indexing cost and degraded retrieval when relevance spans multiple pages. We propose DocPC, a document-level visual retrieval framework based on Representative Page Composition: selecting representative pages and composing them into a single grid image for document-level indexing, reducing indexed images, vectors, and storage by 10.1x and end-to-end indexing time by roughly 7.7x. To handle multi-positive supervision prevalent at the document level, we combine multi-positive contrastive learning with sparsely scheduled listwise optimization. We also introduce DocViRe, a benchmark with multi-positive relevance annotations. DocPC-ColQwen achieves NDCG@5 of 44.09 on DocViRe, outperforming the strongest page-level baseline at 38.91 while reducing storage by 10.1x. Code is available at https://anonymous.4open.science/r/DocPC-Document-Level-Visual-Retrieval-via-Representative-Page-Composition-1D52. Data is available at https://huggingface.co/datasets/anonymous-7219/docpc.

</details>


### [14] [Data Citation for Large Language Models: A Challenge](https://arxiv.org/abs/2608.25663)
*Gianmaria Silvello*

Main category: cs.IR

TL;DR: 本文论述了大型语言模型的数据引用问题——这是一个不同于文档级引用接地、且更难解决的开放挑战。论文提出三个研究方向：训练数据属性（将影响估计转化为对模型参数中吸收语料库的引用）、推理时的数据引用（在正确的粒度和固定性下识别数据集、子集及查询结果）、以及引用知识图谱事实（定义单条三元组引用的含义及信用沿溯源传播的方式）。这些工作需跨数据库、信息检索、知识表示和人工智能社区的协作。


<details>
  <summary>Details</summary>
Motivation: 现有研究仅将引用视为验证工具并应用于文本文档，忽视了学术引用在数据层面应承担的信用和溯源功能。随着LLM越来越多地充当信息访问中介，如何让模型引用数据以保持输出可验证、溯源可追踪、信用惠及数据创建者和策展者，成为一个未被充分解决的挑战。

Method: 论文采用论证性/前瞻性研究方法，首先区分数据引用与文档级引用接地（前者更困难），然后提出三个具体的研究方向：1) 训练数据归因——将影响估计转化为参数内吸收语料库的引用；2) 推理时数据引用——在合适的粒度和固定性上标识数据集、子集和查询结果；3) 知识图谱事实引用——定义单条三元组引用的语义及信用沿溯源传播机制。

Result: 论文提出了一个清晰的框架，将LLM数据引用问题分解为三个可操作的研究方向，并明确指出每个方向的核心难点：影响估计到引用的转化、粒度与固定性选择、以及三元组引用的语义约定与信用传播。同时识别出该问题需要多个学科社区的协作。

Conclusion: LLM的数据引用是独立于文本引用接地且更富挑战性的问题，其解决需要数据库、信息检索、知识表示和AI社区联合攻关。这三个研究方向共同支撑可验证性、溯源性和信用分配三大目标。

Abstract: Large language models increasingly mediate access to information, and a growing body of work asks whether they cite the sources behind their outputs. That work treats citation as a verification device and applies it to textual documents. Scholarly citation serves two further functions, credit and provenance, and it applies to data as much as to text. This paper argues that data citation for large language models is an open challenge, distinct from document-level citation grounding and harder to solve. We ask how such models should cite data so that outputs stay verifiable, provenance stays traceable, and credit reaches data creators and curators. We set out three research directions. Training data attribution has to turn influence estimates into references for corpora absorbed into model parameters. Data citation at inference time has to identify datasets, subsets, and query results at the right granularity and fixity. Citing knowledge graph facts has to define what a reference to a single triple denotes and how credit propagates along provenance. Progress on all three depends on joint work across the database, information retrieval, knowledge representation, and artificial intelligence communities.

</details>


### [15] [Query Expansion Is More Than Generation: Improving Dense Retrieval through Better Integration](https://arxiv.org/abs/2608.25521)
*Siyuan Sun,Mihai Surdeanu*

Main category: cs.IR

TL;DR: 本文提出AnchorQE，一种无需训练的查询扩展集成方法。它分别编码原始查询和扩展文本，再通过一种无监督在线策略估计插值权重进行融合。该策略仅在未标注测试流的一小部分上运行，当扩展既检索性能强又与原始查询的检索证据一致时给予高信任度。在TREC-DL、LoTTE和BEIR基准上，较扩展拼接等基线提升最高达12.89%。


<details>
  <summary>Details</summary>
Motivation: 大语言模型无需任务特定训练即可生成查询扩展，但同样的扩展常常使冻结的稠密检索器性能变差。以往研究多关注生成什么文本，而较少系统关注生成文本如何被集成进稠密检索器。作者发现当固定生成扩展不变时，性能退化往往可归因于集成方法本身。

Method: 提出AnchorQE方法：1) 训练免费，分别编码原始查询和扩展；2) 使用无监督在线策略估计插值系数，该策略在未标注测试流的一小部分上运行；3) 插值权重仅在扩展同时满足检索能力强且与原始查询检索证据一致时赋予高信任。

Result: 在TREC-DL、LoTTE和BEIR基准上，相比常用的仅扩展或文本级拼接基线，AnchorQE提升检索效果最高达12.89%。同时，在线估计插值系数的策略相比在开发集上调优的固定权重，提升最高达3.81%。

Conclusion: 查询扩展的集成方式（而非仅生成内容）对稠密检索性能有重要影响；通过分离编码与在线自适应插值，可以在无需训练的情况下显著改善LLM生成扩展在冻结稠密检索器上的效果。

Abstract: Large language models (LLMs) can generate query expansions without task-specific training, yet the same expansions often make a frozen dense retriever worse. We identify an underexplored factor: prior work has often focused on what text is generated, while how generated text is incorporated into dense retrievers has received less systematic attention. By holding generated expansions fixed, we show that performance degradation can often be attributed to the integration method itself. We introduce AnchorQE, a training-free method that separately encodes the original query and its expansion before interpolating them. The interpolation factor is estimated using an unsupervised online strategy that operates over a small part of the unlabeled test stream. Intuitively, our strategy assigns high expansion trust only when expansions are both retrieval-strong and consistent with the original query's retrieved evidence. We show that AnchorQE improves retrieval effectiveness by up to 12.89% when compared to widely-used expansion-only or text-level concatenation baselines across TREC-DL, LoTTE, and BEIR. Further, we show that our online strategy to estimate the interpolation factor outperforms a fixed weight tuned on a development partition by up to 3.81%.

</details>


### [16] [TransRetrieval: Scaling Up Transformer-Based Retrieval for Industrial Recommendation](https://arxiv.org/abs/2608.25528)
*Zhifei Zheng,Yunfei Liu,Bin Liu,Qiren Zhu,Hanbing Liu,Ziru Xu,Han Zhu,Jian Xu,Qi Qi,Bo Zheng*

Main category: cs.IR

TL;DR: 本文提出TransRetrieval，一个基于Transformer的检索框架，通过加权平均聚合、目标token压缩和位置风格域嵌入三大技术，解决了推荐检索中特征异构导致Transformer难以扩展的问题，在工业和公开数据集上实现了随计算预算和跨域数据的稳健log-linear扩展。


<details>
  <summary>Details</summary>
Motivation: 将扩展律(scaling laws)应用于推荐检索面临特征异构的挑战：不同特征域naively堆叠Transformer层会产生严重的token范数分歧(token-norm divergence)，导致收益递减，无法有效利用更大的计算预算和跨域数据。

Method: 提出三大核心组件：(1) 加权平均聚合(weighted average aggregation)，恢复Transformer依赖的同质token假设；(2) 目标token压缩(target token compression)，将每个候选的FLOPs降低85%同时保留交叉注意力表达能力；(3) 位置风格域嵌入(position-style domain embeddings)，以极低成本统一多域数据，使跨域数据成为扩展资产。

Result: 在400亿交互的工业数据集和公共KuaiRand基准上，将计算量从每目标0.1扩展到2 MFLOPs，Recall@2000分别提升+19.3和+22.2个百分点，验证了稳健的log-linear扩展。在线A/B测试中，在保持与生产基线相同的端到端延迟约束下，平台收入提升2.53%。

Conclusion: TransRetrieval成功将Transformer的扩展优势引入推荐检索领域，通过解决特征异构这一核心障碍，使检索模型能够同时随计算预算和跨域数据规模进行有效扩展，并在实际生产环境中带来了显著的经济收益。

Abstract: Applying scaling laws to recommendation retrieval is hindered by feature heterogeneity: naively stacking Transformer layers yields diminishing returns because heterogeneous fields produce severe token-norm divergence. We present TransRetrieval, a Transformer-based retrieval framework that scales with both computational budget and cross-domain data. The key enabler is (1) weighted average aggregation, which restores the homogeneous-token assumption Transformers rely on. Building on this, we introduce (2) target token compression that cuts per-candidate FLOPs by 85% while preserving cross-attention expressiveness, and (3) position-style domain embeddings that unify multiple domains at negligible additional cost, turning cross-domain data into a scaling asset. On a 40-billion-interaction industrial dataset and the public KuaiRand benchmark, scaling compute from 0.1 to 2 MFLOPs per target yields +19.3/+22.2 pt Recall@2000, confirming robust log-linear scaling. In online A/B tests, TransRetrieval lifts platform revenue by 2.53% under the same end-to-end latency constraint as the production baseline.

</details>


### [17] [An Event is Worth One Token: Event Tokenization for Industrial-scale LLM Recommendation](https://arxiv.org/abs/2608.25546)
*Fan Xia,Zhaoheng Zheng,Iman Setayesh,Ruogu Lin,Yiqin Pan,Samarth Mittal,Wentao Bao,Vinti Pandey,Sachin Patil,Jianpeng Cheng,Jun Xiao,Zhuang Wang,Xiangjun Fan,Sri Reddy,Minghai Chen*

Main category: cs.IR

TL;DR: 本文提出事件中心范式AMBER，将每次交互表示为完整时间快照并压缩为事件令牌（Event Token），作为LLM推荐系统的新输入模态。通过引入"快照分辨率"这一新缩放维度，AMBER在工业级排序和检索基准上推进了计算-质量帕累托前沿，且事件令牌可跨模型架构迁移。


<details>
  <summary>Details</summary>
Motivation: 现有LLM推荐系统在每个位置仅编码文本、语义ID或少量类别特征，丢弃了丰富的用户、物品、上下文和结果信号。在自回归建模下，这导致每个位置的查询弱化，且由于每个位置成为后续位置的上下文，退化会沿序列累积放大。

Method: AMBER通过瓶颈事件表示将每个时间快照压缩为紧凑的事件令牌（Event Token），作为新的LLM输入模态。表示通过端到端学习获得，事件令牌在服务时预计算并缓存，从而将快照分辨率与实时服务计算解耦。采用统一tokenizer替代各实体专用tokenizer。

Result: 在工业级排序和检索基准上，AMBER相较于替代推荐范式推进了计算-质量帕累托前沿。在足够容量下，统一tokenizer优于专用per-entity tokenizer，体现跨异构实体类型的正迁移。事件令牌可迁移至非LLM排序器中作为历史特征，带来统计显著提升，且进一步扩展事件Tokenizer容量还有额外提升。

Conclusion: 事件中心范式与快照分辨率缩放维度为LLM推荐系统提供了新的发展方向，通过压缩时间快照为事件令牌，实现了更丰富的信号利用、更高效的推理，以及跨架构的通用性。

Abstract: LLM-based recommendation has scaled along model capacity and sequence length, yet each position encodes only text, semantic IDs, or a few categorical features, discarding rich user, item, context, and outcome signals available at each event. Under autoregressive modeling, this yields weak queries at each position and, since each position becomes context for the next, the degradation compounds across the sequence. We propose an event-centric paradigm that represents each interaction by its full temporal snapshot, and identify a new scaling dimension we term snapshot resolution: the amount of information encoded per event. To efficiently scale snapshot resolution, we introduce AMBER (Autoregressive Modeling via Bottlenecked Event Representation), which compresses each temporal snapshot into a compact Event Token, a new LLM input modality. The representation is learned end-to-end, while Event Tokens are pre-computed and cached for serving, decoupling snapshot resolution from real-time serving compute. On industrial-scale ranking and retrieval benchmarks, AMBER advances the compute-quality Pareto frontier relative to alternative recommendation paradigms. At sufficient capacity, a single unified tokenizer even outperforms dedicated per-entity tokenizers, demonstrating positive transfer across structurally different entity types. AMBER's Event Tokens also transfer across model architectures: when integrated into a heavily optimized non-LLM ranker as serving-time historical features, they yield statistically significant improvements. Further scaling Event Tokenizer capacity provides additional improvements.

</details>


### [18] [When Stale Constraints Go Unchecked: Budgeted Verification Failures in Inherited Agent Memory](https://arxiv.org/abs/2608.25553)
*Kazuki Nakayashiki*

Main category: cs.IR

TL;DR: 本文研究智能体在继承记忆时可能因记录被新权威记录取代而过时的问题。在有限的验证预算（两条记录）下，探索智能体能否发现记录的撤销。实验表明，当约束被取代时，原生分配产生约75-77%的过时一致决策；将验证预算中的一个槽位重新分配给关键溯源路径，可将当前记录一致决策提高约61-74个百分点，且当记录与记忆一致时无副作用。


<details>
  <summary>Details</summary>
Motivation: 智能体继承的固化记忆可能包含已被新权威记录撤销的约束，但在有限的验证预算下，智能体可能无法发现这一撤销。作者旨在探究这种过时错误是否可以通过重新分配验证预算而避免，即错误是否源于验证分配而非根本性限制。

Method: 显式建模记录取代机制：历史溯源不可变，变化的是哪条记录为最新。固定预算为两条记录，比较三种分配：智能体自身分配、将一槽位重新分配给关键溯源路径、或分配给随机记录。在多个领域（主运行、重新措辞复制、留存域）进行实验，并处理了留存域中的时间不一致性（纠正一句后外部存放预先注册的稳健性复制）。

Result: 智能体在约五分之一的情节中检查约束的溯源路径；当约束被取代时，原生分配产生77.3%、74.7%、74.7%的过时一致决策。重新分配一个槽位给关键路径分别提升+74.0、+72.7、+61.3个百分点，六个模型中全部为正；纠正时间不一致后的稳健性复制得到+73.3点。

Conclusion: 干预利用关键路径知识而非调度器，证明过时记忆错误中可归因于验证分配的比例接近其结构上限。记忆系统可能需要独立于相关性的新鲜度或取代信号。

Abstract: An agent that inherits a consolidated memory may inherit a constraint that was true when written and has since been withdrawn by a newer authoritative record. Under a scarce verification budget, does the agent recover the withdrawal, and if not, is the error avoidable without spending more? We model supersession explicitly -- historical provenance is immutable; what changes is which record is current -- and assign by design the memory's form, the world's state (source current or superseded), and the verification policy at a fixed budget of two records: the agent's own allocation, or the same budget with one slot re-assigned to the critical provenance path or to a random record. With a constraint stated, agents inspected its provenance path in about one episode in five; when that constraint had been superseded, native allocation produced stale-consistent decisions in 77.3%, 74.7% and 74.7% of episodes across a primary run, a fresh-wording replication and a held-out domain. Re-assigning one slot to the critical path raised current-record-consistent decisions by +74.0, +72.7 and +61.3 points, positive in six of six models in each of those runs, and changed nothing when the record agreed with the memory. The held-out scenario was later found to contain a temporal inconsistency; a robustness replication with one sentence corrected, deposited externally before execution, gave +73.3 points and is reported alongside the original. The intervention uses knowledge of the critical path and is not a scheduler; it identifies that the share of stale-memory error attributable to verification allocation is close to its structural ceiling. Memory systems may need freshness or supersession signals separate from relevance.

</details>


### [19] [RetrievalRouter: Joint Modality and Architecture Selection for Document Retrieval](https://arxiv.org/abs/2608.25625)
*Emre Kuru,Mehmet Onur Keskin,Reza Farahbakhsh,Noel Crespi*

Main category: cs.IR

TL;DR: 本文提出RetrievalRouter，一个轻量级的查询感知路由器，通过学习查询文本本身来决定使用哪种检索管道（稠密或后期交互、文本或多模态），从而打破检索系统中准确率与延迟不可兼得的权衡。单个可调参数即可暴露完整的准确率-延迟前沿，该路由器在准确率和速度上同时优于所有静态基线。


<details>
  <summary>Details</summary>
Motivation: 现代检索管道在模态（文本/多模态）和检索架构（稠密/后期交互）上存在选择，但最有效的管道代价高昂、速度慢，最快的管道又无法从复杂文档中检索证据。实务人员不得不在遗漏证据与无法接受的延迟之间做选择，且缺乏在查询层面进行调整的原则性依据。作者认为这种权衡是不必要的，因为并非每个查询都需要相同的管道。

Method: RetrievalRouter是一个轻量级、查询感知的路由器，仅从查询文本本身学习哪个检索管道最适合每个查询。它通过一个单一的可调参数暴露完整的准确率-延迟前沿，允许用户在两者之间灵活选择。该路由器的训练数据来自金融和科学语料库的基准数据集。

Result: 在金融和科学语料库的基准上，没有任何静态管道占优。与最佳静态基线相比，RetrievalRouter准确率提升2.5%，速度快12.4倍。与先前自适应策略选择方法相比，在面向准确率的设置中，RetrievalRouter的nDCG@5显著更高；在面向延迟的设置中，其在nDCG@5和延迟方面匹配或数值上优于其他方法。

Conclusion: 检索中准确率与延迟的权衡并非不可避免。通过查询感知的路由策略，可以从查询文本中决定最合适的检索管道，从而同时获得更高的准确率和更低的延迟，为金融、医疗和法律等高风险信息访问场景提供更优的检索方案。

Abstract: Document retrieval increasingly supports high-stakes information access in finance, healthcare, and law. Modern retrieval pipelines vary both in modality (text or multimodal) and in retrieval architecture (dense or late-interaction). These choices impose a hard compromise: the most effective pipelines are too slow and expensive to run at scale, while the fastest fail to retrieve evidence from complex documents. Practitioners must therefore choose between missed evidence and unusable latency, with no principled basis for adapting that choice at the query level. We show that this compromise is unnecessary. Not every query requires the same pipeline. Across benchmarks spanning financial and scientific corpora, no static pipeline dominates. We introduce RetrievalRouter, a lightweight query-aware router that learns, from the query text alone, which retrieval pipeline best fits each query. A single tunable parameter exposes the full accuracy-latency frontier, and for every static baseline, RetrievalRouter offers an operating point that is simultaneously more accurate and faster. Against the best static baseline, RetrievalRouter is 2.5% more accurate and 12.4 times faster. Furthermore, compared with prior adaptive strategy selection methods, RetrievalRouter achieves significantly higher nDCG@5 across accuracy-oriented settings, while matching or numerically outperforming them on both nDCG@5 and latency in latency-oriented settings. Our code and data are available at https://github.com/emrekuruu/retrieval-router.

</details>


### [20] [Hamiltonian Spectral-Temporal Dissipative Dynamics for Sequential Recommendation](https://arxiv.org/abs/2608.25755)
*Shuiying Liao,P. Y. Mok*

Main category: cs.IR

TL;DR: 本文提出哈密顿谱推荐器（HSR），将序列推荐中的用户偏好演化重新概念化为二阶耗散哈密顿系统，在潜相空间中用位置（稳定偏好）和动量（短期倾向）建模，通过频域闭式解、可学习的耗散机制和局部脉冲精化模块，同时捕捉全局周期性、惯性演化和局部冲击，在三个基准数据集上超越基于Transformer和状态空间模型的SOTA推荐器。


<details>
  <summary>Details</summary>
Motivation: 现有序列推荐模型大多将用户偏好演化建模为一阶过程（下一状态仅依赖当前潜表示），但真实用户行为呈现更丰富的动力学特征，包括惯性、周期性和突变，这些无法被一阶假设充分捕捉。

Method: 1）将偏好演化重构为潜相空间中的耗散哈密顿系统（位置=稳定偏好，动量=短期倾向）；2）利用线性时不变结构的控制方程在频域求得闭式解；3）引入可学习的耗散机制建模兴趣自然衰减；4）设计短时局部脉冲精化模块处理稀疏交互日志中的突发行为波动，联合建模全局周期模式、惯性演化和局部冲击。

Result: 在三个基准数据集上的大量实验表明，HSR一致优于基于Transformer和状态空间模型（SSM）的最先进推荐器。

Conclusion: 通过二阶动力学视角重新审视序列推荐，HSR成功捕捉了传统一阶模型忽略的多种用户行为动力学特性，为序列推荐提供了新的建模范式。

Abstract: Sequential recommendation requires understanding how user preferences evolve over time, yet most existing models treat such evolution as a first order process where the next state depends solely on the current latent representation. Nevertheless, real user behavior often exhibits richer dynamics, including inertia, periodicity, and sudden shifts that cannot be fully captured by these first order assumptions. Motivated by these behavioral characteristics, we reconceptualize sequential recommendation through the lens of second order dynamical systems and introduce the Hamiltonian Spectral Recommender (HSR), which recasts preference evolution as a dissipative Hamiltonian system in a latent phase space of position (stable preference) and momentum (short-term tendency). The linear time-invariant structure of the governing equation admits a closed-form solution in the frequency domain. A learnable dissipation mechanism further captures natural interest decay, while a short local impulse refinement module models abrupt behavioral fluctuations commonly observed in sparse interaction logs. This design jointly accounts for global periodic patterns, inertial evolution, and localized shocks, where three phenomena that are underrepresented in existing sequential models. Extensive experiments on three benchmark datasets demonstrate that HSR consistently outperforms state-of-the-art Transformer-based and state space model (SSM)-based recommenders.

</details>


### [21] [PUMA: Post-Hoc Sparsification of Universal Multimodal Embeddings for Efficient Retrieval](https://arxiv.org/abs/2608.25780)
*Matteo Attimonelli,Alessandro De Bellis,Franco Maria Nardini,Claudio Pomo,Cosimo Rulli,Rossano Venturini,Tommaso Di Noia*

Main category: cs.IR

TL;DR: PUMA是一种稀疏自编码器方法，将通用多模态嵌入映射为紧凑的稀疏编码，无需重训主干网络，在保持或提升检索性能的同时将向量存储降低8-16倍，推理速度提升最高25倍。


<details>
  <summary>Details</summary>
Motivation: 通用多模态嵌入器在文本、图像及组合查询的检索中表现优异，但其稠密表示带来了高昂的内存和推理成本。事后稀疏化可以降低这些成本，但在多模态检索领域尚未得到充分探索。

Method: PUMA采用稀疏自编码器方案：预训练阶段保持稠密点积几何结构，随后对稀疏编码器进行检索任务的微调。该过程无需重训骨干模型。

Result: 在五个基准上验证（覆盖文本到图像及组合图像检索），基于Qwen3-VL-Embedding-2B，PUMA在五个数据集中有四个与稠密检索统计上无显著差异或更优；存储降低8-16倍（FP32），在较大候选池中比精确稠密打分快最多25倍。同时识别出事后稀疏化的两个失败模式：pre-TopK支持不足和检索不对齐的活跃支持。

Conclusion: PUMA证明了事后稀疏化可以高效应用于多模态检索，在不牺牲检索质量的前提下实现显著的内存节省和推理加速，为大规模多模态检索提供了实用且高效的方案。

Abstract: Universal multimodal embedders enable retrieval across text, image, and combined queries, but their dense representations incur high memory and inference costs. Post-hoc sparsification could reduce these costs but remains underexplored for multimodal retrieval. We introduce PUMA, a sparse autoencoder recipe that maps universal multimodal embeddings to compact sparse codes without retraining the backbone: a pretraining stage preserves dense dot-product geometry, after which the sparse encoder is fine-tuned for retrieval. We evaluate on five benchmarks covering text-to-image and composed image retrieval. On Qwen3-VL-Embedding-2B, PUMA is statistically indistinguishable from or improves over dense retrieval on four of five datasets. We further identify two failure modes of post-hoc sparsification: insufficient pre-TopK support and retrieval-misaligned active support. PUMA reduces vector storage by 8-16x (FP32) and is up to 25x faster than exact dense scoring on larger candidate pools, enabling efficient multimodal retrieval.

</details>


### [22] [PlanSightRAG: A Visual-First Multimodal RAG for Automating Question Answering and Compliance Checking for Civil Standard Plans](https://arxiv.org/abs/2608.26091)
*Nabaraj Subedi,Shuvo Dip Datta,Ahmed Abdelaty,Shivanand Venkanna Sheshappanavar*

Main category: cs.IR

TL;DR: 提出PlanSightRAG，一个面向土木基础设施合规检查的视觉优先多模态RAG框架。它直接索引和推理平面图图像，集成ColNomic-3B多向量检索、Planner-Retriever-Auditor-Synthesizer智能体流程和MaxSim热力图证据追踪。基于五个州交通部的4,056对基准（1,898页标准图纸），零样本检索达到91.47%的Recall@5，在密歇根州DOT语料上达到91.40%。在合成合规检查图纸上，Qwen2.5-VL-72B管线在提供预解析规则阈值时达到100%的判定准确率，而非VLM的OCR基线仅达76.4%。此外还展示了无需人工规则、直接从规范语料中自主提取数值限制的视觉规则落地能力。


<details>
  <summary>Details</summary>
Motivation: 传统土木基础设施合规检查依赖工程师人工阅读遗留的2D图纸，基于OCR的自动化方法会丢失图纸中的几何信息和布局，难以准确解读平面图。亟需一种能够直接理解图像内容的多模态方案。

Method: 提出PlanSightRAG框架：(1)直接对图纸图像进行索引和推理；(2)集成ColNomic-3B多向量检索；(3)采用Planner-Retriever-Auditor-Synthesizer智能体流程；(4)使用MaxSim热力图作为证据轨迹。同时构建了包含5个州DOT标准图纸的4,056对基准，并使用Qwen2.5-VL-72B进行合成合规图纸的判定。

Result: 零样本检索Recall@5达91.47%，密歇根DOT语料上为91.40%；在合成合规图纸上，Qwen2.5-VL-72B管线在提供预解析规则阈值时达到100%判定准确率，远超OCR基线（76.4%）；能够无人工规则地从规范语料自主提取数值限制。

Conclusion: PlanSightRAG证明了视觉优先的多模态RAG方法在土木图纸合规检查中的有效性，相比传统OCR基线有明显优势，并具备自主视觉规则落地能力。

Abstract: Civil infrastructure compliance checking has long relied on engineers manually reading legacy 2D plans; however, OCR-based automation strips away the geometry and layout essential for interpreting these plans. We present a Visual-First Multimodal Retrieval-Augmented Generation (RAG) framework called PlanSightRAG. It indexes and reasons directly over plan imagery, integrates a ColNomic-3B multi-vector retrieval, an agentic Planner-Retriever-Auditor-Synthesizer, and MaxSim heatmaps as an evidence trail. We introduce a 4,056-pair benchmark from five state Departments of Transportation (DOT) standard plans (1,898 pages). PlanSightRAG achieves 91.47% Recall@5 on zero-shot retrieval, while on a held-out Michigan DOT corpus, it achieves 91.40%. On synthetic, parametrically-generated compliance drawings, our Qwen2.5-VL-72B pipeline reaches 100% verdict accuracy only when supplied a pre-resolved rule threshold, a controlled ceiling that a non-VLM OCR baseline already reaches at 76.4%. Finally, we demonstrate autonomous visual rule-grounding by extracting numeric limits directly from a specification corpus without any human-supplied rules.

</details>


<div id='cs.DS'></div>

# cs.DS [[Back]](#toc)

### [23] [HCC+: Hyperbolic Guarding for Certified Attention Retrieval](https://arxiv.org/abs/2608.24971)
*Liangchen Ge*

Main category: cs.DS

TL;DR: 本文提出了HCC+理论框架，研究双曲空间中注意力检索的Lipschitz稳定性。该框架利用庞加莱球的三个几何性质（指数体积增长、对数覆盖半径、维度无关的打包常数），证明了精确检索和软注意力下的确定性保证，并实现6.1倍的存储压缩。


<details>
  <summary>Details</summary>
Motivation: 现有方法在有限精度表示下缺乏注意力权重保持的确定性保证。双曲空间中的注意力检索稳定性问题尚未被系统研究，需要理论框架来提供确定性（而非概率性）的稳定性能保证。

Method: 提出HCC+框架，利用庞加莱球三个性质：(1)指数体积增长实现查询无关的边界截断；(2)双曲1-中心的对数覆盖半径实现维度无关的关键键识别；(3)与嵌入维度无关的打包常数。通过守护机制（guarding mechanism）辅助证明确定性保证。

Result: 精确检索下每层注意力偏差有界于理想值的10%；软注意力下总变差距离以O(1/√n)衰减（即有限样本方差率）；存储缩减因子相比FP16达到6.1倍；这是非欧几何中首个确定性、查询无关的检索证书。

Conclusion: HCC+为非欧几何中的注意力检索提供了首个确定性的、与查询无关的稳定性保证，兼具理论严谨性与实际存储效率，填补了双曲空间注意力机制有限精度分析的理论空白。

Abstract: We study the Lipschitz stability of attention retrieval in hyperbolic spaces. Existing methods lack deterministic guarantees on attention-weight preservation under finite-precision representations. We introduce HCC+, a theoretical framework exploiting three properties of the Poincaré ball: exponential volume growth enabling query-independent boundary truncation; logarithmic covering radius of hyperbolic 1-centers enabling dimension-independent critical-key identification; and a packing bound with constants independent of the embedding dimension. We prove two deterministic guarantees: for exact retrieval, the per-layer attention deviation is bounded by 10\% of its ideal value; for soft attention, the total variation distance decays as $O(1/\sqrt{n})$, the rate of finite-sample variance. As a consequence of the guarding mechanism, the framework achieves a storage reduction factor of $6.1\times$ relative to FP16. We provide the first deterministic, query-independent retrieval certificate in non-Euclidean geometry.

</details>


### [24] [Fuzzy Pattern Matching in Ordered Structures](https://arxiv.org/abs/2608.25032)
*Armen Kostanyan,Arevik Harmandayan*

Main category: cs.DS

TL;DR: 本文研究了模糊模式匹配问题，将传统模式匹配从精确字符串扩展到模糊模式，涵盖序列和偏序结构两种数据结构，并提出基于"轨迹"（trajectory）的统一框架，该概念推广了KMP算法中的前缀函数。


<details>
  <summary>Details</summary>
Motivation: 模式匹配是计算机科学中的基本问题，但传统方法局限于精确匹配。实际应用中存在模糊模式的需求，且数据不只是线性序列，还可能具有偏序结构。现有方法缺乏针对模糊模式和不同数据结构的统一处理框架。

Method: 提出"轨迹"（trajectory）概念，将KMP算法的前缀函数进行推广。对于序列，寻找匹配模式的片段；对于偏序结构，寻找匹配模式的饱和节点链。算法被表示为转换系统，其中状态（trajectory）伴随相应数据结构一起处理，从而统一了序列和偏序结构上的模糊模式匹配。

Result: 建立了模糊模式匹配的统一框架，能够同时处理序列和偏序结构两类数据结构，并给出了对应的转换系统表示。

Conclusion: 轨迹方法为各种数据结构中的模糊模式匹配提供了统一的理论框架，扩展了KMP算法的经典思想，使其适用于更一般的模糊场景和更复杂的数据结构。

Abstract: The problem of pattern matching, that is, finding all occurrences of a given pattern in a string, is one of the fundamental problems in computer science that has applications in many areas. In this paper, we consider fuzzy patterns, defined as sequences of fuzzy properties over the basic alphabet. We first consider fuzzy pattern matching for sequences of elements of the basic alphabet and then extend the problem to partially ordered sets of nodes labeled by elements of the basic alphabet. For sequences, we seek segments that match the pattern, whereas for partially ordered structures, we seek saturated chains of nodes that match the pattern. The key concept underlying the solutions to these problems is the notion of a trajectory, which generalizes the concept of the prefix function used in the Knuth--Morris--Pratt (KMP) algorithm. A trajectory is processed together with the corresponding data structure, allowing the proposed algorithms to be represented as transition systems whose states are trajectories for sequences and trajectories associated with nodes for partially ordered structures. The trajectory-based approach provides a unified framework for fuzzy pattern matching in various data structures.

</details>


### [25] [Improved Low-Overhead Communication-Efficient String Reconciliation and Edit Distance](https://arxiv.org/abs/2608.25179)
*Michael T. Goodrich,Gonzalo Navarro,Claire A. To*

Main category: cs.DS

TL;DR: 本文提出了一种简单且通信高效的字符串协调（reconciliation）与编辑距离问题算法。当Alice和Bob分别持有长字符串X和Y时，该算法仅需O(k log³ n)比特的通信量即可确定两字符串之间的编辑距离k（k为不相似度），同时计算开销为O(n log k)，且具有高概率保证。


<details>
  <summary>Details</summary>
Motivation: 在数据库同步、文件系统同步以及DNA序列比对等实际场景中，两个通信方需要高效地确定各自持有字符串的相似程度。由于字符串很长，当它们足够相似时，需要设计通信量和计算开销都与不相似程度（而非字符串全长）成比例的算法，以实现高效协调。

Method: 本文提出了一种简单且低开销的通信高效算法，用于解决字符串协调和编辑距离问题。算法通过巧妙的编码与通信策略，使得所需通信量仅取决于编辑距离k（即字符串的不相似程度），而非字符串的总长度n。

Result: 算法仅需O(k log³ n)比特的通信量和O(n log k)的计算时间开销，即可在高概率下确定编辑距离k。

Conclusion: 该算法为字符串协调和编辑距离计算提供了一种通信量与不相似程度成正比的解决方案，显著优于与字符串全长成比例的朴素方法，适用于大规模字符串的高效同步与比较场景。

Abstract: Suppose two parties, Alice and Bob, hold long character strings, $X$ and $Y$, respectively, and they are interested in determining how similar $X$ and $Y$ are. {Moreover, they want to exchange the strings with cost proportional to their degree of dissimilarity.} Such problems arise, for example, in database and file system synchronization operations, as well as in DNA sequence comparisons. Since the strings are long, we are interested in methods that are communication-efficient and have low overhead in terms of the computations that Alice and Bob must perform, when the strings are similar enough. In this paper, we provide a simple low-overhead communication-efficient algorithms for such string reconciliation and edit distance problems, determining the edit distance $k$ between $X$ and $Y$ using only $O(k\log^3 n)$ bits of communication and $O(n\log k)$ time overhead, with high probability.

</details>


### [26] [Exact algorithms for optimal discretization](https://arxiv.org/abs/2608.25197)
*László Kozma,Junqi Tan*

Main category: cs.DS

TL;DR: 本文研究最优离散化问题（用最少水平/垂直线分离平面中两组点）及相关点分离问题。作者提出运行时间为O(1.9602^n)和O(1.8906^n)的精确指数算法，首次突破两个问题的平凡2^n上界。


<details>
  <summary>Details</summary>
Motivation: 最优离散化问题源于监督机器学习的预处理，此前已有固定参数算法（2^{O(k^2 log k)}·n^{O(1)}），但精确指数时间复杂度方面一直缺乏优于平凡2^n的结果。本文旨在填补这一空白。

Method: 通过结构观察双色点集和单色点集的几何性质，设计分支算法。算法能够处理点共坐标的情况，利用点集的特定结构性质来缩减搜索空间，从而获得优于2^n的运行时间。

Result: 最优离散化问题获得O(1.9602^n)时间算法，点分离问题获得O(1.8906^n)时间算法。这两个结果均为首次突破对应问题的平凡2^n指数上界。

Conclusion: 本文通过细致的结构分析，首次为最优离散化和点分离问题提供了优于2^n的精确指数算法，是对这两个问题算法复杂度的实质性改进。

Abstract: The optimal discretization problem asks, given two disjoint sets of points $R$ and $B$ in the plane, for a minimal family of horizontal and vertical lines that separate the two sets, so that no cell delimited by the lines contains points from both sets. The problem arises as a pre-processing in supervised machine learning, and has received significant attention in parameterized algorithmics. Answering the question raised by Bonnet, Giannopoulos, and Lampis [IPEC 2017] and Froese [PhD thesis, 2018], it was shown by Kratsch, Masařík, Muzi, Pilipczuk, and Sorge [SODA 2021] that optimal discretization admits a fixed-parameter algorithm with running time $2^{\mathcal{O}(k^2 \log k)} \cdot n^{\mathcal{O}(1)}$, where $k$ is the solution size and $n = |R| + |B|$.
  In this paper we give an algorithm for optimal discretization that runs in time $\mathcal{O}(1.9602^n)$. We also study the related point separation problem that asks to separate all input points by axis-parallel lines. For this problem we obtain an algorithm with runtime $\mathcal{O}(1.8906^n)$. Our guarantees follow from structural observations about bichromatic and monochromatic point sets, and hold even if points are allowed to share coordinates. To our knowledge, these are the first improvements over the trivial $2^n$ bound for both problems.

</details>


### [27] [Time-Optimal APSP and Matrix Multiplication in Classes of Linear Neighborhood Complexity](https://arxiv.org/abs/2608.25212)
*Édouard Bonnet,Julien Duron,Marcin Pilipczuk,Marek Sokołowski,Szymon Toruńczyk*

Main category: cs.DS

TL;DR: 本文针对具有线性邻域复杂度（linear neighborhood complexity）的图类，提出了O(n²)时间的最优算法，解决全源最短路径（APSP）和邻接矩阵乘法问题，并推广到有界VC密度图类，同时给出了在随机化线性时间内检测三角形、K₄和K₅的子图检测算法。


<details>
  <summary>Details</summary>
Motivation: 线性邻域复杂度是一个非常通用的结构假设，涵盖了大多数稀疏图类（如平面图、排除固定（拓扑）子式的图、有界扩张图）以及许多稠密图类（如有界clique-width、twin-width、merge-width或flip-width的图）。作者希望在这样的统一框架下解决图论中的经典问题（如APSP和矩阵乘法），并回应和解决若干近期论文中提出的开放问题。

Method: 作者提出了在线性邻域复杂度图类上O(n²)时间的预处理方法，使得预处理后的图可以在O(n)时间内完成与任意n维向量的乘法。这一方法被进一步推广到有界VC密度图类。此外，作者还设计了针对三角形、K₄和K₅检测的随机化算法，复杂度分别为线性时间、O(n log⁵ n + m log n)和O(n log⁹ n + m log⁵ n)（K₄也有确定性O(n²)算法）。

Result: 主要成果包括：(1) 在线性邻域复杂度图类上实现APSP的最优O(n²)时间算法；(2) 二次预处理后支持O(n)时间的向量-矩阵乘法；(3) 解决了Bonnet等人在ICALP'26中提出的多个问题，并改进和推广了多篇近期论文的结果；(4) 将结果扩展到有界VC密度图类；(5) 给出了三角形、K₄和K₅的随机化子图检测算法。

Conclusion: 本文通过统一的线性邻域复杂度框架，为一大类稀疏和稠密图提供了APSP和矩阵乘法的最优算法，同时提供了高效的子图检测算法，显著改善和推广了此前多个独立工作的结果，解决了若干开放问题。

Abstract: The notion of linear neighborhood complexity is a very general structural assumption on a graph class, covering most classes of sparse graphs such as planar graphs, graphs excluding a fixed (topological) minor, or bounded expansion graphs, as well as many structured classes of dense graphs, such as graphs of bounded clique-width, twin-width, merge-width, or flip-width. In this work, we present $O(n^2)$-time optimal algorithms for $n$-vertex graphs coming from a class of linear neighborhood complexity for the following problems: $\bullet$ All-Pairs Shortest Paths, $\bullet$ the multiplication of the adjacency matrix $M$ of the input graph with any $n \times n$ matrix. More specifically, after a quadratic preprocessing, we can multiply $M$ with any $n$-vector in $O(n)$ time.
  This solves several questions raised in [Bonnet, Kim, Geniet, Moon; ICALP '26], and improves and generalizes results in several other recent papers [Bonnet, Giocanti, Ossona de Mendez, Thomassé; STACS '23], [Bannach, Marwitz, Tantau; STACS '24], [Anand, van den Brand, McCarty; NeurIPS '26], [Kozma, Opler '26], and [Cardinal, McCarty, Yuditsky '26]. We also extend our results to classes of bounded VC density. In classes of linear neighborhood complexity, we also give a triangle-detection algorithm in randomized linear time $O(n+m)$ in $n$-vertex $m$-edge graphs, a $K_4$-detection algorithm in randomized $O(n \log^5 n + m \log n)$ or deterministic $O(n^2)$ time, and a $K_5$-detection algorithm in randomized $O(n \log^9 n + m \log^5 n)$ time.

</details>


### [28] [A General Framework for Metropolis-Adjusted Dikin Walks: Dimension-Square Mixing on Polytopes and Log-Det Walks on Spectrahedra](https://arxiv.org/abs/2608.25273)
*Zhao Song,Lichen Zhang*

Main category: cs.DS

TL;DR: 本文分析了精确度量的Metropolis-adjusted Dikin walks，通过将提议行列式和反向二次型放在一起处理，利用对数接受率中前导非中心项的抵消效应，结合二阶工具控制中心化波动，为多面体（Lee-Sidford walk）和谱面体（log-det walk）分别给出了改进的混合时间上界，并建立了统一的接受-混合约化框架。


<details>
  <summary>Details</summary>
Motivation: Metropolis-adjusted Dikin walks是解决凸体和谱面体上均匀/加权采样问题的重要马尔可夫链方法，但此前对其精确度量（exact-metric）版本的分析存在困难，主要障碍在于接受率中的波动项难以控制。本文旨在通过巧妙的代数重组和二阶分析工具，获得更紧的混合时间界，并统一多面体与谱面体两种情形的分析框架。

Method: (1) 将提议行列式（proposal determinant）与反向二次型（reverse quadratic form）配对处理，利用完整对数接受率中前导非中心项的精确抵消，将剩余部分转化为可控的中心化波动，并通过二阶工具（second-order tools）加以界定。(2) 提出接受-混合（acceptance-to-mixing）的统一约化命题，作为两种情形的共同分析核心。(3) 对多面体情形，利用提议比较（proposal-comparison）论证，将边界传递到由高精度Lewis权重计算得到的O(1/d)精确度的填充度量上。(4) 对谱面体情形，给定ψ^≥ψ*，采用direct-or-two-seed TensorSRHT构造，实现精确算术实现，并以ψ̂替代ψ*进入混合界。

Result: (1) 对由n个不等式定义的凸多面体和L-Lipschitz凸势能，正则化Lee-Sidford walk在暖启动下以Õ((d²+dL²R²)log(w/δ))步混合。(2) 对具有n×n块的谱面体，log-det walk（log-det walk）以Õ((ψ*nd+dL²R²)log(w/δ))步混合，其中ψ*度量矩阵杠杆（matrix leverage）。(3) 两个分析共享同一接受-混合约化，且谱面体情形可通过TensorSRHT构造实现精确算术执行。

Conclusion: 本文通过精确处理Metropolis-adjusted Dikin walks的对数接受率，成功获得了多面体和谱面体上采样算法的改进混合时间上界。统一的接受-混合约化框架表明，这类算法的分析核心在于接受概率的精细控制；同时，TensorSRHT构造确保了理论界的可计算实现性，为未来在更高维复杂几何体上的采样研究提供了方法论基础。

Abstract: We analyze exact-metric, Metropolis-adjusted Dikin walks by keeping the proposal determinant and reverse quadratic form together. Their leading uncentered terms cancel in the complete logarithmic acceptance ratio, leaving centered fluctuations that can be controlled with second-order tools. For a polytope given by $n$ inequalities and a convex $L$-Lipschitz potential, this yields warm-start mixing in $\widetilde O((d^{2}+dL^{2}R^{2})\log(w/δ))$ steps for the regularized Lee--Sidford walk. For a spectrahedron with $n\times n$ blocks, the log-det walk mixes in $\widetilde O((ψ^\star nd+dL^{2}R^{2})\log(w/δ))$ steps, where $ψ^\star$ measures matrix leverage.
  The two analyses share an acceptance-to-mixing reduction. A proposal-comparison argument transfers the polytope bound to an appropriately padded $O(1/d)$-accurate metric computed from high-precision Lewis weights. For spectrahedra, given $\widehatψ\geψ^\star$, a direct-or-two-seed TensorSRHT construction gives an exact-arithmetic implementation with $ψ^\star$ replaced by $\widehatψ$ in the mixing bound.

</details>


### [29] [Paging with Per-Replacement Maximum Delay](https://arxiv.org/abs/2608.25290)
*Tianhang Lu,Runtian Ren,Shengcai Liu*

Main category: cs.DS

TL;DR: 本文研究经典分页（paging）问题的一个变体：当一次缓存缺失（miss）可以延迟处理时，算法的竞争性结构是否仍然成立。作者提出了"每次替换最大延迟"模型，并证明经典竞争层级在该模型下依然成立，给出了确定性 (5k+3)-竞争阈值LRU算法和随机 5H_k-竞争算法。离线视角则不那么经典：给出了精确动态规划和多项式时间5-近似算法，但发现最远未来使用（FIF）受害选择在物理延迟问题中可能是次优的。


<details>
  <summary>Details</summary>
Motivation: 经典分页将每次缓存缺失与立即替换耦合，但实际系统中替换操作可能因各种原因被延迟。作者希望探究当缺失可以等待时，经典分页的算法结构（竞争性分析、离线最优性等）是否仍然成立，从而理解延迟替换对缓存算法理论的影响。

Method: （1）定义每次替换最大延迟模型：加载待处理页面花费一个单位移动加上其最老未决请求的年龄，并清除整个页面专属片段；瞬时持有率为待处理页面数而非请求数。（2）在线算法：提出确定性 (5k+3)-竞争阈值LRU算法，以及随机 5H_k-竞争算法（对抗 oblivious 对手），后者使用缓存无关的时间窗口构造固定普通分页序列，再通过影子分页算法投影到非主动性物理替换。（3）离线算法：给出精确 O(nk) 单洞动态规划、固定洞数的精确配置动态规划，以及不固定洞数的确定性非主动多项式时间5-近似算法。

Result: 经典竞争层级在该延迟模型中得以保留：确定性下界 Ω(k) 与随机下界 Ω(H_k) 均被匹配。离线方面，精确动态规划（O(nk) 单洞、配置DP固定洞数）和5-近似算法均被给出；同时证明经典 Farthest-in-Future（最远未来使用）受害选择在仅3个页面的物理延迟问题中即为次优。

Conclusion: 延迟替换不破坏经典竞争分析框架，但改变了离线问题的结构：精确求解需要新的动态规划技术，且经典最优离线策略（FFU）在该模型下失效。在线与离线之间的这种不对称性是该延迟模型的重要特征。

Abstract: Classical paging couples every miss to an immediate replacement. We ask what remains of its algorithmic structure when a miss may wait. In our per-replacement maximum-delay model, loading a pending page costs one unit of movement plus the age of its oldest outstanding request and clears the whole page-specific episode. Equivalently, the instantaneous holding rate is the number of pending pages, rather than the number of pending requests.
  The classical competitive hierarchy survives this change. For cache size $k$, we give a deterministic $(5k+3)$-competitive threshold-LRU algorithm and a randomized $5H_k$-competitive algorithm against an oblivious adversary; classical lower-bound instances give matching $Ω(k)$ and $Ω(H_k)$ orders. The randomized algorithm uses cache-independent temporal windows to create an ordinary-paging sequence fixed before any random choices; a shadow paging algorithm is then projected onto nonproactive physical replacements.
  The offline picture is less classical. We give an exact $O(nk)$ dynamic program with one hole, an exact configuration dynamic program for a fixed number of holes, and a deterministic nonproactive polynomial-time $5$-approximation without fixing that number. Yet farthest-next-use victim selection can be suboptimal in the physical delayed problem already with three pages.

</details>


### [30] [A Spectral Local-to-Global Principle for Spin Systems on Graphs with Girth At Least Five](https://arxiv.org/abs/2608.25491)
*Xiaoyu Chen,Kuikui Liu*

Main category: cs.DS

TL;DR: 本文证明了在围长至少为5、最大度为Δ的图上，当颜色数q ≥ (1+δ)Δ时，均匀q-着色的Glauber动力学是快速混合的。该结果还推广到满足局部谱收缩条件的一般多自旋系统，包括反铁磁Potts模型。关键创新是图围长至少为5时一般多自旋系统的谱局部到整体原理，以及对星图上Glauber动力学的新型Fourier分析。


<details>
  <summary>Details</summary>
Motivation: 研究Glauber动力学在q-着色问题上的快速混合性质，这是一个经典的马尔可夫链混合时间问题。此前的结果往往需要更强的条件（如q ≥ 11Δ/6或更严格限制），本文希望将混合条件进一步放松到q ≥ (1+δ)Δ这一接近最优的边界。

Method: 提出了一个针对围长至少为5的图的一般多自旋系统的新谱局部到整体原理，并针对星图上的Glauber动力学发展了一种新的Fourier分析方法。这些工具结合使用，为满足局部谱收缩条件的系统（包括q ≥ (1+δ)Δ的着色问题和q ≥ (1+δ)(1-β)Δ的反铁磁Potts模型）建立了快速混合性。

Result: 证明了当q ≥ (1+δ)Δ且图围长至少为5时，q-着色的均匀分布上Glauber动力学是快速混合的（Rapidly mixing）。结果推广到满足局部谱收缩条件的一般多自旋系统，包括反铁磁Potts模型在q ≥ (1+δ)(1-β)Δ时的情形。

Conclusion: 该工作通过新的谱局部到整体原理和Fourier分析技术，在围长至少为5的图上将q-着色Glauber动力学的快速混合条件显著改进到接近最优阈值q ≥ (1+δ)Δ，并提供了对一般多自旋系统的统一框架。

Abstract: It is proved that, for every $δ\in (0,1)$, the Glauber dynamics for the uniform distribution on proper $q$-colorings is rapidly mixing when $q \geq (1+δ)Δ$ and the underlying graph has girth at least $5$ and maximum degree $Δ= Ω_δ(1)$. This result also extends to general multi-spin systems satisfying a $\textit{local spectral contraction}$ condition, including the anti-ferromagnetic Potts model with $q\geq (1+δ)(1-β)Δ$. These results are achieved by a new spectral local-to-global principle on graphs with girth at least five for general multi-spin systems, and a novel Fourier analysis for Glauber dynamics on a star. The main ideas behind all the proofs were developed through several rounds of interaction with GPT-5.6 Sol Ultra.

</details>


### [31] [Not All Degree Constraints Are Created Equal when Computing Spanning Trees](https://arxiv.org/abs/2608.25530)
*Narek Bojikian,Alexander Firbas,Robert Ganian,Hung P. Hoang,Krisztina Szilagyi*

Main category: cs.DS

TL;DR: 本文研究带局部度约束的最小生成树问题，揭示了三类自然形式化（Specified Degree MST、Bounded Degree MST、Set of Degrees MST）在树深度（treedepth）参数化下的复杂性景观根本性分化：前两者在树深度参数下是固定参数可处理的（FPT），而Set of Degrees MST在树深度参数下是W[1]-难的，即使再结合反馈顶点数仍不改变其困难性。


<details>
  <summary>Details</summary>
Motivation: 近年工作（ICALP 2026）表明这三类问题在标准结构图参数（树宽、路径宽、clique-width）下具有完全相同的参数化复杂度，本文旨在探索在更受限的参数化条件下这些问题的复杂性是否会发生分化，以更好地理解问题的本质结构差异。

Method: 通过引入更严格的参数化（树深度、顶点覆盖数、删除距离到常数路径宽），分别分析三个问题的参数化复杂度。对FPT结果给出算法构造，对W[1]-困难性给出归约证明。

Result: (1) Specified Degree MST和Bounded Degree MST在树深度参数下是FPT的；(2) Set of Degrees MST在树深度参数（即使结合反馈顶点数）下是W[1]-难的；(3) 这种分化是树深度特有的：Set of Degrees MST在顶点覆盖数参数下没有类似的W[1]-难结果；前两个问题在删除距离到常数路径宽参数下不存在FPT算法。

Conclusion: 树深度是这三类度约束最小生成树问题复杂性分化的关键结构参数。之前的统一复杂性景观在更精细的树深度参数下瓦解，揭示了Set of Degrees MST与另外两个问题本质上的计算复杂性差异。

Abstract: We study the computation of minimum spanning trees subject to local degree constraints. Recent work (ICALP 2026) established that three natural formalizations of this problem share the exact same parameterized complexity under standard structural graph parameters, including treewidth, pathwidth and clique-width. This applies to the cases where every vertex has a single target degree (Specified Degree MST), or a degree upper bound (Bounded Degree MST), or is equipped with a set of admissible degrees (Set of Degrees MST).
  In this paper, we investigate these problems under more restrictive parameterizations and reveal that their complexity landscapes fundamentally diverge on bounded-treedepth graphs. Specifically, we prove that the former two problems are fixed-parameter tractable when parameterized by the treedepth of the input graph. In sharp contrast, we show that Set of Degrees MST remains W[1]-hard parameterized by treedepth, even when combined with the feedback vertex number (i.e., deletion distance to treewidth $1$). Finally, we show that this divergence seems to be specific to treedepth: we exclude an analogous W[1]-hardness result for Set of Degrees MST w.r.t. the vertex cover number and also rule out fixed-parameter algorithms for the former two problems w.r.t. deletion distance to constant pathwidth.

</details>


### [32] [Nearly Optimal Strong Coresets for $\ell_p$ Subspace Approximation](https://arxiv.org/abs/2608.26047)
*Honghao Lin,Vahab Mirrokni,David P. Woodruff*

Main category: cs.DS

TL;DR: 本文研究ℓ_p子空间逼近的强coreset问题，将Woodruff和Yasuda (FOCS 2025)的结果从1≤p<2时的Õ_p(kε^{-4/p})改进为Õ_p(kε^{-2})，p>2时从Õ_p(k^{p/2}ε^{-p})改进为Õ_p(k^{p/2}ε^{-2})。该结果在k+1≥C log(1/ε)时匹配已知采样下界（对数因子内）。


<details>
  <summary>Details</summary>
Motivation: ℓ_p子空间逼近是核心的数值线性代数问题，强coreset可在降采样后仍保持对所有低维子空间的逼近质量。此前Woodruff-Yasuda的coreset尺寸在ε依赖上不是最优，本文旨在改进ε的指数依赖并匹配理论下界。

Method: 对于1≤p<2，结合双准则低秩分解、Lewis权重采样以及不依赖于输出维度的经验过程界。对于p>2，对Woodruff-Yasuda构造进行更精细的分析，在行数递推过程中保留采样概率中的截断项，从而获得改进的ε^{-2}依赖。

Result: 1≤p<2时coreset尺寸改进为Õ_p(kε^{-2})，运行时间为Õ_p(nnz(A)+d^ω+kε^{-2})；p>2时coreset尺寸改进为Õ_p(k^{p/2}ε^{-2})，运行时间为Õ_p(nnz(A)+d^ω)，与Woodruff-Yasuda框架的运行时间相匹配。在k+1≥C log(1/ε)条件下匹配已知采样下界。

Conclusion: 本文在两种p值区域分别采用不同技术，都实现了coreset尺寸的ε依赖从ε^{-4/p}或ε^{-p}改进到ε^{-2}，达到最优（对数因子内），同时保持了高效运行时间。

Abstract: We study strong coresets for $\ell_p$ subspace approximation. Given $\A\in\R^{n\times d}$, the goal is to sample and rescale a small number of its rows to form $\Smat\A$ such that \[
  \norm{\Smat\A(\I-\Pmat_F)}_{p,2}^p
  =(1\pm\eps)\norm{\A(\I-\Pmat_F)}_{p,2}^p \] simultaneously for every subspace $F\subseteq\R^d$ of dimension at most $k$, where $\Pmat_F$ is the orthogonal projector onto $F$. Woodruff and Yasuda (FOCS 2025)~\cite{WY25} obtained coreset sizes $\wtO_p(k\eps^{-4/p})$ for $1\leq p<2$ and $\wtO_p(k^{p/2}\eps^{-p})$ for $p>2$. We improve these bounds to $\wtO_p(k\eps^{-2})$ and $\wtO_p(k^{p/2}\eps^{-2})$, respectively. For $1\leq p<2$, our algorithm runs in $\wtO_p(\nnz(\A)+d^ω+k\eps^{-2})$ time. The resulting coreset size matches the known sampling lower bound~\cite{LWW21} up to logarithmic factors when $k+1\geq C\log(1/\eps)$ for an absolute constant $C$. For $p>2$, our algorithm runs in $\wtO_p(\nnz(\A)+d^ω)$ time, matching the running time of the Woodruff--Yasuda framework.
  We use different techniques in the two regimes. For $1\leq p<2$, we combine a bicriteria low-rank split with Lewis-weight sampling and empirical-process bounds independent of the output dimension. For $p>2$, we give a sharper analysis of the Woodruff--Yasuda construction. By retaining the truncation in its sampling probabilities throughout the row-count recurrence, we show that it achieves the improved $\eps^{-2}$ dependence.

</details>


<div id='cs.DB'></div>

# cs.DB [[Back]](#toc)

### [33] [Analyzing and Reducing Search Quality Differences in Vector Similarity Search](https://arxiv.org/abs/2608.25185)
*Sara Mahdizadeh Shahri,Martin Prammer,Jignesh M. Patel,Akshitha Sriraman*

Main category: cs.DB

TL;DR: 本文提出RCheck，一个轻量级运行时系统，用于检测和减少近似最近邻搜索（ANNS）中不同查询之间的召回率差异。RCheck通过动态调整搜索努力（为低于目标召回率的查询增加努力，为高于目标召回率的查询减少努力），在保持高吞吐量的同时提高查询召回率的一致性。在pgvector数据库上的评估显示，RCheck在相同吞吐量下将平均召回率提高了11-93%，并使8-47%更多的查询达到目标召回率。


<details>
  <summary>Details</summary>
Motivation: 现代数据库服务使用ANNS在大规模数据集合上实现可扩展搜索，但现有方法仅优化平均召回率，掩盖了不同查询之间的显著召回率差异。这导致许多查询低于目标召回率（损害用户体验和收入），或高于目标召回率（浪费计算资源）。因此需要能够检测并减少查询间召回率差异的系统。

Method: RCheck采用动态、高效地调整搜索努力的策略：对于低于目标召回率的查询增加搜索努力，对于高于目标召回率的查询减少搜索努力。系统通过调整可用的搜索努力参数来实现这一目标，使其易于部署到现有数据库系统中。

Result: 在广泛使用的生产级pgvector数据库上评估表明，与最先进的全局调优配置相比，在相同吞吐量下，RCheck将平均召回率提高了11-93%，并使得8-47%更多的查询能够达到目标召回率。

Conclusion: 优化平均召回率会掩盖查询间的召回率差异，RCheck通过逐查询动态调整搜索努力，能够在保持高吞吐量的同时有效减少召回率差异，提升更多查询达到目标召回率的比例，且易于部署到现有生产数据库系统。

Abstract: Modern database services scalably search over large data collections via Approximate Nearest Neighbor Search, which improves search performance at the cost of search quality, measured by recall. In practice, a database operator seeks to achieve a target mean recall while maximizing throughput across search queries. We show that optimizing for mean recall masks significant differences in recall across queries even when target recall is met. As a result, numerous queries face (1) below-target recall, hurting user experience and revenue and (2) above-target recall, wasting computation to deliver unnecessarily high search quality. Thus, it is critical to detect and reduce recall differences across queries. We design RCheck, a light-weight run-time system that identifies low-recall queries and reduces recall differences while achieving high throughput. RCheck's key design principle is to dynamically, efficiently adapt search effort by increasing effort for queries below target recall and decreasing effort for those above it. RCheck tunes available search effort parameters, making it readily deployable. We evaluate RCheck using the widely-used production-style pgvector database. At the same throughput, RCheck improves mean recall by 11-93% and enables 8-47% more queries to meet target recall compared to the state-of-the-art globally-tuned configuration.

</details>


### [34] [Bolt-on, Verifiable Provenance for LLM-Powered Data Processing](https://arxiv.org/abs/2608.25210)
*Yiming Lin,Sepanta Zeighami,Aditya G. Parameswaran*

Main category: cs.DB

TL;DR: 本文提出BLIP框架，用于为LLM数据处理任务高效推断可验证的溯源（verifiable provenance），通过识别能够复现相同答案的最小子集输入文本，并保证溯源的正确性。


<details>
  <summary>Details</summary>
Motivation: LLM是强大的数据处理工具，但作为黑盒系统，无法告知答案来源或其可信度。现有的启发式方法（如嵌入相似度或直接询问LLM）无法保证识别出的溯源能真正推导出答案，且常常出错。因此需要一种有保证的溯源机制。

Method: 提出可验证溯源（verifiable provenance）概念，即识别出能复现与完整文本相同（或等价）答案的输入子集，并引入最小性（minimality）约束。针对朴素穷举所有子集代价过高的问题，提出BLIP框架，包含8种保证能找到最小可验证溯源的策略，以及一种结合各策略优势的自适应策略，并扩展到生成多个最小可验证溯源。

Result: 在7个数据集上的实验表明，BLIP生成的溯源总能保证复现答案，相比最佳基线的溯源大小相当的情况下准确率高出30%以上，且成本较低，与在原始数据上的原始查询成本相当。

Conclusion: BLIP提供了一种高效且可保证的LLM数据溯源方法，解决了黑盒LLM缺乏答案可解释性和可信度的问题，且不依赖特定LLM或任务，是一个通用的即插即用（bolt-on）框架。

Abstract: Large Language Models (LLMs) are powerful tools for processing data. However, LLMs are also complex black-boxes, returning answers to queries on data,
  without any indication for where the answer came from or whether it is trustworthy. We introduce the notion of provenance for data processing with LLMs. While
  existing heuristics (such as embedding similarity or directly asking an LLM) could provide some hints for where the answer was derived, they provide no guarantees
  that the answer can be derived using the identified provenance, and indeed, are often incorrect. Instead, we propose the notion of verifiable provenance wherein we
  identify a subset of the input text that reproduces the same (or equivalent) answer as that on the complete text, and introduce the notion of minimality, where the
  verifiable provenance is as small as possible. To identify such a provenance, a naive solution would require checking all possible subsets of the source data with the
  LLM, which is prohibitively expensive. We present BLIP, a bolt-on framework for efficiently inferring a small-sized verifiable provenance for any LLM-powered data
  processing task, with any LLM. As part of BLIP, we introduce eight strategies, each guaranteed to find a minimal verifiable provenance, as well as an adaptive
  strategy that combines their strengths to reduce cost further. We further extend BLIP to produce multiple minimal verifiable provenances. Experiments on seven
  datasets show that the provenance generated by BLIP is always guaranteed to reproduce the answer - achieving over 30% higher accuracy than the best-performing
  baseline with a comparable provenance size. Moreover, BLIP incurs a low cost, comparable to the original query on the original data.

</details>


### [35] [DBcover: A White-box SQL Test Generation Framework for Coverage Improvement](https://arxiv.org/abs/2608.25573)
*Yankai Rong,Shuang Liu,Jinhao Dong,Qiang Yin,Wei Lu,Jianhua Wang,Xiaoyong Du*

Main category: cs.DB

TL;DR: DBcover是一个基于LLM的白盒SQL测试生成框架，通过动态分析提取SQL到执行路径的对应关系并构建知识图谱作为上下文，引导LLM生成覆盖未覆盖代码区域的SQL测试用例。在PostgreSQL、MySQL和KingbaseES上分别达到80.1%、82.3%和较高的覆盖率。


<details>
  <summary>Details</summary>
Motivation: RDBMS测试中实现高覆盖率具有挑战性。传统模糊测试基于随机SQL生成，无法捕获SQL输入与内部执行路径之间的对应关系；符号执行则面临高昂的成本和可扩展性限制。

Method: DBcover包含两部分：1) 上下文构建——使用轻量级动态分析提取SQL到路径的对应关系和调用图作为全局上下文，收集目标函数周围的源代码信息作为局部上下文，并将其组织为统一的知识图谱；2) 两阶段测试生成——先选择执行路径接近未覆盖目标的语义相关种子，再用全局和局部上下文引导LLM生成能触发未覆盖代码区域的SQL测试用例。

Result: 在PostgreSQL和MySQL上分别达到80.1%和82.3%的覆盖率，并在企业级RDBMS KingbaseES上也有效，证明了对闭源系统的实际适用性。

Conclusion: DBcover通过上下文推理将LLM与白盒测试相结合，有效解决了传统测试方法在RDBMS中覆盖率不足的问题，并展示了在闭源企业数据库上的实用性。

Abstract: Relational Database Management Systems (RDBMSs) are the backbone of modern data-intensive applications, making reliability and robustness critical. However, achieving high coverage in RDBMS testing remains challenging because of large codebases and complex execution logic. Traditional fuzzing relies on random SQL generation and cannot capture the correspondence between SQL inputs and internal execution paths, while symbolic execution suffers from prohibitive cost and scalability limitations.
  We propose DBcover, an LLM-driven white-box SQL test generation framework based on contextual reasoning. DBcover uses lightweight dynamic analysis to extract SQL-to-path correspondence and call graphs as global context, and collects source-level information around target functions as local context. These contexts are organized in a unified knowledge graph for efficient retrieval and reuse. DBcover then performs two-phase test generation: it first selects a semantically relevant seed whose execution path is close to the uncovered target, and then guides the LLM with global and local context to generate SQL test cases that trigger previously uncovered code regions. Experiments show that DBcover achieves 80.1% and 82.3% coverage on PostgreSQL and MySQL, and is also effective on the enterprise RDBMS KingbaseES, demonstrating its practical applicability to closed-source systems.

</details>


### [36] [PolyMemDB: A Polyglot Database System for AI Memory Management](https://arxiv.org/abs/2608.25577)
*Yu Wang,Jiaheng Lu*

Main category: cs.DB

TL;DR: PolyMemDB 是一个面向个人智能体长期记忆管理的多语言存储系统，采用多模态存储架构（图、向量、概率、时空数据），并通过概率推理引擎结合时间衰减与半环聚合来解决长期事实冲突、提供数据溯源和透明推理链。


<details>
  <summary>Details</summary>
Motivation: 现有记忆系统存在两大局限：一是依赖单一存储范式导致多维数据碎片化；二是缺乏细粒度的数据溯源能力，无法解决长期交互中的事实冲突，加剧了LLM的幻觉问题。

Method: PolyMemDB 包含两大核心组件：1) 多语言（polyglot）存储架构，用于跟踪和管理图数据、向量数据、概率数据和时空数据等多种记忆类型；2) 概率推理引擎，集成时间衰减（temporal decay）与半环聚合（semiring aggregation），用于解析长期事实冲突并提供详细的数据溯源。

Result: 该系统能够有效解决长期事实冲突问题，降低LLM幻觉，同时让用户能够透明地追踪推理链条。

Conclusion: PolyMemDB 为智能体长期记忆管理提供了一种统一的多模态存储与推理方案，兼顾了数据多样性、事实一致性和可解释性，是迈向更可靠的个性化智能体的重要一步。

Abstract: With the widespread adoption of personal intelligent agents, users generate massive, heterogeneous data during long-term interactions. Leveraging this data as long-term memory helps reduce token overhead and deliver personalized experiences. However, existing memory systems face two primary limitations: they rely on single-storage paradigms that fragment multi-dimensional data, and they lack fine-grained data provenance to resolve long-term factual conflicts, thereby worsening LLM hallucinations.
  In this demonstration, we introduce PolyMemDB, a novel system tailored for managing agent memory. PolyMemDB has a polyglot storage architecture designed to track and manage various memory types, including graph, vector, probability and spatial-temporal data. To ensure factual consistency and reduce hallucinations, it features a probabilistic inference engine that integrates temporal decay with semiring aggregation, resolving long-term factual conflicts, providing detailed data provenance, and enabling users to trace reasoning chains transparently.

</details>


### [37] [MetaSieve: Faster Relational Deep Learning through SQL-Based Metapath Selection](https://arxiv.org/abs/2608.25903)
*Fahim Shahriar Khan,Ashraf Aboulnaga*

Main category: cs.DB

TL;DR: MetaSieve是一个元路径选择层，通过在关系数据库中利用SQL连接和聚合操作计算统计量，剪枝无信息量的元路径，从而显著减少关系深度学习（RDL）中GNN训练的子图大小，在不损失甚至提升准确率的前提下大幅降低训练时间。


<details>
  <summary>Details</summary>
Motivation: 关系深度学习（RDL）通过将多表关系数据库建模为图来训练GNN，但训练需要对每个种子节点采样子图，子图大小直接决定训练成本。作者观察到许多由外键链接构成的元路径可以被剪枝而不影响精度，因此希望通过剪枝元路径来减小子图规模，降低训练开销。

Method: 提出了MetaSieve，一个元路径选择层。它利用SQL连接和聚合查询为每个候选元路径扩展计算统计量，并基于一个新的评分函数评估扩展，优先选择轻量但信息量大的元路径。得分低于阈值的元路径被判定为无信息量并剪枝。该选择过程仅依赖数据库统计量和任务标签，与GNN参数无关，因此可集成到多种GNN架构中。

Result: 在RelBench基准测试上，使用多种GNN骨干网络评估表明，MetaSieve一致性地大幅减少每个epoch的训练时间，同时保持甚至常常提升准确率。

Conclusion: MetaSieve证明了通过关系数据库系统的连接和聚合能力进行轻量级元路径选择，可以有效地在保持甚至提升模型精度的同时显著降低RDL训练成本，且具有与GNN架构无关的良好通用性。

Abstract: Relational Deep Learning (RDL) is an effective approach to machine learning over multi-table relational databases. In RDL, a database is modeled as a graph in which each row is a node and each foreign-key relation is an edge, and a graph neural network (GNN) is trained on this graph. Training a GNN requires sampling a subgraph around every seed node in the training set, and the cost of training is largely determined by the size of these subgraphs. This paper aims to reduce subgraph size by leveraging the join and aggregation capabilities of relational database systems. We observe that sampled subgraphs are obtained by following metapaths composed of foreign-key links, and that many of these metapaths can be pruned without loss of accuracy. We present MetaSieve, a metapath selection layer that determines which metapaths to retain and which to prune. For each candidate metapath extension, MetaSieve computes statistics via SQL join and aggregation queries and evaluates the extension based on a novel scoring function that prefers lightweight but informative candidates. Metapaths whose scores fall below a threshold are deemed uninformative and pruned. Metapath selection in MetaSieve is lightweight since it relies only on database statistics and task labels, and it is independent of GNN parameters, so it integrates with diverse GNN architectures for classification and regression. Our evaluation on the RelBench benchmark with multiple GNN backbones shows that MetaSieve consistently reduces per-epoch training time by large margins while maintaining and often improving accuracy.

</details>
