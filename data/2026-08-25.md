<div id=toc></div>

# Table of Contents

- [cs.MM](#cs.MM) [Total: 5]
- [cs.IR](#cs.IR) [Total: 26]
- [cs.DB](#cs.DB) [Total: 9]
- [cs.DS](#cs.DS) [Total: 13]
- [cs.GT](#cs.GT) [Total: 6]
- [cs.IT](#cs.IT) [Total: 14]


<div id='cs.MM'></div>

# cs.MM [[Back]](#toc)

### [1] [ReART: Reference-Guided Retrieval and Refinement for Emotion-Aware Art Generation](https://arxiv.org/abs/2608.22329)
*Qianqian Tang,Jiayi Gao,Ting Lei,Yang Liu*

Main category: cs.MM

TL;DR: ReART是一个参考引导的检索与精炼框架，用于情感感知的艺术图像生成。它通过将文本描述和图像注释分解为结构化视觉字段，在主体、布局、笔触和色调情感四个维度上进行逐字段检索，并利用属性对齐分数(AAS)驱动的精炼循环进行受控编辑修复，在AffectiveArt 2026大挑战赛Track 1中排名第二，AAS达到完美的1.00，总分0.78。


<details>
  <summary>Details</summary>
Motivation: 情感感知的艺术图像生成需要同时满足语义内容、艺术风格和目标情感，但艺术性标题将这三者混合在未充分指定的自由文本中，使得笔触、构图、色调氛围等细粒度视觉属性难以具体落地，导致模型无法准确对齐这些关键视觉维度。

Method: ReART框架包含两个核心阶段：(1)将测试标题和EmoArt数据库中每个图像注释分解为结构化视觉字段，并在主体、布局、笔触线条、色调情感四个维度上进行逐字段检索，获取角色特定的视觉参考，配合结构化提示进行初始合成；(2)对于任何AAS轴低于阈值的样本，启动AAS驱动的精炼循环，诊断失败原因，构建包含保留元素、修复错误、避免操作等约束的修复计划，按修正目的路由参考，并在结构保持约束下进行受控编辑。

Result: 该系统在AffectiveArt 2026大挑战赛Track 1中排名第二，实现了完美的AAS（属性对齐分数）1.00，以及0.78的综合得分。代码已开源。

Conclusion: ReART证明了通过结构化字段分解、逐字段视觉参考检索和AAS驱动的迭代精炼，可以有效解决艺术图像生成中语义、风格和情感多维度对齐的难题，并展示了诊断式修复在生成质量提升中的关键作用。

Abstract: Emotion-aware artistic image generation requires a model to satisfy semantic content, artistic style, and target emotion simultaneously. The key challenge is that artistic captions conflate these axes into underspecified free-form text, making fine-grained visual attributes such as brushwork, composition, and tonal atmosphere difficult to ground concretely. We present ReART, a reference-guided retrieval and refinement framework. Our method decomposes test captions and each image annotation in the EmoArt database into structured visual fields, and performs field-wise retrieval over subject, layout, brush-line, and tone-mood dimensions to retrieve role-specific visual references that supply the perceptual detail text alone cannot convey; these references are used alongside a structured prompt for initial synthesis. For samples where any Attribute Alignment Score (AAS) axis falls below threshold, an AAS-driven refinement loop diagnoses failures, constructs constrained repair plans specifying elements to keep, errors to fix, and operations to avoid, routes references by correction purpose, and performs controlled editing under structural preservation constraints. Our system ranks 2nd in Track 1 of the AffectiveArt 2026 Grand Challenge, achieving a perfect AAS of 1.00 and an overall score of 0.78. Code is available at https://github.com/oceanflowlab/ReART.git.

</details>


### [2] [Motion-Aware Reasoning from Speech to Mask Tracks: Runner-up Solution for the MeViS-Audio Track of the 8th LSVOS Challenge 2026](https://arxiv.org/abs/2608.22337)
*Jinxing Zhou,Suiyi Zhao,Yanghao Zhou,Ruohao Guo*

Main category: cs.MM

TL;DR: Speech2MaskTrack 是一种针对语音引导的指称视频目标分割方法，在 MeViS-Audio 赛道上获得第二名。它融合语音转写、结构化约束编译、SAM3.1 多候选实例追踪、TRACE 排序、词法存在门控以及 GPT 辅助恢复等模块，以应对语音指令中运动描述的目标分割与无目标处理。


<details>
  <summary>Details</summary>
Motivation: 语音引导的指称视频目标分割任务中，语音承载的是语言指令而非声学线索，因此需要打通语音识别、运动中心的时间定位、掩码追踪以及显式的无目标处理等环节，这是一个多模块协同的复杂挑战。

Method: Speech2MaskTrack 首先将语音查询转写并编译为包含类别、数量、方向、交互角色和时间阶段的结构化约束。SAM3.1 枚举多个实例轨迹，TRACE 基于完整轨迹的运动与关系证据进行排序。一个冻结的词法存在门控可抑制 SAM3.1 基础预测；当门控判定目标存在时，使用全表达式条件化的 SaSaSa2VA 轨迹替换 SAM3.1 掩码。仅当输出为空时才进入 GPT 辅助恢复，该过程在查询级和掩码级验证下再次调用 SaSaSa2VA。

Result: 该方案在 8th LSVOS Challenge 的 MeViS-Audio 赛道官方排名中获得第二名。

Conclusion: Speech2MaskTrack 通过多模块流水线设计与分层决策（基础预测、门控替换、GPT 恢复），有效解决了语音指称视频目标分割中的复杂约束理解和无目标处理问题，并在竞赛中取得了优异成绩。

Abstract: Speech-guided referring video object segmentation aims to recover the mask tracks of objects specified by a spoken motion description. Here, speech carries a linguistic instruction rather than acoustic evidence from a sounding object, so a solution must connect speech recognition, motion-centric temporal grounding, mask tracking, and explicit no-target handling. We introduce Speech2MaskTrack, our approach for the MeViS-Audio track of the 8th LSVOS Challenge. Speech2MaskTrack transcribes the spoken query and compiles it into structured constraints over category, count, direction, interaction role, and temporal phase. SAM3.1 enumerates multiple instance tracks, which TRACE ranks using complete-trajectory motion and relation evidence. A frozen lexical presence gate may suppress the ranked SAM3.1 base prediction. When the gate predicts that a target is present, an available full-expression-conditioned SaSaSa2VA track replaces the SAM3.1 mask. Only outputs that remain empty enter GPT-assisted recovery, which invokes SaSaSa2VA again under query- and mask-level verification. Speech2MaskTrack achieved second place in the official challenge ranking.

</details>


### [3] [Adaptive Hierarchical Representation Alliance for Multimodal Learning](https://arxiv.org/abs/2608.22863)
*Chunlei Meng,Pengbin Feng,Jacqueline J. Pang,Chih-Ting Liao,Rong Fu,Zhaolu Kang,Zhongxue Gan,Chun Ouyang*

Main category: cs.MM

TL;DR: 提出AHRA框架，通过层次化共享-私有专家结构解决多模态模型中语义粒度不匹配问题，在六个基准上超越强基线并保持鲁棒性。


<details>
  <summary>Details</summary>
Motivation: 现有多模态模型通常在最后一层潜在空间对齐各模态，隐式假设任务相关证据在各模态中出现在相同语义深度。但通过CKA分析发现存在语义粒度不匹配：文本线索通常需要更深的上下文抽象，而视觉和声学线索往往在浅层或中层就能提供判别性感知证据。这种不匹配会压平细粒度的模态私有线索，并在噪声、不平衡或缺失输入下降低可靠性。

Method: 提出自适应层次化表征联盟（AHRA），包含：(1) 将每个模态分解为跨语义层次的共享和私有流；(2) 用共享对齐和私有去相关进行正则化；(3) 通过跨模态专家路由共享信息；(4) 用稀疏控制的软门控机制（前景考试）增强任务相关私有token；(5) 层次化共融合模块进行层内专家协调和层间语义选择。

Result: 在图像-文本分类、多模态意图识别和三模态情感分析等六个基准上，AHRA一致优于强基线，并在噪声和缺失模态设置下保持稳健。

Conclusion: 通过显式建模模态间语义粒度的差异，AHRA能够保留细粒度的模态私有线索，提升多模态模型在复杂、不完整输入下的可靠性。

Abstract: Multimodal models often align language, vision, and audio in a single final-layer latent space, implicitly assuming that task-relevant evidence emerges at the same semantic depth across modalities. Using layer-wise CKA analysis, we observe that this assumption leads to semantic granularity mismatch: textual cues usually require deeper contextual abstraction, whereas visual and acoustic cues often provide discriminative perceptual evidence in shallow or middle layers. This mismatch can flatten fine-grained modality-private cues and reduce reliability under noisy, imbalanced, or missing inputs. To address this, we proposed Adaptive Hierarchical Representation Alliance (AHRA), a hierarchical shared--private expert framework. AHRA factorizes each modality into shared and private streams across semantic levels, regularizes them with shared alignment and private decorrelation, routes shared information through a cross-modal expert, and enhances task-relevant private tokens with modality-specific experts guided by a sparsity-controlled soft-gating mechanism (foreground exam). A hierarchical co-fusion module then performs intra-level expert coordination and inter-level semantic selection. Experiments on six benchmarks across image-text classification, multimodal intent recognition, and trimodal sentiment analysis show that AHRA consistently improves over strong baselines and remains robust under noisy and missing-modality settings.

</details>


### [4] [SACHA: Semantic-Aware Compression for 3D Gaussian Head Avatars](https://arxiv.org/abs/2608.23133)
*Zihan Zhang,Shanzhi Yin,Xinju Wu,Bolin Chen,Ru-Ling Liao,Jie Chen,Shiqi Wang,Yan Ye*

Main category: cs.MM

TL;DR: SACHA是一个动态头部虚拟形象压缩框架，结合语义感知密度控制与外观-运动分解，实现紧凑表示和高质量新视角渲染，在率失真性能上优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 现有可动画3D高斯头部虚拟形象虽然提供高保真渲染，但需要大量存储和传输成本；已有方法忽略了不同头部语义区域的视觉显著性差异，且缺乏对训练后头像序列的高效压缩。

Method: 1) 语义感知密度控制：通过区域自适应密化与剪枝，在不同头部区域间引导高斯图元的自适应分配；2) 外观-运动分解压缩：仅传输头部先验参数表示运动，减少序列的时间冗余。

Result: SACHA在率失真性能上优于现有高斯头部虚拟形象表示和压缩方法，同时保持高质量的新视角和新表情渲染。

Conclusion: 语义感知的图元分配与外观-运动分解的结合，能够在紧凑表示与视觉保真之间取得良好平衡，为动态高斯头部虚拟形象的高效传输提供了有效方案。

Abstract: Animatable 3D Gaussian head avatars offer high-fidelity and flexible facial rendering, but typically require substantial storage and transmission costs for numerous Gaussian primitives. Existing Gaussian head avatar methods overlook the visual saliency of different head semantic regions for more appropriate Gaussian primitive allocation, as well as the efficient compression of trained head avatar sequences. To tackle this obstacle, we propose SACHA, a dynamic head avatar compression framework that leverages both semantic-aware density control and appearance-motion decomposition to achieve compact representation and high-quality novel-view rendering of head avatar sequences. Specifically, the semantic-aware density control guides the adaptive allocation of Gaussian primitives across different head regions with region-adaptive densification and pruning. In addition, the appearance-motion decomposed compression further reduces the temporal redundancy of the avatar sequence by transmitting only head-prior parameters for avatar movements. Together, these designs enable a compact representation for efficient transmission of dynamic Gaussian head avatars while preserving visual fidelity. Experiments demonstrate that SACHA achieves a superior rate-distortion performance over existing Gaussian head avatar representation and compression methods while maintaining high-quality novel-view and novel-expression rendering.

</details>


### [5] [Mind the Couch! Eliciting MLLM Reasoning in Interior Design via Weak-to-Strong Task Vector Injection](https://arxiv.org/abs/2608.23242)
*Yuxuan Yang,Jingyao Wang,Luntian Mou*

Main category: cs.MM

TL;DR: 本文提出DART-I机制，通过将弱专家的确定性先验（空间距离与色彩特征）转化为方向性任务向量，以残差方式注入冻结的MLLM潜在空间，在不微调的情况下解决室内设计中多模态大模型的空间碰撞与美学失调问题。


<details>
  <summary>Details</summary>
Motivation: 现有MLLM在室内设计等重度约束空间中常因视觉编码丢失高频局部拓扑细节和细粒度美学变化而产生幻觉，导致物理空间碰撞和视觉美学不协调；且传统文本提示方法存在信息损失。

Method: DART-I采用三步流程：(1) 使用轻量级弱专家显式提取连续空间距离与色彩排版特征；(2) 通过线性投影网络将确定性先验转化为方向性任务向量；(3) 将这些向量作为残差项动态注入冻结的MLLM潜空间，引导其进行精确推理。核心范式从有损文本提示转向直接潜在干预，利用弱到强的确定性规则锚定因果推理。

Result: 在多个基准上的大量实验证明了DART-I的有效性和优势，无需微调MLLM即可实现精确推理，避免了昂贵的计算成本和灾难性遗忘。

Conclusion: DART-I跳出传统范式，通过残差任务向量注入的方式实现了对冻结MLLM的精准引导，为室内设计等专业受限场景下的多模态推理提供了高效且有效的解决方案。

Abstract: Multimodal Large Language Models (MLLMs) have demonstrated great performance, yet they often suffer from severe modality misalignment when confronted with densely constrained spaces for interior design. Due to the loss of high-frequency local topological details and fine-grained aesthetic shifts during visual encoding, existing MLLMs frequently hallucinate, yielding physical spatial collisions and visual aesthetic dissonance. To address this, we propose Dual-prior Activation Residual Task-vectors Injection mechanism (DART-I) for MLLMs. It shifts the paradigm from lossy text-prompting to direct latent intervention, utilizing weak-to-strong deterministic rules to anchor the causal reasoning of MLLMs for interior design. Specifically, DART-I operates in three steps: it first explicitly extracts continuous spatial distance and color typography features from images using extremely lightweight weak experts; subsequently, it transforms these deterministic priors into directional task vectors via a linear projection network; these vectors are dynamically injected as residual terms into the latent space of the frozen MLLMs, steering MLLMs towards precise reasoning for interior design. Stepping outside the conventional paradigms, our method achieves precise reasoning without fine-tuning the MLLMs, effectively bypassing expensive computational costs and catastrophic forgetting. Extensive experiments on various benchmarks demonstrate the effectiveness and advantages of DART-I.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [6] [Retrieval Needs Multivectors: An Exponential Separation](https://arxiv.org/abs/2608.21494)
*Mihir Agarwal,Viraj Agrawal,Sabyasachi Basu,Ankit Garg,Kirankumar Shiragur*

Main category: cs.IR

TL;DR: 本文首次证明了单向量嵌入与多向量嵌入在文档排序任务上的指数级表达能力差距，并基于理论构造提出了新的检索基准ANDOR，实验验证了该差距的真实存在。


<details>
  <summary>Details</summary>
Motivation: 尽管已有工作通过理论和基准（如LIMIT）指出了基于嵌入的检索模型在表达能力上的局限，且多向量嵌入通常优于单向量嵌入，但两者之间精确的表达能力差距尚未被充分理解。

Method: 1) 理论部分：在Jayaram工作的基础上，首次构造了一族显式的查询和文档集合及其相关性矩阵，证明单向量嵌入若要正确排序所有相关文档需要指数级大小，而多项式规模的多向量嵌入即可胜任，从而建立两者在文档排序任务上的指数级分离。2) 实验部分：基于理论构造提出ANDOR基准，测试了零样本和微调设置下SOTA单向量与多向量模型的表现。

Result: SOTA单向量嵌入模型在ANDOR零样本设置下表现不佳，微调后仅有小幅提升；多向量模型则一致优于单向量模型，且微调后显著提升，与理论预测高度一致。

Conclusion: 单向量与多向量嵌入在文档排序表达能力上存在根本性的指数级差距，ANDOR基准能够有效揭示该差距，为未来检索模型的设计提供了理论指导和评估工具。

Abstract: Recent works have highlighted the expressive limitations of embedding based retrieval models through both theoretical analyses and challenging benchmarks such as LIMIT. While multi-vector embeddings consistently outperform single-vector embeddings, the precise representational gap between them remains poorly understood. In this work, following Jayaram's work, we provide the first explicit family of query and document sets, together with their relevance matrices, for which single-vector embeddings that rank all relevant documents above irrelevant ones require exponential size, whereas polynomial-size multi-vector embeddings suffice. Our result establishes an exponential separation between the expressive power of single-vector and multi-vector embeddings for the task of ranking of documents as opposed to approximating numerical scores as in the work of Jayaram.
  Motivated by our theoretical construction, we introduce ANDOR, a new retrieval benchmark that naturally instantiates these hard examples. We show that state-of-the-art single-vector embedding models perform poorly on ANDOR in the zero-shot setting and exhibit only marginal improvements after fine-tuning, highlighting the inherent difficulty of the benchmark compared to prior work. In contrast, multi-vector models consistently outperform their single-vector counterparts and improve substantially with fine-tuning, closely aligning with our theoretical predictions.

</details>


### [7] [Why didn't more people see it? Recommendation: Transparency for providers](https://arxiv.org/abs/2608.21641)
*Meysam Varasteh,Robin Burke*

Main category: cs.IR

TL;DR: 本文提出一种代理建模方法，从物品提供者（内容创作者）的视角解释推荐系统中物品曝光率的全局影响因素，而非传统的用户侧解释。通过训练代理模型逼近推荐系统的曝光分布，并量化各特征对曝光决策的贡献，在两个数据集和三种推荐模型上验证了该方法的高保真度和可解释性。


<details>
  <summary>Details</summary>
Motivation: 推荐系统的透明度研究大多聚焦于接收推荐的用户视角，但物品提供者（内容创作者）对自身内容如何获得或未获得曝光缺乏了解。这一视角的缺失是现有研究的空白，需要从系统层面解释物品曝光的影响因素。

Method: 采用代理建模（surrogate modeling）方法：训练一个代理模型来近似推荐系统产生的曝光分布，取代对单个用户-物品对的解释，通过量化每个特征的贡献度，解释推荐模型在整个用户群体上的决策驱动力。

Result: 在两个数据集和三种推荐模型上的实验表明，代理模型能以高保真度捕捉所有三种推荐系统的全局行为，且各模型中影响力最大的因素在不同模型和域之间存在有意义的差异。

Conclusion: 提出的代理建模方法成功填补了物品提供者视角的透明度研究空白，能够以高保真度解释推荐系统在系统层面上的曝光机制，为内容创作者提供了理解平台推荐逻辑的新工具。

Abstract: Transparency in recommender systems has been widely studied from the perspective of those receiving recommendations, yet the needs of item providers, the creators whose content is distributed through these platforms, remain largely unexplored. Providers often lack insight into how their items do or do not receive exposure in users' recommendation lists. In this work, we address this gap by proposing a surrogate modeling approach to explain item exposure at a system level. Rather than explaining individual user-item pairs, we train a proxy model to approximate the exposure distribution produced by a recommender. By quantifying the contribution of each feature, we seek to explain the factors driving the recommendation model's decisions across the entire user base. We evaluate our approach on two datasets and three recommendation models. Results show that the surrogate model captures the global behavior of all three recommenders with high fidelity and that the most influential factors vary meaningfully across models and domains.

</details>


### [8] [Why This, Not That? Mining User Profiles for Pair-wise Counterfactuals](https://arxiv.org/abs/2608.21662)
*Meysam Varasteh,Veronika Bogina,Noam Koenigstein,Robin Burke*

Main category: cs.IR

TL;DR: 本文提出推荐系统中的一个新任务——物品排名的成对解释（pairwise interpretation of item rankings），回答"为什么物品A排名高于物品B"这一比较性问题。作者基于人际沟通心理学的研究发现，提出应使用反事实学习（counterfactual learning）技术来识别用户画像中影响物品相对排名的关键物品，并通过多个数据集验证了该方法的有效性。


<details>
  <summary>Details</summary>
Motivation: 现有推荐系统解释工作大多聚焦于对单个物品的解释，且近期方法往往与推荐算法本身的逻辑相脱节。作者基于人际沟通心理学的研究发现，提出成对比较解释这一新任务，并认为有效的解决方案必须扎根于推荐算法的实际运作机制。

Method: 提出一类基于反事实学习（counterfactual learning）的技术，通过分析用户画像中哪些物品对两个物品的相对排名产生了贡献，来识别可作为比较性解释基础的候选物品。

Result: 在多个数据集上验证了该方法能够有效识别出用户画像中与物品相对排名相关的关键物品，可作为比较性解释的潜在基础。

Conclusion: 成对排名解释是推荐系统解释领域一个有前景的研究方向，反事实学习方法能够有效揭示影响物品相对排名的用户画像因素，为生成更有意义的比较性解释奠定基础。

Abstract: The topic of explanation in recommender systems has seen steady research attention since the earliest days of the field. With some exceptions, this work has focused on the explanation of single items in a recommendation list and, especially recently, has emphasized approaches that are decoupled from the logic of the recommendation algorithm itself. Based on findings in the psychology of interpersonal communication, we propose a new task, pairwise interpretation of item rankings, asking the comparative question ``Why is item A ranked higher than item B?''. An effective solution to this task, we argue, is inherently grounded in the operation of the recommendation algorithm. We propose a class of techniques based on counterfactual learning to uncover the items in a user's profile that have contributed to the relative ranking of items. Using multiple datasets, we show that it is possible to identify such items as potential basis for comparative explanation.

</details>


### [9] [Revisiting N2DCG: An Empirically Grounded Reformulation of Carousel Recommendation Evaluation](https://arxiv.org/abs/2608.21877)
*Jingwei Kang,Santiago de Leon-Martinez,Maarten de Rijke,Harrie Oosterhuis*

Main category: cs.IR

TL;DR: 本文指出现有N2DCG指标在评估旋转木马式（carousel）推荐系统时存在两个关键局限：理想排序违反布局约束、折扣函数不符合真实用户浏览行为。作者提出改进版N2DCG，通过尊重约束的归一化和基于实证数据的折扣函数进行重构，并在真实眼动数据和模拟实验上验证了其有效性。


<details>
  <summary>Details</summary>
Motivation: 旋转木马式界面广泛用于视频和音乐流媒体服务，但缺乏合适的评估指标。N2DCG虽然被提出用于此场景，但其从单列表网页搜索场景借用的假设在二维旋转木马布局中并不成立，导致评估结果不准确。

Method: 作者识别出N2DCG的两个关键缺陷：(1) 用于归一化的理想排序违反了旋转木马布局的约束条件；(2) 折扣函数与经验观察到的用户浏览行为不符。针对这两点，提出重构的N2DCG，采用尊重约束的归一化方法，并使用基于实证数据的折扣函数。

Result: 在真实眼动数据上，改进后的指标更好地反映了用户的实证行为；在基于经验检查模式模拟的旋转木马布局比较实验中，新指标也能更准确地预测比较结果。

Conclusion: 新的N2DCG重构版本纠正了原有指标的理论缺陷，提供了更符合用户在旋转木马布局中实际浏览行为的评估方式，为该场景下的推荐系统评估提供了更可靠的指标。

Abstract: Carousel interfaces have been widely used in video and music streaming services, yet it remains unclear how to properly evaluate recommender systems in these two-dimensional layouts. N2DCG has been proposed to address this gap by adapting NDCG to carousel-based recommendation, but it relies on unverified assumptions borrowed from the single-list web-search setting that do not transfer well to two-dimensional carousel layouts.
  We identify two substantial limitations of N2DCG: its ideal ranking, used for normalization, violates carousel constraints, and its discount function does not reflect user browsing behavior observed in empirical data. To address both limitations, we propose a reformulation of N2DCG that normalizes appropriately by respecting constraints and uses an empirically grounded discount function. We validate the proposed metric, showing that it better reflects users' empirical behavior on real-world eye-tracking data and better predicts the comparison results of carousel layouts simulated based on empirical examination patterns.

</details>


### [10] [Enhancing Group Recommendation with Memory-Augmented Reasoning in LLM Agent](https://arxiv.org/abs/2608.21939)
*Qimeng Niu,Bowen Hao,Zixuan Zhang,Shuyu Qu,Hongzhi Yin*

Main category: cs.IR

TL;DR: 本文提出AGR，一个基于LLM的推荐智能体，通过记忆模块（基于token哈希表动态管理群组/用户历史交互）和推理模块（多步推理：群组兴趣收集、共识精炼、多维评估和可解释推荐生成）来解决群组推荐中的偏好动态演化和共识形成过程建模问题。采用SFT+GRPO的RFT范式训练，在LastFM和Douban数据集上超越现有最先进方法。


<details>
  <summary>Details</summary>
Motivation: 现有LLM-based群组推荐方法将交互历史视为固定文本，忽略了群组/用户偏好随时间的自然演化，且缺乏对复杂群组决策过程的显式建模。

Method: AGR包含两个核心模块：(1) Memory Module：采用基于token的哈希表动态管理群组和用户的交互历史，支持插入、更新、检索、遗忘和摘要操作；(2) Reasoning Module：执行群组兴趣收集、群组共识精炼、多维评估和可解释推荐生成的多步推理。训练采用RFT范式，先用SFT赋予模型调用模块的基本能力，再用GRPO增强其自主协调模块的能力。

Result: 在LastFM和Douban数据集上的实验表明，AGR在推荐准确性和可解释性方面均显著优于现有的最先进方法。

Conclusion: AGR通过动态记忆管理和显式的多步推理，成功超越了传统黑盒方法，实现了完全可解释的群组推荐，模型已开源。

Abstract: The core challenge in group recommendation lies in modeling the dynamic evolution of user preferences and explain?ing the consensus formation process. Existing Large Language Model (LLM)-based methods, despite improved interpretability, treat interaction history as fixed text, ignoring the natural evolution of group/user preferences over time, and lacking explicit modeling of the complex group decision-making process. To address these issues, we propose AGR, a LLM-based agent, which consists of a Memory Module and a Reasoning Module. The Memory Module employs a token-based hash table to dynamically manage the historical interactions of groups and users. This design supports fundamental operations including insertion, updating, retrieval, forgetting of irrelevant records, and summarization of evolving group and user profiles for efficiently tracking. Based on these retrieved dynamic profiles, the Reason?ing Module then performs a multi-step reasoning process includ?ing Group Interests Collection, Group Consensus Refinement, Multi-dimensional Evaluation and Explainable Recommendation Generation, thereby moving beyond black-box inference to de?liver fully interpretable recommendations. In practice, we adopt the Reinforcement Fine-Tuning (RFT) paradigm, where we first use Supervised Fine-Tuning (SFT) to equip the model with basic capabilities for invoking the Memory and Reasoning modules, and then employ Group Relative Policy Optimization (GRPO) to enhance its autonomous ability to coordinate these modules. Experiments on LastFM and Douban datasets demonstrate that AGR significantly outperforms existing state-of-the-art methods in both recommendation accuracy and explainability. Our model is open-sourced at https://huggingface.co/niuqimeng/AGR.

</details>


### [11] [FashionKG-RAG: Knowledge Graph-Enhanced Retrieval-Augmented Generation for Fashion Question Answering](https://arxiv.org/abs/2608.22688)
*Yujuan Ding,Linyin Luo,Shijie Wang,Xu Yuan,Yunshan Ma,Yi Bin,Wenqi Fan,Qing Li*

Main category: cs.IR

TL;DR: 本文提出FashionEcoKG——一个覆盖时尚领域全生态、由专家级精度构建的知识图谱，通过三阶段智能体流水线从权威教材中提取知识并增强结构连接；同时提出PG-RAG（Pruning-Grounding RAG）框架，通过双粒度路径重排序模块（PSR剪枝语义排序+GAR接地智能体排序）处理时尚查询的概念密集性和语言噪声，在时尚QA数据集上显著优于非RAG和现有KG-RAG基线。


<details>
  <summary>Details</summary>
Motivation: 时尚是知识密集型领域，决策依赖多类型知识整合。尽管LLM在众多领域取得突破，但在时尚领域仍受幻觉和领域专业化不足的限制。现有的时尚知识图谱局限于产品级属性或商品关系，无法捕捉更广泛的时尚生态系统，难以有效支撑LLM的检索增强生成。

Method: 1) 构建FashionEcoKG：通过三阶段智能体流水线，从权威教材中提取高保真知识核心，并通过跨域增强和生成式扩展强化结构连通性，形成领域全覆盖的知识图谱。2) 提出PG-RAG框架：包含Dual-Granularity Path Re-Ranking (DGPR)模块，由两个阶段组成——Pruning-based Semantic Ranking (PSR)将查询提炼为骨架形式提升检索召回率，Grounding-based Agentic Ranking (GAR)对候选路径对照原始完整查询进行逐点审查以确保全局相关性。

Result: 在构建的时尚QA数据集上，PG-RAG有效利用FashionEcoKG提升了检索和答案准确率，在多个指标上优于非RAG方法和现有的KG-RAG基线方法。

Conclusion: 本文证明了领域级知识图谱结合双粒度RAG框架能够有效缓解LLM在时尚领域的幻觉问题，提升领域专业化能力。FashionEcoKG提供了全面的时尚知识基础设施，PG-RAG则提供了一种无需训练的、可处理专业查询噪声的检索增强方案，为时尚领域的知识驱动决策提供了可行路径。

Abstract: Fashion is a knowledge-intensive domain in which effective decision-making depends on integrating multiple types of knowledge. Although Large Language Models (LLMs) have transformed many areas, their application in fashion remains limited by hallucinations and weak domain specialization. Knowledge Graph (KG)-based Retrieval-Augmented Generation (RAG) offers a promising way to add structured knowledge to LLMs. However, existing fashion KGs are typically restricted to product-level attributes or item relations, and fail to capture the broader fashion ecosystem. To bridge these gaps, we propose \textbf{FashionEcoKG}, a comprehensive, domain-wide knowledge graph built with expert-level precision and professionalism. It is constructed through a three-stage agentic pipeline that extracts high-fidelity knowledge cores from authoritative textbooks and strengthens structural connectivity through cross-domain augmentation and generative expansion. To leverage this resource, we further develop \textbf{PG-RAG} (Pruning-Grounding RAG), a training-free framework designed to handle the conceptual density and linguistic noise of fashion queries. Specifically, we introduce a Dual-Granularity Path Re-Ranking (DGPR) module of two stages. The Pruning-based Semantic Ranking (PSR) module distills each query into a skeleton form to improve retrieval recall, while the Grounding-based Agentic Ranking (GAR) performs point-wise scrutiny of candidate paths against the original full query to ensure global relevance. Experiments on a curated fashion QA dataset show that PG-RAG effectively leverages FashionEcoKG to improve retrieval and answer accuracy, outperforming both non-RAG and existing KG-RAG baselines.

</details>


### [12] [From Click Modeling to Offline and Off-Policy Evaluation in Carousel Recommendation](https://arxiv.org/abs/2608.22022)
*Jingwei Kang*

Main category: cs.IR

TL;DR: 这篇博士论文研究围绕轮播式（carousel）推荐界面的点击建模与评估问题。由于轮播界面将多个排序列表以二维方式呈现，用户行为受排名、行组织、视口限制和物品上下文共同影响，使得点击反馈的解释变得复杂。作者提出重新思考轮播点击的建模方式，并开发从日志交互数据中评估轮播推荐策略的方法，涵盖点击模型设计框架、离散选择模型、轮播特定离线指标以及离策略评估方法。


<details>
  <summary>Details</summary>
Motivation: 轮播界面在现代推荐系统中广泛使用，但与传统的单一排序列表不同，轮播同时呈现多个可横向滑动的排序行。这种排名与二维布局的紧密耦合使得用户行为不仅受物品偏好影响，还受行组织、视口约束和物品上下文影响，导致用户反馈难以解读，给推荐评估带来新挑战。现有方法未能充分解决这一问题。

Method: 作者的研究分多个阶段推进：(1) 研究用户在轮播界面中的交互行为；(2) 开发点击模型设计框架，优先考虑观测变量之间的数学关系而非潜在行为假设；(3) 使用离散选择模型将点击表示为选择；(4) 开发轮播特定的离线评估指标；(5) 计划开发离策略评估方法，从日志交互数据中估计推荐策略性能。

Result: 目前已完成的成果包括对用户如何与轮播界面交互的行为研究，以及一个强调观测变量间数学关系的点击模型设计框架。正在进行的工作包括基于离散选择模型的点击表示和轮播特定离线指标。未来计划是开发离策略评估方法。

Conclusion: 该论文的预期贡献是建立一个连接轮播点击建模与离线和离策略评估的完整研究体系，从而更可靠地改进轮播推荐策略的性能。

Abstract: Carousel interfaces are widely used in modern recommendation systems. Unlike traditional interfaces that present a single ranked list, carousels simultaneously present several ranked lists to the user, as horizontally swipeable rows stacked on top of each other. In this design, the rankings are closely tied to the two-dimensional layout. Consequently, user behavior is shaped not only by item preference, but also by row organization, viewport constraints, and item context. This tight coupling between ranking and presentation complicates the interpretation of user feedback, introducing new challenges for recommendation evaluation.
  My PhD research aims to address these challenges by rethinking how carousel clicks are modeled and how carousel recommendation policies can be evaluated from logged interaction data. So far, I have studied how users interact with carousel interfaces and developed a click model design framework that prioritizes mathematical relationships between observed variables over latent behavioral assumptions. Building on these results, my ongoing work includes a project using discrete choice models to represent clicks as choices, alongside a project that develops carousel-specific offline metrics. As a next step, I plan to develop off-policy evaluation methods that estimate the performance of recommendation policies from logged interactions. Taken together, the expected contribution of my thesis is a connected body of work that links carousel click modeling with offline and off-policy evaluation, so that carousel recommendation policies can be improved more reliably.

</details>


### [13] [SARCLIP: A Scalable CLIP-Based Retrieval System for Seventeenth-Century Spanish American Notary Records](https://arxiv.org/abs/2608.22036)
*Chandrasekhar Syamala,Parshad Suthar,Hulayyil Alshammari,Viviana Grieco,Praveen Rao*

Main category: cs.IR

TL;DR: SARCLIP是一个部署于阿根廷国家档案馆的检索系统，基于CLIP ViT-B/16模型进行对比微调，实现了对17世纪西班牙美洲公证文书中超过1360万个词图像块的大规模近似最近邻检索。系统通过FAISS索引扩展检索规模，利用Rocchio伪相关反馈优化结果，并通过人机协同的标注与重训练闭环持续改进。


<details>
  <summary>Details</summary>
Motivation: 历史手稿档案因字迹不一致、古体拼写和缺乏大规模可靠转录，难以应用标准文本搜索技术。现有系统仅在小规模子集上验证，无法满足实际档案馆完整语料的检索需求。

Method: 基于CLIP ViT-B/16模型，使用古文字学专家标注数据做对比微调；采用FAISS索引实现大规模近似最近邻检索；通过Rocchio伪相关反馈优化top-k结果；搭建可视化浏览、画布式补丁标注与周期性模型重训练的人机闭环。

Result: 系统从仅覆盖5个卷轴的研究原型扩展为覆盖超过100个卷轴、1360万+词图像块的近完整语料的交互式部署工具，支持搜索、浏览、标注和重训练的完整工作流。

Conclusion: SARCLIP证明基于CLIP的多模态检索范式可有效支撑大规模历史手稿档案的实际应用场景，并通过人机协同反馈机制实现可持续的系统改进。

Abstract: Historical manuscript archives resist standard text search due to inconsistent handwriting, archaic orthography, and the absence of reliable transcriptions at scale. We present SARCLIP (Spanish American Notary Records Meets CLIP), a deployed retrieval system for the National Archives of Argentina's seventeenth-century Spanish American notary records, a corpus of more than 13.6 million word-image patches spanning over 100 microfilm rolls ("rollos"). SARCLIP is built on a CLIP ViT-B/16 model contrastively fine-tuned on paleography-expert-annotated data, and extends prior work by (1) scaling approximate nearest-neighbor retrieval to the near-complete corpus via a FAISS index, (2) refining top-k results through pseudo-relevance feedback (Rocchio), and (3) closing a human-in-the-loop cycle through visual document browsing, canvas-based patch annotation, and periodic model retraining. Unlike the system's initial research prototype, which evaluated retrieval on a small five-rollo subset, SARCLIP is demonstrated as a complete, interactive tool operating over the near-complete corpus. Attendees experience the full search, browse, annotate, and retrain workflow live during this demonstration.

</details>


### [14] [GrOIL: Graph-Grounded Domain Ontology Induction with Constrained LLM Mediation](https://arxiv.org/abs/2608.22135)
*Maruf Ahmed Mridul,Abid Talukder,Oshani Seneviratne*

Main category: cs.IR

TL;DR: 本文提出了一种七阶段图接地(grounded)的流水线，将领域文档自动转换为完整的、可审计的OWL术语公理(TBox)，无需任何无约束生成步骤。该流水线将文档编码为统一话语超图(UDH)，通过图证据逐步构建类层次、属性和限制公理，LLM仅用于受限的图接地调解任务。在寿险领域评估中，该方法在能力问题(CQ)覆盖率上显著优于直接和Multi-agent LLM基线(0.85 vs 0.63/0.62)，并达到与人工构建参考本体相当的关键词覆盖率。


<details>
  <summary>Details</summary>
Motivation: 现有的自动本体构建系统无法同时满足语料接地、词汇一致性、公理级表达力和端到端溯源这四项要求。直接从领域文档构建正式本体面临生成不可控、缺乏可审计性等问题，需要一种结合图结构证据与受限LLM使用的新方法。

Method: 提出七阶段图接地流水线：(1) 文档编码为UDH捕获实体参与和话语依赖；(2-7) 后续阶段将此图证据转化为类层次、类型化对象/数据类型属性、限制公理。LLM仅用于狭窄的、图接地的调解任务。同时设计了配对的ABox填充程序和基于SPARQL的功能评估方法，每个输出术语都携带从原始段落开始的完整决策链。

Result: 在寿险领域(两个既有基准+新100份合同语料，覆盖10种产品类型)上，流水线在CQ覆盖率上优于直接和Multi-agent LLM基线(一份定期寿险合同上0.85 vs 0.63/0.62，另一份合同上0.77 vs 0.40/0.44)；关键词覆盖率与人工参考本体相当；结构化间隙-重叠推理表现良好。本体增长分析显示词汇在规模化时趋于饱和。

Conclusion: 图接地流水线无需人工TBox工程即可从大规模文档语料中产出稳定、可复用的领域表示，兼顾了可审计性和表达力，证明将LLM限制在严格图接地任务中可有效替代完全自由生成的本体构建方式。

Abstract: Constructing formal ontologies from domain documents requires simultaneously enforcing corpus grounding, vocabulary consistency, axiom-level expressivity, and end-to-end provenance, a combination no existing automatic system delivers. We present a seven-stage graph-grounded pipeline that converts domain documents into a complete, auditable Web Ontology Language (OWL) Terminological Box (TBox) without any unconstrained generation step. Documents are first encoded as Unified Discourse-Hypergraphs (UDH) capturing entity participation and discourse dependencies; subsequent stages transform this graph evidence into a class hierarchy, typed object and datatype properties, and restriction axioms, with Large Language Model (LLM) usage restricted to narrow, graph-grounded mediation tasks. A paired Assertional Box (ABox) population procedure grounds named individuals in the induced TBox, enabling SPARQL-based functional evaluation. Every emitted term carries a full decision chain from raw source passages through each pipeline stage, making the TBox directly auditable and suitable for targeted human refinement. Evaluated on the life insurance domain using two established benchmarks and a new 100-contract corpus spanning ten product types, our pipeline achieves strong results across all evaluation dimensions, outperforming direct and multi-agent LLM baselines on competency-question (CQ) coverage (0.85 vs. 0.63 and 0.62 on one term-life contract; 0.77 vs. 0.40 and 0.44 on another contract), while also attaining high keyphrase coverage comparable to a manually-constructed reference ontology and strong performance on structured gap-and-overlap reasoning, all without any manual TBox engineering. Ontology growth analysis provides evidence consistent with vocabulary saturation at scale, demonstrating that the pipeline produces stable, reusable domain representations from large document corpora.

</details>


### [15] [Prompt-Based Abstention Fails Under Misleading Context: A Controlled Study of Small Frozen RAG Models](https://arxiv.org/abs/2608.22228)
*Yohanes Andre Setiawan*

Main category: cs.IR

TL;DR: 本文提出GRAB-RAG基准，用于系统评估RAG系统在面对缺失证据与误导性证据时的弃权（abstention）行为差异，发现现有提示式弃权策略能处理缺失证据但难以应对误导性证据（41.6%仍会回答错误），并对比了生成端冲突检查与NLI验证器等策略的权衡。


<details>
  <summary>Details</summary>
Motivation: 现有RAG系统中的弃权机制将"缺失证据"和"误导性证据"视为同一问题对待，但实际中模型只在上下文缺失时弃权，面对误导性上下文时仍会自信作答。这种不对称性缺乏系统性的评估基准来量化。

Method: 构建GRAB-RAG配对基准，在Natural Questions和HotpotQA上对同一问题设置四种上下文条件（支持性、退化、缺失、误导性）。误导性条件通过编辑黄金段落使其支持错误答案并混入检索段落中。测试五种弃权策略在三个小规模冻结模型（3.8B-8B）上的表现，包括显式弃权提示、思维链、生成端冲突检查以及NLI验证器。

Result: 模型在缺失证据时可靠弃权，但显式弃权提示下仍有41.6%的误导性问题被回答，其中63%的回答逐字复述了植入的错误实体。思维链帮助有限。生成端冲突检查将错误率降至13.3%但牺牲了覆盖率；NLI验证器恢复覆盖率但在参数记忆与误导段落一致时失效。

Conclusion: 基于提示的弃权只检查上下文是否充分，而不检查上下文是否正确。现有的验证器都无法在不牺牲安全性与覆盖率平衡的前提下弥合这一差距，提示式弃权需要从"充分性检查"转向"正确性检查"。

Abstract: Missing and misleading evidence are not the same problem in retrieval-augmented generation (RAG), but prompt-based abstention treats them alike. Models abstain when context is absent, not when it is misleading. We introduce GRAB-RAG (Graded Abstention Benchmark for Retrieval-Augmented Generation), a paired benchmark that tests the same questions across four context conditions (supportive, degraded, missing, and misleading) in Natural Questions and HotpotQA. In the misleading condition, we edit a gold passage to support a wrong answer and place it among other retrieved passages. We test five abstention policies on three small frozen models (3.8B--8B) across two QA benchmarks. Models abstain reliably when evidence is missing, but under explicit abstention prompting still answer 41.6% of misleading questions, with 63% of those answers echoing the planted wrong entity verbatim. Chain-of-thought provides little additional benefit. A generator-side conflict check cuts the rate to 13.3% but discards many correct answers, while an NLI verifier recovers that coverage but fails when parametric memory and the misleading passage agree on the same wrong answer. Prompt-based abstention asks whether context is sufficient, not whether it is correct. Neither verifier closes this gap without trading coverage for safety.

</details>


### [16] [GRAFT: Graph-Distilled Generative Retrieval for Facet-Aware Scientific Literature Exploration](https://arxiv.org/abs/2608.22381)
*Italo Luis da Silva,Hanqi Yan,Yujing Wang,Jiangnan Ye,Lin Gui,Yulan He*

Main category: cs.IR

TL;DR: 本文提出Graft，一种生成式检索器，通过将论文按问题、方法、结果、贡献四个维度构建类型化图谱，并将其蒸馏为以论文自身的方面文本为标识符的生成式检索器。通过覆盖感知蒸馏和图加权倒数排名融合两大技术，解决了朴素蒸馏中语料覆盖不足和生成标识符支持性不足的问题，在LitWeave语料上达到图谱教师的91% Recall@20，且每个返回的论文都带有揭示它的方面标签。


<details>
  <summary>Details</summary>
Motivation: 现有文档级检索器将论文间的关系压缩为单一相似度分数，无法解释论文为何相关；基于引用和相似度的检索局限于已知邻域，而科学发现需要探索式检索；生成式检索虽能直接生成文档标识符支持探索，但现有方法未能利用论文多维关系图谱。

Method: 1) 构建方面类型化图谱：以问题、方法、结果、贡献四个维度为边类型连接论文。2) 覆盖感知蒸馏：通过反向邻居回退、最小覆盖阈值和边重要性加权使每篇论文可学习。3) 图加权倒数排名融合：用查询-候选边权重缩放排名，丢弃不支持的候选。4) 标识符为论文自身的方面文本。

Result: 在包含11,359篇NLP论文的LitWeave语料上，Graft恢复图谱教师91%的Recall@20，无需最近邻索引或编码器即可推理；在语料外查询论文上优于图谱教师；以0.922的精确率再现图谱的方面标签，每个返回的论文都带有揭示它的方面标签而非不透明分数。

Conclusion: Graft证明生成式检索器可以从类型化关系图谱中蒸馏，实现探索式检索的同时保持图谱的方面标签可解释性，克服了朴素蒸馏在覆盖率和支持性上的两大局限，为科学发现提供了可解释的探索式检索方案。

Abstract: Scientific papers may relate by problem, method, result, or contribution, but document-level retrievers collapse these into a single similarity score without saying why they are related. Citation- and similarity-based retrieval alone also confines search to the neighbourhood of what is already known, whereas generative retrieval generates document identifiers directly, enabling the exploratory retrieval that scientific discovery depends on. We connect papers in a graph whose edges are typed by these four facets, derived from facet items and citation signals, and distil it into a generative retriever whose identifiers are the papers' own facet text. Two graph properties do not survive naive distillation. First, because every training pair is an edge, naive enumeration indexes just 84% of the corpus. Coverage-aware distillation makes every paper learnable through a reverse-neighbour fallback, a minimum-coverage threshold, and edge-importance weighting. Second, constrained decoding guarantees that every generated identifier is a valid paper, but not that the graph connects it to the query. Graph-weighted reciprocal rank fusion scales each candidate's rank term by its query-candidate edge weight, dropping unsupported ones. On LitWeave, our constructed corpus of 11,359 NLP papers, Graft recovers 91% of its graph teacher's Recall@20 with no nearest-neighbour index or encoder at inference, and outperforms the graph teacher on query papers outside the corpus. It reproduces the graph's own facet labels at 0.922 precision, so every returned paper arrives labelled with the facet that surfaced it rather than an opaque score.

</details>


### [17] [Rethinking Item Tokenization in Generative Recommenders: From Fixed Atoms to Semantic Subwords](https://arxiv.org/abs/2608.22734)
*Xinrui Miao,Mingjia Yin,Jiaqing Zhang,Wei Guo,Yong Liu,Yuyang Ye,Hao Wang,Enhong Chen*

Main category: cs.IR

TL;DR: 本文提出语义子词分词（SST）方法，解决生成式推荐系统中固定长度语义ID序列导致的"注意力过载"问题，通过项级子词分词（IST）和基于行为的共现增强（BCA）将建模能力从低层项内依赖转向高层项间行为转换，在三个公开数据集和三个生成式推荐骨架上取得一致提升。


<details>
  <summary>Details</summary>
Motivation: 生成式推荐系统中，物品被编码为固定长度的语义ID序列，但这种细粒度表示在用户上下文建模时会导致"项内注意力过载"——模型将过多注意力花在低层的项内依赖关系上，而非高层的项间行为转换规律，从而损害推荐效果。

Method: 提出SST框架，包含两个组件：(1) 项级子词分词（IST），将稳定的相邻原子token合并为紧凑的语义子词token，减少编码器中的项内重组；(2) 行为诱导共现增强（BCA），注入粗粒度的语义前缀转换信号，引导释放后的建模能力聚焦于项间行为规律。同时保持目标解码的固定长度。

Result: 在三个公开数据集和三个生成式推荐骨架上进行大量实验，SST相较于固定长度和可迁移的变长语义ID基线均取得一致的性能提升。

Conclusion: 通过语义子词分词有效缓解了项内注意力过载问题，验证了在保留固定长度解码的同时使用变长语义子词表示历史项的有效性，代码已开源。

Abstract: In generative recommender systems, items are typically tokenized into fixed-length semantic ID sequences for autoregressive next-item prediction. However, for user-context modeling, this fine-grained representation triggers Intra-item Attention Overload: excessive attention is spent on low-level intra-item dependencies rather than high-level inter-item behavioral transitions.
  To address this, we propose Semantic Subword Tokenization (SST), which represents historical items as variable-length semantic subwords while preserving fixed-length target decoding. SST first applies Item-level Subword Tokenization (IST) to merge stable adjacent atom tokens into compact semantic subword tokens, thereby reducing intra-item reassembly in the encoder. It then introduces Behavior-induced Co-occurrence Augmentation (BCA) to inject coarse-grained semantic prefix transition signals, guiding the freed modeling capacity toward inter-item behavioral regularities. Extensive experiments on three public datasets and three generative recommender backbones show empirical improvements of SST over fixed-length and transferable variable-length SID baselines. Code is available at https://github.com/mxrcandy/Semantic-Subword-Tokenization.

</details>


### [18] [Risk-Aware Reranking for Agentic Tool Retrieval](https://arxiv.org/abs/2608.22751)
*Qinfei Li,Xiaoxuan Dong,Jin Zhang,Dexu Yu,Wenhao Deng,Junchen Fu,Youhua Li,Hanwen Du,Chunxiao Li*

Main category: cs.IR

TL;DR: 本文研究风险感知的工具检索（risk-aware tool retrieval），提出一个轻量级重排序框架，在冻结的一阶段检索器之上，将查询相关性与工具级暴露风险分开建模，并通过显式参数控制安全性与实用性的权衡，同时在ToolGraph上平滑分数并可选应用基于规则的安全约束。作者在UltraTool和Seal-Tools上标注了6,108个工具的五级风险等级，定义了衡量top-k结果中高风险工具暴露的指标。实验表明该方法相比仅相关性的检索器及重排序基线改善了相关性-安全性权衡。


<details>
  <summary>Details</summary>
Motivation: 工具检索决定了向LLM代理暴露哪些外部工具，是执行前安全边界的关键环节。与文档检索不同，工具检索暴露的是可执行动作，对某个任务有用的工具对另一个任务可能不必要甚至危险。然而现有工具检索方法主要优化语义相关性，安全评估也常聚焦于执行后的失败而非检索阶段引入的风险。

Method: 提出轻量级重排序框架：在冻结的一阶段检索器之上，(1) 分别建模查询条件下的相关性和工具级暴露风险；(2) 通过显式参数控制安全性与实用性的权衡；(3) 在ToolGraph上平滑分数；(4) 可选应用基于规则的安全约束。同时构建风险标注数据集（5级序数风险等级）并定义检索时安全评估指标。

Result: 在UltraTool和Seal-Tools上的实验表明，该方法相比仅相关性的检索器和重排序基线，改进了相关性-安全性权衡；规则过滤变体为安全关键部署提供了保守的操作点。

Conclusion: 检索阶段过滤可以在执行前减少暴露给代理的候选动作空间，作为下游工具使用安全机制（即执行后防护）的补充。

Abstract: Tool retrieval determines which external tools are exposed to an LLM agent for a user query or task, making retrieval a critical pre-execution safety boundary. Unlike document retrieval, tool retrieval exposes executable actions: a tool that is useful for one task may be unnecessary or risky for another. However, existing tool-retrieval methods primarily optimize semantic relevance, and safety evaluations often focus on failures after tool execution rather than risks introduced during retrieval. We study risk-aware tool retrieval, where the goal is to retrieve useful tools while reducing exposure to higher-risk tools. We propose a lightweight reranking framework on top of a frozen first-stage retriever. The framework models query-conditioned relevance and tool-level exposure risk separately, combines them through an explicit parameter controlling the tradeoff between safety and utility, smooths scores over a ToolGraph, and optionally applies rule-based safety constraints. To support retrieval-time safety evaluation, we annotate 6,108 tools across UltraTool and Seal-Tools with five ordinal risk levels and define metrics that measure risky-tool exposure in the top-$k$ results. Experiments on UltraTool and Seal-Tools show that our approach improves the relevance--safety tradeoff over relevance-only retrievers and reranking baselines, with the rule-filtered variant providing a conservative operating point for safety-critical deployments. These findings indicate that retrieval-stage filtering can reduce the candidate action space exposed to agents before execution, complementing downstream tool-use safeguards. The code and supplementary materials are available at: https://github.com/qli447/risk-aware-tool-retrieval-release.

</details>


### [19] [Same Agent, Different Answers: A Repeat-Aware Audit of Corpus-Induced Answer Churn in Retrieval-Augmented QA](https://arxiv.org/abs/2608.22856)
*Jingjie Ning,Xueqi Li*

Main category: cs.IR

TL;DR: 本文提出了"精确性盲区答案变动"（accuracy-blind answer churn）这一隐藏现象：检索增强问答系统在索引扩展后，即使所有其他配置保持不变，也可能产生不同答案，但聚合准确率因正负抵消而掩盖了这些变化。作者引入"快照兼容性审计"（Snapshot Compatibility Audit）方法来估算超额答案变动，通过从跨快照分歧中减去同快照重复分歧来隔离索引更新效应。实验在Natural Questions和TriviaQA上验证，发现即使准确率几乎不变甚至反向变化，答案层级的兼容性仍可能显著失败。


<details>
  <summary>Details</summary>
Motivation: 现有检索增强QA系统的评估主要依赖聚合准确率，但这类指标可能掩盖索引扩展后答案变动带来的隐藏不兼容问题。用户在实际部署中可能在不同时间获取到不同答案，即使系统声称"未变"。作者希望揭示这一被忽视的现象，并提出系统性审计方法。

Method: （1）提出Snapshot Compatibility Audit方法：计算跨快照答案分歧（cross-snapshot disagreement）与同快照重复分歧（same-snapshot repeat disagreement）之差，得到归因于索引更新的"超额答案变动"（excess answer churn）。（2）实验设计：将冻结的FineWeb前缀从一个shard扩展到七个shard；在400题Natural Questions（预注册）和200题TriviaQA（预注册）上进行评估，使用normalized-exact和blinded-semantic两种匹配度量。（3）额外进行结果盲的100题子集复制实验，使用第二个DeepSeek生成器和不同的服务配置以验证鲁棒性。

Result: 在400题NQ研究中，normalized-exact和blinded-semantic超额变动分别为6.44和10.25个百分点，而精确匹配准确率仅变化-1.50点；事后分析发现40/400题目存在重复稳定的语义翻转。200题TriviaQA研究中超额变动更小但方向一致，而精确匹配准确率反向移动。100题复制实验中发现8.75个百分点的语义超额变动，即使精确匹配上升了3.00个百分点。

Conclusion: 答案层级的兼容性失败可以在没有明显或方向一致的效用变化下发生。作者建议检索增强系统的发布应同时审计兼容性和效用，而非仅依赖聚合准确率指标。

Abstract: A retrieval-augmented QA system can return different answers after an index expansion even when its requested model identifier, prompt, retrieval policy, evidence depth, rendering, and exposed generation controls are held fixed. Aggregate accuracy may hide these changes when gains and losses cancel, while ordinary generation variability makes one-shot comparisons overstate update effects. We call the hidden phenomenon accuracy-blind answer churn and introduce the \emph{Snapshot Compatibility Audit}, which estimates excess answer churn by subtracting same-snapshot repeat disagreement from cross-snapshot disagreement. We instantiate it by expanding one frozen FineWeb prefix from one to seven shards. In a preregistered 400-question Natural Questions study, normalized-exact and blinded-semantic excess churn are 6.44 and 10.25 percentage points while exact-match accuracy changes by only $-1.50$ points. A post-hoc analysis finds repeat-stable semantic flips on 40/400 questions. A separately preregistered 200-question TriviaQA study yields smaller, directionally consistent excess churn while exact-match accuracy moves in the opposite direction. An outcome-blind post-hoc 100-question subset replication with a second DeepSeek generator and serving configuration finds 8.75 pp of semantic excess churn even as exact match rises by 3.00 percentage points. Answer-level compatibility can therefore fail without a conspicuous or consistently directed utility shift. Retrieval-augmented releases should audit compatibility alongside utility.

</details>


### [20] [WARP: Wasserstein-Aligned RAG for Population Opinions](https://arxiv.org/abs/2608.22859)
*Aman Singh Thakur,Aditya Agrawal,Alwarappan Nakkiran,Alex Karlsson*

Main category: cs.IR

TL;DR: 本文提出WARP，一种后检索校准算法家族，通过Wasserstein-1距离将检索文档的情感强度分布校准到人群观点分布，解决了RAG系统中少数派观点被top-k检索丢失的问题。在三个评论领域（35K文档、156查询、26实体）上，WARP将分布误差至少降低43%，且LLM评审在86%的情况下更偏好WARP生成的答案。


<details>
  <summary>Details</summary>
Motivation: 标准top-k检索按查询相似度排序文档，而非按其对人群的代表性，导致少数派观点在RAG摘要中消失。现有的多样性重排方法（如MMR、DPP）没有目标分布可依，而基于KL/JS散度的校准方法将观点桶视为无序的，混淆强正面和强负面的代价与相邻桶错误相同，存在根本缺陷。

Method: WARP包含三个变体以适配稠密、稀疏、可变候选池场景。核心思路分两步：首先恢复被余弦排名埋没的欠代表观点，然后使用Wasserstein-1距离选择文档子集，使其情感强度分布与人群目标分布匹配，从而捕捉KL/JS散度忽略的序数结构。三个变体在校准质量和速度之间做权衡。

Result: 在三个评论领域、35K文档、156查询和26实体的实验中，WARP的领域匹配变体将分布误差至少降低43%，且延迟低于亚秒级。在生成质量方面，五审LLM评审小组在86%的决定性比较中（k<=5）更偏好WARP生成的答案。

Conclusion: WARP通过利用Wasserstein距离的序数结构信息，有效解决了RAG中观点汇总的代表性问题，在不牺牲延迟的前提下显著提升了检索证据对人群观点分布的校准质量，并带来了下游生成质量的实质提升。

Abstract: RAG systems are increasingly used to summarize what large collections of documents say. A user asks "What do people think about X?" and receives an answer that reads as consensus. But standard top-k retrieval ranks documents by query similarity, not by how faithfully they represent the population, so minority views quietly disappear. Existing fixes fall short. Diversity re-rankers like MMR and DPP spread retrieved documents apart, but with no target distribution to aim for. Calibration methods based on KL or JS divergence do target one, yet treat opinion bins as unordered: confusing strong positive with strong negative costs no more than an adjacent-bin miss.
  We introduce WARP, a family of post-retrieval algorithms that calibrate retrieved evidence to the population's opinion distribution. WARP first recovers underrepresented opinions that cosine ranking may bury, then uses Wasserstein-1 distance to select documents whose sentiment-intensity distribution matches the population target, capturing the ordinal structure ignored by KL and JS divergence. We develop three variants for dense, sparse, and variable candidate pools, trading off calibration quality and speed. Across three review domains spanning 35K documents, 156 queries, and 26 entities, WARP's domain-matched variants reduce distributional error by at least 43% with sub-second latency. These gains carry through to generation: a five-judge LLM panel prefers WARP-generated answers in 86% of decided comparisons at k <= 5.

</details>


### [21] [Cascading Relevance-driven Recommendation Network for CTR Prediction in Trigger-Introduced Recommendation](https://arxiv.org/abs/2608.22973)
*Kaixuan Chen,Wenwen Wang,Xing Fang,Yang Huang,Jing Wang*

Main category: cs.IR

TL;DR: 本文提出级联相关性驱动的推荐网络（CRRN），用于解决新兴的触发引入推荐（TIR）场景中触发项与目标项相关性不足的问题。CRRN通过触发-目标交互层、级联兴趣融合模块和类别辅助成对损失三个核心组件，显式建模用户触发意图并自适应融合即刻与个性化兴趣，在工业和公开数据集上均优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 在触发引入推荐（TIR）场景中，用户点击的触发项蕴含较强但模糊隐式的即时兴趣，不同于传统搜索与推荐场景。现有方法依赖大量标注数据，缺乏对触发相关性的探索，影响用户沉浸式体验，因此需要设计能够强调触发与目标之间交互和关联的推荐模型。

Method: 提出CRRN框架，包含三个核心组件：1）触发-目标交互层（Trigger-Target Interaction layer），基于个性化门控提取触发与目标的交互特征；2）级联兴趣融合模块（Cascading Interest Fusion module），显式估计用户触发意图，并通过级联注意力块自适应融合即时兴趣与个性化兴趣；3）类别辅助成对损失（Category-assisted Pairwise Loss），借助触发与目标之间的类别关联来增强触发相关性。

Result: 在工业和公开数据集上的大量实验表明，CRRN优于现有最先进方法；在线A/B测试进一步验证了该方法的有效性。

Conclusion: CRRN通过精细建模触发与目标之间的相关性，有效解决了TIR场景中触发相关性不足的问题，提升了推荐性能及用户体验，代码已开源。

Abstract: E-commerce has emerged as crucial platforms for people's daily consumption and shopping interests. There is a new recommendation scenario, Trigger-Introduced Recommendation (TIR), where users click interested product, which is defined as the trigger item, containing their instant interest, and in the undertaking page following the relevant target items. Distinguished from traditional search and recommendation scenarios, trigger contains relatively strong instant interest, which is more vague and implicit compared to search terms. Relying on large amounts of labeled data, existing methods lack the exploration of trigger relevance, which affects users' immersive experience. To alleviate this problem, we propose the Cascading Relevance-driven Recommendation Network (CRRN) to emphasize the interaction and relevance between trigger and target, comprising three essential components: 1) the Trigger-Target Interaction layer extracts interaction features of trigger and target based on personalized gating. 2) Cascading Interest Fusion module explicitly estimates users' trigger intention and fuses instant and personalized interests adaptively with cascading attention blocks. 3) Category-assisted Pairwise Loss enhances trigger relevance with the guidance of category association between trigger and target. Extensive experiment results show that CRRN outperforms recent state-of-the-art methods on both industrial and public datasets. Online A/B tests further validate the effectiveness of our method. Our code is available at https://github.com/a-little-cabbage/CRRN.

</details>


### [22] [Hypergraph Embedding Indexing for Efficient Dense Vector Retrieval](https://arxiv.org/abs/2608.22980)
*Kishore Konda*

Main category: cs.IR

TL;DR: 本文提出超图嵌入索引（HEI），将文档按照高度激活的潜在嵌入维度组合来组织，实现倒排索引式候选生成，同时保持密集嵌入的语义排序能力。通过构建多个互补超图提升检索覆盖率，并提出激活多样性作为衡量坐标倒排索引效率的诊断指标。


<details>
  <summary>Details</summary>
Motivation: 现有的近似最近邻（ANN）索引将嵌入视为高维空间中的不可分割点，未能利用嵌入维度之间的组合信息。作者希望弥合倒排索引的快速候选生成与密集嵌入的语义排序能力之间的鸿沟。

Method: 提出Hypergraph Embedding Index (HEI)框架，基于高度激活的潜在嵌入维度组合来组织文档；构建多个互补超图以提升检索覆盖率；引入激活多样性（activation diversity）作为诊断指标，分析嵌入激活的统计性质对坐标倒排索引效率的影响。

Result: 多个互补超图的构建能够在不产生单超图维度增加所带来组合爆炸的情况下，显著改善检索覆盖率。同时，嵌入激活的统计特性对坐标倒排索引的效率有重要影响，激活多样性可作为衡量嵌入可索引性的有效指标。

Conclusion: HEI框架验证了利用嵌入维度组合进行索引的可行性，为密集向量检索提供了一种新的索引范式，并揭示了影响坐标倒排索引效率的关键统计性质。

Abstract: Dense vector retrieval has become the foundation of modern semantic search, yet existing approximate nearest neighbor (ANN) indexes treat an embedding as an indivisible point in a high-dimensional space. In this work, we propose the Hypergraph Embedding Index (HEI), a framework that instead organizes documents according to combinations of highly activated latent embedding dimensions. This formulation enables inverted-index style candidate generation while preserving the semantic ranking capabilities of dense embeddings. We further demonstrate that constructing multiple complementary hypergraphs substantially improves retrieval coverage without the combinatorial growth associated with increasing the dimensionality of a single hypergraph. Finally, we establish that the statistical properties of embedding activations strongly influence coordinate-inverted indexing efficiency, introducing \emph{activation diversity} as a diagnostic metric governing embedding indexability in coordinate-inverted frameworks.

</details>


### [23] [A Dual-Expert Strategy Integrating LLMs to Mitigate Negative Transfer in Cross-Domain Sequential Recommendation](https://arxiv.org/abs/2608.23131)
*Hyeongjun Yun,Kihyuk Song,Jaegul Choo,Chung Park*

Main category: cs.IR

TL;DR: 本文提出DuELRec，一种基于LLM的跨域序列推荐模型，通过域门控双专家框架和双采样token-to-item对比学习，解决LLMRec中token级文本建模与item级协同信号错位导致的负迁移问题，在两个数据集十个域上超越26个SOTA方法。


<details>
  <summary>Details</summary>
Motivation: 现有LLMRec方法主要建模token级别的自回归文本模式，忽视了item级别的协同信号，导致跨域知识转移时出现语义错位和负迁移，降低了CDSR任务的推荐性能。

Method: 1) 域门控双专家框架：配备item-aware注意力转换模块，将文本subtoken聚合为item级表示并施加块级注意力掩码；单域专家限制自回归注意力于同域item，跨域专家允许跨所有域，通过门控机制自适应融合。2) 双采样token-to-item对比学习：从单域和跨域item池进行随机负采样，使LLM捕获item级协同信号。

Result: 在两个真实数据集、十个域上的大量实验表明，DuELRec在推荐性能上优于26种SOTA方法。

Conclusion: 通过域门控双专家机制和item级对比学习，DuELRec有效缓解了LLMRec中的负迁移问题，显著提升了跨域序列推荐的性能。

Abstract: Cross-Domain Sequential Recommendation (CDSR) predicts the next item a user will interact with based on their historical interaction sequences across multiple domains. Recent approaches leverage Large Language Models (LLMs) finetuned on textual representations of cross-domain user sequences to retrieve the recommended items, referred to as LLMRec. However, LLMRec primarily models the autoregressive patterns of token-level item texts, while overlooking item-level collaborative signals. This semantic misalignment often leads to distorted knowledge transfer across domains-termed negative transfer degrading performance in the CDSR task. To address this issue, we propose a novel LLM-based CDSR model, DuELRec: Domain-Gated Dual Experts with LLMs for Cross-Domain Sequential Recommendation. We propose a domain-gated dual-expert framework, equipped with an item-aware attention transformation module, which aggregates textual subtokens into item-level representations and enforces block-level attention masking. The single-domain expert restricts autoregressive attention to items within the same domain, while the cross-domain expert allows it across all domains. A gating mechanism adaptively fuses their outputs, using single-domain signals to reduce cross-domain noise that causes negative transfer. Second, we introduce a dual-sampling token-to-item contrastive learning objective that allows LLMs to capture the item-level collaborative signals from both single- and cross-domains. This is achieved by transforming token-level item texts into item-level representations and applying stochastic negative sampling from both single- and cross-domain item pools for contrastive learning. Extensive experiments on two real-world datasets across ten domains show that our model outperforms 26 state-of-the-art methods in recommendation performance.

</details>


### [24] [The Disconnect Between Better Descriptive Reasoning Trace Quality and Recommendation Effectiveness](https://arxiv.org/abs/2608.23154)
*Gustavo Penha,Juan Elenter,Claudia Hauff,Hugues Bouchard,Paul Bennett,Mounia Lalmas*

Main category: cs.IR

TL;DR: 本文通过2×2因子实验（物品表示：Title vs SID；语义锚定：最小 vs 深度SID对齐）首次系统对比了生成式推荐中自然语言标题与语义ID的描述性推理轨迹质量。研究发现，引入显式描述性推理轨迹在标准SFT和RL训练下反而降低了传统离线推荐效果，深度SID对齐虽能提升轨迹质量但无法改善离线推荐性能。


<details>
  <summary>Details</summary>
Motivation: 现有工作致力于改进生成式推荐中的显式自然语言描述性推理轨迹，包括将思维链推理与语义ID（SID）预测结合。然而，SID是晦涩的学习型标识符而非自然语言，需要昂贵的对齐成本才能让LLM对其进行推理。已有研究缺乏对语义ID与自然语言标题在推理轨迹质量上的受控比较。

Method: 在三个Amazon产品领域上，使用共享的Qwen3-1.7B骨干模型，进行2×2因子设计实验：因子一为物品表示（Title vs SID），因子二为语义锚定（最小 vs 深度SID对齐）。分别在标准SFT和RL训练范式下评估描述性推理轨迹质量与传统离线推荐效果。

Result: （1）引入显式描述性推理轨迹在标准SFT和RL训练下反而降低传统离线推荐效果；（2）自然语言标题产生的轨迹明显更有依据且更可解释；（3）深度SID对齐改善描述轨迹质量但未改善传统离线推荐效果；（4）更丰富的奖励信号可部分恢复性能。

Conclusion: 提升描述性推理轨迹质量本身不足以在本文所研究的训练目标和评估协议下持续改善传统离线推荐效果，说明推理轨迹质量与推荐有效性之间并非简单的正向关系。

Abstract: Recent work has focused on improving explicit natural-language descriptive reasoning traces for generative recommendation. This includes systems that augment semantic ID (SID) prediction with chain-of-thought reasoning. However, because SIDs are opaque learned identifiers rather than natural language, they require costly alignment before an LLM can reason over them. This provides a controlled experimental setting in which both item representation (Title vs. SID) and semantic grounding (minimal vs. extensive SID alignment) can be varied independently. We therefore present the first controlled comparison of descriptive reasoning trace quality across semantic IDs and natural-language titles in a 2 x 2 factorial study on three Amazon product domains using a shared Qwen3-1.7B backbone. We find that introducing explicit descriptive reasoning traces reduces traditional offline recommendation effectiveness under standard SFT and RL training, even though natural language titles produce substantially more grounded and interpretable traces. Extensive SID alignment improves descriptive trace quality but not traditional offline recommendation effectiveness, while a richer reward signal partially recovers performance. Overall, our results show that improving descriptive reasoning trace quality is not, by itself, sufficient to consistently improve traditional offline recommendation effectiveness under the training objectives and evaluation protocols studied here.

</details>


### [25] [Evaluating Modern RAG: Textual, Multimodal, Dense, and Late Interaction Pipelines](https://arxiv.org/abs/2608.23176)
*Emre Kuru,Mehmet Onur Keskin*

Main category: cs.IR

TL;DR: 本文提出了一种基于数据驱动的量化选择方法论，帮助实践者根据文档语料库的特点和资源约束，在传统文本RAG流水线和多模态RAG流水线之间做出最优选择，并提供了权衡检索性能与系统效率的实用指导。


<details>
  <summary>Details</summary>
Motivation: 传统文本RAG流水线在处理包含布局、表格和视觉元素的文档时效果不佳，而多模态流水线虽然通过视觉语言模型提升了检索质量，但计算和内存成本显著增加。实践者缺乏系统化的方法来权衡这两种方案的利弊。

Method: 提出一种量化的、数据驱动的选择方法论，对当代文本和多模态RAG流水线（包括稠密检索和晚期交互架构）进行系统评估，分析其性能与资源消耗的权衡关系。

Result: 给出了不同流水线在检索效果和系统效率之间的权衡分析，并为实践者提供了针对特定文档语料库选择合适RAG流水线的可操作指导。

Conclusion: 不存在适用于所有场景的通用最优RAG流水线，选择应基于文档特征（如是否依赖布局/视觉信息）和可用资源约束进行数据驱动的决策。

Abstract: Retrieval-augmented generation (RAG) systems have traditionally relied on text-based pipelines that extract and retrieve information from documents. While efficient and lightweight, these approaches often struggle with documents where meaning is conveyed through layout, tables, and visual elements. Recent advances in multimodal pipelines, powered by vision-language models (VLMs), improve retrieval quality by jointly encoding visual and textual signals, but at increased computational and memory cost. We propose a quantitative, data-driven selection methodology that guides practitioners in choosing the most appropriate RAG pipeline for a given document corpus based on empirical effectiveness and resource constraints. We evaluate contemporary textual and multimodal pipelines, including dense and late-interaction architectures, analyze their trade-offs, and provide actionable guidance for balancing retrieval performance with system efficiency.

</details>


### [26] [Which Histories Matter for Time Series Forecasting? Learning Predictive Relevance with Future Supervision](https://arxiv.org/abs/2608.23221)
*Yong-Hoon Choi,Youngjin Cho*

Main category: cs.IR

TL;DR: 本文提出一种新的时序预测历史检索范式：不再将过去相似性作为有用性的代理，而是定义"预测相关性"为基于推理时信息的期望未来效用，仅在训练时使用真实未来作为特权监督。方法采用归一化模式检索器生成粗候选集，轻量级残差MLP学习列表级未来兼容性目标，推理时评分仅依赖过去信息。


<details>
  <summary>Details</summary>
Motivation: 传统时序预测的历史检索将过去相似性等同于有用性，但相似的历史未必对未来预测有用。作者提出关键问题：哪些历史样本应该被期望对查询有意义？需要区分"相似性"与"预测相关性"。

Method: 1) 定义预测相关性为条件于推理时信息的期望未来效用，真实未来仅在训练阶段作为特权监督；2) 归一化模式检索器（Pattern）生成粗候选集；3) 轻量级残差MLP学习列表级未来兼容性目标，推理时仅用过去信息评分；4) 最优相关性分解为候选级效用和查询特定兼容性，据此设计候选先验（Candidate-Prior）和洗牌未来（Shuffled-Future）控制实验。

Result: 在六个基准上，重排序器提升了Pattern检索性能，并在全部12个确认任务上优于匹配协议的SARAF检索规则。架构匹配消融实验证明正确的未来监督（而非MLP或额外上下文）是查询特定场景中性能提升的驱动因素。替代相似性实验表明强last-value锚定的L2规则在某些领域仍更优，而未来监督相关性在Solar等查询特定相关领域表现突出。

Conclusion: 历史相关性是结构化且领域依赖的，不存在普遍最优的检索规则。预测相关性可被分解为候选级效用与查询特定兼容性，未来监督的重排序方法在特定领域（尤其是查询特定相关场景）显著优于传统相似性检索。

Abstract: Historical retrieval for time-series prediction commonly treats past similarity as a proxy for usefulness. We ask a different question: which historical examples should be expected to matter for a query? We define predictive relevance as expected future utility conditioned on inference-time information, using realized futures only during training as privileged supervision. A normalized-pattern retriever first forms a coarse candidate set, and a lightweight residual multilayer perceptron (MLP) learns a listwise future-compatibility target while keeping inference-time scoring strictly past-only. Our method retains similarity-based candidate generation but reranks its candidates by a more predictive relevance criterion. Optimal relevance decomposes into candidate-level utility and query-specific compatibility, motivating Candidate-Prior and Shuffled-Future controls. Across six benchmarks, the reranker improves Pattern retrieval while revealing candidate-global, query-specific, and mixed relevance regimes. On all 12 confirmatory tasks, it improves Pattern and outperforms a matched-protocol Stationarity-Aware Retrieval-Augmented Time Series Forecasting (SARAF) retrieval rule. Architecture-matched ablations show that correct future supervision, rather than the MLP or added context alone, drives gains in query-specific regimes. Alternative-similarity experiments show that a strong last-value-anchored L2 rule remains superior in some domains, whereas future-supervised relevance is particularly strong where our diagnostics indicate query-specific relevance, especially on Solar. Candidate-pool diagnostics show that this contrast is not explained solely by coarse Pattern retrieval. Overall, historical relevance is structured and domain dependent rather than governed by a universally superior retrieval rule.

</details>


### [27] [Retrieval-Augmented Classification of Environmental Mitigations in Hydropower Licensing Documents](https://arxiv.org/abs/2608.23241)
*Hong-Jun Yoon,Tom Ruggles,Joanna Lee,Debjani Singh*

Main category: cs.IR

TL;DR: 本文针对FERC水电许可文档中环境缓解义务的分类问题，提出了一种混合系统，结合BERT检测的高召回率和RAG分类的零样本覆盖能力，在135类多标签分类任务上超越了单独使用任一方法。


<details>
  <summary>Details</summary>
Motivation: 识别和分类FERC水电许可文档中的环境缓解义务是一项劳动密集型任务，需要深厚的领域专业知识。面临的关键挑战是标签极度稀缺：135个类别中有40个完全没有训练样本，26个少于5个样本，传统监督学习方法无法处理未见类别。

Method: 提出三种方法：(1) 监督式BERT管道，在代表性充足的类别上有效但对未见类别F1为零；(2) RAG管道，通过检索类别定义来条件化分类，实现全标签空间的零样本泛化；(3) 混合系统，结合BERT检测的高召回率与RAG分类的零样本覆盖能力。

Result: 在2017年全部许可文档（5,860段落、135个类别）上评估，混合系统Micro F1达到0.524，优于仅BERT管道（0.477）和仅RAG管道（0.416），在所有训练支持桶中均表现最佳。

Conclusion: 混合方法有效解决了标签稀缺条件下的多标签分类问题，通过发挥BERT检测与RAG零样本推理的互补优势，实现了全面且更优的分类性能。

Abstract: Identifying and classifying environmental mitigation obligations in Federal Energy Regulatory Commission hydropower licensing documents is a labor-intensive task requiring deep domain expertise. We formulate this as a multi-label classification problem over a structured 135-category taxonomy and address the central challenge of severe label scarcity: 40 of 135 categories have no training examples, and 26 have fewer than five. A supervised Bidirectional Encoder Representations from Transformers (BERT)-based pipeline, while effective on well-represented categories, achieves F1 of zero on unseen classes regardless of augmentation strategy. We introduce a Retrieval-Augmented Generation (RAG) pipeline that conditions classification on retrieved category definitions, enabling zero-shot generalization across the full label space. We further propose a hybrid system that combines BERT detection with RAG classification, exploiting the high recall of fine-tuned detection and the zero-shot coverage of retrieval-augmented reasoning. Evaluated on the full set of 2017 license documents (5,860 paragraphs, 135 categories), the hybrid achieves a Micro F1 of 0.524, outperforming the BERT-only pipeline (0.477) and the RAG-only pipeline (0.416) across all training-support buckets.

</details>


### [28] [Hierarchical Exponential-Gaussian Mixtures for Watch-Time Distribution Prediction](https://arxiv.org/abs/2608.23356)
*Sofia Gulevskaia,Mikhail Trapeznikov,Aleksandr Poslavsky,Alexander D'yakonov*

Main category: cs.IR

TL;DR: 本文提出一种层次化指数-高斯混合模型（HEGM），用于短视频推荐中的观看时长（WT）预测。通过大规模重现研究，作者发现现有EGMN模型存在方差坍缩、组件冗余和组件失活等问题，并通过层次化跳过观看分解、KL方差正则化、结构化初始化等手段解决这些问题。实验表明HEGM在排序准确性和阈值事件预测上均有提升，并通过1.5个月的线上A/B测试验证了其显著性效果。


<details>
  <summary>Details</summary>
Motivation: 短视频推荐需要精确的观看时长预测，但WT分布呈现近零膨胀、长尾和多模态特性。虽然EGMN通过建模完整的条件分布取得了最优性能，但大规模重现研究发现其存在方差坍缩、组件冗余和组件失活等严重缺陷，亟需改进。

Method: 提出HEGM模型，包含四个关键改进：(1)层次化跳过观看（skip-watch）分解；(2)基于KL的方差正则化防止方差坍缩；(3)结构化初始化减少组件冗余；(4)移除强制高斯偏移和熵正则化。这些设计旨在增强混合模型的稳定性和可解释性。

Result: 在公开数据集和大规模工业数据集上，HEGM在排序准确性和阈值事件预测方面均优于基线，同时保持有竞争力的点估计精度，并显著提升了混合模型的稳定性和可解释性。1.5个月的生产A/B测试确认了统计显著的参与度提升。

Conclusion: HEGM有效解决了EGMN的多个失效模式，在WT预测任务上实现了更稳定、更可解释且更准确的模型，并通过线上验证证明了实际业务价值。

Abstract: Accurate watch-time (WT) prediction is an important requirement for short-video recommendations. Yet WT distributions are near-zero-inflated, long-tailed and multimodal. The recent Exponential-Gaussian Mixture Network (EGMN) models the full conditional WT distribution rather than a single point estimate and achieves state-of-the-art performance. Our large-scale reproduction study reveals that EGMN is vulnerable to variance collapse, component redundancy, and inactive components. We propose a Hierarchical Exponential-Gaussian Mixture (HEGM) model that addresses these failure modes through a hierarchical skip-watch decomposition, KL-based variance regularization, structured initialization, removing the forced Gaussian shift and the entropy regularizer. Across public and large-scale industrial datasets, HEGM improves ranking accuracy and threshold-event prediction, while maintaining competitive point-estimation accuracy and substantially improving mixture stability and interpretability. A 1.5-month production A/B test confirms statistically significant engagement lifts. Our code and models are publicly released at https://github.com/rw404/HEGM.

</details>


### [29] [Towards a Densing Law for User Representation Learning at Billion-Scale Capacity](https://arxiv.org/abs/2608.23392)
*Bin Dou,Junru Zhang,Zhaoyi Yuan,Wuliang Huang,Letian Gong,Baokun Wang,Huan Li,Yu Cheng,Weiqiang Wang*

Main category: cs.IR

TL;DR: 本文提出"用户行为稠密化定律"（User Behavioral Densing Law），用于刻画数据规模与最小充分分词容量之间的定量关系，并据此开发了自适应变长分词方法ALGN，解决十亿级规模下用户表征学习中原始数据扩展的瓶颈问题。


<details>
  <summary>Details</summary>
Motivation: 工业场景中用户表征学习通过增加用户数量、行为序列长度和模型规模来扩展，但现有方法面临两大挑战：（1）十亿级容量下原始文本用户行为数据扩展存在瓶颈，性能提升随数据规模增大而递减，分词可缓解这一问题；（2）缺乏对分词配置应如何随数据规模扩展的定量分析。

Method: 首先在十亿级支付宝数据集上进行原始数据与分词数据的扩展对比实验，揭示原始数据扩展瓶颈与分词带来的持续收益；其次通过理论分析和系统实验总结最小充分分词配置随数据规模变化的定量规律；最后基于该定律开发ALGN自适应变长分词方法，优化容量分配。

Result: 发现最小充分分词容量与输入数据大小（以token计）的对数之间近似呈线性关系，且缩放斜率随分词方法和数据源系统性变化，反映表征空间冗余度和源内独特性差异。ALGN在多样数据源、分词方法和下游任务上均优于现有基线，验证了定律的泛化性和可靠性。

Conclusion: 该定律为大规模用户表征学习中的分词配置选择提供了实用指导，ALGN方法的提出进一步将理论规律落地为实际优化方案，验证了理论指导实践的有效性。

Abstract: User representation learning in real-world industrial scenarios is commonly scaled by increasing user amount, behavioral sequence length and model size. However, existing methods face two challenges: (i) Bottleneck for raw data scaling at billion-scale capacity, as performance exhibit diminishing performance gains with larger-scale raw text user behavioral input, which can be mitigated by tokenization. (ii) Lack of quantitative analysis of how tokenization configurations should scale with data size. In this report, we propose User Behavioral Densing Law for characterizing the quantitative relationship between data scale and the minimum sufficient tokenization capacity. Firstly, we conduct a pilot study on raw & tokenized scaling comparison on billion-scale Alipay dataset, revealing the raw data scaling bottleneck and the sustained gains enabled by tokenization. To derive the scaling pattern governing the minimum sufficient tokenization configuration at different data scales, theoretical analysis and systematic experiments are employed to summarize the quantitative scaling pattern. We find an approximately linear relationship between the logarithms of minimum sufficient tokenization capacity and input data size measured by tokens, and the scaling slope varies systematically with the tokenization method and data source, reflecting differences in representation-space redundancy and intra-source uniqueness. Guided by the proposed law, we further develop ALGN, an adaptive variable-length tokenization method that improves capacity allocation. Extensive experiments across diverse data sources, tokenization methods, and downstream tasks demonstrate the generalizability and reliability of the User Behavioral Densing Law, providing practical guidance for tokenization configuration selection in large-scale user representation learning. Moreover, ALGN outperforms existing baselines.

</details>


### [30] [Adaptive Item-based Collaborative Structures via Noise Rescheduling in Diffusion for Generative Recommendation](https://arxiv.org/abs/2608.23400)
*Jiaqi Wang,Tianying Liu,Heng Chang,Jihong Guan,Wengen Li,Shuigeng Zhou*

Main category: cs.IR

TL;DR: 本文提出ANR-DiffRec，一个将基于物品的协同过滤结构编码进离散扩散模型的统一框架，通过显式引入物品共现矩阵作为协同先验，并提出基于物品的自适应噪声重调度机制，在多个基准上超越现有最先进生成式推荐模型。


<details>
  <summary>Details</summary>
Motivation: 离散扩散模型（DDM）已被引入推荐系统，通过迭代去噪将用户历史建模为令牌生成过程。然而现有方法未能显式整合基于物品的协同过滤信息，具体表现为：(1) 物品表示偏重语义、缺乏扩散训练的协同先验；(2) 去噪过程使用均匀噪声调度，忽略物品层面的自适应结构依赖。这两点缺陷限制了推荐准确性。

Method: ANR-DiffRec框架包含两个核心组件：(1) 显式引入物品共现矩阵来引导语义ID生成，为离散扩散训练提供结构化协同先验；(2) 提出基于物品的自适应噪声重调度机制，根据局部上下文可恢复性和行为感知的物品依赖动态调整去噪权重，联合建模物品内部结构上下文与物品间协同信号，实现结构感知去噪。

Result: 在多个推荐基准数据集上的大量实验表明，ANR-DiffRec在性能上持续优于最先进的生成式推荐模型。

Conclusion: 通过将物品级协同结构显式编码进离散扩散过程，ANR-DiffRec有效弥补了现有DDM推荐方法缺乏协同过滤信息的问题，验证了结构感知去噪对生成式推荐的显著提升作用。

Abstract: Discrete Diffusion Models (DDMs) have recently been introduced to recommendation systems, modeling user history as a token generation process via iterative denoising. However, while effective at capturing user-level sequential patterns, these methods often fail to explicitly integrate item-based collaborative filtering information, a critical component for accurate recommendation. This deficiency manifests in two key aspects: (1) the item representation is often semantic-focused, lacking collaborative priors for diffusion training; and (2) the denoising process employs a uniform noise schedule, treating all tokens indiscriminately and ignoring item-level adaptive structural dependencies. To bridge this gap, we propose ANR-DiffRec, a unified framework designed to encode item-based collaborative structures into discrete diffusion for generative recommendation. First, we explicitly incorporate an item co-occurrence matrix to guide semantic ID generation, providing a structured collaborative prior for discrete diffusion training. Second, we introduce an item-based adaptive noise rescheduling mechanism that dynamically adjusts denoising weights according to both local contextual recoverability and behavior-aware item dependencies. Specifically, the proposed strategy jointly models intra-item structural context and inter-item collaborative signals, enabling structure-aware denoising during diffusion training. Extensive experiments on multiple benchmarks demonstrate that our method consistently outperforms state-of-the-art generative recommendation models. Code: https://github.com/CalmaQi/ANR-DiffRec.

</details>


### [31] [Robustness of IR Models to Collection Growth](https://arxiv.org/abs/2608.23419)
*Emmanouil Georgios Lionis,Debasis Ganguly,Sean MacAvaney*

Main category: cs.IR

TL;DR: 本研究正式定义了检索器对非相关文档添加的鲁棒性概念，并通过合并两个主题重叠极小的集合进行实证评估。作者将检索模型分为不依赖其他文档的MDA模型和依赖其他文档的MDD模型，发现两类模型在添加非相关文档后均出现性能下降，但MDA在检索任务中优于MDD，而两者在重排序任务中效果相当。


<details>
  <summary>Details</summary>
Motivation: 实际应用中文档集合是动态的，经常有新文档加入。理想情况下，检索器的有效性不应因添加非相关文档而下降，但这一概念尚未被正式定义和系统评估。此外，模型对集合中文档的依赖方式（如BM25的IDF、listwise重排序器的上下文文档）可能影响其鲁棒性，值得深入研究。

Method: 作者首先形式化了检索器对非相关文档添加的鲁棒性概念。然后通过合并两个主题重叠可忽略的集合进行实证评估，并提出了模型分类框架：MDA（Multi-Document-Agnostic，不依赖其他文档）和MDD（Multi-Document-Dependent，依赖其他文档），涵盖了BM25等传统模型和listwise重排序器等现代模型。

Result: 实验结果显示，所有测试模型在添加非相关文档后都表现出一定程度的性能下降，即无论MDD还是MDA模型都不是完全鲁棒的。有趣的是，在检索任务中MDA模型比MDD模型更有效，而在重排序任务中两类模型效果相当。

Conclusion: 研究揭示了现有IR系统在动态环境下普遍存在的鲁棒性缺陷，指出非相关文档的加入会导致所有模型性能退化。同时，模型对集合依赖方式的不同带来了能力差异，MDA在检索中表现更优，这为未来设计更鲁棒的检索系统提供了重要参考。

Abstract: Information Retrieval (IR) systems seek to identify relevant documents within a collection. In practical applications, collections are dynamic, with documents frequently added. We argue that ideally, a retriever's effectiveness should not decrease when non-relevant documents are added to a collection. This study formalises this concept and empirically evaluates it by merging two collections with negligible topic overlap. We hypothesise that the way an IR model conditions its ranking on other documents in a collection (e.g., the IDF component in BM25 or contextual documents in listwise rerankers) plays an important role in its robustness to the addition of non-relevant documents. We broadly classify models as those that do not depend on other documents (Multi-Document-Agnostic, MDA) and those that do (Multi-Document-Dependent, MDD). Our results show that neither MDD nor MDA models are fully robust to the addition of non-relevant documents, as all models exhibit some performance degradation. Interestingly, among the models we test, MDA is more effective than MDD for retrieval, whereas MDD and MDA rerankers are equally effective.

</details>


<div id='cs.DB'></div>

# cs.DB [[Back]](#toc)

### [32] [Residual Privacy Budgeting with Weighted Scarcity Allocation for Online Query Answering](https://arxiv.org/abs/2608.22185)
*Mina Khoshmehr,Fernando Beltran*

Main category: cs.DB

TL;DR: 本文研究在线差分隐私查询应答问题，在有限零集中差分隐私(zCDP)合约下，提出"残余隐私预算"框架：每个到达的查询先利用先前DP输出中可复用的支持，再为满足精度阈值仅花费新增预算。控制器将可行情形（精确分配最小残余支持）与稀缺情形（按查询难度加权分配有限支持）分离，并引入查询影响因子(QIF)作为难度诊断信号。理论证明包括zCDP组合性、残余最小性、可行域内相对离线最优的1-竞争性，以及稀缺情形下不存在优于1/n竞争比的不可行性结果。


<details>
  <summary>Details</summary>
Motivation: 实际部署中差分隐私查询并非一次性到达，而是序列化到达且携带不同精度阈值、可能与已发布信息重叠。现有DP机制多假设批处理或固定查询模式，缺乏对在线、异构、重叠查询场景的系统处理方法。

Method: 提出残余隐私预算分配框架：每个查询先利用先前DP输出的可重用支持（逆方差融合证明标量高斯精确重用下的加性支持），再仅对剩余需求消耗新预算。控制器分两种情况：可行情形精确分配最小残余支持；稀缺情形采用加权短缺守恒优化器根据查询难度分配有限支持，权重由QIF定义（诊断查询不稳定性而非重要性）。

Result: 证明了zCDP组合性质、残余最小性、可行域内相对离线最优的1-竞争性，以及忽略历史信息的分配器会产生可避免支出。稀缺性不可行性结果显示任何在线分配器无法保证优于1/n的阈值满足竞争比。

Conclusion: QIF稀缺层是对固有困难在线问题的一种设计选择而非最优保证。该工作为流式/在线差分隐私查询应答提供了理论框架，明确了可行与不可行区域的边界。

Abstract: In many practical deployments of differential privacy, queries do not arrive all at once. We study online differentially private query answering under a finite zero-concentrated differential privacy (zCDP) contract. In this setting, queries arrive sequentially, carry different accuracy thresholds, and may overlap with information already released. We formulate this setting as residual privacy budgeting: for each arriving query, the mechanism first credits reusable support from previous DP outputs and then spends new budget only on the remaining support required to satisfy the current threshold. The controller separates feasible cases, where the minimal residual support is allocated exactly, from scarcity cases, where a weighted shortfall-conservation optimiser assigns limited support according to query difficulty. We define the weight using the Query Influence Factor (QIF), a diagnostic signal for query difficulty and instability rather than query importance. For scalar Gaussian exact reuse, inverse-variance fusion justifies additive support. We prove zCDP composition, residual minimality, 1-competitiveness against the offline optimum in the feasible regime, and avoidable expenditure for allocators that ignore released history. A scarcity impossibility result shows that no online allocator can guarantee a competitive ratio better than 1/n in threshold satisfaction, contextualising the QIF scarcity layer as a design choice for an inherently hard online problem.

</details>


### [33] [JetStream: Generating Query Accelerators for Existing Database Systems](https://arxiv.org/abs/2608.22476)
*Akhilesh Balasingam,Amadou Ngom,Geoffrey X. Yu,Tim Kraska*

Main category: cs.DB

TL;DR: JetStream是一个面向查询特定工作负载、基于LLM的数据库加速器生成系统。与现有方法（如GenDB、Bespoke OLAP）不同，JetStream不是替换数据库原生存储，而是扩展现有DBMS。它通过分阶段的、度量驱动的智能体工作流生成查询特定的加速器，包括持久化辅助状态，并通过引擎中立的基板与引擎特定的适配器集成到底层数据库引擎中。在TPC-H SF=20上，JetStream实现833倍于DuckDB的几何平均只读加速，远超GenDB（34.07倍）和Bespoke OLAP（12.35倍），并支持动态工作负载与增量维护。


<details>
  <summary>Details</summary>
Motivation: 现有LLM驱动的数据库系统生成工作假设静态数据，并完全替换数据库原生存储。作者希望突破这一局限，让生成的查询特定加速器能够集成到现有DBMS中而非取代它，从而支持动态工作负载和实际生产环境的使用。

Method: JetStream由三部分构成：(1) 一个分阶段、度量驱动的智能体工作流，用于生成并优化查询特定加速器，必要时生成持久化辅助状态；(2) 一个固定的、引擎中立的基板，提供执行、事务协调、状态管理和维护的公共接口；(3) 一个独立的合成工作流，生成连接基板与底层引擎的引擎特定后端适配器。对具有状态性的加速器，JetStream还生成维护逻辑，并使用运行时策略在增量维护、重建和惰性修复之间进行选择以应对数据库变化。

Result: 在TPC-H SF=20上，JetStream生成的状态性加速器实现833倍于DuckDB的几何平均只读加速（GenDB为34.07倍，Bespoke OLAP为12.35倍）。在每60秒刷新一次的TPC-H场景下，JetStream保持375倍的几何平均工作负载加速。此外，JetStream能泛化到未见过的全新工作负载，在DuckDB和PostgreSQL上分别实现102倍和486倍的几何平均只读加速。

Conclusion: 实验结果证明，激进的生成式特化可以无缝集成到现有DBMS中，并支持动态工作负载。JetStream显著超越了此前的生成式数据库系统方法，同时保持了与现有数据库引擎的兼容性，展示了LLM驱动数据库特化的实用化方向。

Abstract: Recent work has shown that LLMs can synthesize highly specialized database systems for fixed workloads, but existing approaches typically assume static data and replace the database's native storage with generated representations. We present JetStream, a system for generating query-specific accelerators that instead extend an existing DBMS.
  JetStream consists of three parts. First, a staged, measurement-driven agentic workflow generates and optimizes query-specific accelerators, including persistent auxiliary state when beneficial. Second, a fixed, engine-neutral substrate provides the common interfaces for execution, transaction coordination, state management, and maintenance. Third, a separate synthesis workflow generates engine-specific backend adapters that connect the substrate to the underlying engine. For stateful accelerators, JetStream also generates maintenance logic and uses a runtime policy to choose among incremental maintenance, rebuilds, and lazy repair as the database changes.
  On TPC-H at SF=20, stateful accelerators generated by JetStream achieve an 833x geomean read-only speedup over DuckDB, compared with 34.07x for GenDB and 12.35x for Bespoke OLAP. Under TPC-H refreshes every 60 seconds, JetStream maintains a 375x geomean workload speedup. We also show that JetStream generalizes to new, unseen workloads, achieving geomean read-only speedups of 102x over DuckDB and 486x over PostgreSQL. These results show that aggressive generated specialization can be integrated seamlessly with existing DBMSes and support dynamic workloads.

</details>


### [34] [DAGSmith: Dependency-Aware Rewriting for dbt-Style SQL Pipelines](https://arxiv.org/abs/2608.22551)
*Jie Liu,Lin Ma,Barzan Mozafari*

Main category: cs.DB

TL;DR: 本文提出DAGSmith，据作者所知是首个面向SQL流水线DAG的、依赖感知的源到源重写系统。它利用流水线中显式的模型依赖关系（上下游信息、消费方式、位置）作为优化信号，结合LLM提出流水线级重构，并通过等价性检查、学习成本模型和全局冲突消解，在Tuva dbt开源项目上实现了42.6%的运行时间减少和67.7%的计算成本降低。


<details>
  <summary>Details</summary>
Motivation: 现代分析工作负载日益组织为重复的SQL流水线（如dbt项目），包含数百上千个相互依赖的SQL模型，形成DAG结构。然而传统查询优化器和重写器一次只处理单个查询，物化视图选择和多查询优化只处理较狭窄的复用形式，未利用流水线级信息（中间结果如何被消费、下游输出依赖哪些计算、昂贵工作相对数据缩减的位置、哪些结果值得持久化、刷新调度与输入变化及输出需求的关系）。

Method: DAGSmith将显式依赖视为优化信号：1) 分析每个变换的上游输入、下游消费者及在流水线DAG中的位置；2) 使用LLM提出流水线级重构建议；3) 分离SQL生成与等价性检查，拒绝不安全的重写；4) 用学习成本模型重新调优持久化选择；5) 选择全局兼容、无冲突的重写集合。实现了依赖边简化、非局部语义复用、下游感知剪枝、流水线感知工作放置、重写-物化协同优化及频率感知优化。

Result: 在开源Tuva dbt项目上，DAGSmith将运行时间减少42.6%，仓库计算成本降低67.7%，比最先进的单查询重写方案分别提升98.1%和348.3%。

Conclusion: 通过利用流水线DAG中显式依赖所蕴含的全局信息，DAGSmith证明了依赖感知的源到源重写系统能够显著超越传统逐查询优化方法，为SQL流水线优化开辟了新的方向。

Abstract: Modern analytics is increasingly organized as recurring SQL pipelines rather than isolated SQL statements. Tools such as dbt, which have gained extreme popularity in recent years, allow teams to write each transformation as SQL and make dependencies between transformations explicit, producing directed acyclic graphs (DAGs) with hundreds or thousands of interdependent SQL models. Traditional query optimizers and source-to-source query rewriters operate on one query at a time, while materialized-view selection and multi-query optimization address narrower forms of reuse. They do not exploit the pipeline-level information exposed by explicit dependencies: how intermediate results are consumed, which downstream outputs depend on each computation, where expensive work sits relative to data reduction, which results are worth persisting, and how refresh schedules relate to input change and output demand.
  We introduce DAGSmith, to the best of our knowledge the first holistic dependency-aware source-to-source rewriting system for SQL pipeline DAGs. DAGSmith treats explicit dependencies as optimization signals. It analyzes each transformation with its upstream inputs, downstream consumers, and position in the pipeline DAG, uses an LLM to propose pipeline-level refactorings, separates SQL generation and equivalence checking to reject unsafe rewrites, retunes persistence choices with a learned cost model, and selects a globally compatible, conflict-free set of rewrites. This enables dependency-edge simplification, non-local semantic reuse, downstream-aware pruning, pipeline-aware work placement, rewrite-materialization co-optimization, and frequency-aware optimization. On the open-source Tuva dbt project, DAGSmith reduces elapsed time by 42.6% and warehouse compute cost by 67.7%, 98.1%/348.3% larger than state-of-the-art single-query rewriting.

</details>


### [35] [HAWKEYE: Seeing One Layer Deeper -- A Cohesion-Aware Structural Channel for Temporal Link Prediction](https://arxiv.org/abs/2608.22727)
*Jiacheng Ding,Xiaofei Zhang*

Main category: cs.DB

TL;DR: 本文提出HAWKEYE，一种用于时序链路预测的凝聚力感知结构通道。它替代DyGFormer等模型中粗糙的1-2位邻居共现计数结构通道，维护经典的k家族凝聚指标（degree→k-core→k-truss），并构建2跳凝聚桥特征。实验表明，在六个多种子验证数据集上，将其嵌入DyGFormer可将AP/MRR提升+0.6到+10.8点；在tgbl-subreddit基准上几乎翻倍基线MRR（0.103→0.204）；流式管线可在五分钟内处理67M边的tgbl-flight图。论文还刻画了其收益边界：增益与图的无训练2跳discAUC相关，在退化或饱和图上消失。


<details>
  <summary>Details</summary>
Motivation: 现有TLP模型是多通道信息聚合器（交互历史、时间编码、结构通道），前两者已被精细打磨，而结构通道仍是粗糙的附带品——如DyGFormer仅用1-2位邻居共现计数。测量发现，在稀疏时序图上，经典的1跳共同邻居信号近乎随机（discAUC≈0.50），因为节点间几乎不共享直接邻居；真正有区分度的信号在2跳层——2跳凝聚桥，其discAUC可达0.73-0.98。这激励作者提出更精细的结构通道。

Method: HAWKEYE是一个凝聚力感知的结构通道：1）增量维护经典的k家族凝聚指标（degree→k-core→k-truss）；2）基于这些指标构建2跳凝聚桥特征。它是即插即用的替代品，可直接替换时序图模型的原生结构通道，无需改动骨干网络。

Result: 1）在六个多种子验证数据集（uci, enron, USLegis, CanParl, reddit, mooc）上，将HAWKEYE嵌入DyGFormer比共现通道提升测试AP/MRR +0.6到+10.8点。2）在二部图推荐基准tgbl-subreddit上，3种子单遍struct-only消融显示HAWKEYE几乎翻倍基线MRR（0.103±0.003→0.204±0.005，三种子平均+10.1点）。3）流式管线可扩展到67M边的tgbl-flight图，每次通过仅需五分钟。4）论文刻画了何时有效：增益与图的无训练2跳discAUC相关，在退化或饱和图上消失，具有可预测的边界。

Conclusion: HAWKEYE证明了结构通道在TLP中的重要性远超此前认知——通过将结构信号从1跳提升到2跳凝聚桥，并利用k-family凝聚指标，能在不改变骨干网络的条件下显著提升性能。该工作提供了清晰的收益边界预测机制，并开源了所有代码、数据和图表生成脚本。

Abstract: State-of-the-art temporal-link-prediction (TLP) models are, in essence, multi-channel information aggregators: they combine an interaction-history channel, a time-encoding channel, and a structure channel. The first two have been refined relentlessly; the structure channel remains a crude afterthought -- DyGFormer encodes it as a 1--2-bit neighbour-cooccurrence count. We begin with a measurement: on sparse temporal graphs the classical 1-hop common-neighbour signal is near-random (discriminative AUC $\approx 0.50$), because two nodes almost never share a direct neighbour; the genuinely discriminative signal lies one hop deeper -- the 2-hop cohesive bridge, whose discAUC reaches 0.73--0.98, on both bipartite and non-bipartite graphs. Motivated by this, we propose HAWKEYE, a cohesion-aware structural channel that incrementally maintains the classical k-family of cohesiveness indicators (degree $\to$ k-core $\to$ k-truss) and forms 2-hop cohesive-bridge features. HAWKEYE is a drop-in replacement for a temporal-graph model's native structure channel, with no change to the backbone. Swapping HAWKEYE into DyGFormer improves test AP/MRR over the cooccurrence channel by +0.6 to +10.8 points across six multi-seed-validated datasets (uci, enron, USLegis, CanParl, reddit, mooc). On the bipartite recommendation benchmark tgbl-subreddit, a 3-seed single-pass struct-only ablation shows HAWKEYE nearly doubling the baseline test MRR (0.103$\pm$0.003 $\to$ 0.204$\pm$0.005, +10.1 points across all three seeds); the streaming pipeline scales to the 67M-edge tgbl-flight in five minutes per pass. We further characterise when it helps: the gain tracks a graph's training-free 2-hop discAUC and vanishes on degenerate or saturated graphs -- a predictable boundary. All code, data, and figure-generation scripts are released.

</details>


### [36] [Why We Created Yet Another Memory Framework: Understanding MGA's Role in Next-Gen Database Systems](https://arxiv.org/abs/2608.22853)
*Vikramraj Sitpal,Pei Li,Shubham Kumar,Somansh Reddy Satish,Ravi Thammaiah,Nagarajan Muthukrishnan*

Main category: cs.DB

TL;DR: 本文介绍了Oracle AI数据库中的"托管全局区"(MGA, Managed Global Area)，一种范围化的共享内存抽象，用于解决企业级数据库系统中共享内存的效率与隔离权衡问题。MGA允许组件显式定义分配源、成员资格和协调语义，支持动态进程成员和模块化内存使用。在TPC-H哈希连接和ONNX Runtime推理等负载上，MGA分别将连接密集型查询延迟降低35%，模型共享内存占用降低90%，大模型推理延迟降低37%。


<details>
  <summary>Details</summary>
Motivation: 现代数据库系统虽有多种内存区域，但在生产约束下难以实现高效的内存共享。现有抽象在粗粒度全局共享与严格进程隔离之间存在权衡，导致数据复制、内存碎片化以及受控共享支持不足等问题。随着工作负载多样化以及系统需要容忍进程故障并保持可预测性能，这些挑战愈发严峻。

Method: 提出MGA（Managed Global Area），一种范围化的共享内存抽象。MGA允许组件显式定义分配源、成员资格和跨选定进程的协调语义，并与生产数据库引擎集成。与Oracle中完全共享的内存区域（如SGA）不同，MGA支持动态进程成员和模块化内存使用，不强制系统级可见性，在细粒度共享和进程隔离之间提供了更灵活的平衡。

Result: 在并发执行下，MGA将TPC-H连接密集型查询延迟降低最多35%；在ONNX Runtime推理场景中，MGA启用的模型共享将内存占用减少最多90%，并将大模型推理延迟降低最多37%。

Conclusion: 动态范围化的共享内存能够在生产数据库系统中同时提升效率与性能可预测性，表明通过灵活的共享内存抽象可以在企业级系统中兼顾性能与隔离需求。

Abstract: Despite the presence of multiple memory regions in modern database systems, supporting an efficient form of memory remains a challenge under production constraints. In enterprise-grade data systems, existing abstractions impose a trade-off between coarse-grained global sharing and strict process isolation, resulting in data copying, memory fragmentation, and limited support for controlled sharing. These challenges become more pronounced as workloads grow more diverse, and systems must tolerate process failures while maintaining predictable performance. This paper introduces the Managed Global Area (MGA), a scoped shared-memory abstraction in Oracle AI Database that addresses these limitations. MGA allows components to explicitly define allocation source, membership, and coordination semantics across selected processes while integrating with a production database engine. Unlike fully shared memory regions in Oracle, such as the System Global Area (SGA), MGA supports dynamic process membership and modular memory usage without imposing system-wide visibility. We evaluate MGA on analytical and AI workloads that stress shared-memory execution, including TPC-H hash joins and ONNX Runtime inference. Under concurrent execution, MGA reduces latency for join-intensive TPC-H queries by up to 35%. For ONNX- based inference, MGA-enabled model sharing reduces memory footprint by up to 90% and lowers large-model inference latency by up to 37%. These results demonstrate that dynamically scoped shared memory can improve both efficiency and predictability in production database systems.

</details>


### [37] [Closed-Form Predicate-Level Shapley Attribution for Sliding-Window Aggregates](https://arxiv.org/abs/2608.23087)
*Pouya Khani,Ira Assent*

Main category: cs.DB

TL;DR: 本文提出了一种在滑动窗口聚合上高效计算谓词级Shapley值的方法，将指数级的精确计算简化为O(1)的增量维护，仅需每个谓词维护三个汇总（count、sum、sum of squares），并证明了SUM、COUNT、AVG及方差等矩多项式聚合具有封闭解形式，而MAX、MIN和分位数则不具备。


<details>
  <summary>Details</summary>
Motivation: 流式引擎能实时报告滑动窗口聚合结果，但无法解释聚合值为何是当前值。用户需要谓词级（如地区、客户层级）的归因分析，而精确Shapley值计算随窗口大小呈指数级增长，现有估计器又忽略了连续窗口间的大量重叠信息。

Method: 作者证明对SUM、COUNT、AVG及总体/样本方差，精确的谓词级Shapley值可以用每个谓词仅三个增量维护的汇总统计（count、sum、sum of squares）来表达封闭形式，系数仅依赖于两个运行中的调和数。归因计算简化为每个滑动O(1)的汇总更新，无需枚举联合体。重叠和组合性谓词通过布尔签名的原子细化来精确求解。作者还刻画了该现象的一般性：所有矩多项式聚合都具备这种形式，而MAX、MIN和分位数在任何固定矩阶下都不具备。

Result: 在超过10,000个窗口上与暴力Shapley值计算匹配到浮点精度，在N=10^6时每滑动约2μs（比逐窗口重算相同公式快3,200倍），并在290万条纽约出租车行程数据上以每秒约180万次汇总更新的速度解释了夜间票价峰值。

Conclusion: 该方法将滑动窗口聚合的归因分析从指数级计算降低到常数级增量更新，且对重叠窗口的复用使得性能提升显著，为流式系统中的实时可解释聚合分析提供了实用且理论完备的解决方案。

Abstract: Streaming engines report sliding-window aggregates in real time, but they do not explain \emph{why} an aggregate takes its current value. A natural target is the Shapley value from cooperative game theory, which axiomatically distributes an aggregate among the tuples in the window. Practitioners, however, ask predicate-level questions (e.g., how much a region or customer tier contributed to an average or variance spike). Exact Shapley computation is exponential in the window size, and existing estimators discard the massive overlap between consecutive windows.
  We show that for SUM, COUNT, AVG, and population/sample variance, exact predicate-level Shapley values admit closed forms in three additively maintained summaries per predicate (count, sum, and sum of squares), with coefficients that depend only on two running harmonic numbers. Attribution therefore reduces to $O(1)$ summary updates per slide for registered predicates, with no coalition enumeration. Overlapping and compositional predicates are answered exactly via atomic refinement of Boolean signatures. We further characterize the phenomenon: every moment-polynomial aggregate admits such a form, while MAX, MIN, and quantiles provably do not at any fixed moment order.
  Experiments match brute-force Shapley values to floating-point precision on over $10{,}000$ windows, sustain $\approx\!2\,μ$s per slide up to $N=10^6$ ($3{,}200\times$ faster than per-window recomputation of the same formulas), and explain a nighttime fare spike on 2.9M NYC taxi trips at $\approx\!1.8$M summary updates per second.

</details>


### [38] [Teach Your DBMS to LIKE Strings: Fast and General Pattern Matching for Wildcard Joins and Filters](https://arxiv.org/abs/2608.23307)
*Lam-Duy Nguyen,Pascal Ginter,Duc-Tam Nguyen,Thomas Neumann,Viktor Leis*

Main category: cs.DB

TL;DR: 本文针对现代数据库系统中通配符（LIKE）查询性能差的问题，提出了两种优化技术：基于Aho-Corasick算法的新型通配符连接算法，以及利用代码生成技术为LIKE谓词生成专用代码的通配符过滤方法。实验表明，新方法在连接和过滤场景下分别比DuckDB快30.6倍、比Umbra快114.75倍，过滤性能提升13.3倍。


<details>
  <summary>Details</summary>
Motivation: 现代应用需要从文本中提取模式，但当前DBMS对通配符操作（LIKE谓词）处理效率低下：连接操作退化为嵌套循环，过滤操作依赖昂贵的解释执行。这些问题严重制约了文本分析的性能。

Method: 1) 提出基于Aho-Corasick算法的新型通配符连接算法，将多模式匹配引入连接处理，显著降低时间复杂度；2) 利用代码生成（code-generation）基础设施，为LIKE谓词动态生成专用代码，消除逐元组解释模式的开销。

Result: 实验结果表明：新型通配符连接算法相对DuckDB和Umbra分别实现最高30.6倍和114.75倍的加速；新型通配符过滤方法在过滤密集型压力测试中达到13.3倍的加速比。

Conclusion: 所提出的两种技术（Aho-Corasick通配符连接与代码生成式通配符过滤）能有效弥补当前数据库系统在通配符查询处理上的短板，有望成为现代查询引擎中高性能文本分析的关键技术组件。

Abstract: Nowadays, modern applications do more than just store text -- they need to derive meaningful insights from it. To do that, they usually rely on wildcard queries with LIKE predicate to extract patterns. However, modern database management systems (DBMSs) handle these wildcard operations poorly, resorting to nested loops for joins and expensive interpreted evaluation for filters. To address the former, we propose a new join algorithm based on the Aho-Corasick algorithm, which significantly reduces the time complexity. For wildcard filtering, we leverage the code-generation infrastructure to improve performance: we generate specialized code for the LIKE predicate, eliminating the overhead of interpreting the pattern per tuple. Our experimental results show that the new wildcard join algorithm significantly outperforms both baseline DuckDB and Umbra, achieving speedups of up to 30.6x and 114.75x, respectively. The new wildcard filter approach likewise outperforms both baselines, achieving a speedup of 13.3x in a filter-focused stress benchmark. We believe these two techniques will play key roles for high-performance text analytics in modern query engines.

</details>


### [39] [EXPLAIN Yourself! Finding Query Planner Stalls Across DBMSes](https://arxiv.org/abs/2608.23402)
*Geoffrey X. Yu,Ryan Marcus,Tim Kraska*

Main category: cs.DB

TL;DR: 本文通过轻量级智能搜索，在七个DBMS（包括四个商业系统）中发现了至少一个查询的规划时间超过三分钟，证明"查询规划是低开销操作"这一常见假设并不总是成立，并分析了导致慢规划的各种病理模式。


<details>
  <summary>Details</summary>
Motivation: 研究人员和实践者通常假设查询规划是低成本的，但这可能是一个错误的假设，慢速规划可能导致资源占用和拒绝服务风险，值得深入调查。

Method: 使用轻量级智能搜索（lightweight agentic search）在七个DBMS中寻找规划时间过长的查询，并对找到的查询进行分析，比较各系统对每种病理模式的响应。

Result: 在七个系统中每个系统至少发现一个查询规划时间超过三分钟；虽然触发慢规划的查询大多因系统而异，但存在共通的病理模式，包括相关子查询、CTE展开、重复子查询表达式、析取连接和常量折叠，这些问题影响了多个系统；同时发布了查询和参数化病理套件供研究者测试。

Conclusion: 查询规划不能总是被视为可预测的低成本操作，其延迟和鲁棒性值得数据库研究者和工程师进一步关注。

Abstract: Query planners are typically expected to produce optimized plans quickly, leading many researchers (including the authors of this paper) and practitioners to design systems that assume query planning is a low-cost operation. Using a lightweight agentic search, we show that this assumption does not always hold. Across seven DBMSes, including four commercial systems, we find at least one query per system that takes more than three minutes to plan. In addition to being slow to plan, such queries risk tying up database resources without performing useful work, creating a potential denial-of-service vector. We analyze the queries our search uncovers and compare how the seven systems respond to each pattern. We find that although the queries triggering slow planning are largely DBMS-specific, recurring pathologies involving correlated subqueries, CTE expansion, repeated subquery expressions, disjunctive joins, and constant folding affect multiple systems. We release our uncovered queries along with a curated suite of parameterized query pathologies that researchers and database engineers can use to test planner robustness. Overall, our results show that query planning cannot always be treated as a predictably inexpensive operation and that its latency and robustness deserve further attention from both database researchers and engineers.

</details>


### [40] [Chimera: Efficient Multi-Vector Retrieval via GPU-CPU Co-Processing](https://arxiv.org/abs/2608.23553)
*Yanqi Chen,Juelin Liu,Alexandra Meliou,Xiao Yan*

Main category: cs.DB

TL;DR: Chimera是一个GPU-CPU协同处理系统，用于多向量检索，通过将低精度量化编码存储在GPU上进行候选生成和过滤，高精度数据保留在CPU内存中，并采用GPU-CPU协作打分方案，完全避免向量数据传输，在相同召回率下实现高达16.0倍的QPS提升。


<details>
  <summary>Details</summary>
Motivation: 多向量检索在语义搜索中至关重要，但计算复杂度和内存成本高。现有GPU系统PLAID受限于CPU-GPU数据传输瓶颈，查询时需要将向量数据从主机内存传输到GPU，导致延迟高。作者希望消除这一传输瓶颈，充分发挥GPU计算优势。

Method: Chimera采用异构架构：将高度压缩的低精度量化码存储在GPU端用于高效的候选生成和过滤，同时将高精度数据保留在CPU内存中。查询时通过GPU-CPU协同打分方案，避免向量数据传输并实现计算重叠。

Result: 在真实数据集上的实验表明，Chimera在相同召回率水平下，QPS比现有方法提升高达16.0倍。

Conclusion: Chimera通过GPU-CPU协同处理和精心设计的存储分层策略，有效消除了多向量检索中的数据迁移瓶颈，显著提升了检索性能，为大规模多向量检索提供了更高效的解决方案。

Abstract: Multi-vector retrieval has become a core primitive in semantic search applications that require fine-grained matching, such as information retrieval, recommender systems and bioinformatics. However, its high computational complexity and memory costs make low-latency retrieval difficult. Prior systems have attempted to optimize query latency, but their designs remain CPU-centric. While GPUs offer substantial computational advantages, their limited memory capacity necessitates a heterogeneous architecture in which the dataset resides in host memory and the GPU serves as an accelerator. Existing GPU-based system, PLAID, is bottlenecked by CPU-GPU data movement, as vector data must be transferred from host memory to the GPU at query time. We propose Chimera, a GPU-CPU co-processing system for multi-vector retrieval that eliminates this transfer bottleneck. Chimera stores highly compressed, low-precision quantization codes on the GPU while maintaining high-precision data in CPU memory. At query time, it leverages GPU-resident data for efficient candidate generation and filtering, and further refines results through a GPU-CPU collaborative scoring scheme that completely avoids vector data transfer while enabling computation overlap. Experiments on real-world datasets demonstrate that Chimera significantly outperforms existing approaches, achieving up to 16.0x higher QPS at the same recall level.

</details>


<div id='cs.DS'></div>

# cs.DS [[Back]](#toc)

### [41] [Graph Spectral Sparsification is in Catalytic Logspace](https://arxiv.org/abs/2608.21594)
*Cassandra Marcussen,Edward Pyne,Ronitt Rubinfeld*

Main category: cs.DS

TL;DR: 本文提出了一种催化对数空间算法用于图的光谱稀疏化，输出包含 O(nε⁻²log n) 条边的 ε-光谱稀疏化器，与 Spielman 和 Srivastava 的有效电阻采样结果匹配，并引入了压缩-或-随机范式中全新的压缩技术。


<details>
  <summary>Details</summary>
Motivation: 为图光谱稀疏化问题寻找催化对数空间算法，这是一个此前未知是否属于确定性 NC 或 SC 的新自然问题，同时为催化对数空间领域提供新的工具。

Method: 核心方法是基于图拟随机性视角构造悲观估计器，该估计器本身可在催化对数空间中计算，并由此得到简单的确定性贪心稀疏化算法；随后将悲观估计器转化为原地字符串压缩算法，利用势函数定义字符串上的测度并实现原地算术编码。

Result: 获得了匹配 Spielman-Srivastava 有效电阻采样边数的催化对数空间光谱稀疏化算法，压缩技术显著区别于以往催化计算领域的所有工具。

Conclusion: 该工作为催化对数空间提供了首个光谱稀疏化算法和全新的压缩技术，有望在其他问题中得到进一步应用。

Abstract: We give a catalytic logspace algorithm for the problem of graph spectral sparsification. Given an undirected graph $G$ on $n$ vertices and $\varepsilon>0$, our algorithm outputs an $\varepsilon$-spectral sparsifier of $G$ with $O(n\varepsilon^{-2}\log n)$ edges, matching the effective resistance sampling of Spielman and Srivastava (STOC 2008). This gives a new, natural problem in catalytic logspace that is not known to be in deterministic $\mathbf{NC}$ or $\mathbf{SC}$. Our main contribution is an entirely new technique in the compress--or--random paradigm for catalytic logspace that we believe will have further applications.
  We first analyze effective-resistance sparsification using a pessimistic estimator that can itself be computed in catalytic logspace. The estimator is motivated by the viewpoint of graph quasirandomness and immediately gives a simple, deterministic greedy algorithm for graph sparsification.
  Subsequently, we show that such a pessimistic estimator can be transformed into an algorithm that performs an in-place compression of a string with bad potential. Our algorithm is based on using the potential function to define a measure over strings, and implementing arithmetic coding using this measure in-place. This compression technique is substantially distinct from all prior tools in the field of catalytic computation.

</details>


### [42] [An 8/5 Rounding for Half-Integral Forest-BCR via Root Supports and Circuit Rank](https://arxiv.org/abs/2608.21739)
*Morteza Alimi*

Main category: cs.DS

TL;DR: 本文研究Steiner Forest问题的根分配二分切割松弛（Forest-BCR）中半整数可行解的舍入问题。Byrka等人提出的递归框架已有16/9的保证率，本文通过保留每个投影半单位LP质量的朝向和根标签，改进分析将保证率提升至8/5，并通过构造实例证明通用投影密度界的渐近紧致性。


<details>
  <summary>Details</summary>
Motivation: Steiner Forest问题是一个经典的组合优化问题，Forest-BCR松弛的半整数可行解舍入是关键挑战。Byrka等人[IPCO 2025]证明了基于最大投影LP密度选择顶点集、购买最小生成树并收缩的递归框架具有16/9的近似率，本文旨在改进这一分析以获得更好的保证率。

Method: 文章保留了每个投影半单位LP质量的朝向和根标签。关键观察：(1) 简单投影中，度为2的终端处的割约束决定了每个关联需求的根分配向量，半整数性将其限制为两种可能——单一根赋值（迫使该根支持内部产生超额出度）或分裂到两个根的赋值（迫使两个根的支持重叠）；(2) 对分裂根图的每个连通分量C，证明β_C ≥ L_C/2（β_C为根支持并集的环秩，L_C为全投影中度为2的顶点数）；(3) 将该不等式得到的密度证书与普通度数和相平衡，得到密度至少5/8的顶点集，再通过继承的收缩引理转化为8/5的舍入保证。(4) 对所有q≥3构造规范化的半整数点，其最大投影密度恰为5q/[2(4q-1)]，证明通用投影密度界的渐近紧致性。

Result: 将上述递归框架的近似保证率从16/9改进到8/5，并证明该通用投影密度界是渐近紧致的（通过构造密度恰为5q/[2(4q-1)]的实例，该值在q→∞时趋近于5/8）。

Conclusion: 通过更精细地追踪半整数解的根分配结构，显著改进了Steiner Forest的Forest-BCR松弛舍入保证率，同时确认了该分析框架在投影密度意义上已达到渐近最优。

Abstract: We study the rounding of a supplied half-integral feasible solution of the root-assignment bidirected cut relaxation for Steiner Forest (Forest-BCR). Byrka, Grandoni, and Traub [IPCO 2025] proved a $16/9$ guarantee for a recursive framework that normalizes the LP point, selects a vertex set of maximum projected LP density, buys a minimum spanning tree on that set, contracts it, and recurses. We prove that the same framework has guarantee $8/5$.
  The new analysis keeps the orientation and the root label of each projected half-unit of LP mass. In a simple projection, the cut constraints at a terminal of degree two determine the root-assignment vector of every demand incident with it, and half-integrality leaves only two possibilities: a unit assignment to one root, which forces excess outdegree inside that root's support, or a split assignment to two roots, which forces overlap between their supports. For every connected component $C$ of the split-root graph this yields $ β_C\ \ge\ \frac{L_C}{2}$, where $β_C$ is the circuit rank of the union of the root supports in $C$ and $L_C$ is the number of its vertices of degree two in the full projection. Balancing the density certificate obtained from this inequality against the ordinary degree sum gives a vertex set of density at least $5/8$, and the inherited contraction lemma turns that into the $8/5$ rounding.
  For every $q\ge3$ we also construct a normalized half-integral point whose maximum projected density is exactly $5q/[2(4q-1)]$, so the universal projected-density bound is asymptotically tight.

</details>


### [43] [The Parameterized Periodicity Lemma](https://arxiv.org/abs/2608.21912)
*Rikuya Hamai,Yuto Nakashima,Shunsuke Inenaga*

Main category: cs.DS

TL;DR: 本文证明了参数化字符串的Fine–Wilf型最优界：若字符串s含σ个不同字母，具有参数化周期p和q，且|s| ≥ p+q+(σ-3)d+1（其中d=gcd(p,q)），则d也是s的参数化周期。同时给出了匹配的下界实例，证明该界对任何σ≥2都是最优的。


<details>
  <summary>Details</summary>
Motivation: Fine–Wilf定理是字符串周期理论中的经典结果，但将其推广到参数化字符串时面临挑战。此前工作（Apostolico和Giancarlo、Ideguchi等人、Hamai等人）逐步改进参数化版本的长度界，但尚未达到最优。本文旨在建立参数化字符串的Fine–Wilf型最优界。

Method: 首先建立参数化字符串的Fine–Wilf型上界：若|s| ≥ p+q+(σ-3)d+1则d是s的参数化周期。然后构造匹配的下界反例实例，证明该长度上界是紧的，即任何更短的长度都不能保证结论成立。

Result: 证明了参数化字符串的Fine–Wilf型最优界为p+q+(σ-3)d+1，并通过构造下界实例证明了该界对任意σ≥2都是最紧的，优于此前的p+q和p+q+min(p,q)(σ-2)等结果。

Conclusion: 本文完整解决了参数化字符串的Fine–Wilf型周期定理的最优长度界问题，为该领域后续研究（如参数化平方的计数等）提供了最优的理论基础。

Abstract: Fine and Wilf [Proc. Amer. Math. Soc. 1965] showed that any string of length at least $p+q-d$ with periods $p$ and $q$ also has period $d=\gcd(p,q)$. For parameterized strings, Apostolico and Giancarlo [Discrete Appl. Math. 2008] proved an analogue with length bound $p+q$, assuming that the two induced bijections commute. Ideguchi et al. [SPIRE 2023] removed this assumption and gave the bound $p+q+\min(p,q)(σ-1)$, where $σ$ is the number of distinct letters. This was later improved by Hamai et al. [SPIRE 2024] to $p+q+\min(p,q)(σ-2)$, which was used to bound the number of non-equivalent parameterized squares. In this paper, we establish the optimal Fine--Wilf type bound for parameterized strings. Namely, if a string $s$ containing $σ$ distinct letters has parameterized periods $p$ and $q$ and satisfies $|s| \ge p+q+(σ-3)d+1$, where $d=\gcd(p,q)$, then $d$ is also a parameterized period of $s$. We also give matching lower-bound instances, proving that our bound is optimal for any $σ\geq 2$.

</details>


### [44] [Computing the Determinant via the Generalized Euclidean Algorithm](https://arxiv.org/abs/2608.21932)
*Janina Reuter*

Main category: cs.DS

TL;DR: 本文提出了一种具有自然几何解释的行列式算法，用于计算 $\mathbb{Z}^{d\times d}$ 矩阵的行列式，比当前最快的确定性算法快约 $d^{0.1213}$ 倍，位复杂度为 $\tilde{O}(d^{\omega(2)}\log\|B\|)$。


<details>
  <summary>Details</summary>
Motivation: 计算整数矩阵的行列式是计算数学中的基本问题。现有的最快确定性算法需要 $\tilde{O}(d^{\omega(1)+1}\log\|B\|)$ 位运算，作者希望通过改进算法来降低复杂度，并利用 Klein 和 Reuter (STOC 2025) 提出的新颖算法思想来加速。

Method: 作者改编了 Klein 和 Reuter 引入的格子基计算算法思想，该思想可视为将欧几里得算法从 $\mathbb{Z}$ 推广到 $\mathbb{Z}^d$。通过将这些技术适配到行列式计算场景，算法具有自然的几何解释，并且其位复杂度等价于对输入矩阵 $A\in\mathbb{Z}^{d\times 2d}$（满足 $\|A\|=\|B\|$）应用广义欧几里得算法的复杂度。

Result: 新算法的位复杂度为 $\tilde{O}(d^{\omega(2)}\log\|B\|)$，相比此前最快的确定性算法 $\tilde{O}(d^{\omega(1)+1}\log\|B\|)$，加速因子为 $d^{\omega(1)+1-\omega(2)}\approx d^{0.1213}$。

Conclusion: 通过将广义欧几里得算法的思想应用于行列式计算，作者成功超越了现有最快的确定性行列式算法，实现了显著的复杂度改进，且算法具有自然的几何直觉。

Abstract: We present an algorithm with a natural geometric interpretation for computing the determinant of a matrix $B\in\mathbb{Z}^{d\times d}$. It improves upon the current fastest deterministic algorithms by a factor of $d^{ω(1)+1-ω(2)}\approx d^{0.1213}$, where $ω(k)$ denotes the exponent required for multiplying a $d\times d$ matrix with a $d\times d^k$ matrix. Our approach builds on a recent result of Klein and Reuter (STOC 2025), who introduced a novel algorithmic idea for lattice basis computation that can be viewed as extending the Euclidean algorithm from $\mathbb{Z}$ to $\mathbb{Z}^d$. By adapting their techniques, we compute the determinant with the same bit complexity as applying the generalized Euclidean algorithm to an input matrix $A\in\mathbb{Z}^{d\times 2d}$ with $\|A\| = \|B\|$, namely $\tilde{O}(d^{ω(2)}\log\|B\|)$. Prior to this work, the fastest deterministic algorithm for computing the determinant required $\tilde{O}(d^{ω(1)+1}\log\|B\|)$ bit operations.

</details>


### [45] [True Work-Efficiency in Parallel Derandomization](https://arxiv.org/abs/2608.21987)
*Mohsen Ghaffari,Cheng Jiang*

Main category: cs.DS

TL;DR: 本文解决了并行去随机化（parallel derandomization）中长期存在的工作量开销问题，将已知技术的开销从 poly(log n) 降低到 poly(log log n)，并最终实现 O(m+n) 的线性工作边界，达到真正的工作高效（work-efficient）并行去随机化。


<details>
  <summary>Details</summary>
Motivation: 已知的并行去随机化技术在小深度（polylogarithmic depth）下至少会引入 poly(log n) 的工作量开销，这导致对于最大独立集、最大匹配和 (Δ+1)-着色等基础问题，需要至少 poly(log n) 个处理器才能胜过单处理器算法。Ghaffari 和 Grunau 最近将其改进到 poly(log log n)，但尚未达到理论上的最优（线性工作）；本文旨在彻底解决这一开放问题。

Method: 本文在 Ghaffari 和 Grunau [FOCS '25] 提出的并行去随机化新方法基础上进一步深化，将工作量开销从 poly(log log n) 降低到常数级别，从而实现对 n 顶点、m 边图上的问题达到 O(m+n) 的线性总工作量边界。

Result: 获得了 O(m+n) 的线性工作边界，实现了真正的工作高效并行去随机化，这是该研究方向的理论最优结果。

Conclusion: 本文为并行去随机化这一研究线画上了句号，证明在保持 polylogarithmic depth 的同时可以达到线性工作量，从而使得这些并行算法在适度处理器数量下也能切实优于单处理器算法。

Abstract: A longstanding limitation of known techniques for parallel derandomization was that they incurred at least polylogarithmic overhead in work. For instance, for fundamental and frequently used problems such as maximal independent set, maximal matching, and $(Δ+1)$-coloring, where $Δ$ denotes the maximum degree of the graph, the best-known deterministic parallel algorithms with polylogarithmic depth used $Ω((m+n)\mathrm{poly}(\log n))$ work on $n$-vertex, $m$-edge graphs; see, e.g., Luby [FOCS '88]. Consequently, at least $\mathrm{poly}(\log n)$ processors were needed for these algorithms to outperform straightforward single-processor algorithms. Recently, Ghaffari and Grunau [FOCS '25] introduced a new parallel derandomization method that substantially reduced the overhead from $\mathrm{poly}(\log n)$ to $\mathrm{poly}(\log\log n)$, achieving work bounds of $O((m+n)\mathrm{poly}(\log\log n))$. In this paper, we settle this line of research by obtaining linear work bounds of $O(m+n)$, thereby achieving truly work-efficient parallel derandomization.

</details>


### [46] [Near-Optimal Bounds for Sketching the Schatten--1 Norm](https://arxiv.org/abs/2608.22247)
*Lin F. Yang*

Main category: cs.DS

TL;DR: 本文研究了用随机化无偏线性测量来估计任意固定 n×n 矩阵核范数所需的最少测量数 k_ε(n)。证明了上下界分别为 n²/(log n)^{A_ε} 和 C_ε·n²{loglog(e^e n)}²/log(e n)，两者在 polylog 因子内紧致，从而几乎解决了 Li, Nguyen, Woodruff (2019) 留下的开放问题，表明复杂度为 n^{2-o(1)}。


<details>
  <summary>Details</summary>
Motivation: 核范数估计是矩阵低秩恢复和压缩感知中的核心问题。此前 Li, Nguyen, Woodruff (2019) 给出的通用线性测量界为 Ω(n) 下界和平凡的 O(n²) 上界，中间存在巨大的间隙。本文旨在缩小这个间隙，确定核范数估计的真正测量复杂度。

Method: 上界：采用固定高斯矩阵做随机测量，解码器结合隐式低秩恢复与对高稳定秩残差的矩估计。下界：构造矩匹配谱（moment-matched spectra），随机化其奇异向量，通过奇数阶张量估计和 Fisher 信息路径论证来比较所有低维观测。

Result: 证明了 k_ε(n) 的下界为 n²/(log n)^{A_ε}，上界为 C_ε n²{loglog(e^e n)}²/log(e n)。上下界在 polylog 因子内紧致，即复杂度为 n^{2-o(1)}。特别地，对任意固定 c>0，O(n^{2-c}) 次测量是不可能的。

Conclusion: 本文几乎完全解决了核范数估计的测量复杂度问题，证明了所需测量数为 n^{2-o(1)}，这与平凡的 O(n²) 上界只差 polylog 因子。这一结果深刻揭示了核范数估计本质上"几乎需要全部矩阵元素"的测量，远高于此前已知的 Ω(n) 下界。

Abstract: Let $k_ε(n)$ be the smallest number of real linear measurements needed by a randomized, oblivious sketch that estimates the nuclear norm of every fixed real $n\times n$ matrix within a factor $1\pmε$, with probability at least $2/3$. For every fixed $0<ε<1$, the proved result is $$
  \frac{n^2}{(\log n)^{A_ε}}
  \;\le\; k_ε(n) \;\le\;
  C_ε\frac{n^2\{\log\log(e^e n)\}^2}{\log(e n)} $$ for all sufficiently large $n$, where $A_ε,C_ε$ depend only on $ε$.
  Previously, the best bounds for general linear sketches were $Ω(n)$ and the trivial $O(n^2)$ upper bound (Li, Nguyen, Woodruff, 2019). The theorem therefore nearly resolves the open measurement-complexity question left by that work: the displayed lower and upper bounds are tight up to polylogarithmic factors. In particular, the complexity is $n^{2-o(1)}$, and for every fixed $c>0$, $O(n^{2-c})$ measurements are impossible. The upper bound is obtained by a fixed Gaussian sketch whose decoder combines implicit low-rank recovery with moment estimation on a high-stable-rank residual. The lower bound constructs moment-matched spectra, randomizes their singular vectors, and compares every low-dimensional observation through an odd-order tensor estimate and a Fisher-information path argument.

</details>


### [47] [Recovery Beats Storage: Improved Space for Preprocessed 3SUM](https://arxiv.org/abs/2608.22355)
*Amir Carmel,Yakov Kosoburd,Robert Krauthgamer*

Main category: cs.DS

TL;DR: 本文针对带预处理且目标未知的3SUM问题，提出了一种统一构造方法，利用Fiat-Naor数据结构按需恢复配对列表，取代了先前Kirkpatrick等人分别处理重轻目标并显式存储配对列表的方案，将空间复杂度改进为Õ(n^max(2-ε, 11/6-ε/3))，同时保持相同的查询时间，并首次在亚二次空间下支持自适应查询。


<details>
  <summary>Details</summary>
Motivation: 先前Kirkpatrick等人[ICALP 2026]首次给出亚二次空间的3SUM预处理算法，但其方案针对重目标和轻目标采用不同机制，且为重目标显式存储求和为目标值的所有配对列表，这些列表主导了空间开销。作者希望统一构造，减少空间占用，并支持自适应查询（先前方案不支持）。

Method: 提出统一构造，对所有查询使用单一机制。核心思想是用Fiat-Naor数据结构[ SICOMP 1999]来反转函数(a,b)↦(a+b mod p)，从而按需恢复所需的配对列表，而不是预先显式存储这些列表。这避免了重目标配对列表的空间开销。

Result: 空间复杂度从Õ(n^(2-2ε/3))改进为Õ(n^max(2-ε, 11/6-ε/3))，查询时间保持Õ(n^(3/2+ε))不变。此外，该构造首次在亚二次空间下支持自适应选择的查询。

Conclusion: 通过利用Fiat-Naor数据结构的按需反转能力，成功简化了3SUM预处理问题的构造，同时改进了空间复杂度并新增了自适应查询支持，为后续相关工作提供了更优的基准。

Abstract: The 3SUM problem asks, given sets $A,B,C$ of integers, whether there exist $a\in A$ and $b\in B$ whose sum belongs to $C$. In the preprocessed variant with unknown $C$, one preprocesses sets $A$ and $B$, each of size $n$, and subsequently answers a query specified by subsets $A'\subseteq A$, $B'\subseteq B$ and a target set $C'$ of size $O(n)$, by solving the 3SUM instance $(A',B',C')$. Kirkpatrick, Kuszmaul, Mathialagan, and Vassilevska Williams [ICALP 2026] gave the first algorithm with subquadratic space for this problem, achieving $\tilde{O}(n^{3/2+ε})$ query time using $\tilde{O}(n^{2-2ε/3})$ space, for every $ε\in[0,1/2]$. Their algorithm employs separate mechanisms for heavy and light targets, and for each heavy target it stores explicitly the list of pairs $(a,b)$ summing to it; these lists dominate the space bound. We present a unified construction that uses a single mechanism for all queries. Instead of storing these lists of pairs, we recover them on demand by leveraging the Fiat--Naor data structure [SICOMP 1999] to invert the function $(a,b)\mapsto (a+b\bmod p)$. This simplification improves the space bound to $\tilde{O}(n^{\max(2-ε, 11/6-ε/3)})$, while maintaining the same query time. Moreover, our construction is the first to achieve subquadratic space while supporting adaptively chosen queries.

</details>


### [48] [Beyond the Static Barrier for Ordinary Dynamic Approximate Membership](https://arxiv.org/abs/2608.22413)
*Qizhi Chen,Zhebei Shen,Zhehan Yu*

Main category: cs.DS

TL;DR: 本文证明了一个严格的空间分离结果：在任意固定错误率ε下，普通动态近似成员查询过滤器所需的持久状态空间H至少为(log₂(1/ε)+a_ε^c)·n - o(n)，其中a_ε^c是显式变分阈值。该证明通过保留父接受质量与后继资源之间的依赖关系实现，核心结构步骤是共同延续输运引理和联合后验KL界。


<details>
  <summary>Details</summary>
Motivation: 动态近似成员查询（dynamic approximate membership）是数据结构领域的核心问题，静态与动态过滤器之间的空间开销差异是长期未解决的严格分离问题。此前的工作往往依赖选择性阈值、二分见证或数值假设，无法在所有固定错误率下给出严格的分离证明。作者希望通过保留条件熵论证中父代外部质量1-X（而非用1-ε替代）来获得更紧的分析上界。

Method: 作者采用信息论+概率论的分析框架。具体包括：(1) 共同延续输运引理（common-continuation transport lemma）作为结构步骤，在合法删除-插入词下将分支特定幸存者支撑输运到单一后继状态，迫使产生被接受资源池；(2) 联合后验KL界给出分支特定的幸存者支撑；(3) 关键的创新在于条件熵论证中保留父代外部质量1-X，而非将其替换为1-ε，形成两变量解析包络（two-variable analytic envelope），无需选择性阈值、二分见证或数值假设。

Result: 对于每个固定ε∈(0,1)，容量为n、宇宙大小为u的普通动态过滤器（具有零假阴性、点态假阳性概率至多ε、任意历史依赖、免费公共随机带、至多H位持久状态），在仅要求u/n→∞的条件下，满足H ≥ (log₂(1/ε)+a_ε^c)·n - o(n)。其中a_ε^c是显式变分阈值，通过保持父接受质量与后继资源之间的依赖获得。

Conclusion: 作者成功地在任意固定错误率下建立了静态与普通动态近似成员查询之间的严格空间分离，且该下界在极弱的假设（仅u/n→∞）下成立。该工作通过保留条件熵论证中父代外部质量的依赖结构，消除了此前证明中对选择性阈值等辅助条件的依赖，使结果具有完全的普遍性。

Abstract: We prove a strict space separation between static and ordinary dynamic approximate membership at every fixed error rate. For each fixed $\varepsilon\in(0,1)$, a capacity-$n$ ordinary dynamic filter over a universe of size $u$, with zero false negatives, pointwise false-positive probability at most $\varepsilon$, arbitrary history dependence, a free public random tape, and at most $H$ bits of persistent state, satisfies \[
  H\ge
  \bigl(\log_2(1/\varepsilon)+a_\varepsilon^{\rm c}\bigr)n-o(n), \] under only $u/n\to\infty$. The constant $a_\varepsilon^{\rm c}$ is an explicit variational threshold obtained by preserving the dependence between the parent accepted mass and the successor reservoir.
  The structural step is a common-continuation transport lemma. A joint posterior KL bound gives a branch-specific survivor support; the same legal delete--insert word transports that support to one successor state, forcing an accepted reservoir. We then keep the parent outside mass $1-X$ in the conditional-entropy argument instead of replacing it by $1-\varepsilon$. This yields a two-variable analytic envelope, with no selected thresholds, dyadic witnesses, or numerical assumptions.

</details>


### [49] [Fast Metric Decompositions in High Dimension](https://arxiv.org/abs/2608.22488)
*Robert Krauthgamer,Asaf Petruschka,Nir Petruschka*

Main category: cs.DS

TL;DR: 本文研究了高维空间中概率度量分解的快速采样算法。针对ℓ∞空间设计了基于稀疏邻域覆盖的填充分解算法，运行时间为Õ(nd²)，填充参数为Õ(log n)；针对ℓ₂空间设计了近最优分离的分解算法，运行时间接近线性n^{1+o(1)}，分离参数为Õ(√log n)。两方面的结果均优于同等运行时间的已知算法，且技术可应用于spanner和最近邻搜索。


<details>
  <summary>Details</summary>
Motivation: 度量分解是涉及距离的算法设计中的基本工具，但在高维空间中快速计算度量分解具有挑战性。现有算法的运行时间或分解质量（如填充/分离参数）不够理想，需要更高效的算法来提升性能。

Method: 对于ℓ∞空间，基于Indyk (JCSS'01)的ℓ∞几何性质构造新的稀疏邻域覆盖，并利用Conroy和Filtser (STOC'25)的最新覆盖-分解归约技术。对于ℓ₂空间，设计了能达到近最优分离参数的分离分解算法，运行时间接近线性。

Result: ℓ∞空间的填充分解算法运行时间为Õ(nd²)（关于n近线性），填充参数为Õ(log n)；ℓ₂空间的分离分解算法达到近最优分离参数Õ(√log n)，运行时间为n^{1+o(1)}（几乎线性）。两个结果均比同运行时间的已知算法改进√log n倍因子。

Conclusion: 本文在ℓ∞和ℓ₂高维空间中实现了更快且质量更优的度量分解算法，其技术可推广至spanner构造和最近邻搜索等应用领域，为该方向提供了重要的理论进展。

Abstract: Metric decompositions are a fundamental tool in the design of algorithms involving distances. We study fast algorithms for sampling from probabilistic metric decompositions of $n$-point sets in $\ell_\infty$ and $\ell_2$ spaces of high dimension $d$. For $\ell_\infty$, we design a padded-decomposition algorithm that runs in time $\tilde{O}(nd^2)$, which is near-linear in $n$, and achieves padding parameter $\tilde{O}(\log n)$. Our algorithm constructs a new sparse neighborhood cover that is based on geometric properties of $\ell_\infty$ [Indyk, JCSS'01], and utilizes recent reductions between covers and decompositions [Conroy and Filtser, STOC'25]. For $\ell_2$, we design a separating-decomposition algorithm that achieves near optimal separation $\tilde{O}(\sqrt{\log n})$ in almost-linear time $n^{1+o(1)}$. Our bounds improve over known algorithms with similar running time by a factor $Ω(\sqrt{\log n})$, and the techniques have additional applications to spanners and nearest-neighbor search.

</details>


### [50] [A tight lower bound for malicious online bipartite matching with limited recourse budget](https://arxiv.org/abs/2608.22524)
*Julia Baligacs,Bartłomiej Bosek,Paweł Putra,Marek Sokołowski,Anna Zych-Pawlewicz*

Main category: cs.DS

TL;DR: 本文研究单侧在线二分图匹配问题（带回溯资源）。作者针对"恶意匹配"设定（每次到达后维护的匹配被替换为最坏情况的匹配）给出了Ω(n log² n)的下界，从而推翻了一个长期存在的猜想，并解决了该恶意变体问题的渐近回溯复杂度。此外，对于扩展图给出了O(n log n)的上界。


<details>
  <summary>Details</summary>
Motivation: 在线二分图匹配的回溯复杂度问题长期悬而未决：下界为Ω(n log n)，上界为O(n log² n)，仅对森林等受限图类有紧的上界。SAP算法被猜想为最优，所有已知上界分析不依赖于算法维护的具体匹配，因此同样适用于更难的"恶意匹配"设定。此前有人猜想恶意设定仍具有O(n log n)的回溯界，本文旨在验证或推翻这一猜想。

Method: 作者针对恶意匹配设定构造了一个下界Ω(n log² n)，通过精心设计的对抗性输入实例，迫使任何算法（即使每次都能获得最坏情况的匹配）都需要至少n log² n次重新分配操作。该下界与已有的上界O(n log² n)相结合，确定了恶意变体问题的精确渐近复杂度。此外，对于扩展图，作者给出了改进的O(n log n)回溯上界。

Result: 1) 证明了恶意匹配设定的下界为Ω(n log² n)，推翻了此前关于恶意设定仍具有O(n log n)回溯界的猜想。2) 结合已有上界，完全解决了恶意匹配变体问题的渐近回溯复杂度（精确为Θ(n log² n)）。3) 对扩展图给出了O(n log n)的紧上界。

Conclusion: 本文的关键发现是：恶意匹配设定与原问题的复杂度存在本质区别——恶意设定需要Θ(n log² n)的回溯代价，严格高于原问题猜想的下界Ω(n log n)。这暗示SAP算法的最优性分析可能需要更精细地利用算法维护的具体匹配结构，而不能仅依赖与具体匹配无关的通用分析。同时，对扩展图的结果进一步揭示了图结构对回溯复杂度的影响。

Abstract: We study one-sided online bipartite matching with recourse. In this setting, one side of a bipartite graph is known in advance, while vertices on the other side arrive online together with their incident edges. After each arrival, the algorithm must maintain a maximum-cardinality matching while minimizing the total number of reallocations, also known as the recourse budget. Despite extensive work, the exact recourse complexity of the problem remains unsettled: the best lower bound is $Ω(n \log n)$, whereas the best upper bound is $\mathcal{O}(n \log^2 n)$, where $n$ denotes the number of online vertices. Tight upper bounds of $\mathcal{O}(n \log n)$ are known only for restricted graph classes, such as forests.
  The best known upper bounds are attained by a very simple and natural algorithm SAP, which after each arrival applies a shortest augmenting path, and it is conjectured to be optimal. All known upper bound analyses of this algorithm do not depend on the particular maximum matching maintained by the algorithm. Consequently, they also apply to a more difficult problem, which we call the malicious matching setting: after each arrival, the maintained matching is replaced by a worst-case maximum matching for the next step. This led to the conjecture that the malicious setting still admits an $\mathcal{O}(n \log n)$ recourse bound, in line with the conjectured optimal complexity of the original model.
  Our main result is an $Ω(n \log^2 n)$ lower bound for the malicious matching setting, thus disproving the conjecture. Together with the previous upper bound, this settles the asymptotic recourse complexity of the malicious variant of the problem. We complement our lower bound with an upper bound of $\mathcal{O}(n \log n)$ for expander graphs.

</details>


### [51] [Asymptotically Tight Fractional Online Matching Under Edge Arrivals](https://arxiv.org/abs/2608.23350)
*David Wajc*

Main category: cs.DS

TL;DR: 本文通过证明分数在线匹配（边到达模型）的最优竞争比为 1/2+Θ(1/n)，填补了已知上下界之间的渐近差距。值得注意的是，该算法由 ChatGPT 根据单一提示提出并分析，作者在数小时的讨论中进行了完善。


<details>
  <summary>Details</summary>
Motivation: 分数在线匹配在边到达模型下的最优竞争比长期存在上下界之间的渐近差距，作者希望精确确定该问题的竞争比，从而彻底解决这一理论问题。

Method: 该算法由 OpenAI 的 ChatGPT 根据单一提示提出并分析，作者通过与 ChatGPT 的多轮往返讨论在数小时内完善了算法呈现。具体数学方法未在摘要中详述，但目标是证明竞争比为 1/2+Θ(1/n)。

Result: 证明了分数在线匹配（边到达模型）的最优竞争比为 1/2+Θ(1/n)，成功闭合了已知上下界之间的渐近差距。

Conclusion: 该问题的最优竞争比已被精确确定，这是对在线匹配理论的一个重要贡献。同时，这项工作也展示了 AI 辅助数学研究（特别是 ChatGPT 作为算法设计与分析工具）的潜力。

Abstract: In this brief note, we close the asymptotic gap between known upper and lower bounds for fractional online matching under edge arrivals. We prove that the optimal competitive ratio for this problem is $1/2+Θ(1/n)$. The algorithm was suggested and analyzed by OpenAI's ChatGPT Sol based on a single prompt. The presentation was streamlined over a few hours, based on a back and forth discussion with the author, who assumes responsibility for any errors.

</details>


### [52] [A Geometric Derivation of the Bitner--Ehrlich--Reingold Loopless Gray Code Algorithm](https://arxiv.org/abs/2608.23357)
*Andrew Au*

Main category: cs.DS

TL;DR: 本文通过几何重建的方式，解释了Bitner-Ehrlich-Reingold（BER）算法中焦点指针数组为何存在及其工作原理。作者将翻转位位置的序列（标尺序列）实现为递归扩展树的中序遍历，通过装饰祖先信息、压缩栈操作、预测栈槽值等步骤，推导出基于分支的循环无生成器，最终引导出BER算法的两个赋值操作。


<details>
  <summary>Details</summary>
Motivation: BER算法以每个码字常数工作量生成二进制反射格雷码，其紧凑更新容易表述，但焦点指针数组为何应该存在、其内在逻辑是什么，缺乏直观解释。作者希望重建一条通往该算法的几何路径，揭示指针结构背后的深层原理。

Method: 方法包括五个重建步骤：（1）将翻转位位置的有限前缀（标尺序列，OEIS A007814）实现为递归扩展树的中序遍历；（2）用最近的右侧祖先装饰每次激活；（3）将相邻输出之间的所有栈操作压缩为后继跳转；（4）通过预测未来槽值来准备有界层级索引栈；（5）将左右子节点的两种未来角色（继承延续与默认延续）一起调度，导出BER算法的两个赋值。

Result: 给出了经过证明的基于分支的循环无（loopless）生成器，并展示了每个栈槽写入都可视为对同一层级下一次激活的预测。通过将水平位置视为时间，直观解释了焦点指针算法的两个赋值操作的由来。

Conclusion: 作者申明这是一篇重建推导，而非对Bitner、Ehrlich或Reingold历史推理过程的声称。该工作为理解BER焦点指针算法提供了几何直观和构造性证明，源于作者此前非正式公开阐述的扩展。

Abstract: The Bitner--Ehrlich--Reingold algorithm generates the binary reflected Gray code with constant work per codeword, using a focus-pointer array. Its compact update is easy to state but gives little indication of why such pointers should exist. This note reconstructs a geometric route to the algorithm. The sequence of flipped bit positions is the ruler sequence, OEIS A007814. We realize its finite prefixes as in-order traversals of recursively expanding trees, decorate each activation with its nearest ancestor to the right, contract all stack operations between successive outputs into successor jumps, and prepare the bounded level-indexed stack by predicting its future slot values. This gives and proves a branch-based loopless generator. The construction grew from an earlier informal public exposition by the author. Viewing horizontal position in the tree as time then explains each stack-slot write as a prediction for the next activation at the same level. Finally, the two possible future roles---an inherited continuation for a right child and a default continuation for a left child---are scheduled together, leading to the two assignments of the published focus-pointer algorithm. The account is a reconstructed derivation, not a claim about the historical reasoning of Bitner, Ehrlich, or Reingold.

</details>


### [53] [Linear Hashing is Not That Awesome](https://arxiv.org/abs/2608.23502)
*Or Zamir*

Main category: cs.DS

TL;DR: 本文研究了线性哈希族（$h(x)=((ax+b)\bmod p)\bmod m$）在将n个元素哈希到m=Θ(n)个桶时的最大桶负载问题。作者证明了对于任意足够大的素数p，存在n个键的集合，其期望最大负载为$n^{Ω(1/\log\log n)}$，从而证明线性哈希不具有多对数级别的最大负载保证。该下界同样适用于经典的multiply-shift哈希族。作者通过将最大负载问题与算术Kakeya集的密度变体建立等价关系来完成证明，并利用Green和Ruzsa关于包含长等差数列的小集合的构造。


<details>
  <summary>Details</summary>
Motivation: 通用哈希族中，最大桶的期望大小已知的平凡下界为Ω(log n/log log n)，平凡上界为O(√n)。对于线性哈希，此前最好的改进是Knudsen在2019年给出的$\tilde{O}(n^{1/3})$上界。一个核心开放问题是线性哈希的最大负载是否能达到polylogarithmic级别，本文试图回答这个问题的否定面。

Method: 作者的核心技术贡献是将最大负载问题与一种算术Kakeya集的密度变体之间建立等价关系。随后，利用Green和Ruzsa构造的包含长等差数列的小集合来完成下界证明。具体地，他们构造了一组键，使得线性哈希在这些键上的期望最大负载为$n^{Ω(1/\log\log n)}$。此外，该等价关系还揭示了：任何对Knudsen上界的实质性改进都将意味着关于标准算术Kakeya集的新结果。

Result: 作者证明了对于任意充分大于n的素数p，存在n个键使得线性哈希的期望最大负载为$n^{Ω(1/\log\log n)}$，该下界排除了线性哈希具有polylogarithmic最大负载的可能性。同样的下界也推广到了multiply-shift哈希族。此外，作者建立了最大负载问题与算术Kakeya集密度变体之间的等价关系，这一关系具有进一步的理论意义。

Conclusion: 本文证明了线性哈希族和multiply-shift哈希族在多对数最大负载上是不可能的，其下界为$n^{Ω(1/\log\log n)}$。更引人注目的是，等价关系的建立表明，要改进Knudsen的$\tilde{O}(n^{1/3})$上界，本质上需要解决关于标准算术Kakeya集的新问题，这揭示了哈希理论与加法组合学之间深刻而有趣的联系。

Abstract: Consider the canonical universal hash family $h(x)= ((ax+b)\text{ mod } p)\text{ mod } m$, where $a,b$ are chosen uniformly from $\mathbb Z_p$, which we call linear hashing, being used to hash $n$ elements into $m=Θ(n)$ buckets. For any universal family, the expected size of the largest bucket is at least $Ω(\log n / \log\log n)$ and at most $O(\sqrt{n})$. The only improvement upon these trivial bounds for linear hashing is a 2019 upper bound of $\tilde{O}(n^{1/3})$ by Knudsen. We show that for any $p$ sufficiently larger than $n$, there is a set of $n$ keys whose expected maximum load is $n^{Ω(1/\log\log n)}$, proving linear hashing does not have a polylogarithmic maximum load. We extend the same bounds to the classical multiply-shift hash family of Dietzfelbinger, Hagerup, Katajainen, and Penttonen.
  We prove an equivalence between the maximum load problem to a density variant of arithmetic Kakeya sets. We then complete the lower bound using a construction of Green and Ruzsa of a small set containing long arithmetic progressions with every difference in a prescribed range. Surprisingly, our equivalence also implies that any substantial improvement over Knudsen's upper bound would imply new results about standard arithmetic Kakeya sets.

</details>


<div id='cs.GT'></div>

# cs.GT [[Back]](#toc)

### [54] [Metric Distortion of Social Welfare Functions](https://arxiv.org/abs/2608.21790)
*Fatih Erdem Kizilkaya,Aaryaman Aggarwal,Evi Micha*

Main category: cs.GT

TL;DR: 本文首次将度量失真（metric distortion）从单一赢家选择扩展到社会福利函数（输出候选人的完整排序），通过位置权重向量统一了单赢家投票和委员会选举。研究三种信息体制：已知权重向量（递归扩展给出3倍最优失真）、共享未知权重（界限为1+(β-1)·range(w)）、以及异质未知权重（两种归一化下最优失真为Θ(m)）。


<details>
  <summary>Details</summary>
Motivation: 传统度量失真理论只关注选择单一赢家的社会选择函数，但在许多实际场景中需要输出完整的候选人排名（如委员会选举、排序聚合），而非仅选出最优者。作者希望将度量失真框架推广到社会福利函数，并理解不同信息体制下排名输出带来的额外失真代价。

Method: 提出位置权重向量模型，定义排名的代价为位置加权距离之和。关键技术包括：递归应用单赢家规则逐位构建排名；分析递归扩展的失真上界（3β）；利用Fractional Veto的结构特性证明其最优性；针对未知权重场景提出归一化假设（unit-sum和unit-top）并给出上下界分析。

Result: 已知权重时，递归扩展任何失真为β的单赢家规则得到至多3β的失真，且3β不可改进（Fractional Veto的递归扩展达到最优失真3）；共享未知权重时失真上界为1+(β-1)·range(w)；异质未知权重时，在unit-sum和unit-top两种归一化下最优失真均为Θ(m)。

Conclusion: 该工作将度量失真理论系统性地扩展到排序输出场景，揭示了从单赢家到完整排序的失真放大因子，并为不同信息体制下的最优算法提供了理论边界，对理解排序聚合的近似最优性具有重要意义。

Abstract: Metric distortion has primarily been studied for social choice functions, which select a single winner from ordinal preferences. We extend this framework to social welfare functions, which output a ranking of $m$ candidates. We associate each voter $v$ with a monotone weight vector $\mathbf{w}_v = (w_{v1},\ldots,w_{vm})$, specifying the importance of the $i$-th position for voter $v$, and define the cost of a ranking as the position-weighted sum of their distances to the ranked candidates. This model generalizes both single-winner voting and committee selection. We consider three information regimes.
  First, we study the setting where the positional weight vectors are known. A natural approach recursively applies a single-winner rule with distortion $β$ to construct the ranking one position at a time. We show that this yields distortion at most $3β$ in general. This gap is not merely an artifact of the analysis: we show that no analysis based solely on per-round guarantees can certify a bound better than $2β$. By exploiting structural properties specific to Fractional Veto of Kizilkaya and Kempe, we show that its recursive extension achieves the optimal distortion of 3.
  Second, when all voters share the same unknown weight vector, recursively applying any social choice function with distortion $β$ achieves distortion at most $1+(β-1)\text{range}(\mathbf{w})$, where $\text{range}(\mathbf{w})=(w_1-w_m)/w_1$ denotes the normalized range of the common weight vector $\mathbf{w}$. Finally, we study unknown heterogeneous weights. Without further assumptions, every rule has unbounded distortion. We therefore consider two natural normalizations: unit-sum, where each voter distributes one unit of value across the ranking, and unit-top, where every voter assigns unit value to the first position. Under both models, we show that the optimal distortion is $Θ(m)$.

</details>


### [55] [The Complexity of Minimizing Subsidies in Envy-Free House Allocation](https://arxiv.org/abs/2608.22216)
*Sijia Dai,Minming Li,Xiaowei Wu,Yong Zhang*

Main category: cs.GT

TL;DR: 本文研究房屋分配问题中的无嫉妒公平分配，通过引入补贴（subsidy）来实现无嫉妒分配并最小化总补贴。对于二元效用，证明总补贴上界为(n-1)且紧确；证明最小化补贴问题在二元效用下也是NP难的；但对有限类型代理的二元效用可在多项式时间求解；最后给出了两类代理一般效用的多项式时间算法。


<details>
  <summary>Details</summary>
Motivation: 房屋分配问题是一类经典的单边匹配问题，无嫉妒性（envy-freeness）是最广泛采用的公平标准之一。然而无嫉妒分配并不总是存在，因此需要引入补贴来解决这一挑战，并希望以最小总补贴实现无嫉妒分配，这在理论上和实际应用中都具有重要意义。

Method: 本文从理论复杂度角度系统分析了问题：(1) 对二元实例证明总补贴上界(n-1)的充分性及紧确性；(2) 通过归约证明最小化总补贴在二元效用下同样是NP难的；(3) 对有限类型代理的二元效用给出多项式时间算法；(4) 对两类代理的一般效用设计多项式时间算法求解最小补贴。

Result: 得到了以下关键结果：二元效用下总补贴至多(n-1)即可保证无嫉妒且该界紧确；最小化总补贴在二元效用下是NP难的；有界类型数量的二元效用问题可多项式求解；两类代理的一般效用问题存在多项式时间算法。

Conclusion: 本文完整刻画了补贴下无嫉妒房屋分配问题的复杂度图景：从一般情形的NP难到有界类型的多项式可解，并在二元效用与一般效用之间进行了细致的复杂度区分，为该问题提供了坚实的理论基础。

Abstract: The house allocation problem is a classical one-sided matching problem that concerns the assignment of a set of $m$ houses to $n$ agents according to their preferences, where each agent is assigned exactly one house. Among the various objectives studied in this setting, envy-freeness is one of the most widely adopted fairness criteria. As envy-free house allocations do not always exist, we address this challenge by introducing subsidies and aim to compute allocations that achieve envy-freeness with minimum total subsidy. For binary instances, we show that a total subsidy of at most $(n-1)$ suffices to guarantee envy-freeness in house allocation, and this bound is tight. Building on the known NP-hardness for general utilities, we further show that computing an allocation that minimizes the total subsidy is NP-hard, even under binary utilities. However, when there are only a bounded number of types of agents with binary utilities, the problem can be solved in polynomial time. Finally, we present a polynomial time algorithm that computes the minimum subsidy required to achieve envy-freeness for two types of agents with general utilities.

</details>


### [56] [On Deterministic Optimal Mechanisms in a Two-Item Setting for Distributions with Nondecreasing Density](https://arxiv.org/abs/2608.22371)
*Thirumulanathan D*

Main category: cs.GT

TL;DR: 本文研究了在两家异质商品出售给单一买家的情况下，设计收益最优拍卖机制的问题。当至少一个商品的最低估值足够高时，最优机制是确定性的。论文提供了计算阈值的方法，并给出了个体销售机制为最优的充分条件。此外，论文还提出了该结果可扩展到三商品情形的猜想。


<details>
  <summary>Details</summary>
Motivation: 异质多商品下的最优拍卖机制设计是机制设计领域的经典难题。与同质商品不同，异质商品的最优机制往往涉及复杂的捆绑销售和随机化策略。本文的核心动机在于找出在何种条件下，确定性的（更简单的）机制即为最优，从而简化机制设计和计算复杂度。

Method: 论文假定买家对两种商品的估值独立且分布密度为正、非递减且在支撑集上连续可微。通过分析最低估值（c1或c2）的阈值条件，证明当至少一个最低估值足够高时最优机制为确定性的。同时提供了一种计算该阈值的方法，并给出了个体销售机制（而非捆绑销售）为最优的充分条件。

Result: 主要结果表明：当至少一个商品的最低估值足够高时，最优机制是确定性的；当c1低而c2高时，最优策略是以最低估值c2出售商品2，从而将问题简化为仅关于商品1的一维最优机制问题。论文还提出了一个猜想：在三商品情形下，若c1和c2低而c3高，则以最低估值c3出售商品3，问题可简化为二维情形。

Conclusion: 论文通过界定最低估值的阈值，识别出一类简化情形下的最优机制结构，为异质商品拍卖机制设计提供了理论指导。三商品情形的猜想虽有初步结果支撑，但仍待进一步验证和完善。

Abstract: Consider the problem of designing a revenue-optimal auction mechanism when two heterogeneous items are sold to a single buyer having independent valuations over the items. The distributions of the buyer's valuation for the items are assumed to have densities that are positive, nondecreasing, and continuously differentiable on their support sets $[c_i,c_i+b_i]$ in the positive axis. I prove that the optimal mechanism is deterministic if at least one of the minimum valuations (i.e., either $c_1$ or $c_2$) is sufficiently high. I provide a method to calculate the threshold of $(c_1,c_2)$ beyond which the optimal mechanism is deterministic. I also provide a sufficient condition on the distributions of buyer's valuations for which the individual sale mechanism is optimal.
  I show that when $c_1$ is low and $c_2$ is high, it is optimal for the seller to sell item $2$ at the minimum valuation $c_2$, thus effectively reducing the problem to finding the optimal mechanism in the one-dimensional setting only for item $1$. I conjecture with promising preliminary results that this result can be extended to the three-item setting. Specifically, I conjecture that when $c_1$ and $c_2$ are low but $c_3$ is high, it is optimal for the seller to sell item $3$ at the minimum valuation $c_3$, thus effectively reducing the problem to finding the optimal mechanism in the two-dimensional setting for items $1$ and $2$.

</details>


### [57] [Randomized Strategyproof Facility Location: Two Facilities and Beyond](https://arxiv.org/abs/2608.22484)
*Hau Chan,Jianan Lin,Chenhao Wang*

Main category: cs.GT

TL;DR: 本文研究了多设施选址问题中的随机策略证明机制。针对两个设施，提出了配对距离机制（Pairwise-Distance），在Ptolemaic空间（包括欧几里得空间和Hilbert空间）上策略证明且近似比为4；进一步提出混合距离机制（Hybrid-Distance），以固定概率混合比例机制与配对距离机制，近似比紧为约3.5186，突破了长期以来的因子4基准。对于更多设施的情形，提出了Inverse-Square机制（n个代理、k=n-1个设施）在任意度量空间上策略证明且近似比为Θ(√n)；以及Gap-Product机制（线上k个设施），当k=3时策略证明且具有6-近似比，而对k≥4不策略证明。


<details>
  <summary>Details</summary>
Motivation: 多设施选址是社会选择理论中的经典问题，目标是设计策略证明机制（激励代理如实报告位置）同时最小化社会成本（代理到最近设施的距离之和）。此前[Lu et al., EC'10]的机制仅有因子4的近似比，长期未获突破；同时对于更多设施的情形，已有机制的近似比高度依赖代理数量n，缺乏常数保证。

Method: (1) Pairwise-Distance机制：按报告位置对之间距离成比例的概率采样一对位置作为设施位置。(2) Hybrid-Distance机制：以固定概率λ*选择经典Proportional机制，以1-λ*选择Pairwise-Distance机制。(3) Inverse-Square机制：以最近邻距离平方的反比概率省略一个报告，其余报告作为设施位置。(4) Gap-Product机制：选择k个报告，每个选择集合的权重为连续选中报告之间间隔的乘积。

Result: (1) Pairwise-Distance在Ptolemaic空间上策略证明，近似比为4。(2) Hybrid-Distance在Ptolemaic空间上策略证明，具有紧近似比(74+4√3)/23≈3.5186，突破因子4基准。(3) Inverse-Square机制在任意度量空间上策略证明，近似比为Θ(√n)，优于此前n/2的比率。(4) Gap-Product机制在k=3时策略证明且为6-近似，对k≥4不策略证明。

Conclusion: 本文通过精巧的概率设计方案，在多个维度上推进了多设施选址问题的策略证明机制研究：首次打破了两设施问题长期以来的因子4近似壁垒；将设施数量接近代理数量的场景的近似比从O(n)大幅改善至O(√n)；并在直线上三设施问题中首次给出常数近似比的策略证明机制。同时明确刻画了Gap-Product机制策略证明性的边界（k≤3）。

Abstract: We design and analyze randomized strategyproof mechanisms for multi-facility location under the utilitarian social-cost objective, the sum of the agents' distances to their nearest facilities. For two facilities, the Pairwise-Distance mechanism locates facilities at a pair of reported locations sampled with probability proportional to their distance. It is strategyproof on Ptolemaic spaces, including Euclidean and Hilbert spaces as special cases, and has an approximation ratio of \(4\). The resulting Hybrid-Distance mechanism is a fixed-probability mixture: it selects the classical Proportional mechanism [Lu et al., EC'10] with probability \(λ^*=\frac{5+4\sqrt3}{23}\) and Pairwise-Distance with probability $1-λ^*$. It is strategyproof on Ptolemaic spaces and has a tight approximation ratio of \(\frac{74+4\sqrt3}{23}\approx3.5186\), breaking the long-standing factor-\(4\) benchmark of [Lu et al., EC'10].
  We complement the two-facility results by studying more facilities. First, for \(n\) agents and \(k=n-1\) facilities, we introduce the Inverse-Square mechanism, which omits one report with probability proportional to the inverse square of its nearest-neighbor distance and locates facilities at all remaining reports. It is strategyproof on any metric space and has an approximation ratio of \(Θ(\sqrt{n})\), improving the previous best-known ratio of \(\frac{n}{2}\) [Escoffier et al., ADT'11]. Second, for $k$ facilities on the line, we introduce the Gap-Product mechanism, which locates facilities at \(k\) reports and weights each set by the product of the gaps between consecutive selected reports. When \(k=3\), it is strategyproof and has a \(6\)-approximation, replacing the previous \(n\)-dependent guarantee [Fotakis and Tzamos, EC'13] by a constant, whereas it is not strategyproof for any \(k\ge4\).

</details>


### [58] [Equilibrium in Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2608.22840)
*Maurizio D'Andrea,Bar Light*

Main category: cs.GT

TL;DR: 本文提出了一种新的随机博弈解概念——马尔可夫贝叶斯粗略相关均衡（MBCCE），并证明了标准的分散式强化学习算法（如异步actor-critic和投影策略梯度法）在有限时间内能收敛到近似MBCCE。作者引入了一个匹配的遗憾概念（自适应马尔可夫粗略遗憾，AMCR），并展示了该遗憾的消失与MBCCE之间的联系。


<details>
  <summary>Details</summary>
Motivation: 标准随机博弈解概念（如马尔可夫完美均衡和马尔可夫粗略相关均衡）计算上非常困难，导致标准的分散式强化学习算法通常不被期望收敛到这些均衡。因此需要研究这些算法实际生成的均衡是什么样的。

Method: 作者定义了一种新的解概念MBCCE（在观察状态后、行动建议前，没有玩家能通过改变当前行动获益的分布），建立了与有限正规型博弈中CCE的类比，引入了AMCR遗憾概念，并证明AMCR消失等价于经验分布收敛到MBCCE。关键在于将实现AMCR归结为两个标准学习任务：各状态的外部遗憾最小化和当前联合策略的准确评估。作者随后通过两种算法（一种基于新双时间尺度随机逼近分析的分散式异步actor-critic算法，以及标准的episodic多智能体投影策略梯度法）验证了这些条件。

Result: 在温和条件下，两种自然设计的RL算法（分散式异步actor-critic和episodic多智能体投影策略梯度）都能生成近似MBCCE，且作者为两者建立了明确的有限时间收敛速率。

Conclusion: 作者提出了一种可行且可学习的随机博弈均衡概念MBCCE，弥合了理论上计算困难的均衡解概念与实践中标准分散式强化学习算法实际行为之间的鸿沟。

Abstract: Standard solution concepts for stochastic games, such as Markov perfect equilibrium and Markov coarse correlated equilibrium, are computationally difficult, and thus, standard decentralized reinforcement-learning algorithms should not generally be expected to converge to them. In this paper, we study the equilibrium generated by such algorithms. In particular, we introduce a new solution concept for stochastic games, Markov Bayes coarse correlated equilibrium (MBCCE), defined as a distribution over states and stationary policy profiles such that, after observing the state but before observing her recommended action, no player can gain by choosing a different current action, with the sampled policy profile governing play thereafter. We discuss the parallels between MBCCE and coarse correlated equilibrium (CCE) in finite normal-form games and show that MBCCE retains several of its key properties. We then introduce a corresponding regret notion, adaptive Markov coarse regret (AMCR), and show that vanishing AMCR implies that every accumulation point of the empirical distribution of realized states and policy profiles is an MBCCE. Crucially, we show that achieving AMCR reduces to two standard learning tasks: minimizing external regret at each state and accurately evaluating the current joint policy. We then prove that under mild conditions these properties hold for two natural RL algorithmic designs: a decentralized asynchronous actor--critic algorithm through a new two-timescale stochastic-approximation analysis, and a standard episodic multi-agent projected policy-gradient method. Hence, both algorithms generate approximate MBCCEs, and we establish explicit finite-time convergence rates for both.

</details>


### [59] [Multi-Winner Voting with Argumentative Ballots](https://arxiv.org/abs/2608.23247)
*Ryuta Arisaka,Hirotaka Ono*

Main category: cs.GT

TL;DR: 本文提出了带有论证性选票的多赢家投票（MVArg），将审批投票推广为论证性选票，允许选民表达可废止的偏好，并系统研究了其理论性质，包括投票者凝聚力、正当代表公理（JR、PJR、EJR）的推广，以及相关的复杂度与可满足性结果，全部在Lean 4中形式化验证。


<details>
  <summary>Details</summary>
Motivation: 传统多赢家投票（MV）使用审批投票，选民只能简单表达"支持"或"不支持"，无法表达更细致、可废止的偏好。为提升表达力，作者引入论证性投票，使选民能够表达候选人间带有论证关系的偏好，从而更贴合现实决策场景。

Method: 1) 概念上将审批投票推广为论证性投票（argumentative ballots），允许表达可废止偏好；2) 推广了投票者凝聚力和正当代表公理JR、PJR、EJR到论证性框架；3) 通过形式化证明（Lean 4）建立多项理论结果，包括表达力对比、公理满足性及计算复杂度分析。

Result: 1) MVArg严格比审批投票的多赢家投票（MV）更具表达力；2) 凝聚力与正当代表概念是MV对应概念的直接推广（保守推广）；3) MVArg版的JR总能被满足，但PJR和EJR不一定总能满足；4) 验证JR满足性已属coNP-hard，但仍可在多项式时间内构造出满足JR的赢家集合；全部结果在Lean 4中机械验证通过。

Conclusion: MVArg作为更具表达力的投票框架，在保持JR可满足性与可构造性的同时，PJR和EJR的不可满足性揭示了表达力提升带来的理论代价。形式化验证确保了所有结果的可靠性，为后续研究论证性投票的公理体系奠定了基础。

Abstract: We introduce multi-winner voting with argumentative ballots (MVArg) and investigate theoretical properties. As our conceptual contribution, we generalise approval ballots to argumentative ballots, thereby allowing voters to express defeasible preferences over candidates. We accordingly generalise voter cohesion and justified representation axioms JR, PJR and EJR. As our theoretical contribution, we establish several key results. First, MVArg is strictly more expressive than multi-winner voting with approval ballots (MV). Second, our notions of cohesion and justified representation are conservative generalisations of their counterparts in MV. Third, the MVArg counterpart of JR can always be satisfied, whereas the counterparts of PJR and EJR cannot always be. Fourth, although verifying whether a winner set satisfies the MVArg counterpart of JR is already coNP-hard, such a winner set can be constructed in polynomial time. All definitions, propositions, auxiliary lemmas and theorems have been formalised and mechanically checked in Lean 4.

</details>


<div id='cs.IT'></div>

# cs.IT [[Back]](#toc)

### [60] [Average Finite-Blocklength Packet Error Rate over Nakagami-$m$ Fading via a Logistic--Lerch Approximation](https://arxiv.org/abs/2608.21564)
*Aamir Mahmood*

Main category: cs.IT

TL;DR: 本文针对有限块长（FBL）编码在Nakagami-m衰落信道下的平均分组错误率（PER）评估问题，提出用斜率匹配的Logistic函数近似条件瀑布曲线，使其Nakagami-m平均化为单一Lerch超越函数项，在经典中断极限与FBL瀑布之间插值，且闭合形式支持非整数衰落参数，并可组合为有效容量目标实现QoS感知的速率选择。


<details>
  <summary>Details</summary>
Motivation: 有限块长编码在衰落信道上的平均PER评估需要对正态近似给出的块错误瀑布曲线关于衰落分布积分，而对一般Nakagami-m信道该积分不可解，缺乏闭合表达式。

Method: 用斜率匹配的Logistic函数近似条件瀑布曲线，推导其Nakagami-m平均化为单一Lerch超越函数项；给出关于速率和块长的显式归一化常数；支持非整数衰落参数；将该近似组合为有效容量目标以制定QoS感知的速率选择规则。

Result: 该闭合形式在标称FBL工作区域内对m均匀地以约1%精度匹配正态近似积分，而中断基线和线性化基线在高分集度时误差超过几个百分点。

Conclusion: 所提Logistic近似闭合形式在精度上显著优于中断基线和线性化基线，并在支持非整数m及QoS感知速率选择方面提供额外价值。

Abstract: Evaluating the average packet error rate (PER) of finite-blocklength (FBL) coded transmission over fading requires integrating the block-error waterfall, given by the normal approximation, against the fading distribution, which is intractable for general Nakagami-$m$ channels. This letter approximates the conditional waterfall by a slope-matched logistic function and shows that its Nakagami-$m$ average reduces to a single Lerch-transcendent term that interpolates between the FBL waterfall and the classical outage limit, with norming constants explicit in rate and blocklength. The closed form supports non-integer fading and, composed into an effective-capacity objective, yields a quality-of-service (QoS) aware rate-selection rule. It matches the normal-approximation integral to about 1\% uniformly in $m$ over the nominal FBL operating region, while outage and linearization baselines exceed several percent at high diversity.

</details>


### [61] [Construction and Design of MPAC Codes](https://arxiv.org/abs/2608.21757)
*Fangbo Yi,Zuoxin Cai,Zhongjun Yang,Li Chen,Huazi Zhang,Wenxin Liu,Yuan Li*

Main category: cs.IT

TL;DR: 本文提出改进极化调整卷积（MPAC）码及其混合译码方案，在性能-复杂度权衡上优于原有PAC码和CRC-polar码。MPAC码仅对部分信息比特进行卷积变换，配合混合Fano-连续消除（HFSC）译码，并通过coset-wise分析优化最小权重码字数量，在相近复杂度下取得更优性能。


<details>
  <summary>Details</summary>
Motivation: PAC码虽然性能优异，但其复杂度过高；CRC-polar码在性能与复杂度之间也难以达到最优平衡。作者希望探索一种部分卷积变换的方案，在保留PAC码性能优势的同时降低译码复杂度，实现更好的性能-复杂度权衡。

Method: 1) 提出MPAC码：仅对部分信息比特施加卷积变换，其余比特直接进入内层极化变换；2) 设计混合Fano-连续消除（HFSC）译码：卷积变换比特用Fano译码恢复，其余比特用SC译码恢复；3) 通过coset-wise分析刻画最小权重码字（MWC）数量，发现部分卷积变换能更有效地利用冻结集的行组合来改善码字；4) 通过减少MWC数量对MPAC码进行优化设计。

Result: 数值结果表明，在相似的译码复杂度预算下，MPAC码相比使用Fano译码的PAC码和使用SCL译码的CRC-polar码，提供了有竞争力的译码性能，同时整体复杂度更低。

Conclusion: 部分卷积变换（MPAC）结合混合译码（HFSC）是一种有效的折中方案，能够在不显著牺牲性能的前提下降低译码复杂度，优于其原型PAC码和CRC-polar码。

Abstract: This paper proposes modified polarization-adjusted convolutional (MPAC) codes and their hybrid decoding that achieves an improved performance-complexity tradeoff. For MPAC codes, only a subset of the information bits undergo the convolutional transform. The output is then combined with the remaining information bits for the inner polar transform. Correspondingly, the convolutionally transformed bits are recovered by Fano decoding, while the remaining information bits are recovered by the successive cancellation (SC) decoding, constituting the hybrid Fano-successive cancellation (HFSC) decoding. The MPAC codes are further designed by the coset-wise analysis that characterizes the number of minimum weight codewords (MWCs). It is discovered that a partially convolutional transform can improve the codeword through utilizing the row combinations of the frozen set efficiently. This property enables the MPAC codes to outperform their prototype polarization-adjusted convolutional (PAC) codes and cyclic redundancy check (CRC)-polar codes. Furthermore, MPAC codes can be optimized by reducing the number of MWCs. Our numerical results demonstrate that, with a similar decoding complexity budget, the MPAC codes offer competent decoding performance when compared with PAC codes using Fano decoding and CRC-polar codes using SC list (SCL) decoding.

</details>


### [62] [Quantum Locally Repairable Codes from Negacyclic and Repeated-Root Cyclic Codes over Small Fields](https://arxiv.org/abs/2608.21852)
*Ruipan Yang,Qiang Fu,Liangdong Lu*

Main category: cs.IT

TL;DR: 本文系统研究了通过CSS构造从小域（q∈{2,3,4,5,7}）上的经典constacyclic码得到的量子局部可恢复码（qLRCs）。作者证明了非零对偶包含的λ-constacyclic码仅当λ²=1时存在，揭示了constacyclic码的局部性等于其对偶码最小距离减一，并给出了纯度判据。通过duadic码和二次剩余码构造了无限族纯qLRCs，并通过具体计算分类了中等长度的qLRCs参数。


<details>
  <summary>Details</summary>
Motivation: 量子局部可恢复码（qLRCs）是量子纠错与分布式存储交叉领域的新对象，但大多数已知构造需要大的字母表。本文旨在系统研究小域上基于constacyclic码的qLRC构造，探索能否在小字母表下获得好的参数。

Method: 通过CSS构造，将经典constacyclic码转化为量子码。首先证明constacyclic码对偶包含的条件限制（λ²=1，即退化为negacyclic和repeated-root cyclic码）。然后建立constacyclic码的局部性与其对偶码最小距离之间的联系，并给出量子码的纯度判据。利用奇似duadic码（其分裂由μ₋₁给出）构造纯qLRCs，并特化到q元二次剩余码（素数长度p≡3 mod 4）获得无限族。最后通过具体计算对中等长度码进行参数分类。

Result: 推导出：非零对偶包含λ-constacyclic码仅存在于λ²=1的情况；constacyclic码的局部性等于对偶码最小距离减一；奇似duadic码产生纯qLRCs；q元二次剩余码给出无限族纯qLRCs，具有无界最小距离和可认证的局部性。计算分类获得了第一批来自repeated-root cyclic码的二进制qLRCs，以及许多cyclic码无法达到的参数集。

Conclusion: 研究表明constacyclic码为小域上构造qLRCs提供了一个系统且有效的途径，特别是duadic码和二次剩余码能产生无限族具有良好参数的纯qLRCs，而repeated-root cyclic码则提供了cyclic码无法覆盖的额外参数集。

Abstract: Quantum locally recoverable codes (qLRCs), introduced recently by Golowich and Guruswami, allow any single-qudit erasure to be recovered from a small set of other qudits. Most known constructions require a large alphabet. We systematically investigate qLRCs obtained, via the CSS construction, from classical constacyclic codes over small fields $\Ff_q$ with $q\in\{2,3,4,5,7\}$. First, we prove that a nonzero dual-containing $λ$-constacyclic code exists only when $λ^2=1$, so that negacyclic and (repeated-root) cyclic codes exhaust the constacyclic route to qLRCs. Second, we show that the locality of a constacyclic code equals the minimum distance of its dual minus one, and we give a simple purity criterion for the resulting quantum codes. Third, we show that odd-like duadic codes whose splitting is given by $μ_{-1}$ yield pure qLRCs; specializing to $q$-ary quadratic residue codes of prime length $p\equiv 3 \pmod 4$ gives an infinite family of pure qLRCs with unbounded minimum distance and certified locality. Finally, by means of concrete computations, we obtain a classification of qLRCs from cyclic, negacyclic, and repeated-root cyclic codes of moderate lengths, which contains the first binary qLRCs from repeated-root cyclic codes and many parameter sets that cyclic codes cannot attain.

</details>


### [63] [Tight Weighted Second-Order Asymptotics for the Wyner--Ahlswede--Körner Problem Under Regular Posterior Geometry](https://arxiv.org/abs/2608.21991)
*Daming Cao*

Main category: cs.IT

TL;DR: 本文针对有限字母表上的Wyner-Ahlswede-Körner问题，在局部正则性条件下确定了精确的加权正态近似。通过凸化、随机删除过程和鞅中心极限定理等工具，弥合了可达性与逆界之间的色散间隙。


<details>
  <summary>Details</summary>
Motivation: 已知的逆界色散界仅保留条件期望的方差，而可达性由加权优化器信息密度的方差控制，两者之间存在缺失的条件方差项，需要证明该项是真实的固定组合波动并加以解决。

Method: 首先将辅助变量优化表示为后验单纯形上的凸化问题，利用对偶亏空量化次优性；随后对联合类型进行随机删除过程，得到支持函数分数、非负可预测亏空和鞅的精确似然分解；通过后验定位和重心逆变换识别鞅的可预测方差，使用方差完备化构造建立鞅中心极限定理。

Result: 通过对经验联合类型取平均的固定类型高斯界，得到的总色散等于可达性方差，从而确立了精确的加权正态近似。将唯一性要求放宽为方差可辨识条件，并以二元对称特例验证假设并给出闭式解。

Conclusion: 本文成功精确确定了Wyner-Ahlswede-Körner问题的加权正态近似色散，填补了可达性与逆界之间的理论空缺，并为该问题提供了完整的二阶精度刻画。

Abstract: This paper determines the exact weighted normal approximation for the finite-alphabet Wyner--Ahlswede--Körner problem under local regularity of the posterior optimization. Liu's type-based achievability is governed by the variance of the weighted optimizer information density, whereas the known converse dispersion bound retains only the variance of its conditional expectation given the source pair. We show that the missing conditional-variance term is a genuine fixed-composition fluctuation. The converse first represents the auxiliary-variable optimization as a convexification problem on the posterior simplex and uses the associated dual deficit to quantify the suboptimality of code-induced posteriors. After conditioning on a joint type, a random deletion process yields an exact likelihood decomposition into a support-function score, a nonnegative predictable deficit, and a martingale. Posterior localization and barycentric inversion identify the martingale's predictable variance, while a variance-completion construction permits a martingale central limit theorem without conditioning on a terminal event. Averaging the fixed-type Gaussian bound over empirical joint types gives a total dispersion equal to the achievability variance. The uniqueness requirement is further relaxed to a variance-identifiability condition over all optimal posterior decompositions. A binary symmetric specialization verifies the assumptions and gives a closed form.

</details>


### [64] [Optimal Condition Numbers in Low-Rank Positive Semidefinite Matrix Sensing](https://arxiv.org/abs/2608.22418)
*Mingxuan Sun,Zhiqiang Xu*

Main category: cs.IT

TL;DR: 本文研究了半正定矩阵感知映射的稳定性问题，引入了双Lipschitz常数并定义了全局条件数，给出了仅依赖于秩r和底层域的确定性通用下界，并证明了随机秩一高斯测量在下界上是渐近尖锐的，即渐近最优的。


<details>
  <summary>Details</summary>
Motivation: 研究半正定矩阵感知映射Φ_A(X)的稳定性，即量化映射恢复信息的稳健性，通过定义条件数来刻画这种稳定性，并探索最优测量设计。

Method: 引入双Lipschitz常数和全局条件数（上下Lipschitz常数之比），推导确定性通用下界；随后分析随机秩一高斯测量，证明下界的渐近尖锐性；最后应用于ℓ1残差PhaseLift型估计器的稳定性保证。

Result: 给出了仅依赖于秩r和底层域的全局条件数确定性下界；证明随机秩一高斯测量在下界上是渐近尖锐的，因此是渐近最优的测量方案；在最优采样规模下推导出ℓ1残差PhaseLift型估计器的稳定性保证。

Conclusion: 随机秩一高斯测量在渐近意义下达到最优条件数下界，为半正定矩阵感知的稳定性提供了理论保证，并为PhaseLift型估计器在实际应用中的稳健性提供了理论支撑。

Abstract: In this paper we focus on the stability of positive semidefinite matrix sensing maps $Φ_{\mathcal{A}}(X)=(\langle A_i,X\rangle)_{i=1}^m$ where $A_i\succeq 0$, $X\succeq0$ and $\operatorname{rank}(X)\le r$. We introduce the bi-Lipschitz constants of $Φ_{\mathcal{A}}(X)$ and define the global condition numbers as the ratio of upper and lower Lipschitz constants. We give deterministic universal lower bounds for these condition numbers, that depend only on the rank $r$ and on the underlying field. We then investigate the random rank-one Gaussian measurements and show that our lower bounds on condition numbers are asymptotically sharp, and therefore the random rank-one Gaussian measurements are asymptotically optimal. As an application, we derive the stability guarantees for an $\ell_1$-residual PhaseLift-type estimator at the optimal sampling scale.

</details>


### [65] [Secure Wireless Information Transfer and Energy Harvesting in HAPS-Based Network](https://arxiv.org/abs/2608.22495)
*Khaled Humadi,Gunes Karabulut-Kurt,Wessam Ajib,Wei-Ping Zhu*

Main category: cs.IT

TL;DR: 本文提出了一种针对高空平台站（HAPS）网络的物理层安全与无线携能通信（SWIPT）联合优化框架，通过友好干扰与功率传输节点（配备零陷波束成形天线）同时实现安全通信和能量收集，利用随机几何工具推导了联合速率-能量覆盖概率和平均保密速率表达式。


<details>
  <summary>Details</summary>
Motivation: HAPS具有强视距链路特性，虽然有利于广域覆盖，但也增加了被窃听的风险；同时地面设备的能量受限是重大运营约束。现有工作缺乏对HAPS网络中物理层安全与无线能量传输协同设计的系统性研究。

Method: 提出一个集成友好干扰和功率传输节点的安全无线信息与能量收集框架，节点配备零陷能力天线以降低窃听者接收质量并为合法用户提供射频能量；用户端采用时间切换（time-switching）的无线信息与功率传输架构；利用随机几何工具推导联合速率-能量覆盖概率和平均保密速率的可处理闭式表达式。

Result: 数值与蒙特卡洛仿真验证了分析的正确性，揭示了时间分配因子、零陷区域半径和干扰功率之间的关键权衡关系；结果表明协调良好的零陷干扰可同时支持安全通信和充足的无线功率传输，但需要合理选择系统参数以平衡能量收集、通信可靠性和保密性能。

Conclusion: 该框架证明了在HAPS网络中，通过合理设计的零陷波束成形干扰与功率传输机制，可以同时实现物理层安全和无线能量收集，为偏远地区的低功耗设备提供安全且可持续的广域连接方案。

Abstract: High-altitude platform station (HAPS) serves as a promising enabler for wide-area connectivity of low-power wireless devices, particularly in remote and underserved regions. However, the strong line-of-sight characteristics of HAPS links increase the risk of eavesdropping, while the limited energy budget of ground devices remains a major operational constraint. In this work, we propose a secure wireless information and energy harvesting framework for HAPS-based networks in the presence of spatially distributed eavesdroppers. The proposed system integrates friendly jamming and power transfer nodes equipped with null-steering capability antennas, such that they not only degrade the reception quality at eavesdroppers but also act as additional radio-frequency energy sources for legitimate users. A time-switching wireless information and power transfer architecture is adopted at the user side. Under this framework, we derive tractable expressions for the joint rate-energy coverage and the average secrecy rate using stochastic geometry tools. Numerical and Monte Carlo results validate the developed analysis and reveal key design trade-offs among the time allocation factor, null-steering-zone radius around each user, and jammer transmit power. In particular, the results show that properly coordinated null-steering jamming can simultaneously support secure communication and adequate wireless power transfer, while an appropriate choice of system parameters, such as time allocation factor and jamming power, is required to balance harvested energy, communication reliability, and secrecy performance.

</details>


### [66] [Average-Radius List-Decodability of Random Linear Codes](https://arxiv.org/abs/2608.22663)
*Venkatesan Guruswami,Shilun Li,Mihir Singhal*

Main category: cs.IT

TL;DR: 本文证明了对于任意素数幂 q 和任意 p ∈ (0, 1-1/q)，随机 F_q-线性码在率为 1 - h_q(p) - ε 时，以至少 1 - q^{-Ω(n)} 的概率是 (p, C_{p,q}/ε)-平均半径列表可译的。这扩展了 Guruswami, Håstad 和 Kopparty (2010) 关于标准列表译码的结果到更强的平均半径保证，且保持相同的 O(1/ε) 列表大小。


<details>
  <summary>Details</summary>
Motivation: 平均半径列表译码是比标准列表译码更强的保证，但此前对于线性码，该结果仅对二元线性码已知，而对任意字母表的一般（非线性）随机码已知。作者的目标是将该结果推广到任意有限域上的线性码。

Method: 作者通过概率方法证明随机 F_q-线性码在特定速率下满足平均半径列表可译性，使用了与 Guruswami, Håstad 和 Kopparty 类似的技术框架，但针对平均半径（而非标准半径）进行了推广，涉及对重心周围码字平均距离的分析。

Result: 对于任意素数幂 q 和任意 p ∈ (0, 1-1/q)，随机 F_q-线性码以速率 1 - h_q(p) - ε 达到 (p, C_{p,q}/ε)-平均半径列表可译，失败概率至多为 q^{-Ω(n)}，列表大小为 O(1/ε)。

Conclusion: 该结果将二元线性码的平均半径列表译码结果推广到任意有限域上的线性码，并在列表大小方面达到与标准列表译码相同的最优 O(1/ε) 阶，填补了任意字母表线性码在该方向上的空白。

Abstract: We prove that for every prime power $q$ and every $p \in (0, 1-1/q)$, a random $\mathbb{F}_q$-linear code of rate $1 - h_q(p) - ε$ is $(p, C_{p,q}/ε)$-average-radius list-decodable with probability at least $1 - q^{-Ω(n)}$, i.e., for every center $y \in \mathbb{F}_q^n$, the $C_{p,q}/ε$ codewords closest to $y$ have average fractional Hamming distance at least $p$ from $y$. This extends a similar result for (standard) list-decoding due to Guruswami, Håstad, and Kopparty (2010) to the stronger average-radius guarantee, with the same $O(1/ε)$ list size. For average-radius list-decoding, such a result was previously known only for binary linear codes (Guruswami, Li, Mosheiff, Resch, Silas, and Wootters, 2021) and for general (non-linear) random codes over arbitrary alphabets (Elias, 1991).

</details>


### [67] [Soft GRAND under Channel Switching and Drift](https://arxiv.org/abs/2608.22685)
*Behrooz Razeghi*

Main category: cs.IT

TL;DR: 本文研究了GRAND（Guessing Random Additive Noise Decoding）解码器在信道切换或漂移场景下的性能。当解码使用的后验概率与实际匹配的后验存在偏差时，查询秩会增加，导致有限预算下的解码错误率上升。论文给出了查询秩的对数上界，并针对不同信道场景（切换和漂移）提出了误差界和解决方案（状态路径混合、导频刷新）。


<details>
  <summary>Details</summary>
Motivation: GRAND解码器依赖后验概率排序猜测候选噪声序列，但在实际通信中信道可能发生切换（channel switching）或漂移（channel drift），导致用于排序的后验与真实匹配后验不一致。这种失配会增加查询秩并降低有限预算下的解码性能，因此需要理论分析和解决方案。

Method: 1) 将查询秩的log值与匹配后验自信息加上正的对数后验失配项联系起来，给出上界；2) 利用精确随机子集碰撞概率推导GRANDAB（带放弃的GRAND）的错误界；3) 对无记忆信道切换场景，提出状态路径混合方法，证明在低于最小成分信道容量下误差一致消失；4) 对漂移场景，提出导频刷新（pilot refresh）方案来约束失配，并得到跟踪上界的连续最小化解；5) 通过广义高斯BPSK实验验证所提方法。

Result: 论文证明了信道失配下查询秩的增长可由后验自信息与失配项界定；在信道切换场景下，状态路径混合可在最小成分容量以下实现一致消失的误差；在漂移场景下，导频刷新能有效约束失配。广义高斯BPSK实验验证了两种场景下的理论结果。

Conclusion: 本文为GRAND解码器在非平稳信道（切换和漂移）下的鲁棒性提供了理论保证，证明了在一定条件下（如路径类大小次线性、低于最小容量）可通过状态路径混合和导频刷新等方法恢复渐近最优性能，扩展了GRAND在实际信道场景中的适用性。

Abstract: Under channel switching or drift, the posterior used to order soft GRAND queries can differ from the matched correction posterior, which can increase rank and finite-budget decoding error. We bound log query rank by matched posterior self-information plus positive log-posterior mismatch; exact random-subset collision probabilities yield GRANDAB error bounds. For switching among memoryless channels with capacity-achieving uniform input, a state-path mixture yields vanishing error uniformly over admissible paths below the minimum constituent capacity when log path-class size is sublinear. For drift, pilot refresh bounds mismatch and yields the continuous minimizer of a tracking upper bound. Generalized-Gaussian BPSK experiments evaluate both.

</details>


### [68] [Entropy of Bernoulli Measures Conditioned on Affine Subspaces and a Problem of Ancheta--Massey](https://arxiv.org/abs/2608.22837)
*Yihong Wu*

Main category: cs.IT

TL;DR: 本文将一个关于线性编码率失真最优性的经典结果（Ancheta对p=1/2的Bernoulli源证明）推广到所有p<1/2的情况。核心论证是界定仿射子空间条件下后验分布熵与其边缘分布的关系，该证明由GPT-5.6 Sol在作者引导的交互过程中发现，本文旨在传达简化证明并建立与编码理论和自旋玻璃理论的联系。


<details>
  <summary>Details</summary>
Motivation: 信息论中已知线性编码在无损压缩中可达到熵界，但在有损压缩中严格劣于率失真函数。Massey提出了一个悬而未决的问题：线性编码的最优率是否仅通过线性无损压缩一部分比特并用零估计其余部分来实现？Ancheta仅对p=1/2给出了肯定回答，本文将其推广到所有p<1/2的情况，填补了这一理论空白。

Method: 核心方法是证明一个关键不等式：将仿射子空间上的后验分布熵以其边缘分布（marginals）为界进行限定。这一证明最初由GPT-5.6 Sol在与作者的迭代交互中发现，本文提供了一个简化版本，并建立了与编码理论和自旋玻璃理论的联系。

Result: 成功将Ancheta关于线性编码最优性的结果从Bernoulli源参数p=1/2推广到所有p<1/2的情况，验证了Massey猜想在这更大参数范围内成立。

Conclusion: 本文通过推广Ancheta定理，进一步确认了线性编码在有损压缩中的最优策略格局：对于p<1/2的Bernoulli源，最优线性率确实可通过部分线性无损压缩加零估计实现。同时展示了AI辅助数学发现（GPT-5.6 Sol参与证明）的可行性，并揭示了该问题与编码理论和自旋玻璃理论的深层联系。

Abstract: A textbook result in information theory is that linear encoders achieve the entropy for lossless compression of Bernoulli source with parameter $p$. For lossy compression, however, linearity is known to incur strict suboptimality compared to the rate-distortion function. Massey asked whether the optimal rate for linear encoding is achieved simply by compressing a fraction of the bits linearly and losslessly and estimating the rest by zero \cite{Massey1978}. For $p=\frac12$, Ancheta answered this question affirmatively \cite{Ancheta1978}. This note extends Ancheta's result to all $p<\frac12$.
  The key argument is to bound the entropy of the posterior distribution conditioned on an affine subspace in terms of its marginals. The proof was discovered by GPT-5.6 Sol in an interactive process guided by the author. The purpose of the present note is to communicate a simplified version of this proof and to make connections with the existing literature on coding theory and spin glass theory.

</details>


### [69] [React or Predict? A Spectral Rule for Wireless Threshold Detection](https://arxiv.org/abs/2608.22900)
*Aamir Mahmood,Nho Duc Tran*

Main category: cs.IT

TL;DR: 本文提出一个实用的三阶段规则，用于决定无线传感器是否应进行预测性前视（lookahead）预警，以在过程跨越安全阈值前及时向远程监控端报警。


<details>
  <summary>Details</summary>
Motivation: 无线传感器必须在被监控过程跨越安全阈值之前向远程监控器发出警报，否则警报可能为时已晚。传感器可以基于当前估计做出反应，也可以提前预测并更早触发警报，但这种前视的价值并不显而易见——在某些系统中它能提供早期警报机会，而在另一些系统中则无法跨越报警边界，因此需要一个系统性的决策准则。

Method: 本文给出一个实用的三阶段规则：(1) 代数谱测试——在设计阶段判断前视在结构上是否有用，当阈值方向是动力学矩阵的非负特征值左特征向量时，前视是冗余的；(2) 闭式信道分解——证明当信道质量退化时，更深层次的预测变得更有价值，因为更长的提前时间窗口允许更多次跨阈前的传输尝试；(3) 仿真验证——大增益还需要保留预测幅值，振荡动力学通过旋转放大收益，双传感器设置揭示了感知-信道权衡。

Result: 仿真表明：更深的预测在信道退化时价值更大；大幅增益需要保留预测幅值；振荡动力学通过旋转增强预测收益；双传感器场景揭示了感知能力与信道质量之间的权衡关系。

Conclusion: 前视预测的价值并非在所有系统中都一致，可以通过代数谱测试在结构上判定其必要性，且其收益随信道质量下降而增加，但需保留足够的预测幅值才能真正获益。

Abstract: A wireless sensor must alert a remote monitor before a monitored process crosses a safety threshold; an alarm arriving afterward may be too late. The sensor can react to its current estimate or predict ahead and trigger earlier, but the value of such lookahead is not obvious. In some systems it creates an early-alarm opportunity unavailable to the current test, while in others it cannot cross the alarm boundary. This letter gives a practical three-stage rule for deciding when to predict. First, an algebraic spectral test decides at design time whether lookahead is structurally useful: it is redundant exactly when the threshold direction is a left-eigenvector of the dynamics with a non-negative eigenvalue. Second, a closed-form channel decomposition shows that deeper prediction becomes more valuable as the channel degrades, because longer lead windows permit more pre-crossing transmission attempts. Third, simulations show that large gains also require retained prediction magnitude; oscillatory dynamics amplify the benefit through rotation, and a two-sensor setting reveals a sensing-channel tradeoff.

</details>


### [70] [Deep Learning-Based Multi-User Communication Design for Dense IoT Networks: Interference-Aware Finite-Blocklength Communication and Preliminary MIMO Extensions](https://arxiv.org/abs/2608.22923)
*Arkadeep Sinha,Shubham Paul,R. Manivasakan*

Main category: cs.IT

TL;DR: 本文提出了一种基于深度学习的端到端多用户通信设计，针对干扰受限的有限块长IoT场景，将先前的2用户SiameseNet收发机框架扩展到2、4和8用户场景，利用学习到的冗余实现干扰抑制和噪声鲁棒性，在无需联合检测的情况下取得优于传统非正交接入方案的BLER性能。


<details>
  <summary>Details</summary>
Motivation: 密集IoT网络在有限频谱和大量多用户干扰下需要可靠通信，同时要保持接收机复杂度可控。现有的传统通信方案在短/中等块长场景下性能受限，需要一种新的端到端多用户通信设计来应对干扰受限环境。

Method: 扩展先前的2用户SiameseNet收发机框架至2、4和8用户场景，利用深度学习学习的冗余进行干扰抑制和噪声鲁棒性处理；无需联合检测，每用户解码器复杂度随用户数近似线性扩展；并分析了潜在空间中的码字距离与有效每用户速率的关系，同时探索了2x2 MIMO固定信道下的扩展。

Result: 与传统非正交接入基线相比，该方法在多种场景下展现出优异的BLER性能；鲁棒性测试表明其在干扰失配和不均等干扰强度下依然有效；潜在空间分析证实学习到的码字距离随有效每用户速率降低而增大，与BLER改善相吻合；初步2x2 MIMO结果显示其扩展潜力。

Conclusion: 基于深度学习的端到端多用户通信设计在干扰受限IoT场景中表现优异，无需联合检测即可实现强干扰抑制，且解码器复杂度可控，具有向多天线IoT网关扩展的潜力。

Abstract: Dense IoT networks require reliable communication despite limited spectrum and substantial multi-user interference while maintaining manageable receiver complexity. This work introduces a deep-learning-based end-to-end multi-user communication design for interference-limited finite-blocklength IoT scenarios, focusing on short and medium blocklengths.
  We extend a prior 2-user SiameseNet transceiver framework to accommodate 2, 4, and 8 users, leveraging learned redundancy for interference suppression and noise robustness. Compared to conventional non-orthogonal access baselines, our method demonstrates strong Block Error Rate (BLER) performance across various scenarios without resorting to joint detection; the per-user decoder scales roughly linearly with the number of users.
  Further, we examine the robustness under interference mismatch and unequal interference strengths, critical for practical deployments with heterogeneous devices. The Latent-space analysis reveals that the learned codeword distance increases as the effective per-user rate decreases, corroborating with the observed BLER improvements. In addition, we also present preliminary results for a 2X2 MIMO setup under fixed-channel CSIT and CSIR, indicating potential for extending the framework to IoT gateways with multiple antennas.

</details>


### [71] [Compressed sensing matrices from orthogonal spaces over finite fields of odd characteristic](https://arxiv.org/abs/2608.23062)
*Kanittakorn Moonchaisook,Poom Kumam,Songpon Sriwongsa*

Main category: cs.IT

TL;DR: 本文利用奇特征有限域上正交空间的三类子空间的关联关系构造确定性矩阵，用于压缩感知。这些矩阵具有可显式计算维数和相干性的优点，并通过相干性估计建立了满足受限等距性质（RIP）的充分条件，同时与DeVore构造进行了数值对比。


<details>
  <summary>Details</summary>
Motivation: 压缩感知需要满足RIP性质的测量矩阵，而随机矩阵虽然效果好但难以验证和存储。确定性矩阵的可验证性和结构化特性使其成为理想替代，但现有确定性构造（如DeVore构造）在测量数与稀疏恢复保证之间存在权衡，需要探索新的确定性矩阵构造方法。

Method: 利用奇特征有限域上正交空间中的三类子空间（全迷向子空间、非迷向子空间等）之间的关联关系构造确定性测量矩阵。通过精确计算矩阵维数和相干性（coherence），并基于相干性估计推导矩阵满足受限等距性质（RIP）的充分条件。

Result: 构造出的确定性矩阵具有可显式计算的维数和相干性，并在给定稀疏度水平下建立了满足RIP的充分条件。数值结果展示了所提构造与DeVore确定性构造在测量数、相干性和稀疏恢复保证之间的权衡。

Conclusion: 本文提供了一类新的基于有限几何（正交空间子空间关联）的确定性压缩感知矩阵构造，丰富了确定性矩阵的设计方法，并通过理论分析和数值比较验证了其可行性与竞争力。

Abstract: In this paper, we construct deterministic matrices from subspaces of orthogonal spaces over finite fields of odd characteristic and investigate their applicability to compressed sensing. The construction is based on incidence relations among three types of subspaces, yielding families of matrices with explicitly computable dimensions and coherence. Using coherence-based estimates, we establish sufficient conditions under which these matrices satisfy the Restricted Isometry Property for prescribed sparsity levels. We also provide numerical comparisons with DeVore's deterministic construction to illustrate the trade-off between the number of measurements, coherence, and sparse recovery guarantees.

</details>


### [72] [Breakdown of Edgeworth Expansion in Finite-Blocklength Regime and Exact Absorption via $q$-Deformation](https://arxiv.org/abs/2608.23303)
*Hiroki Suyari*

Main category: cs.IT

TL;DR: 本文提出了一个q-变形框架来解决有限块长（FBL）机制下Edgeworth展开的结构性失效问题，通过几何变形信息密度空间替代加法多项式扰动，动态缩放q-对数参数以精确吸收三阶偏度，同时保持全局非负性，并实现了与Cornish-Fisher界相当的精度。


<details>
  <summary>Details</summary>
Motivation: 在有限块长（FBL）机制下，传统的Edgeworth渐近展开在深尾部区域会产生非物理的负概率，这严重限制了其在超可靠通信（如6G和URLLC）中评估运行极限的适用性。

Method: 提出q-变形框架，将加法多项式扰动替换为信息密度空间的几何变形；通过动态缩放q-对数参数来精确吸收三阶偏度，同时保持全局非负性；并建立通用渐近匹配以涵盖高阶渐近尺度。

Result: 数值结果表明，该方法在精度上与最先进的Cornish-Fisher界相匹配，且消除了负概率的风险，提供了稳健且计算稳定的框架。

Conclusion: 该框架为超可靠通信中评估运行极限提供了稳健且计算稳定的基础，克服了传统渐近方法在深尾区域产生负概率的根本缺陷。

Abstract: This paper addresses the structural breakdown of the Edgeworth expansion in the finite-blocklength (FBL) regime, where conventional asymptotic approximations yield unphysical negative probabilities in the deep-tail region. We propose a $q$-deformed framework that resolves this inconsistency by replacing additive polynomial perturbations with a geometric deformation of the information density space. Motivated by the linearization of nonlinear dynamics, we prove that dynamically scaling the $q$-logarithmic parameter exactly absorbs the third-order skewness while preserving global nonnegativity. We establish a universal asymptotic matching, demonstrating that the framework encapsulates higher-order asymptotic scales. Numerical results confirm that the proposed method matches the state-of-the-art precision of the Cornish-Fisher bound without the risk of negative probabilities. The framework offers a robust and computationally stable foundation for evaluating operational limits in ultra-reliable communications such as 6G and URLLC.

</details>


### [73] [Exact Rate Exponent Tradeoff for New Classes of Distributed Hypothesis Testing Problems](https://arxiv.org/abs/2608.23465)
*Zhenduo Wen,Amin Gohari,Michèle Wigger*

Main category: cs.IT

TL;DR: 本文刻画了一类单向分布式假设检验问题的精确速率-误差指数权衡，证明了一个基于辅助接收机技术的最新上界与已知下界相吻合，并在特定条件下（对依赖检验及DSBS源）证明了该上界的紧致性。


<details>
  <summary>Details</summary>
Motivation: 分布式假设检验中，精确的速率-误差指数权衡尚未完全刻画，现有的上界（通过辅助接收机技术获得）与下界之间存在间隙，需要证明二者匹配以闭合该间隙。

Method: 将辅助接收机技术得到的II型误差指数上界进行松弛，使其与Han下界具有相同的内泛函形式，仅在外层速率约束上有所差异。随后在特定的分布场景下（对依赖检验以及DSBS源）证明该上界的紧致性。

Result: 证明了松弛后的上界在对依赖检验和DSBS源（满足0 < κ₁ ≤ κ₀ < 1/2条件）下是紧的，且对DSBS源在任意通信速率R ≥ 0下都给出了精确的误差指数刻画。

Conclusion: 通过证明上界与下界的匹配，本文为单向分布式假设检验问题提供了精确的速率-误差指数权衡刻画，特别是对DSBS源在所有速率下均获得闭合形式的精确结果。

Abstract: We characterize the exact rate--exponent tradeoff for new classes of one-way distributed hypothesis testing problems by demonstrating that a recent upper bound, derived via the auxiliary-receiver technique, coincides with known lower bounds. We achieve this by relaxing the upper bound on the type-II error exponent into a form that shares the same inner functional as Han's lower bound, differing only in the outer rate constraint. Furthermore, we prove that this upper bound is tight for testing against dependence and for the doubly symmetric binary source (DSBS) with crossover probabilities $κ_0$ under the null hypothesis and $κ_1$ under the alternative hypothesis, provided $0 < κ_1 \leq κ_0 < \frac12$. The characterization of the exact error exponent for the DSBS source holds for every communication rate $R \geq 0$.

</details>
